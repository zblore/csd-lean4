# BACKLOG — the single canonical open-items list

> ## ▶ OPEN WORK — the actionable list (rewritten 2026-08-04)
>
> The table further down is now **mostly a graveyard**: of its ~19 rows, ten are struck
> through and nine of those were discharged on 2026-08-03. It is kept as the construction
> record. **This is the live list.** It is ordered so that the top item is the one that
> unblocks the programme's single remaining scientific blocker.
>
> ### Q. The numbered quick queue (added 2026-08-12 — say "do Qn" to reference)
>
> | # | Item | Pillar | Cx | Status | Row |
> |---|---|---|---|---|---|
> | ~~Q1~~ | ~~Derived-ε ontic bridge~~ **DONE 2026-08-12** (`Empirical/CSD/QuantumChaos/DerivedCoupling.lean`, 4 pins): ★★ `deficitKick_record_halfLife` — `δ·μ((intact n)ᶜ) ≤ n·‖W−1‖`, the §H5 half-life with `ε` DERIVED; route = the overlap-deficit trigger (`1 − Re⟨u,Wu⟩/‖u‖²`, ray-well-defined, continuous by `momentMap`-style quotient descent, pointwise `≤ ‖W−1‖` by Cauchy–Schwarz) + **Markov over μ_FS** (`measure_deficitTrigger_le`) + the generic `triggeredRecordKick`; `W = 1` ⟹ a.s. persistence (`deficitKick_persists_of_id`). Same `‖W−1‖` knob as `carrier_persistence_window` — one quantity, both levels. Recorded refinements, not claimed: the threshold-free sharp version (needs the deficit's law); the `FieldConfig` reindex | Chaos/records | M | **done** | H7 residue |
> | ~~Q2~~ | ~~Half-life attainment~~ **DONE 2026-08-12** — in two halves, one of which predated the row: (i) **generic attainment was already settled** (`HalfLifeAttainment.lean`, `cyclicKick_halfLife_attained` — the bound is an EQUALITY on the cyclic kick; the Q2 row description was stale on this); (ii) **the derived coupling bites, exactly** (`DerivedCoupling.lean` bite section, 4 pins): for the qubit phase flip `diag(−1,1)` the deficit **is** `2·momentMap` (`overlapDeficit_phaseFlipW`), so the trigger is a moment super-level set and the **Duistermaat–Heckman law computes the coupling exactly** — `μ_FS(trigger) = 1 − δ/2` (`measure_deficitTrigger_phaseFlipW`), strictly in `(0,1)` on `δ ∈ (0,2)` (`deficitKick_phaseFlip_bites`), with the half-life at the exact rate (`deficitKick_phaseFlip_halfLife`). Honest note recorded in-module: `‖W−1‖ = 2` makes the Q1 Markov bound trivial on this window — the generic bridge is for drives whose deficit law is unknown; where the law is available (DH), use it | Chaos | M | **done** | §H thread |
> | ~~Q3~~ | ~~Chaos diagnostics: SFF growth / OTOC beyond the gate~~ **DONE 2026-08-12** (`CV/ChaosBounds.lean` Q3 section, 3 pins): the pair that completes the diagnostics beyond the light-cone gate. (i) ★★ **Slow scrambling** (`otoc_interactingU_le`): for disjointly supported `A`/`B`, `otoc(interactingU, A, B, n) ≤ 4n·|τ||λ|C·‖A‖‖B‖` — the free comparison commutes exactly (`commute_of_disjointSupport` + free-pow support), so the whole OTOC is priced by the telescoped Duhamel distance (`norm_unitary_pow_sub_pow_le` + `interactingU_dist_le`): **linear growth cap, no fast scrambling in the kicked-diagonal family**. (ii) ★ **Exact revival** (`sff_freeFieldU_revival`): at `τ = 2π` the integer-spaced spectrum (`oscEnergy n = n + ½`) makes every phase `e^{−iπK}` (`freeField_phase_two_pi`), so `sff = 1` at **every** period — the clean integrable baseline (no dip–ramp–plateau) the cap is measured against. Not claimed: any lower bound on OTOC growth (would need a specific drive computed out, like Q2's DH route) | Chaos | M | **done** | §H thread |
> | ~~Q4~~ | ~~Entropy-ledger link~~ **DONE 2026-08-12** (`Empirical/CSD/QuantumChaos/EntropyLedger.lean`, 7 pins): the ledger made precise and priced, strictly as retrodiction reliability (every quantity a present-tense measure of a present-tense set). Chain: ★ `measure_retrodictionSuccess_compl_le` (reading the register now retrodicts the formation value except on `≤ n·ε`); ★ `erosionFraction_monotone` (**the ledger's substrate is one-way** — second-law shape at the register, from carrier antitonicity alone); ★★ `ledgerEntropy_le` (below half-filling, the two-cell coarse entropy `binEntropy(erosion) ≤ binEntropy(n·ε)` — **one coupling knob, three readings**: reliability, erosion, entropy); ★ `vonNeumannEntropy_ledgerState` (the measure-side ledger **is** `S(diag(1−e, e))`, the object TH2's pinch H-theorem governs — the link the row asked for). Instantiation with **no free parameter**: phase flip + Q2's DH coupling `ε = 1−δ/2` (`deficitKick_phaseFlip_reliability` / `_ledger`) — closes the Q1→Q2→Q4 chain. Fine-grained entropy stays constant throughout (measure preservation); all production is coarse. Not claimed: Fano-type converse (needs joint-distribution conditional entropy); any thermodynamic entropy beyond the register's own coarse-graining | Chaos/records | M–L | **done** | H7 follow-up |
> | ~~Q5~~ | ~~Shor-9 concatenation + Lindblad tier~~ **DONE 2026-08-13**, both halves. **(i) Shor-9 by concatenation** (`Empirical/QM/QEC/ShorNine.lean`, 9 pins): the E1 row's combinator option delivered — ★ `tel9_bkron_vkron` (`(A⊗B⊗C)(u⊗v⊗w) = Au⊗Bv⊗Cw`) reduces every 512-dim statement to the proven 8-dim block facts; **nothing above 8 dims is entry-wise**. All eight stabilisers fix `shorLogical`; syndrome tables ★ `innerStab_syndrome_X` (3-qubit pattern in the hit block, injective) + ★ `outerStab_syndrome_Z` (block pattern, **independent of the inner position**); corrections ★★ `shor_corrects_X` / `shor_corrects_Z_degenerate` (**recovery `Z₁` of the identified block works whichever qubit was hit** — the code's degeneracy as a theorem) / `shor_corrects_XZ`, all 9 positions; with `pauli_decomposition` this is the discretised "corrects an arbitrary single-qubit error". Not claimed: two-error patterns, 9-qubit syndrome collapse, fault tolerance. **(ii) Lindblad semigroup tier** (`LF6/LindbladSemigroup.lean`, 6 pins): `Φₜ = exp(tℒ)` for an **arbitrary** GKSL generator in the ℝ-endomorphism algebra — ★ semigroup law, ★★ the master equation `d/dt Φₜρ = ℒ(Φₜρ)` (`hasDerivAt_exp_smul_const'`), ★ trace preservation and ★ Hermiticity preservation at the flow level (series arguments: `HasSum.mapL` / `HasSum.star`, termwise from the generator tier). **CP of `e^{tℒ}` remains the recorded Mathlib-scale gap** (Lie–Trotter), unchanged on the GKSL row. Proof-engineering: multiple module-system instance-defeq walls (`Complex.mulAux` unexposed) — worked around via `module` tactic, pointwise `ext`, induction-not-`smul_pow`, and the `exp_add_of_commute_of_mem_ball` (𝕂 := ℝ) route; recorded in the module docstring + memory | QI ladders | L | **done** | E1 |
> | Q6 | Composable finite-key QKD — **deprioritised (author note 2026-08-13): this is core-QM QIT reproduction (Lean-QIT's territory, not CSD's floor); the existing E91/BB84 twins already show the reconstruction carries crypto. Pick up only if a CSD-specific angle appears** | QI ladders | XL | open, deprioritised | E3 |
> | Q7 | CV programme, post-Stage-5 — **Stage 6 OPENED 2026-08-13** ([`eft-stage6-plan.md`](eft-stage6-plan.md), the cone's consequences; continuum + RG postures unchanged): ~~CV-20~~ ★★★ **the Lieb–Robinson velocity DONE same day** (`norm_commutator_velocity_le`: outside `v·t ≤ d`, `v = 2e²‖S‖`, the commutator ≤ `2‖A‖‖B‖e^{−d}` — Stage 5's recorded non-claim discharged; constant not claimed optimal); ~~CV-21~~ ★★ **vacuum clustering DONE same day** (`diag_entry_mul_of_disjointSupport` + `vacuum_clustering`: no vacuum correlations across disjoint mode sets — the statics companion to the cone, landed stronger than planned at any configuration); ~~CV-22~~ **Wick four-point: gate run same day, PASSED, landed pattern-resolved** (`eqFourPoint` table: `3/4` all-equal needing `2 < N`, `1/4` two-pairs, `0` singleton patterns — exactly `Σ_pairings ∏ ½δ`). **Stage 6 COMPLETE 2026-08-13, opened and closed same day**; further CV stages are new queue decisions, not part of this row | CV/EFT | L | **done (Stage 6 complete)** | E4 |
> | Q8 | Manifold forms on `ℂℙ^{N-1}` (A4 manifold half) | Geometry | XL | **Mathlib wall** | A4 |
> | Q9 | DPI → SSA ladder | QI ladders | L | **user-deferred 2026-07-23 — leave alone** | E2 |
> | Q10 | Non-factorising-Σ no-signalling from primitives | Foundations | Research | scoping-first | C1 item 16 |
> | ~~Q11~~ | ~~U(N) / hTPP discharge — scoping session~~ **SCOPED 2026-08-13** ([`unitary-tpp-scoping.md`](unitary-tpp-scoping.md)) — the deliverable (the map + a named first brick) is delivered; no theorems claimed. **The map:** Hardy **survives** as the minimal-scope programme (probabilities = LF1's LLN already a theorem; continuous reversibility = Σ's native flow, absorbed by the substrate; composites = `compositeAlgReconstruction`, landed; simplicity/subspaces non-questions at this scope). CDP's grammar **declined** (ontology-free circuit framework; purification is theorem-shaped in CSD — the dilation machinery runs the other direction; harvest = local discriminability, already `TensorReconstruction`'s generation hypothesis). MM's **requirement style survives, the Lie-theoretic lever deferred** — at minimal scope `wigner_rigidity_unitaryGroup` replaces the group classification outright; MGM 2019 not relied on (Kent-disputed). **The reduction (§4):** both conditioners are ONE record-level premise — `transProb` is a record observable (the Born rate any context containing `q` assigns to `p`, via `bornRateBasis_eq_inner_sq`), so `hTPP` converts to "the projected flow preserves record statistics" and the measure posit to "invariant under every statistics-preserving symmetry", `U(N)` never named in a premise (the group becomes a Wigner OUTPUT). Premise conversion in the `fubiniStudy_forced_by_symmetry` template, NOT elimination (two operational premises survive; the papers owe their motivation); NOT the §13.2 trap (statistics-preservation is not measure-preservation; the `UnitarySelection` guard stands); D1 (`G`-from-dynamics) untouched and still obstructed (`obsFlow_not_uniquely_ergodic`) — the operational route sidesteps it. Maximal scope (full GPT reconstruction) named and deferred as a future queue decision. **First brick → Q18** | Necessity | Research | **done (scoping)** | necessity audit |
> | Q12 | Fibre mechanism from de-isolation dynamics | Σ structure | Research | open | E5 |
> | Q13 | Trilemma exhaustiveness over all arenas | Measurement | Research | open | E5 |
> | Q14 | Poisson-argument paper section | Paper (authorial) | — | unblocked since B1 | A2 |
> | ~~Q15~~ | ~~SigmaLayer role split~~ **DONE 2026-08-13** (the consolidation moment G10 named: the record-layer tranche is stable and the M/L brick queue is empty). The split is exactly the namespace/directory mismatch, measured mechanically: of 115 files in `SigmaLayer/`, **74 declared `namespace CSD.RecordLayer`** and moved to `CsdLean4/RecordLayer/`; 41 genuine `CSD.SigmaLayer` files remain. **Namespaces unchanged, so all 1,700+ axiom pins and the Headlines facade drift-guard survive untouched** — only module paths moved. 85 reference surfaces rewritten (root, `Headlines.lean`, all importers, `check-claims.sh` scope ledgers, `docs.yml` smoke page, `docs/TOUR.md`, `docs/PATHS.md`). Policy per G10's spirit: **historical BACKLOG/plan rows keep pre-split paths** (they are construction records; the archaeology stays true to its date). Residues, recorded not queued: the `Tests/AxiomAudit/SigmaLayer.lean` part still carries both namespaces' pins (G9's classifier keys on namespace, placement never affects resolution — a `RecordLayer` part split is cosmetic); G10(b)/(c) declines reaffirmed | Infra | L | **done** | G10 |
> | Q16 | Metrology A4 (decoherence) | Empirical | XL | half-unblocked by Q5 (semigroup tier); still gated on CP-of-`e^{tℒ}` (Mathlib wall) | metrology plan |
> | ~~Q17~~ | ~~Headline-census refresh~~ **DONE 2026-08-13** — all four asks delivered. **(i) Admission criteria defined** (`VALIDATION-LEDGER.md`, new section): terminal + claim-bearing + pinned + distinct, with the census sources named — "the 30" is no longer a codex-session artifact but a rule, re-run when a tranche closes with starred headliners. **(ii) Census run, ledger extended 31 → 51** (CL-032…CL-051, all `qualified` with load-bearing posits named; promotion stays sign-off): the necessity audit's named strongest-direction omissions (`stone_continuous`, `no_exact_finite_ccr`, the three `MeasurementConstraints` no-gos, `compositeAlgReconstruction`, `posMeasure_noRecord_pointer`), the A7-faithful `qubitBorn`, the 08-12/13 tranche's starred headliners (derived coupling, DH-exact rate, entropy ledger, Shor-9 degeneracy, the Lindblad master equation, LR velocity, vacuum clustering, the general-`N` third horn), the Q18 conversions, and the two boundary-discharging record-layer ★★ (`povm_sector_born` vs CL-026, `pointer_luders_born_prep`). Headlines facade + drift guard extended to 51 (all constants elaborate; `check-validation-ledger.sh` OK at 51). **(iii) Necessity classification re-run** (`necessity-audit.md`, addendum 2026-08-13): CL-031 (which the 08-09 audit predated and had never classified) + the twenty new rows, same rubric; **updated tally 2 necessity / 13 conditional necessity / 9 impossibility / 24 sufficiency / 3 instantiation** — the body's "strongest-direction results are mostly not among the declared headlines" is discharged. **(iv) The Q18 ledger stroke landed in the same addendum**: CL-012 and the Schrödinger-chain rows re-annotated — posits carry a proven conversion to record-level premises (conversion, not elimination; §13.2 trap and D1 untouched) | Necessity/infra | M | **done** | validation ledger + necessity audit |
> | ~~Q18~~ | ~~The Q11 first brick~~ **DONE 2026-08-13, same day as its scoping** (`RecordLayer/StatisticsRigidity.lean`, 8 pins) — all seven scoped items landed, none weakened: `exists_context_extending_ray` (every ray is an outcome of some context, Gram–Schmidt extension), `recordKernel` + `recordKernel_well_defined` (the operational pairwise statistic, defined through `bornRateBasis`, apparatus-independent, inner-product-free), ★★ `recordKernel_eq_transProb` (**transition probabilities ARE record observables** — the kernel identification), ★ `recordStatisticsPreserving_iff_transProbPreserving` (the operational and geometric premises are THE SAME predicate), `recordStatisticsPreserving_unitary`/`_conjProj` (both branches inhabited), `recordStatisticsPreserving_realisation` (the operational symmetry group is semi-unitary, Wigner through the iff), ★ `projectedFlow_unitary_of_record_statistics` (W3+Bargmann consuming the record-level premise in place of `hTPP` — a premise conversion, honestly a thin wrapper), ★★ `measure_eq_fubiniStudy_of_record_statistics_invariant` (**`U(N)` in the proof, never the statement** — the necessity audit's requested conversion). Scope-noted as predicted: the FS-invariance converse needs `conjProj` pushforward invariance (absent) and the discharge direction does not need it. *Snag ledger:* the forecast `rep`/`mk` defeq class never bit (the `show`-idiom sufficed); the two actual snags were **name drift** — `Complex.norm_ofReal` gone (superseded by the exact-shape simp lemma `norm_smul_inv_norm`) and `Set.restrict` deprecated for `Set.domRestrict` in the PiL2 extension lemma's signature. Ledger stroke deferred to Q17 as scoped: re-annotate CL-012 + the Schrödinger-chain rows (posit renamed, not removed), admit the two ★★ | Necessity | M | **done** | Q11 scoping |
> | ~~Q19~~ | ~~C1 positive witness + contextual capstone~~ **DONE 2026-08-13, same day as its review** (reviewer handoff amended per the session review, then landed; `LF6/C1BellConsistency.lean` amended in place — no parallel capstone file — + `Tests/Witnesses/SingletBell.lean` WS-H upgrade; 5 production pins in the Dynamics part + 1 witness pin). **The gap was real**: CL-031's reproduction slot had NO inhabitant anywhere (the recorded non-vacuity covered only the compatibility conjunct), so the corpus proved "non-contextual can't do the singlet" without proving "contextual can" — the second half being CSD's own claim. **Landed**: `ReproducesSingletTableAt` (the full-table predicate — the review's one substantive correction: a correlation-only witness can carry degenerate marginals, so the positive model MUST be the table, the E-1 lesson applied in advance); the explicit model (per context, the first torus coordinate of `(KSigma 4, kMuPsi)` read through the four cumulative `circleCell` arcs of the context's own `P_st` weights — `kMuPsi = δ ⊗ vol_T²` makes the arc masses exact, no atomless abstraction); ★ `singletContextualModel_table` (**the full table at EVERY context**, marginals ½, correlations −a·b) + `integral_wing_mul_of_table` (table ⇒ correlation — `correlation` IS the weighted table sum by definition); ★ `singletContextualModel_reproduces` (the first inhabitant of `ReproducesSingletAtCHSH`); ★★ `c1_singlet_contextual_capstone` (**existence ∧ obstruction**: a measurable shared-context family reproduces the singlet and no global CHSH assignment is compatible with it — the C1 separation two-sided, both halves witnessed); WS-H re-export `kMuPsi_singlet_contextual_model`. Existing names untouched (`kMuPsi_no_global_chsh_assignment` etc. — pins stand); CL-031 retained as the general obstruction. Docs: `C1-FORMAL-SUPPORT.md` + validation-hardening WS-H entry amended. *Snags:* `Finset.sum_eq_single` side-goal ordering (inline the side proofs); the bare-negation `∉ setOf` unifier trap (ascribe the membership type); `integral_indicator_const` now lands in the `Measure.real` API; `Measure.volume_eq_prod` takes explicit type args. **CL-052 ADMITTED (author sign-off 2026-08-13, same day)**: `c1_singlet_contextual_capstone` is ledger row CL-052 (qualified; facade + drift guard extended to 52; classification INSTANTIATION carrying CL-031's impossibility on the witness — necessity-audit addendum). CL-031 retained; the two rows read together, existence + no-go — the citation pair for the revised C1 paper | Entanglement/C1 | S/M | **done** (CL-052 admitted) | C1 stack review |
> | ~~Q20~~ | ~~C1 §4/§5.1 discharge on the exhibited model~~ **DONE 2026-08-13/14** — ⚠️ *provenance: written in a separate (remote-control) session and left uncommitted at HEAD `ecf0c86`; recovered 2026-08-14, verified build-clean (0 errors, 0 warnings, 4009 jobs) + pins + guards, then committed — the exact K1-precedent failure mode (`INDEX.md` K1 row), caught by a status check.* Content: **(i)** `LF3.OperationalNoSignalling` made a real predicate stack (`RemoteMarginalInvariantA/B` — one `μ` across contexts, which IS measurement independence, now named in-module rather than implicit) and **inhabited for the first time** by ★ `singletContextualModel_no_signalling` — until Q20 the paper's §4 cited kernel-level finite sums with no measure or outcome map in them; general route `wingA_marginal_of_table` / `wingB_marginal_of_table` (marginals from the table, any family). **(ii)** `no_product_partition_realises_singlet`'s reproduction hypothesis `ReproducesSinglet` also had no inhabitant; ★ `singletContextualModel_not_product` discharges it on the exhibited model — **C1 §5.1 as a theorem about an exhibited object** (the model's wings provably cannot be setting-local). **(iii)** the Λ → `SigmaSpace` rename across the C1 chain (prose-alignment with the papers' Σ vocabulary). **(iv)** honesty additions to `C1-FORMAL-SUPPORT.md`: the outcome family is a **chosen partition, not a readout** (deriving it from de-isolation dynamics is MD-1, open); a dilated system-plus-pointer version was written and REMOVED same day (it closed the gap only by leaving Σ — wrong direction); measurement independence named as a genuine Bell premise. 2 pins (Dynamics part). Ledger note: neither theorem admitted as a row (author's tranche made no tsv change — respected); both are census candidates at the next Q17-criteria sweep | Entanglement/C1 | M | **done** (recovered + committed) | C1 paper §4/§5.1 |
> | Q21 | **EFT Stage 7 — SCOPED 2026-08-17** ([`eft-stage7-plan.md`](eft-stage7-plan.md), the Q11 mold: gates and abort criteria agreed in advance, feasibility checked before scoping — `gibbsState` index-generic ✓, finite-dim KMS is entrywise algebra for diagonal `H` ✓, Thermo→CV import direction clean ✓, walk-collapse threshold pattern generalises ✓). Three rows: **CV-23** the Wick closure ((a) packaged `Σ_pairings ∏ ½δ` — strengthens the `eqFourPoint` table in place per CONVENTIONS §8.3b; (b) time-separated four-point; (c) `2n`-point with explicit truncation threshold, GATED on a six-point pass, abort to (a)+(b)); ~~CV-24~~ the thermal tier — **DONE 2026-08-17, first row landed** (`CV/ThermalPropagator.lean`, 7 pins: ★★ `thermal_kms` **exact at the cutoff** — the corpus's first KMS statement, the Thermo↔CV verticals joined; closed-form Boltzmann state via `cfc_eq_conj_diagonal` at `U = 1`, `Z = z^K`, the thermal mode marginal, the thermal propagator with the truncation edge explicit, and the `β → ∞` vacuum limit recovering `freeTwoPoint` — no residue; full record in the `future-work.md` strike); **CV-25** the channel-level RG **scoping session only** (doc, one pass, abort to unqueued; the Stage-4 no-go is the floor). Non-goals restated: no continuum limit, no RG theorems, no velocity optimality; the departures tower stays theory-gated. **Labelled breadth per the charter** (the isolated-piece image, not the reconstruction frontier). Say "do CV-23" / "do CV-24" / "do CV-25" | CV/EFT | (a) S, (b) M, (c) L-gated; M/L; Research-gated | **queued (scoped)** | Q7 residues + Thermo↔CV |
>
> **Settled non-goal (2026-08-12, author decision):** the beyond-horizon permanence toy
> (expanding-chain "Λ > 0 ⇒ records beyond the cosmological horizon persist forever") is
> **off the queue** — its physical referent is unmeasurable in principle from inside the
> horizon, so it fails the present-tense-checkable test however formalizable it is. Do
> not re-propose; the idea stays recorded in the vault navigation note only.
>
> ### A. The blocker, split into what is doable and what is not
>
> The one open scientific requirement is Paper C A2 / TN6's **joint-arena Hamiltonian
> flow**: measurement generated by a scalar `𝓗` on the whole arena, whose *actual* flow
> creates Born-weighted records. What exists is fibrewise Schrödinger with the base frozen
> (`PointerGeneration.lean`'s dated boundary note). Verified 2026-08-04: Mathlib now has
> manifolds, `ContMDiff`, `Diffeomorph`, `DerivationBundle` and `IntegralCurve`, but **no
> symplectic form, no Poisson bracket, no exterior derivative**. So exactly one arrow is
> missing — `𝓗 ↦ X_𝓗 = ω⁻¹d𝓗` — and everything downstream of "here is the flow" is
> expressible today. The blocker therefore splits:
>
> | # | Size | Task | Blocked? |
> |---|---|---|---|
> | ~~A1~~ | **DONE 2026-08-04** (`SigmaLayer/JointFlowTransfer.lean`, 5 pins) | ★★ `IsJointLift.outcomeSector_eq` — the record preimages of a joint lift and of the fibrewise witness are **the same set**, not merely the same measure: `arenaRecord` is a cylinder over the pointer, so horizontal back-reaction is *invisible* to it. Landing and both Born bounds then transport with **no measure-theoretic work**. ★ `weights_conserved` (what the Poisson argument delivers) and ★ `moment_marginal_unchanged` (the honest replacement for the no-collapse theorem — under a lift `pointerEvolve_base_marginal_unchanged` genuinely fails, and what survives is that the moment-register data is unchanged). Non-vacuous (`isJointLift_pointerEvolve`). Conditional by design; the paper discharges the hypotheses. **Scope:** the single measurement stroke — the protocol's two-time laws and persistence are statements about a propagator family, not one map, and are not restated. Formerly: (`_of_` form, `CONVENTIONS.md` §8.3). For *any* flow `Φ` on the arena with **(1)** the `(m, θ₁, q)`-marginal of `pointerPrep` preserved, **(2)** the moments `m` **and** `θ₁` conserved, **(3)** pointer component = the fixed-weight propagator `exp(-it·H_eff(w))` — prove landing, persistence and the ε-Born sandwich hold verbatim. Pure measure theory; the substantive half, since it makes horizontal back-reaction **harmless**. Non-vacuous: the current fibrewise flow satisfies (1)–(3) with the base fixed. ⚠️ *Hypotheses restated %s.* The first draft said "(1) arena-measure preservation, (2) `basinIndex` conserved", and **both were wrong**: Born is measured against `pointerPrep`, a base-**Dirac slice** that is Liouville-null, so "Hamiltonian ⇒ Liouville" says nothing about it; and `pointer_landing` (the smooth witness's actual landing theorem) is hypothesised on `shrunkCell` membership, not on `basinIndex` — conserving `m` and `θ₁` implies both selectors, conserving `basinIndex` alone does not. ⚠️ Also: no perturbative softening is free — `circleCell` is half-open and `rep` jumps at 0, so the selector is discontinuous in `(m, θ₁)`; *approximate* conservation buys nothing without a separate collar estimate | **NO** |
> | A2 | **Paper** — ✅ **B1 discharged 2026-08-04; unblocked** | Write the Poisson argument: `{wᵢ,wⱼ} = 0` because the weights depend only on the moment coordinates `mᵢ` (abelian-torus moment map ⇒ pairwise commuting) and one register coordinate `θ₁`; hence `m` and `θ₁` are conserved, discharging A1's (2). Three things %s that must be in the paper: **(a) the dependency** — `clampDiv` is `max`/`min`, Lipschitz but **not `C¹`**, so `{wᵢ,wⱼ}` is *undefined on the current weights*: B1 is a **prerequisite**, not an independent nicety, and even after it the available smoothness is the universal-cover lift under `2ε < r`, `r < 1`; **(b) scope** — the commutation holds for `momentContext`, not for an arbitrary `ContextField`, and since the capstone's `every_basis` ranges over all bases the claim must read "for each fixed context, with *its own* moment family" (distinct contexts' families do not commute with each other); **(c) what the lift costs** — the base point `p` is **not** conserved (only its moments are), so `pointerEvolve_fst` (`… .1 = y.1 := rfl`) fails and with it `pointerEvolve_base_marginal_unchanged`, the theorem the corpus advertises as the smooth horn's no-collapse boundary. The honest replacement is "the **moment** marginal is unchanged" — say so, or a reader will notice the lift silently repeals a theorem the corpus leans on. **Bonus, verified:** the product form is not an idealisation — `couplingH w = ∑ⱼ wⱼ • pointerH j` is linear in `w` and the FS moment map is linear in its generator, so `𝓗 = Σⱼ wⱼ(x)hⱼ(q)` **identically** | **NO** (authorial) |
> | ~~A3~~ | **DONE 2026-08-04** (`SigmaLayer/ChartBracket.lean`, 4 pins) | ★ `poissonBracket_eq_zero_of_disjoint` — and the statement is **not** the naive one: `𝓗 = Σⱼ wⱼ(x)hⱼ(q)` is *not* momentum-free (it depends on the pointer's momenta), so "both independent of momenta" would be false of `𝓗`. What holds is **disjoint support**: `w` momentum-free, its position indices disjoint from `𝓗`'s momentum indices — exactly the arena's base-vs-pointer product structure. Plus `poissonBracket_comm_of_momentumIndep` (`{wᵢ,wⱼ}=0`), `hamiltonianField` (explicit in canonical coordinates — no `ω⁻¹` needed), and ★ `conserved_of_bracket_eq_zero`/`weight_conserved_of_disjoint`, which feed A1's `weights_conserved`. **Scope:** a chart *model*; nothing transports it to the arena — that transport IS A4. `dω = 0` unstated because in a canonical chart it is automatic, which is why this is weaker than the manifold claim. Formerly: — the formalisable fragment of A2. `Mathlib/Analysis/InnerProductSpace/KahlerForm.lean` already gives the pointwise Kähler triple and Mathlib has `fderiv`, so a *chart-level* `{f,g} = ω(∇f,∇g)` with `{wᵢ,wⱼ} = 0` and weight conservation is buildable as concrete computations. Would NOT give `dω = 0` or the global statement — a local theorem, weaker than the paper's, but machine-checked | **NO** |
> | A4 | **Linear fragment DONE 2026-08-06** (`Mathlib/Analysis/InnerProductSpace/HamiltonianVectorField.lean`, 4 pins + `SigmaLayer/PointerHamiltonianField.lean`, 1 pin) | The manifold identification `X_𝓗 = ω⁻¹d𝓗` itself. **What landed:** the identification at the linear level, where a single global chart covers everything — ★ `hamiltonian_duality` (`ι_X ω = dH` for any observable whose differential is `g`-represented, no inverse formed), ★★ `quadraticEnergy_hamiltonian_duality` (the Hamiltonian vector field of the quantum energy `½⟨x,Ax⟩` IS the Schrödinger field `-(i•Ax)` — Kibble/Ashtekar–Schilling as a theorem), and ★★ `coupling_hamiltonian_duality` (the same, on the witness's own fixed-weight generator `couplingH w` — so the fixed-weight loop closes at the formalisable level: energy → field (here) → flow (`rampedU_schrodinger`) → form-preservation (`schrodinger_flow_kahler_symplectomorphism`)). **Wall re-verified per the check-impossible-first rule, and it MOVED:** Mathlib's pin now carries `extDeriv` on **normed spaces** (`Analysis/Calculus/DifferentialForm/`, with `d²=0`) — the 2026-08-04 "no exterior derivative" note is superseded for flat space; manifold forms remain that file's own TODO. Flat `dω = 0` for the constant fundamental form **LANDED 2026-08-06** (`Mathlib/Analysis/InnerProductSpace/KahlerClosed.lean`, 3 pins): generic `extDeriv_const` (constant differential forms are closed — the Mathlib-gap lemma), `Kahler.fundamentalFormAlt` (ω packaged as a continuous alternating 2-form, pointwise-tied by `fundamentalFormAlt_apply`), ★ `Kahler.extDeriv_fundamentalFormAlt` (`dω = 0` on the flat tangent model). Foundational-triple-only | **Manifold form: YES** — §2a wall, same as A1/A3, but *narrowed*: the blocked residue is now exactly the quotient/manifold packaging (forms on `ℂℙ^{N-1}`/the arena), not the identification itself |
>
> ### B. Not blocked by anything — ordinary work
>
> | # | Size | Task |
> |---|---|---|
> | ~~B1~~ | **DONE 2026-08-04** | ~~**Substitute the `C^∞` profiles into the protocol stack.**~~ Landed: `SigmaLayer/SmoothProfile.lean` extracts the profile primitives *below* the pointer stack (they had lived **above** it, which is why the witness could not be built on them), and `pointerWeights` is now `smoothArcWeight`. The plateau interface being identical by construction, **every downstream proof transferred with no changes** — both targets green at 3773/3797 including all audit pins. New: `contDiff_pointerWeights_lift`. **A2's prerequisite is discharged: `{wᵢ,wⱼ}` is now a well-formed expression on the corpus's weights.** Deliberately NOT substituted: the time ramp, which is not a phase-space function and whose swap would change the capstone's `generation` field to carry a rate factor — recorded as B1b. Formerly: Confirmed outstanding by the Opus review: `grep` finds **zero consumers** of `smoothArcWeight`/`smoothPointerRamp`/`smoothRampedU_schrodinger` outside their own module, and the capstone's `generation` field is still the trapezoid, window-restricted `rampedU_schrodinger`. `PointerSmoothProfile.lean` proves the profiles and the global-time ODE, but `PointerProtocol`/`PointerBorn` still run on trapezoids. Recorded as "mechanical" — recording is not doing, and the fifth review was right to list it as outstanding |
> | ~~B1b~~ | **DONE 2026-08-04** (commit ad53638) — ⚠️ *this row sat marked-open for several commits after it was finished, because the edit that should have struck it through was in a script block that aborted on an earlier error; found 2026-08-04 while re-reading the list. The exact staleness class the whole day was spent correcting.* `pointerRamp` is now `(π/2)·smoothTransition`, `C^∞` (`contDiff_pointerRamp`), plateau interface unchanged so the two-time law, freezing and persistence were untouched. The one casualty, as predicted: `rampedU_schrodinger` is **window-free** but carries the rate factor `smoothTransition′(t)`, and the capstone's `generation` field was updated to match. Formerly: substitute the smooth *time ramp* (`smoothPointerRamp`) into the protocol. Not needed for A2 (the ramp is not a phase-space function); its consequence is that the capstone's `generation` field gains the time-dependent factor `smoothTransition′(t)`, trading the constant-generator form for a window-free one. A deliberate trade, not an omission |
> | ~~B2~~ | **DONE 2026-08-04** (`SigmaLayer/NullSeamLift.lean`, 3 pins) | ★★ `nullSeamLiftClosure` — the third horn on `T² × ℂℙ²`, real dimension 6. Giving the register its conjugate coordinate removes the parity obstruction; the construction is otherwise unchanged (the crossing reads `θ₁`, `θ₂` rides along as a conjugate variable should), so every transfer is a cylinder argument and Born stays **exact**. ⚠️ *Earned:* the parity obstruction. *Not earned, not claimed:* the symplectic **form** — still §2a-scoped (A4); even dimension is necessary, not sufficient. Formerly: — earns the symplectic vocabulary the third horn currently cannot claim (`S¹ × ℂℙ²` is 5-dimensional, odd). Same kissing-crossing construction; the seam/Born theorems should carry over |
> | ~~B3a~~ | **DONE 2026-08-04** (`SigmaLayer/PointerFrequency.lean`, 2 pins) | ★ `pointer_born_frequency` — the ε-Born **frequency** layer: i.i.d. trials of the smooth witness's preparation converge a.s. with the limit inside `[rⱼ−2ε, rⱼ+2(N−1)ε]` (`pointerSectorProb_mem_window` carries the `ℝ≥0∞` sandwich across `toReal`). Small exactly as scoped: LF1's `freq_tendsto_of_iid` is already generic over (space, probability measure, event), so only the transport was new. The limit is **bracketed, not pinned** — the ε-horn's price; `ε → 0` sharpens the window only *across* witnesses, since each `ε` is a different propagator |
> | ~~B3b~~ | **DONE 2026-08-05 — brick 2 landed the same day** (`SigmaLayer/PointerLudersMarginal.lean`, 6 pins), **closing the row and with it the "smooth-witness Lüders composition" extension**: ★ `pointerRelocate_measurePreserving` (the owed piecewise invariance — `measurePreserving_of_partition` over record cylinders + the no-record piece, each cylinder fixed by its own slot swap exactly because the relocation never moves the pointer), ★ `pointerLudersStroke_measurePreserving` (the **whole two-stroke composite conserves Liouville** — collapse as relocation on the smooth horn), `pointerProtocol_outcomeSector` (sector = the brick-2b propagator's record-cylinder preimage), ★★ `pointer_luders_marginal` (**the Lüders theorem**: conditioned on the outcome-`i` sector — a base cylinder, the bank plays no part in outcome selection — the post-stroke system marginal IS the slot-`i` calibration), ★ `pointer_luders_born` (follow-up statistics = the collapsed state's Born weights, any context field), ★★ `pointer_luders_born_prep` (on the witness's own `pointerPrep`: `2ε < rate i` makes the conditioning non-vacuous via the ε-Born lower bound — records **and** update on one arena). *Sizing postmortem:* the warning "not a wiring job" was right about the shape but the cost had already been paid — `cond_prod_cylinder` and `Measure.map_eval_pi'` (swap tranche) plus `measurePreserving_of_partition` (staged exactly for this) carried both halves, and `swap_luders_marginal`'s proof skeleton transported nearly verbatim. One new snag for the ledger: unification of `pointerRelocate_of_record`'s implicit arena point whnf-exploded through `couplingUU` (the matrix exponential) — fixed by stating the equation with an explicit `show` goal first so the implicit is solved structurally, never by reduction (same family as the B5-geom module-system defeq notes). Scope unchanged and stated in-module: rank-one only, one bank per measurement, the composite is not a `MeasurementProtocol` (relocation is a triggered map; its Hamiltonian generation stays the same recorded extension as the swap witness's), and the ε lives in *which* outcome occurs — the conditioned marginal itself is exact. Formerly: **BRICK 1 LANDED 2026-08-05** (`SigmaLayer/PointerLuders.lean`, 3 pins) — the composed arena `(Σ × ℂℙ^N) × bank`, the readout `pointerIndex` off the record regions, the record-triggered `pointerRelocate` (★ never moves the pointer, so the record survives its own relocation), ★ `pointerBankSwap_measurePreserving`, and the two-stroke composite `pointerLudersStroke` defined. **Brick 2 owes:** piecewise invariance of `pointerRelocate` itself (the partition argument `swapG` uses, with record cylinders for register arcs) **and** the conditioned post-measurement marginal — the actual Lüders theorem. ⚠️ *Sizing note 2026-08-05:* the conditioning is where the swap and join witnesses' difficulty actually lived (`swap_luders_marginal`, `join_luders_marginal` are both large), so brick 2 is **not** a wiring job and should start from a clean context. The staged `Mathlib/MeasureTheory/PiecewisePreserving.lean` is the likely tool for the invariance half. Until then this is arena-and-dynamics, not collapse. Formerly: — the genuinely large half, and not a wiring job. The smooth witness *provably does not collapse* (`pointerEvolve_base_marginal_unchanged`), so composition needs a **new arena** carrying pointer *and* bank (`KSigma × Pointer × bank`), a two-stroke protocol whose second stroke is triggered by the **pointer record** rather than a torus arc, and the conditioning computation for the post-measurement marginal. That is the scale of the original swap/join witnesses |
> | ~~B3, formerly~~ | superseded by B3a/B3b | Smooth-witness Lüders composition + the ε-Born LLN. The LLN half is the smaller one: the i.i.d. frequency layer already exists and applies to arena measures |
> | ~~B4~~ | **DONE 2026-08-04** (`SigmaLayer/PovmSectorBorn.lean`, 2 pins) | ★★ `povm_sector_born` — the join protocol's **outcome sector** at the dilated preparation carries exactly `⟨ψ, Eᵢψ⟩`, i.e. the *dynamical* statement the prose had wrongly attached to the selector-level theorem; `povm_sector_born_canonical` for every POVM. A two-line composition of `join_sector_born` with `sum_block_normSq_dilate` — which is itself the evidence that the audit's finding was a **description** defect, not missing mathematics. Formerly: (the shape `join_sector_born` has), discharging the scope note the 2026-08-04 audit added to `PovmDynamics.lean` |
> | ~~B5-geom~~ | **DONE 2026-08-05** (`SigmaLayer/NoRecordGeometry.lean`, 4 pins) | ★ `noRecord_subset_closure_strict` (the core, over an arbitrary index set of record moments — one proof serves the all-`j` and pair consumers), ★ `noRecord_subset_closure_interior` (the row's stated lemma), ★ `recordRegion_pair_compl_regular` (the exact `hreg` shape `posMeasure_noRecord_of_correlates` consumes), and ★★ `posMeasure_noRecord_pointer` — **B5 closed outright**: the third leg instantiated on the pointer with *no* geometric hypothesis left. The recorded construction was correct and was used verbatim (`feedReady` = feed weight into the ready component; both branches of the family; the strict drop via `div_lt_div_of_pos_left`). The recorded snags were dodged as prescribed: no `set`/`rep` binding — `induction q using Projectivization.ind` gives the representative with zero defeq debt — and both limit branches done by hand (`tendsto_one_div_add_atTop_nhds_zero_nat` + `Tendsto.smul_const`/`Complex.continuous_ofReal`, no `simpa`). One *new* snag class, recorded for the next module: under the module system the elaborator would not delta-unfold `feedReady`/`recordRegion` during unification **when metavariables were present** (`Function.update_self` as a term proof failed where `rfl` succeeded on the identical goal) — the working idioms are `rfl`, `show`-then-exact, typed `have`s to cross set-membership/inequality defeq, and explicit `f`/typed `(0 : Fin (K+1))` arguments to `Finset.add_sum_erase`. Estimated one focused hour; took about that. Formerly: The single remaining hypothesis of B5: `noRecord_subset_closure_interior`, i.e. `{q | ∀ j, mⱼ₊₁ q ≤ ½} ⊆ closure {q | ∀ j, mⱼ₊₁ q < ½}` on `Pointer K`. **The construction is settled and correct — feed weight into the ready component, leaving all record components untouched, so every record numerator is fixed while the norm strictly grows and every record moment strictly drops.** Family: `c n = if v 0 = 0 then ((1/(n+1) : ℝ) : ℂ) else ((1 + 1/(n+1) : ℝ)) • v 0` — scaling in the second branch **keeps the phase**, which is what makes convergence immediate and avoids any chart argument. Perturbed vector `w n = toLp 2 (update (ofLp v) 0 (c n))`. **Two supporting lemmas compiled clean and are worth rewriting verbatim:** `update_zero_succ` (record components unchanged, via `Function.update_of_ne (Fin.succ_ne_zero j)`) and `norm_sq_update_zero_gt` (norm strictly grows, via `EuclideanSpace.norm_eq` + `Real.sq_sqrt` + splitting the sum with `Finset.add_sum_erase` at `0`). The strict-drop step also worked (`LF4.momentMap_mk` + `div_lt_div_of_pos_left`). **WHERE IT SNAGGED — elaboration, not mathematics:** (i) binding the representative with `set v := q.rep` creates a defeq mismatch later (`v 0` vs `(Quotient.out q).1.ofLp 0`) that breaks the final `Tendsto` — **use `obtain`/plain `let`-free style instead**; (ii) the ℂ-valued `Tendsto c atTop (𝓝 (v 0))` needs the two branches done by hand rather than `simpa`, since `tendsto_one_div_add_atTop_nhds_zero_nat` does not line up with `1 + (↑n+1)⁻¹` under `simpa`. Closing the ray-limit uses `Projectivization.continuous_mk'` composed with `tendsto_subtype_rng.mpr`, then `mem_closure_of_tendsto`. Estimated one focused hour from a clean context |
> | ~~B5~~ | **DONE 2026-08-05** — closed outright: the geometric hypothesis is discharged (B5-geom row above, `SigmaLayer/NoRecordGeometry.lean`) and ★★ `posMeasure_noRecord_pointer` states the leg on the pointer with every hypothesis either the witness's own dynamics or a theorem — **exact-a.e. records force Dirac calibration, as a theorem**. Scope unchanged: this is the *local* leg; general exhaustiveness over all arenas stays research. (Earlier same-day state:) **DONE-MODULO-GEOMETRY 2026-08-05** (upgraded from PARTLY DONE the same session — the forcing step is now proved: `exists_noRecord_of_meets_two` inverts `no_everywhere_correlation`, and `posMeasure_noRecord_of_correlates` chains it to the bound. **All dynamical hypotheses discharged**; one *geometric* hypothesis remained — that the no-record set is contained in the closure of its interior, true of moment regions but not then constructed. Original entry:) **PARTLY DONE 2026-08-04** (`SigmaLayer/SharpenedNoGo.lean`, 2 pins) — ★ `posMeasure_noRecord_of_isOpenMap`: an **open-map** propagator on an **open** ready set cannot hide the no-record set in a null set, because the image is an open neighbourhood and any neighbourhood of a boundary point meets the no-record **interior**. That is exactly why the Dirac calibration escapes. ⚠️ **Sharpened, not closed**, and two things are explicitly *not* proved: (a) the *forcing* step — that some ready state must land in `closure (interior R)` — is a hypothesis here, needing `no_everywhere_correlation`'s connectedness plus a regularity condition on the no-record set; (b) the **quantitative** `O(δ)` fattening I had asserted in `NullSeamWitness.lean` is **withdrawn** — it needs an estimate on the landing moments, not soft topology, and has been corrected at source. Formerly:: continuity + a positive-width ready region force a positive-measure no-record set, so exact-a.e. records force Dirac calibration — the trilemma's third leg as a theorem |
> | ~~B6~~ | **RETIRED 2026-08-06 (author decision, `CONVENTIONS.md` §7): Mathlib PRs, while nice, do not advance CSD and are not a need of this repository — off the todo list, not deferred.** The staging discipline is untouched (Cat-1 files stay CSD-free in `CsdLean4/Mathlib/` as code hygiene), the option §7 keeps open stays open, and the readiness record below stands so that a future decision to upstream starts from a green sheet rather than a scoping exercise. Formerly: **M** — **now the queue head**: the 2026-08-04 deprioritisation ("should not jump ahead of B3b/B5") is spent — both closed 2026-08-05/06 — and F1's interface pass plus the readiness scan below removed the known blockers | Mathlib upstream batch 1 — was **67** staged files (the row's earlier "68" was a one-off; recounted 2026-08-06 via `git ls-files`). ⚠️ *Question answered 2026-08-04 — they are NOT residue.* The ~25 `QuantumInfo/Reversible/` arithmetic files are **live corpus**: 26 references from the root module and five `Empirical/QM/Measurement*Adder*` modules import them. The ecdsa.fail extraction removed the ECDLP track; the reversible-arithmetic circuits stayed because the QM-side measurement-adder work is built on them. So the batch is genuine, none droppable. **Readiness scan 2026-08-06 (post-F1):** (i) **zero corpus imports** — no staged file imports `LF*`/`SigmaLayer`/`Empirical`; the staging discipline held; (ii) ~~one real namespace blocker~~ **CLEARED 2026-08-06**: `CSD.StoneC1.*` renamed to `Matrix.StoneC1.*` (dotted top-level names kept — the SMul-diamond note in the file explains why no `namespace` block), all corpus consumers + AxiomAudit pins updated in the same commit, full library green; the five other `CSD.` hits are docstring cross-references, edited at PR time; (iii) **naming clean** — the staged tree's (E) entries are all compliant camelCase (long-but-descriptive), no theorem-style def names; (iv) `Reversible/` no-API rows = 0 after F1, so the batch goes out wearing the interface mathlib review will demand |
>
> ### C. ~~Yours, not mine~~ — **MOVED OUT OF THIS REPOSITORY'S QUEUE 2026-08-06**
> (author decision, same stroke as B6: paper work is not a need of the csd-lean4
> repository; it is tracked with the author's writing, outside this queue. The rows
> stay below as the *record of what the papers owe the corpus*, because the corpus is
> the source of truth for the divergences — but nothing here is repository work.)
>
> | # | Task |
> |---|---|
> | ~~C1~~ *(off-queue)* | **Paper C A7** — amend base-only `Ωᵢ(M) ⊂ ℂℙ^{N-1}` to the fibred ontic-sector reading |
> | ~~C2~~ *(off-queue)* | **Paper C A2 / TN6 alignment** — the papers sit *behind* the corpus, and the gap has **grown** since this row was written: the measurement fork is a **trilemma** with its third leg now a theorem (B5), the smooth witness's boundary is fibrewise **and it now carries a Lüders update** (B3b), POVM Born is **sector-level** (B4 discharged the selector-only boundary this row still names), and mixed-degenerate updates exist at every rank (D3a). A reader coming from the papers is told something the corpus has corrected several times over |
> | ~~C3~~ | **RESOLVED 2026-08-06** (`v1.1.0-finite-qm-closed` tag notes + README History table): `v1.0.5-trilemma`'s pre-audit annotation stands as published history (never rewritten); `v1.0.6-audited` supersedes it in the tag graph and the README History row for `v1.0.6` now records that supersession in prose, so a reader of the release history meets the correction without archaeology. Formerly: Decide whether `v1.0.5-trilemma`'s annotation (written pre-audit) should be superseded in the release notes; `v1.0.6-audited` already supersedes it in the tag graph |
>
> ### D. Guard and hygiene residue
>
> | # | Size | Task |
> |---|---|---|
> | ~~D1~~ | **DONE 2026-08-06** (`check-claims.sh` (8b), the scope-expiry ledger) | Every open-scope file now carries the BACKLOG row its boundary waits on (`DECLARED_SCOPE_WAITS`), or `none` for permanent/rowless boundaries; when a tagged row is struck `~~DONE~~`, the check **fails** until the site is superseded at source. Negative-tested (striking D3 fires on `MixedLuders.lean`). §E items got stable IDs E1–E5 so long-horizon waits are taggable. *Motivating case found while building the ledger:* `PointerGeneration.lean` still said the Lüders composition "is a recorded extension, not delivered here" a day after B3b delivered it — fixed at source same commit. Honest residue: `none`-tagged boundaries stay untracked by construction (the improvement is that every boundary is now *classified* and the tracked ones expire mechanically); the (8) KNOWN-LIMIT note is narrowed, not deleted. Formerly: Close check (8)'s documented blind spot: it fires when a boundary claim is added or removed, **not** when the fact beneath a claim that stays put changes. Candidate: cross-reference each open-scope site against BACKLOG rows marked DONE |
> | D4 | **record only** | Check (7) is **structurally blind to `born_*` names** (independent Opus review, 2026-08-04) — `nullSeam_born_left/right` assert Born for statements containing no preparation, ray or moment map. Not closable as an inventory: there are **278** born-named declarations. Recorded rather than pretend-closed |
> | ~~D2~~ | **DONE 2026-08-06** (`check-claims.sh` (7d), the abstract-Sigma instantiation inventory) | Every declaration whose **result type** is `KahlerOnticSetup` must appear in `DECLARED_KAHLER_INSTANTIATIONS` with its concrete `Sigma` and a verified EVEN real dimension — set equality, like (7a). The signature scan strips parenthesised binder groups so consumers `(d : KahlerOnticSetup N)` are exempt (they inherit their argument's parity and assert nothing). Six sites in-tree, all read from their `Sigma :=` field and all even (`trivialKahlerOnticSetup`, `unitaryFlowSetup`, `rotationSetup`, `manyToOneSetup`, `manyToOneRotationSetup`, `manyToOneSchrodingerSetup`). Formerly: Close check (7)'s: it cannot see an odd-dimensional *instantiation* of an abstract `Sigma` (`KahlerOnticSetup` and friends carry no parity claim of their own) |
> | ~~D3a~~ | **DONE 2026-08-06** (`SigmaLayer/MixedJoinLuders.lean`, 4 pins) | Degenerate-on-mixed: ★ `mixed_join_sector_born` (the mixed **block** Born rule, `∑_{k:bk=i} Tr(ρ|eₖ⟩⟨eₖ|)`, via the block spectral bridge `spectral_block_born_eq_traceForm` — sum interchange + one rank-one bridge per block member), ★ `mixed_join_post_bayes` (Bayes on a degenerate outcome, same `cond_finsetSum` engine as rank one), ★★ `mixed_join_luders` — **block Lüders composed with Bayes**: the conditioned system marginal is the Bayes mixture of the per-component block posts `epistemicMeasure [Πᵢψⱼ]`, at rank ≥ 2 genuinely distinct (the record does NOT erase classical ignorance — the density-operator update `ρ ↦ ΠᵢρΠᵢ/Tr(ρΠᵢ)` as a mixture, contrasting `mixed_luders_followup`'s rank-one vertex collapse). Rode `JoinClosure` exactly as `MixedLuders`'s scope note predicted — "mechanical" was accurate here. Stated under `hproj` (every component meets block `i`); the zero-likelihood filtered refinement is bookkeeping, recorded in-module. `MixedLuders`'s scope note superseded at source; expiry ledger re-tagged |
> | ~~D5~~ | **DONE 2026-08-13** (the `check-contradictions.sh` shortlist, worked as the guard's own header instructs — "an advisory reading list, not a gate") | The 8 flagged identifiers re-read. **Seven benign**, all in the guard's documented false-positive classes: filename-as-identifier (`BACKLOG.md`); window-slicing on same-statement pairs (`isFubiniStudyKahler` — `NonTrivialSetup` and `ManyToOnePillars` say the identical thing); a correction-narrative line (`CircleFibre.lean` vs the 2b′ retraction block); scope-compatible pairs (`SectorData` — HY-3's doc sweep vs the Gates placeholder TODOs; `PointerInvariantOn` — proved on `ShearWitness`, deliberately a hypothesis in the `RecordPersistence` interface; `productSector_hasFubiniStudyPushforward` and `FiniteQMClosure` — proved-with-a-known-open-extension, the guard's own pre-classified example class). **One real, and the guard earned its keep**: `TheoremTargets.lean`'s B7 docstring said `BornFromFlow` "is proved from this together with a Birkhoff hypothesis in concrete models" — **nothing in the corpus proves `BornFromFlow`**, conditionally or otherwise (the audit pin at the `UniqueErgodicity` block and that module's own header both say so); the exact "prose stronger than the statement" class the necessity audit catalogues. Fixed at source with a dated correction note; T3/L7 stay open (= the SO-1 face). *Guard lexicon improved in the same stroke:* `not proved` added to the open-token list — `UniqueErgodicity`'s "What is NOT proved here" was classifying as SETTLED via the bare `proved` token (negation-blindness); such lines now carry both tokens and are skipped as nuanced. The shortlist now reports exactly the 7 benign pairs, by design — the guard has no allowlist, and its instruction is to fix the wrong document, never to soften truthful lines into agreement |
> | ~~D3b~~ | **DONE 2026-08-12** (`SigmaLayer/NullSeamGeneralN.lean`, 5 pins) | ★★ `nullSeamGenClosure` — **the third horn at every `N ≥ 2` and every weight vector**: continuity, measure invariance, exact records off an `N`-point null seam, **exact** Born mass `r i` per cell; `nullSeamGenClosure_uniform` the concrete `1/N` witness. **The construction departed from the recorded per-boundary-rotation sketch, deliberately**: gluing `(j, j+1)`-plane rotations cannot be full-matrix continuous at the wrap (monodromy — the product of `N` quarter-turns is not `1`), so the landing amplitudes are **plateau tents** (`max 0 (gap − infDist to the closed cell)`, cells as closed balls at the corpus's own `cellMid`, `gap = min r / 2`: at most one foreign tent nonzero anywhere, equal tents exactly at the kiss) and the unitary is a **single amplitude-polynomial rotation** `I − f₀f₀ᵀ − aaᵀ + af₀ᵀ − f₀aᵀ` (orthogonal for every unit `a ⊥ f₀`, `seamRotationR_orthogonal`) — continuity reduces to continuity of the tents. Reused: `loSum`/`cellMid`/`rep` (the corpus's own cumulative-cell layer — no new sum machinery), `volume_closedBall`, the two-cell circle toolkit, the `IsOpenQuotientMap` continuity descent and skew-product invariance verbatim. Two-cell witness unchanged as the minimal exhibit; its "general N recorded" scope line superseded at source. Original row: General-`N` for the null-seam witness. "Mechanical" is right about *content* (no new mathematics) but not size: the two-cell witness is an 827-line bespoke construction (circle toolkit, sign/angle profile, the `(1,2)`-plane kissing rotation, per-side landing, seam-null, per-side Born, continuity descent, skew-product invariance), and general `N` re-derives essentially all of it with cyclic index bookkeeping. Construction notes so nothing is re-scoped from scratch: cells = arcs at cumulative weights `c_j = Σ_{k≤j} r_k`; **N seams**, one per consecutive-cell boundary; at boundary `j` the same kissing-crossing rotation but in the `(j, j+1 mod N)`-plane of `ℂℙ^N` (consecutive records kiss at `m_j = m_{j+1} = ½` — `momentMap_add_le_one` gives the geometry at every `N` already); the profile is a piecewise-glued angle function, continuous because each piece's plateau matches its neighbours at the seam; cell-`j` Born mass = `r_j` exactly (arc measure); seam-null = `N` points. A dedicated session's job, from a clean context |
>
> ### E. Longer horizon (unchanged in content; given stable IDs 2026-08-06 so the
> scope-expiry ledger in `check-claims.sh` (8b) can name them)
>
> | # | Item |
> |---|---|
> | ~~E1~~ | ~~Shor-9 concatenation and the Lindblad tier~~ **DONE 2026-08-13** — see the Q5 row (ShorNine.lean + LindbladSemigroup.lean); CP of `e^{tℒ}` stays on the GKSL construction row as the Mathlib-scale residue |
> | E2 | The operator-convexity → DPI → SSA ladder (**L**, still gated on the `CStarMatrix` instance wall) |
> | E3 | Composable finite-key QKD (**XL**) |
> | E4 | The CV programme |
> | E5 | The two research frontiers — the fibre-from-dynamics question and general trilemma exhaustiveness |
>
> ### F. Library-grade quality programme (adopted as standard 2026-08-06 — `CONVENTIONS.md` §9)
>
> Author decision 2026-08-06: the corpus aims for **library-level code** — mathlib review
> standards on the `Mathlib/`-staged tree, the same bar with documented physics-notation
> exceptions elsewhere (the Lean-QIT/Physlib route). Motivated by Ilin & Nugent
> (arXiv 2606.13925); measured by `scripts/check-review-surface.sh` against
> `docs/review-surface-baseline-2026-08-05.txt`; rules in `CONVENTIONS.md` §9. Ordered by
> forcing function, then cost-effectiveness. **F1 is the only externally-forced item**; a
> mathlib reviewer at B6 will demand exactly what it delivers, so it precedes B6.
>
> | # | Size | Task |
> |---|---|---|
> | ~~F1~~ | **INTERFACE PASS DONE 2026-08-06; one named residue** | **`Reversible/` API pass, delivered**: interface lemmas across eleven files — per-constructor lemmas (`gateCost_X/CX/CCX/swap`, `gateWires_*`), per-field wire lemmas for the concrete layouts (`modAddLayout2_*` ×8, `modSubLayout2_*` ×6, `cModAddLayout2_*` ×10, `modReduceLayout2_*` ×6, `cuccaroMulModLayout3_*` ×6), value-level lemmas for the parameterised banks (`cBank_*_val` ×8, `wAdd_*_val` ×10, `wDbl_*_val` ×8, `wMulLoop_bank`/`_X_val`), the bank identifications (`constMulLayout0/1/2/8_bank`), and `andSumPass_def`/`layerWF_iff`. All non-`@[simp]`; no existing proof touched; full library green. **Success metric MET: `Reversible/` (B) rows with `api=0` = 0** (corpus-wide no-API 25 → 8, the remainder being the `private`-exempt `boxSet` + seven `through≤2` tail items). *Build note:* two structure literals (`modAddLayout2`, `cBank`) had their `where`-blocks split by a mis-anchored insertion mid-pass — caught by the build, fixed before commit; the anchor for such insertions is the LAST `h`-field, not the last data field. ~~**Named residue (clean-context item, §9.4):** extract stage lemmas from `cuccaroModAdd_spec`~~ **Residue CLOSED 2026-08-06 by documented decision, not refactor**: the full proof was read end-to-end and assessed — every step is a one-to-three-line citation of a named block lemma (`cuccaroAdd_correct`, `cuccaroSub_correct`, `maskCopy_*`, the `*_preserves_*` frames), so the 313 lines are the irreducible bookkeeping of nine stages × five tracked invariants; per-stage invariant records would add ~150 lines of statement boilerplate for indirection without content. Verdict recorded in the theorem's docstring (which also replaced a misleading legacy "Placeholder spec" doc). **F1 is closed; B6 is unblocked on the interface front** |
> | ~~F2~~ | **DONE 2026-08-06** | Witness-scalar and plumbing interfaces landed: `alphaOff`/`betaOff` value lemmas + `deltaOff` (def + all four setting-pair values), `aAngle_def`/`bAngle_def`/`baseAngle_def` (`CGLMPQudit`), `om2_def` (`MUB3Volume`), `innerProd_def` (`SternGerlach`), `joinArenaMeasure_def`, `paramMeasure_def`, and branch lemmas `joinFstAux_coe_of_ne/_of_eq`, `blockCollapseAux_coe_of_ne/_of_eq` (the exact case splits their unfold sites re-derive). All non-`@[simp]` so no existing proof changes behaviour; unfold sites left in place (converting them is on-touch churn, §9.4 spirit). **`boxSet` deliberately skipped**: it is `private` — §9.1's local-plumbing exemption, recorded here rather than silently dropped. Full library green |
> | F3 | **S — decisions taken 2026-08-06 (`CONVENTIONS.md` §9.2); renames land on-touch** | **Naming standard.** Decided: mathlib naming strict in `Mathlib/`-staged files; literature notation blessed in physics modules where it mirrors the cited source (the `HardyKahler` `A'`/`B'` family — ten (E) rows — is *compliant* under this rule, docstring citation required); theorem-style def names (`*_realisable_for` ×9, `b92_encode`, `P_st`) are defects of form, renamed when next touched, never swept. (E) tracks; no sweep row exists on purpose |
> | F4 | **standing rule, no sweep** | **Rule of two** (`CONVENTIONS.md` §9.3): at a second near-use, generalise the existing single-use lemma instead of cloning. The 1,109-row (C) list is the where-to-look index; obligation `Prop`s are exempt by design (docstring must name the discharging witness). Per-file top offenders (`WignerRigidity` 24, `ShorCore` 21, `CanonicalTypicality` 17, `Grover` 16) get attention on touch only |
> | ~~F5~~ | **DONE 2026-08-06** | Auditor refinements landed: (a) (A) excludes pinned defs and first-letter-case closure twins (119 → 111 at the time; the 8 exclusions verified individually — 6 direct pins, 2 twins incl. `smoothWitnessClosure`); (b) baseline v2 captured (`docs/review-surface-baseline-2026-08-06.txt` — metric v2 + post-F1/F2 corpus; the 08-05 file kept as the pre-standard record) with the §9.5 discipline in the script header; (c) non-blocking unchanged. Known residual: (B) cannot see `private` (the `boxSet` row is §9.1-exempt but still listed) — acceptable noise, noted here rather than special-cased |
> | F6 | **record** | **Ratchet discipline live from now**: any landing that increases the (B) no-API count or adds theorem-style def names states so with justification in its commit message. First exercised retroactively this week: brick 1's `pointerLudersStroke`/`pointerLudersMeasure` entered the lists on 2026-08-05 and left them when brick 2 consumed them — the intended lifecycle for definition-shaped debt: visible, short-lived, paid |
>
> ### G. External-review intake follow-ups (codex 2026-08-06 — classification + verification
> evidence in `audit-sweep-plan.md`'s intake section; these rows are the open remainder)
>
> | # | Size | Task |
> |---|---|---|
> | ~~G1~~ | **DONE 2026-08-06** (transport-theorem route, author decision; `LF2/Preparation.lean`, 2 pins): `MeasureBridgeData.integral_comp_pi` (∫ f∘π dμL = c.toReal • ∫ f dμFS — `bridge_eq` extensionally consumed) + `OperationalPackage.fromPreparation_liouville_apply` (Liouville-prepared operational probability IS the μFS-integral at c = 1); the "Symmetry enters via the bridge argument" docstring over-claim corrected to cite them; `check-semantic-mutations.sh` now REQUIRES the transport theorems (deletion = re-review trigger); CL-003 → qualified. Foundational-triple verified in isolation. Formerly: **F-01 — the LF2 `bridge` argument.** `OperationalPackage.fromPreparation` takes `bridge : MeasureBridgeData` type-level only (deliberate, documented; verified 2026-08-06: zero `bridge_eq` consumption sites corpus-wide). Decide: drop the argument, or state a transport theorem that extensionally consumes `bridge_eq` (an ontic-volume computation carried into the projective probability). Either way, fix `born_rank_one_direct`'s docstring line "Symmetry enters via the `bridge` argument" — it asserts load-bearing status the term dependency does not support. Current state pinned by `scripts/check-semantic-mutations.sh` (CL-003) |
> | ~~G2~~ | **DONE 2026-08-06**: `EffectFn.lean` retitled "the quadratic-form representation layer" with an explicit F-02 scope note (Born-from-volume citations redirected to the LF4 engine); all "volume-ratio" phrasing in `EffectFn`/`Preparation` renamed/reworded. Formerly: **F-02 — docstring precision in LF2.** Reword the "volume-ratio foundational" framing in `LF2/EffectFn.lean` + `LF2/Preparation.lean` (`effectProjFn` is `Re(v†Ev)` by definition — a representation layer); reserve "Born from volume" for the LF4 engine (`MomentBornN`, `BornRegionUncond`). Same genus as the 2026-06-11 item-2 fix |
> | ~~G3~~ | **DONE 2026-08-06** (tighten-keep-name, author decision): the abstract `Prop` pairs replaced by concrete fields `kahler_pointwise : IsFubiniStudyKahler N` + `liouville_isProbability : IsProbabilityMeasure liouvilleMeasure` (the latter an instance — `IsProbabilityMeasure S.liouvilleMeasure` now automatic); all three instances (`trivialKahlerOnticSetup`, `unitaryFlowSetup`, `manyToOneSetup`) carry identical obligations; consumer `unitaryFlowSetup_liouville_isProbability` kept as thin projection for pin stability; PLACEHOLDERS.md / connectivity-manifest L1 / reconstruction-status updated. Manifold residual (dω = 0, top-power) unchanged, honestly open at L1. Formerly: **F-04 — `KahlerOnticSetup` interface.** Consumers quantified over the structure cannot extract the Kähler laws from `kahler_condition : IsKahlerSector` (instances all supply the proved `IsFubiniStudyKahler` core — verified; the weakness is interface-level, not instance-level). Decide: rename per the reviewer (`ProjectiveMeasureFlowSetup`) or tighten the `Prop` pairs to the concrete pointwise-Kähler interface now that `IsFubiniStudyKahler` exists; full strengthening stays gated on Mathlib exterior calculus (connectivity L1) |
> | ~~G4~~ | **DONE 2026-08-06** (`SigmaLayer/SwapLuders.lean`, 1 pin): ★ `swap_luders_iff_calibrated` — post-outcome-`i` system marginal `= τ` ⟺ `ν i = τ`, both directions of "the update is calibration-encoded" as one theorem (forward = `swap_luders_marginal`, converse = the new content); CL-025 updated. Foundational-triple verified in isolation. Formerly: **F-05 — minimal calibration theorem.** `swap_luders_marginal` gives calibration ⇒ Lüders; the new content is the converse: which apparatus hypotheses are *equivalent* to Lüders behavior on the swap witness. Makes "the update is calibration-encoded, not assumption-free" a theorem instead of a scope note |
> | ~~G5~~ | **DECIDED 2026-08-06** (author) | **F-06 — capstone naming: keep names, rename on-touch (F3 class).** `FiniteQMClosure` / `ProjectiveMeasurementCapstone` keep their names; the docstrings carry the witness-index framing, and any future touch renames per the F3 convention (never swept). The L-size genuinely-unified closure (single arena/preparation/dynamics/measurement interface) stays a *listed candidate*, deliberately not queued next to the record-layer frontier |
> | ~~G6~~ | **DONE 2026-08-06** (root half): the 34 root-unreachable non-test modules added to `CsdLean4.lean` (default root now reaches every non-test module — re-verified 0 missing; full library green, 3809 jobs). Reverse-layer imports (16 edges) stay on-touch per LF4-todo §10 — deliberately not swept. Formerly: **F-07 — root repair.** Add the 34 root-unreachable non-test modules (verified list: the SigmaLayer pointer/record tranche + `Empirical/CSD/Eraser*` + `HamiltonianVectorField`/`L2OpNormEntry`) to the `CsdLean4` root, or adopt the reviewer's generated per-layer aggregates; the 16 reverse-layer imports move down-layer on-touch per LF4-todo §10 (no bulk refactor). Audit coverage is intact meanwhile (union of the four roots reaches all 443) |
> | ~~G7~~ | **DECIDED + DONE 2026-08-06** | **`ci.yml`: `windows-latest` leg dropped** (author decision — local development is on Windows, so Windows breakage surfaces immediately anyway; the CI leg roughly doubled cost on the slowest runner class). The reviewer's script wiring is kept: the three new checks join the blocking guard step, the vacuity/contradiction/review-surface trio runs as the advisory step |
> | ~~G8~~ | **DONE 2026-08-06** (`CsdLean4/Headlines.lean`) | **Headline facade module landed**: imports exactly the 27 modules carrying the 30 `validation-claims.tsv` claims, with a full-name **drift guard** (30 top-level `example := @…` lines — a rename/namespace-move/deletion of any headline breaks the build) and a new `check-validation-ledger.sh` block enforcing ledger↔facade import sync. Immediate payoff: the guard caught **four wrong ledger constants** on creation day (CL-025/026/027 recorded `CSD.SigmaLayer.*` for `CSD.RecordLayer.*` theorems; CL-001 missed the `OnticSetup.TrialModel` prefix) — all fixed in the tsv. Imported by the root; full library green. Root rename to `All` NOT taken (kept `CsdLean4` exhaustive; the facade is additive). Formerly: the good half of the codex session's step-4 rearchitecture suggestion |
> | ~~G9~~ | **DONE 2026-08-06** (`Tests/AxiomAudit/` ×8 + umbrella) | **AxiomAudit split by layer, landed**: all 1,709 pin blocks re-sorted by pinned constant's namespace into eight part files (`Foundations` 49, `EmpiricalQM` 174, `EmpiricalCSD` 302, `LF4` 216, `Dynamics` 138, `SigmaLayer` 370, `MathlibStaging` 408, `Extensions` 52 — count-invariant verified), each importing the `CsdLean4` root so placement never affects resolution; the umbrella keeps the `CsdLeanTests` target and coverage story unchanged. **Measured: the full pin sweep now runs in ~26 s wall on a built tree** (parts parallel, largest 14 s) vs the ~13-minute serial monolith; layer-local gate = `lake build CsdLean4.Tests.AxiomAudit.<Part>`. `check-axiom-imports.sh` updated to collect pins from the parts. Codex session step 5 |
> | G10 | **record (declines, 2026-08-06, author decision)** | **Rearchitecture sweep items assessed and NOT adopted:** (a) SigmaLayer role split (codex step 3) — real point, 111 mixed-role files, but file moves across the active frontier layer churn imports/pins/docs mid-programme; **deferred to a consolidation release** when the record-layer tranche stabilizes; (b) corpus-wide shrinking of `@[expose] public section` — consumer-facing benefit delivered by G8 at zero risk; de-exposing changes cross-module defeq (the B5-geom/pointer-unification trap class) — **declined as a sweep**, at most a CONVENTIONS decision for new modules on-touch; (c) reverse-layer import sweep (codex step 2) — stays extract-on-concrete-consumer per LF4-todo §10, **no bulk refactor** (reaffirmed) |
> | ~~G11~~ | **MOVED OFF THIS QUEUE 2026-08-07** (author decision — owner-tracked with the author's own review work, same stroke as §C; not repository work) | **CL-024 residue: human hand pass** on the five highest-risk Wigner tactic blocks named by the 2026-08-06 audit — `sign_link_core` (the global-sign crux), `diagReducedMap_fixes_two_level_general`, `diagReducedMap_fixes_three_level`, `two_level_imrelphase_of_flips` + the `cnorm_±I_sq` conventions, `diagReducedMap_complexSign_all`. ~~The up-to-phase uniqueness formalization~~ **DONE 2026-08-06**: matrix form already existed (`PhaseRigidity.lean`, missed by the audit — both-ways correction recorded in the intake); the `projMap`/`conjProj`-vocabulary form landed in `WignerUniqueness.lean` (`exists_unit_smul_of_projMap_eq` + antiunitary twin via `conjProj_conjProj`, 3 pins). The classical Wigner statement is now complete in-corpus (existence + uniqueness + downstream exclusivity); the hand pass alone promotes CL-024 qualified → validated |
>
> ### H. Quantum-chaos workstream (adopted 2026-08-06 — one-repo architecture, `external-library-map.md`)
>
> | # | Size | Task |
> |---|---|---|
> | ~~H1~~ | **DONE 2026-08-06** | **Architecture layer**: `specs/external-library-map.md` (ownership table, four-way classification rule, single-provider rule, target topology, toolchain facts), charter §"Repository architecture", `CsdLean4/Interop/` (documentation-only until the first dependency). One repo confirmed; no satellite CSD repos |
> | ~~H2~~ | **DONE 2026-08-07** (`Incubator/QuantumChaos/FloquetInterface.lean`, 5 pins + 2 `projMap` functoriality pins) | **`FloquetEvolution` interface landed.** Four-way check done first against the real repositories: Physlib (`leanprover-community/physlib`, 4.32.0, 690 modules) has `FiniteHilbertSpace d` = a structure *wrapping `EuclideanSpace ℂ d`* (adapters will be `.val`-thin) but **zero Floquet/kicked/chaos modules** → class 3 confirmed, `upstream-candidate(physlib)`. Interface: `step : H ≃ₗᵢ[ℂ] H`; `iterate n = step ^ n` with ★ exact information-preservation (`norm_iterate_apply`, `inner_iterate_iterate`); induced ray dynamics `projStep`/`projIterate` (functorial via the new `Projectivization.projMap_refl`/`projMap_trans`; transition-probability preserving — the `wigner_rigidity` hypothesis); inhabitation (`refl`) + the matrix-dynamics adapter seam `ofUnitary` (via `toEuclideanLinearEquiv` + `inner_toEuclideanLin_unitary`). New audit part `Tests/AxiomAudit/Incubator.lean`. All foundational-triple-only |
> | ~~H3~~ | **DONE 2026-08-07** (4 modules, 7 pins) | **The pilot vertical result, landed.** `Incubator/QuantumChaos/KickedIsingPilot.lean`: the explicit two-qubit kicked-Ising Floquet unitary (Ising phase × kick⊗kick, group-multiplication membership; `kronecker_mem_unitaryGroup` = upstream-candidate(mathlib)) + ★ `kickedIsing_changes_marginal` — at `b = π/2` the reduced first-qubit state of the evolved `\|00⟩` flips `\|0⟩⟨0\| → \|1⟩⟨1\|` while `inner_iterate_iterate` keeps every global overlap exactly invariant (the "global information intact, local accessibility moved" clause). `Empirical/CSD/QuantumChaos/{OnticLift,RecordPersistence,Capstone}.lean`: the ontic lift `floquetOnticStep` (= the constant-family `manyToOneSetup` time-1 flow — measure preservation inherited, not re-proved) with ★ period-by-period lift equations; **sure** (set-invariant, not a.s.) record persistence under uncoupled post-record driving; ★★ `floquetPilotClosure` — the four universal clauses bundled for EVERY unitary `U` and base point, honest witness-index framing. All foundational-triple-only. Open continuation (thread, not pilot): coupled post-record driving (record erasure pricing), KI-model reindex to `Fin 4` for the ontic-lift clause, chaos diagnostics |
> | ~~H5~~ | **DONE 2026-08-07** (`Empirical/CSD/QuantumChaos/RecordDegradation.lean`, 3 pins) | **Coupled post-record driving priced — the record half-life bound.** The honest degradation quantity identified first: for measure-preserving coupled steps the record EVENT's probability never changes (that IS measure preservation); what degrades is STABILITY (the trajectory's readout surviving). ★★ `recordIntact_compl_measure_le`: a formed record survives `n` periods of coupled driving except on a set of measure `≤ n·ε`, `ε` = the per-step coupling set's measure (least-change covering + iterate measure preservation) — the discrete-time sibling of `collapse_accuracy_bound`'s erasure pricing. Null coupling ⟹ a.s. persistence; the pilot's uncoupled regime recovered as `ε = 0` (empty coupling set, sure persistence). **Next brick (queued): a small-coupling witness `0 < ε < 1`** (a weakly record-coupled drive on `(KSigma N) × Rec`, e.g. fibre-conditioned record kick) showing the bound bites; then diagnostics (Loschmidt first) and the KI `Fin 4` reindex |
> | ~~H6~~ | **DONE 2026-08-07** (3 bricks, 7 pins) | **The §H5 continuation trio, landed.** (a) **Small-coupling witness** (`Empirical/CSD/QuantumChaos/CouplingWitness.lean`): `triggeredRecordKick` — record kicked exactly when the system enters a trigger region; measure-preserving by Mathlib's skew-product theorem; **the coupling set IS the trigger cylinder**, so coupling strength = trigger measure. CSD instantiation `fibreTriggeredKick`: quarter-ball fibre-arc trigger, coupling **exactly `1/2`** (`AddCircle.volume_closedBall`) — `0 < ε < 1`, the half-life bound genuinely bites (★ `fibreTriggeredKick_record_halfLife`: survival except on `≤ n/2`). (b) **Loschmidt echo** (`Incubator/QuantumChaos/Diagnostics.lean`, class-3): `loschmidtEcho` with endpoint/unit-interval/symmetry API; CSD reading fixed in-module — both drives preserve every overlap exactly, so echo decay measures RELOCATION of preserved information, never loss. (c) **KI `Fin 4` reindex** (`kickedIsingU₄` via `reindex_mem_unitaryGroup`, upstream-candidate(mathlib)): ★ `kickedIsing_pilotClosure` — the concrete model reaches the ontic-lift clause at `Fin 4`, every parameter pair and base point. All foundational-triple-only. Thread continuation: attainment (actual erasure rate vs the bound), SFF/OTOC diagnostics |
> | H4 | **record** | Root-wire every new module (the job-count tell); `Empirical/CSD/QuantumChaos` rows inherit the transport/ledger discipline; guard inventories (`check-claims.sh`) get their rows in the same commit that adds boundary phrases |
> | H7 | **LANDED 2026-08-12 (same day as scoping) — one honest residue below** (`CV/CarrierPersistence.lean`, 4 pins + `Empirical/CSD/QuantumChaos/CarrierPersistence.lean`, 3 pins) | ★★ `carrier_persistence_window` — **the level-1 headline: zero until the cone arrives, the derived Duhamel rate after** (`≤ 2(n−m)·‖W−1‖·‖B‖` for a perturbation outside the readout's `m`-ball). ★ `heisenberg_perturbed_pow_eq` — exactness in the cone-complement is an EQUALITY, for an *arbitrary* unitary perturbation. ★ `heisenberg_diagonal_pow_eq` — **einselection of the configuration basis**: diagonal readouts are eternally intact under the whole diagonal-phase family (a discovery of the build: in this model degradation can ONLY enter through non-diagonal perturbations, which sharpens the physical reading). Level 2: `recordEvent_preimage_step` (events reindex, never decay — `[propext, Quot.sound]` only) + `recordEvent_measure_invariant` + `recordIntact_antitone` (**axiom-free**) — the event/carrier separation as theorems, with the framing constraint honoured throughout. **Residue DISCHARGED 2026-08-12 (Q1, `DerivedCoupling.lean` — the Markov bridge): the ontic derived-ε instantiation** — a concrete record-coupled ontic step in the `fibreTriggeredKick` style whose `recordFlip` measure is *derived from* the level-1 operator bound rather than posited; needs the operator-norm → flip-measure bridge (genuinely new, not assembly). Entropy-ledger follow-up unchanged. Original scoping: **Carrier persistence priced by locality (the CV/record bridge brick — scoped 2026-08-12).** The first statement where a record carrier's stability is priced by field locality rather than posited. ⚠️ **Framing constraint, binding (2026-08-12 discussion):** every clause stated present-tense-checkable; the theorem family is about CARRIERS (readout-cylinder membership), never "the past changing" — the record EVENT is trajectory-indexed and time-invariant by construction, and information is conserved outright (overlaps exact). Say "carrier persistence", never "record degradation". Four clauses: **(a)** event invariance — the sector-selection indicator is a function of the trajectory, constant under evolution (trivial-but-clarifying lemma, ontic side); **(b)** information conservation — cite `inner_iterate_iterate`, don't re-prove; **(c)** THE new content, two halves: *(c1, exact)* a readout supported on site set `T` evolves IDENTICALLY under two drives that differ only on couplings at graph distance `> n` from `T`, for `n` periods — records in the cone-complement are exactly intact (assemble from `heisenberg_graphInteractingU_pow_supportedOn` + `commute_of_disjointSupport`; the OTOC gate `otoc_graphInteractingU_eq_zero` is the same geometry at commutator level); *(c2, bounded)* after cone arrival the readout deviation is `≤ 2n·\|τ\|·\|λ\|·C` at the Duhamel rate (`interactingU_dist_le`, the `one_sub_loschmidtEcho_interacting_le` pattern), with the sharp distance form via `norm_commutator_spatial_factorial_le`; **(d)** operational fixedness — the isolated carrier's pointer-basis repeatability, cite `csd_repeatability_same` (finite-arena; scope note that the CV-mode version rides the `OscillatorBorn` truncation). **Level 2 (ontic lift):** feed the derived (c2) bound into `recordFlip`/`recordIntact_compl_measure_le` following the `fibreTriggeredKick` pattern — `ε` DERIVED from `(τ, λ, C, d)` instead of posited; the §H5 half-life bound with locality doing the pricing. **Follow-up recorded, not queued:** the entropy-ledger link (carrier erosion rate vs coarse entropy production, framed strictly as retrodiction reliability) — same austerity constraint applies |
>
> ### ~~V. Validation-hardening campaign~~ (adopted AND CLOSED 2026-08-12 — [`validation-hardening-plan.md`](validation-hardening-plan.md))
>
> **ALL 13 WORKSTREAMS DONE 2026-08-12, single-day campaign.** Validation machinery
> on the **sufficiency/inhabitation leg** (explicit nontrivial inhabitants of the
> assumption packages, driven through the headline chains) — not reconstruction
> progress, and distinct from the necessity-audit conditionality gap. Landed:
> `--wfail` CI (A) · `Tests/Witnesses/` suite, 8 modules + 22 pins, all
> foundational-triple (B, C, D, E+H, F+G, I, J — incl. the honest
> `Measure.infinitePi` trial model for **every** `OnticSetup`, discharging the
> Examples.lean caveat) · `check-import-hygiene.sh` blocking + 3 permanent
> check-guards probes (L) · advisory `check-std-lint.sh`, 25 s (K) ·
> `compat.yml` forward-compat + macOS (M, O) · `docs.yml` doc-gen4 smoke (N).
> Residue: none blocking; the first scheduled compat/docs runs validate their
> runner plumbing; std-lint's `unusedArguments` list is the F3 on-touch class.
>
> ---
>
> ## What's next, by size & complexity (updated 2026-08-02, post-v1.0.1 + second review) *(HISTORICAL — see the live list above)*
>
> | Size | Item | Notes |
> |---|---|---|
> | ~~S~~ | ~~Conditioning toolkit → staging; standard-basis lemma~~ | **DONE 2026-08-02** (`Mathlib/Probability/ConditionalProbability.lean`; `basisContext_basisFun_rate`) |
> | ~~**M**~~ | ~~Mixed preparations in the dynamical model~~ **DONE 2026-08-03** (`SigmaLayer/MixedSwap.lean`, 2 pins): `mixedSwapPrep ρ = Σⱼ λⱼ • swapPrep [ψⱼ]` (two-stage sampling as a probability measure) and ★ `mixed_swap_sector_born` — the mixed **dynamical** Born rule: the mixture's mass on the protocol's outcome sector is exactly `Tr(ρ\|eᵢ⟩⟨eᵢ\|)`. Per-protocol facts inherited verbatim; the Bayes-conditioned mixture post-ensemble stays a recorded extension | discharged |
> | **M–L** | **Dynamical no-signalling (A6 step 4) + the dynamical eraser process** | A *local* measurement on a composite IS a block-degenerate measurement; `joinWitness_blockLuders` + `reduceB` machinery is most of the route. **Brick 1 LANDED 2026-08-03** (`SigmaLayer/LocalLuders.lean`, 4 pins): the coordinate-form local projectors and ★★ `reduceA_localLuders_mixture` — the Born-weighted mixture of post-measurement `A`-marginals equals the pre-measurement `A`-marginal (the statics core, zero-weight branches honest). **Brick 2 LANDED 2026-08-03** (`SigmaLayer/LocalBlockBridge.lean`, 3 pins): ★ `toComposite_blockProj` — the degenerate-Lüders block projector of `localBlock` (= second projection under `finProdFinEquiv`) IS the local projector, definitionally; ★★ `reduceA_blockLuders_mixture` — **dynamical no-signalling** stated in the join witness's own vocabulary (the post-states and weights of `BlockLudersObligation localBlock`, inhabited by `joinWitness_blockLuders`). **Brick 3a LANDED 2026-08-03** (`SigmaLayer/LocalLudersBasis.lean`, 5 pins): the local Lüders map for an **arbitrary** orthonormal basis on the measured factor — ★★ `reduceA_localLudersOn_mixture`, marginal invariance in **every basis** (Alice cannot detect Bob's outcome or his basis choice; Parseval replaces the ite-collapse). **Brick 3b LANDED 2026-08-03** (`Empirical/CSD/EraserDynamics.lean`, 6 pins): **the dynamical eraser** — mark arm: `localProjB_bellE` (which-path product state) + `marked_no_fringe` (flat screen rate at every phase); erase arm (an instance of `localProjOn` at the genuine ± `OrthonormalBasis`): ★ `erased_amp` — the dynamical post-state's screen amplitudes are **exactly `√2·eraserOut`**, so the `QuantumEraserVolume` fringes, conditional rates (`erased_rate`), the exact dark zero (`erased_dark`), and the `1/2` marker weights (`erased_weight`) are all statistics of the measurement dynamics' own output. **ROW CLOSED 2026-08-03.** Residue discharged same day (`Empirical/CSD/EraserSequential.lean`, 3 pins): ★ `sequential_no_revival` — mark-then-erase stays flat at every phase and outcome: **once a record exists, erasure cannot revive the fringe** (records are statistically irreversible; the erase stroke only rescales the recorded ray, `seqProfile_eq`). The measure-level ensemble integral is **closed as definitional**: for finite outcomes the post ray-ensemble IS the discrete mixture and its barycenter statement IS `reduceA_localLudersOn_mixture`; a Bochner-integral recast adds a `reduceA`-measurability lemma and no content — revisit only if a continuum-outcome layer appears. *(Superseded scoping note:)* **the dynamical eraser process** on the `2 ⊗ 2` composite (`nA = nB = 2`, so `localBlock` applies as-is). (a) *The mark arm*: the computational-basis local marker measurement — post-states `[localProjB j ψ_φ]` are the which-path states, flat screen statistics, no fringe; (b) *the erase arm*: the `±`-basis marker measurement = `localProjB` conjugated by the local Hadamard (`actA`/`actB` idiom from `OnticMarginals`) — post-states identified with `QuantumEraserVolume`'s `eraserOut φ ±` (via `eraserOut_eq_jointAmplitude`), marker weights `1/2` (`eraser_marker_marginal`), conditional fringes and the exact dark zero inherited from the existing typicality theorems; (c) follow-up statistics through `csd_sequential_born`. The measure-level "one integral" form of no-signalling is bookkeeping over bricks 1–2 (the post ray-marginal is a finite discrete mixture) and folds into (c) |
> | ~~**M–L**~~ | ~~Projective-measurement capstone~~ **DONE 2026-08-03** (`SigmaLayer/MeasurementCapstone.lean`, 1 pin): ★★★ `projectiveMeasurementCapstone` — rank-one + every basis + degenerate + **the smooth horn** (four closures, one Prop; the fields quantify over different witnesses by design — the two-horn framing). The single citation for dynamical projective measurement; constituents remain as the construction record. *+`generation` field added 2026-08-03 (fourth review): the Schrödinger ODE is now a capstone field, not a satellite* | the review's step 4, discharged |
> | ~~**M–L**~~ | ~~POVM / instrument dynamics~~ **DONE 2026-08-03** (`SigmaLayer/PovmDynamics.lean`, 5 pins): exactly the planned route — Naimark-dilate, then the *existing* degenerate record machinery on the dilated arena (the ancilla block structure IS `localBlock N K`, so no new dynamics, sectors, or arena were needed). ★ `povm_selector_born` — the POVM Born rule **at the selector level** (⚠️ *corrected 2026-08-04: this row said "dynamical"; the statement is an `epistemicMeasure` of a selector fibre, not of a protocol outcome sector — the sector-level lift is a recorded extension*): the block selector's outcome-`i` fibre at the dilated preparation `[Vψ]` carries exactly `⟨ψ, Eᵢψ⟩` (`degenerate_selector_born` + `born_transfer`); ★ `toComposite_blockProj_dilate` — the record-layer block posts ARE the Naimark–Lüders posts `Πᵢ(Vψ)`; ★★ `povm_instrument` — the join witness's post-marginals satisfy degenerate Lüders at `[Vψ]` (the instrument of the dilation, by Liouville-preserving dynamics); ★★ `naimarkInstrumentClosureCanonical` — via `canonicalNaimark`, **every POVM** has dynamically realised statistics and instrument. Honest scope: the instrument is dilation-relative (a POVM does not determine its instrument — physics, not a defect); realising `V` itself as a unitary-plus-ancilla stroke inside the record dynamics is a recorded extension | discharged |
> | ~~**S–M**~~ | ~~Outcome-conditioned mixed update~~ **DONE 2026-08-03** (`SigmaLayer/MixedLuders.lean`, 3 pins + staged `ProbabilityTheory.cond_finsetSum` — Bayes for finite mixtures, hypothesis-free): ★ `mixed_post_bayes` — the conditioned post-ensemble IS the Bayes-posterior mixture (component `j` at weight `λⱼ·pᵢ|ⱼ/Tr(ρ\|eᵢ⟩⟨eᵢ\|)`); ★★ `mixed_luders_followup` — **the record, not the pedigree, fixes the post-state**: follow-up statistics after outcome `i` on the mixture are exactly `c'.rate [eᵢ]` (at rank one the record erases the classical ignorance; `ρ ↦ ΠᵢρΠᵢ/Tr(ρΠᵢ)` dynamically). Spine: `mixedSwapPrep` **factors** through the common bank (`mixedSwapPrep_eq_prod`), so the pure `swap_luders_born` applies verbatim; positivity is a theorem (`mixed_outcome_pos`). Degenerate-on-mixed = recorded extension (rides `JoinClosure`; posteriors genuinely distinct at rank ≥ 2) |
> | ~~**M**~~ | ~~Degenerate one-protocol package~~ **DONE 2026-08-03** (`SigmaLayer/JoinClosure.lean`, 2 pins): ★★ `degenerateMeasurementClosure` — ready/record/exclusivity/persistence + Liouville + ★ `join_sector_born` (the coarse dynamical Born mass at the canonical join preparation, calibration-independent; spine = `preimage_sector_ae` + `volume_goodTheta`, the Dirac slice of `degenerate_selector_born`) + ψ-dependent degenerate Lüders, one structure on the join protocol for every block structure and preparation. The capstone's `degenerate` field upgraded to this closure (was bare `BlockLudersObligation`) — the fourth review's remaining ⚠️ on the degenerate tier is now ✅ |
> | ~~**M**~~ | ~~`C^∞` ingredients for the ε-corridor witness~~ **DONE 2026-08-03** (`SigmaLayer/PointerSmoothProfile.lean`, 3 pins): `smoothClampDiv`/`smoothArcWeight`/`smoothPointerRamp` — the `Real.smoothTransition` profiles with the **plateau interface verbatim identical** to the trapezoids' (same statements, same hypotheses: `= 1` on the shrunk arc, `= 0` off the open arc, `0`/`π/2` around the stroke). ★ `contDiff_smoothArcWeight_lift` — the arc weight's periodic lift is `C^∞` on the universal cover (both circle-distance kinks fall inside plateaus: centre needs `2ε < r`, cut locus needs `r < 1`; the transition zone sees a locally affine distance); ★ `smoothRampedU_schrodinger` — **Schrödinger at every time**, no open-window restriction (the trapezoid corners are gone, exactly as `PointerGeneration`'s honest scope predicted; outside `[0,1]` the ODE reads `U̇ = 0` — persistence as an ODE). Manifold-level arena smoothness stays §2a-scoped with A1/A3; protocol re-instantiation with the smooth profiles is mechanical (interface identical) and recorded |
> | ~~**S–M**~~ | ~~Cantor-horn witness~~ **DONE 2026-08-03** (`SigmaLayer/NullSeamWitness.lean`, 4 pins) — and **simpler than the devil's-staircase sketch**: no Cantor function; the transition dodges the record gap by crossing **where the record regions kiss** (`m₁ = m₂ = ½`), touching the complement at one projective point per boundary. ★★ `nullSeamClosure` — continuous (`continuous_nullSeamEvolve`), measure-preserving (⚠️ *not* "Liouville": `S¹ × ℂℙ²` is odd-dimensional hence not symplectic — naming corrected 2026-08-03, fifth review; the `T² × ℂℙ²` lift that would earn the word is the row below), records exact and correct off a **two-point** seam (`nullSeam_seam_null`), **exact** Born `r`/`1−r` (`nullSeam_born_left/right`), on `S¹ × ℂℙ²`. **THE TRILEMMA**: each horn pays exactly one price — *seams* (piecewise), *ε-Born* (smooth witness), *Dirac calibration* (this one: exactness at `[f₀]`; positive-width ready fattens the seam to `O(δ)` — `collapse_accuracy_bound` prices it). General `N` = mechanical, recorded |
> | **PARKED** (author, 2026-08-03) | Sharpened no-go (trilemma completeness) | **Parked before starting** — scoping found two different theorems wearing one title. (a) *Local* version, S–M and confidently provable: for the pointer geometry, continuity + a positive-width ready region force a **positive-measure** no-record set of order the calibration width, so exact-a.e. records force Dirac calibration — the trilemma's third leg, rigorous. (b) *General exhaustiveness* — no witness whatsoever combines continuity + positive-width ready + a.e.-exact records + exact Born — quantifies over all arenas and propagators; the IVT/Fubini mechanism leans on the specific moment-region structure and does **not** obviously generalise. Reopen with (a) if the trilemma needs to be a theorem rather than a classification; (b) stays research. Ingredient check done: `fs_volume_eq_dirichlet` (`LF4/MomentBornN.lean`) gives the absolutely-continuous moment pushforward, so the kissing set is FS-null and the argument must run on topology, not measure |
> | **PAPER-FIRST** (was Research) | Joint-arena Hamiltonian lift — **proof strategy now known** | ⚠️ *Row rewritten 2026-08-03 (fifth external review): my earlier pessimism here was wrong.* I wrote that "once the register moves mid-stroke the exact moment-transport argument dies, and adiabatic/Grönwall control is needed." It does not die — **the weights are exact constants of motion**, no adiabatic approximation. The argument, verified: on a product arena `{f(x), g(q)} = 0`, so `{w_k, 𝓗} = Σⱼ hⱼ{w_k, wⱼ}` for `𝓗(x,q) = Σⱼ wⱼ(x)hⱼ(q)`; and the weights depend **only** on the moment coordinates `mᵢ(p)` and the single register coordinate `θ₁`. The `mᵢ` Poisson-commute (moment map of an abelian torus action — `ℂℙ^{N-1}` is the standard completely integrable system, the `mᵢ` a maximal commuting family), `{mᵢ, θ₁} = 0` (different factors), so **any** two functions of that family commute ⇒ `{wᵢ, wⱼ} = 0` ⇒ `dw_k/dt = 0`. **Stronger than the review states:** the same computation gives `{θ₁, 𝓗} = {mᵢ, 𝓗} = 0`, so `basinIndex` — a function of exactly those — is conserved: **the outcome selector survives the true joint flow untouched**, and back-reaction lives entirely in the conjugate variables (`θ₂`, the phases conjugate to the moments), which the record does not read. The existing landing/Born analysis should transfer nearly verbatim. **Why PAPER-FIRST and not a Lean brick:** every step (define `𝓗` on the arena, derive `X_𝓗 = ω⁻¹d𝓗`, prove `{wᵢ,wⱼ}=0`, conserve along the flow) is symplectic-manifold mathematics, and Mathlib has no symplectic-manifold API — the *same* §2a wall as A1/A3, already recorded for the vector-field equation itself. Paper C A2 / TN6 can carry this as mathematics **now**; the corpus records the scoped boundary. Steps 1–6 of the review's package are the paper; step 7 (a second Hamiltonian stroke implementing the reversible Lüders relocation) is the smooth-witness Lüders composition row |
> | **M–L** (scoping done) | Coordinate-model Poisson bracket — the formalisable fragment | The partial Lean route for the row above, **scoped 2026-08-03**: `Mathlib/Analysis/InnerProductSpace/KahlerForm.lean` already delivers the *pointwise* Kähler triple (`g`, `ω`, `J`, J-compatibility, taming) axiom-free, and Mathlib has `fderiv` on normed spaces — so a **chart-level** Poisson bracket `{f,g} = ω(∇f, ∇g)` is buildable, with `{wᵢ,wⱼ} = 0` and weight conservation as concrete computations. What it would NOT give: `dω = 0` (KahlerForm's own honest scope), the global projective statement, or the flow's existence on the manifold — so the result would be a **local/chart theorem**, weaker than the paper's, but real and machine-checked. Worth doing if the trilemma's third leg needs Lean backing; not a substitute for the paper theorem |
> | **M–L** | `T² × ℂℙ²` lift of the null-seam witness | Earns the word "symplectic"/"Liouville" for the third horn: `S¹ × ℂℙ²` is 5-dimensional (odd), so the current invariant measure is a product measure and nothing more. Lifting the register to the full `T²` gives even dimension and lets the same kissing-crossing construction be stated Hamiltonianly. Mechanical geometry; the seam/Born theorems should carry over |
> | ~~**M** (long elapsed)~~ | Mathlib upstream batch 1 — **retired 2026-08-06**, see B6 | Tracked row below; `MATHLIB-GAPS.md` |
> | **M–L** | Smooth-witness Lüders composition + ε-Born LLN | The recorded extensions of the COMPLETED ★ item: compose the pointer stroke with the swap/join relocation machinery on one arena (collapse stays on the exact horn until then), and run the i.i.d. frequency layer over the ε-Born sandwich |
> | ~~**L**~~ | ★ ~~**Compact Kähler pointer witness (`ℂℙ^K`)**~~ **COMPLETE 2026-08-03** | The REOPENED Hamiltonian row's route, **CONFIRMED (author decision 2026-08-03: retain the smooth-Hamiltonian architecture)** — a genuinely (globally) Hamiltonian, continuous record-creating propagator; kills the torus-flux obstruction; the second review's steps 1–3. Execution plan: [`pointer-witness-plan.md`](pointer-witness-plan.md). **Bricks 0–1 LANDED 2026-08-03** (`SigmaLayer/PointerArena.lean`, 5 pins — arena, open positive-measure ready/record regions, `arenaReady_pos`; `SigmaLayer/PointerRotation.lean`, 6 pins — the continuous one-parameter unitary rotation group, ready → record quarter turn, FS invariance). **Brick 2a LANDED 2026-08-03** (`SigmaLayer/PointerCoupling.lean`, 6 pins — Hermitian weighted coupling, unitary exponential propagator, ★ `pointerRot_eq_exp` generation identification by ODE uniqueness, entrywise-Lipschitz weight continuity via Duhamel + the new staged `Matrix.norm_entry_le_l2_opNorm`). **Brick 2b LANDED 2026-08-03** (`SigmaLayer/PointerWeights.lean`, 5 pins — circle-intrinsic weight field, ★ `continuous_pointerEvolve`: **the full arena propagator is continuous**, the property `shearEvolve_not_continuous` forbids every torus-register witness; skew-product Liouville preservation; pure-cell reduction to the quarter rotation); **Brick 3 LANDED 2026-08-03** (`SigmaLayer/PointerLanding.lean`, 6 pins — midpoint separation on the circle, exact moment-map record transport, ★ `pointer_landing`: records created by the continuous propagator with the ontic sector selecting the outcome, shrunk-slice volume = `rⱼ − 2ε` exactly); **Brick 4a LANDED 2026-08-03** (`SigmaLayer/PointerProtocol.lean`, 7 pins — the smooth witness as a `MeasurementProtocol`: two-time law = exponential group property, persistence = freezing, correlation = landing, ★ joint time–state continuity); **Brick 4b LANDED 2026-08-03** (`SigmaLayer/PointerBorn.lean`, 5 pins — the ε-Born sandwich `rⱼ − 2ε ≤ sector ≤ rⱼ + 2(N−1)ε` for the ready-conditioned preparation, no Dirac posit; ★★ `smoothWitnessClosure` — the smooth-horn closure, canonical moment-map instantiation). **Brick 5 LANDED 2026-08-03** (`SigmaLayer/PointerGeneration.lean`, 3 pins — ★ `rampedU_schrodinger`: the Schrödinger ODE with the explicit Hermitian `H_eff`; records-without-back-reaction). **ALL FIVE BRICKS COMPLETE 2026-08-03 — the ★ item is DONE.** Eleven modules, ~40 pins. Remaining offshoots are their own queue rows: the smooth-witness Lüders composition (M–L) and the LLN over the ε-Born sandwich (M) |
> | **L** | Shor-9 concatenation; Lindblad tier | QM-side QEC; open systems |
> | **L** | Operator-convexity ladder → DPI → unconditional SSA | Gated on the `CStarMatrix` instance wall (`MATHLIB-GAPS.md`) |
> | **L** (parked) | Wigner §13 normal form; Lévy concentration | Paused / Mathlib-gated; reopen only with cause |
> | **XL** | Composable finite-key QKD | The security-model tranche |
> | **Authorial** | Papers alignment | A7's fibred reading vs Paper C's base-only `Ωᵢ(M) ⊂ ℂℙ^{N-1}`; ~~the measurement-dynamics architecture~~ **DECIDED 2026-08-03: retain smooth Hamiltonian** (the `ℂℙ^K` witness satisfies the papers rather than the papers bending to the piecewise witness) — remaining: Paper C/D/TN6 edits |
> | **Research** | The fibre-from-dynamics frontier | `sigma-fibre-contextuality.md`; the join/phase machinery is the first real tool |
> | **Programme** | The CV track | After the compact-pointer witness and the capstone — the review's defensible closure point |



> **This is the ONE place open work lives.** Do not add TODO / future-work / "next
> steps" / open items to any other `.md`. The per-phase plans (`LF*-plan`, `shor-plan`,
> `povm-plan`, …) are **historical execution logs** — frozen; read them for how a
> completed layer was built, not for what is open. The status/claims docs
> (`reconstruction-status`, `connectivity-manifest`, `PLACEHOLDERS`, `AXIOMS`,
> `BRIDGE-OBLIGATIONS`) describe *what is proved*; they point here for *what is next*.
>
> Effort key: **S** hrs–day · **M** days–2wk · **L** weeks · **XL** Mathlib-scale.
> Last updated 2026-07-30.
>
> **★ For "how far is finite QM from Σ from closed?" read [`reconstruction-status.md`](reconstruction-status.md) §2a** — the A1–A7 audit and the **scoping decision**. Short version: A1's exterior calculus **on `KSigma`** and A3's smoothness are **permanently scoped** (Mathlib has no manifold DG API; nothing downstream uses them); A4 is **closed as a posit** with `μ_FS` *forced* under unitary symmetry; and the genuinely open rows ~~are **A2, A5, A6, A7** plus the fibred-Σ question~~ *(banner corrected 2026-08-02: A5/A6/A7 were discharged and A2's formalisable half landed that day — see `reconstruction-status.md` §2a; the **Hamiltonian-generation** sub-question of A2 was then REOPENED by the second external review — torus-flux obstruction — and **CLOSED 2026-08-03 at the formalisable level**: the `ℂℙ^K` smooth witness landed, bricks 0–5, `smoothWitnessClosure` + `rampedU_schrodinger`; see the dynamical-measurement row)*. Do not read "Partial" rows as pending work.
>
> ⚠️ **CORRECTION 2026-07-30 — do not extend the "permanently scoped" label to the *fibred* Σ.** It
> was being used that way, here and in `CircleFibre.lean`. The fibred Σ's missing Kähler structure is
> a **dimension-parity** fact, not a Mathlib gap: `ℂℙⁿ⁻¹ × ℝ` and `ℂℙⁿ⁻¹ × AddCircle 1` both have
> real dimension `2n-1`, odd, and no odd-dimensional manifold admits a symplectic — hence Kähler —
> structure. No amount of tooling repairs that. It is **tractable work on `T²`**, not scoped work.
> See the ★★ row below.

---

## M — genuinely closeable, real content

| Item | Status / what's needed | Former source |
|---|---|---|
| ~~**Choi converse** (PSD Choi ⇒ Kraus)~~ | **DONE 2026-07-19** (`LF2/ChoiConverse.lean`). `choi_iff_posSemidef`: a matrix on `Fin M × Fin N` is a Kraus family's Choi matrix **iff** PSD. The feared "vectorization iso" was definitional (the Choi index *is* a product), so the content was the spectral `Kᵢ=√λᵢ·unvec(eᵢ)` reconstruction (`choiOfKraus_krausOfChoi` + `IsHermitian.eq_eigen_outer`). Foundational triple. | `qi-qec-roadmap.md` |
| ~~**Gisin's theorem** (pure entangled ⇒ CHSH violation)~~ | **DONE 2026-07-19** (`LF6/GisinTheorem.lean`). `gisin_chsh_violation`: every entangled `Ψ(c,s)=c\|00⟩+s\|11⟩` (`0<c,0<s`) violates CHSH — the physical `⟨Ψ\|σ·a⊗σ·b\|Ψ⟩` combination `> 2`. Built directly on the existing `psQubit_pauli_correlation`; the feared "general Schmidt decomposition" wasn't needed (the real-Schmidt two-qubit state + its correlation were already in `PartialSchmidtCorrelation.lean`). Trig-free `c,s`-dependent witness giving `2√(1+(2cs)²)`. Closes LF6-6. Foundational triple. | `lf6-plan.md` (LF6-8) |
| ~~**Busch–Gleason** (effect-Gleason, finite-dim)~~ | **DONE 2026-07-21** — see the "Discharge `busch_effect_gleason`" row below. Axiom deleted; corpus now imports **zero** axioms. | `AXIOMS.md §2.2` |
| ~~**Separate the ecdsa.fail track**~~ | **DONE 2026-07-20** — extracted to its own repository and removed from this one (see the section below). | `ecdsafail-two-track.md` |

## M — genuinely closeable, real content (continued)

| Item | Status / what's needed | Former source |
|---|---|---|
| ~~**Born-as-volume at the simplex boundary (zero Born weights)**~~ | **CLOSED-AS-STALE 2026-08-02 (second external review).** The boundary extension this row asks for had **already landed 2026-06-11**: `fs_born_volume_ratio_N_uncond` / `born_frequency_convergence_N_uncond` (`LF4/BornRegionUncond.lean`) are hpos-free for every unit preparation, vanishing weights included, and the empirical engine routes through the hpos-free forms (`ContextVolume.lean`). The row was opened 2026-07-27 without noticing the six-week-old `_uncond` engine — a verify-before-scoping failure, now corrected in the HOM and eraser module docstrings (the record-layer route there stands as a **choice** with its own foundational motivation, no longer as a necessity). *(Original row, superseded:)* OPEN — found 2026-07-27 while building the Hong–Ou–Mandel CSD twin. `fs_born_volume_ratio_N` / `fs_born_volume_ratio_N_apex` / `fsMeasure_bornRegionN` (`LF4/MomentBornN.lean`, `LF4/ObservableCorrespondenceN.lean`) all carry `hpos : ∀ j, 0 < ‖⟨eⱼ,ψ⟩‖²` — **strictly positive** Born weights. It is load-bearing, not decorative: the region for index `i` is `replaceMap b i '' openSimplexFree` and `replaceMap_det b i = b i` (Cramer, `LF4/BornVolume.lean`), so a **zero** Born weight makes the vertex-replacement map singular, puts `b` on the simplex *boundary* (`b ∈ openSimplexFree` fails, since it needs all free coords `> 0` **and** the apex coord `> 0`), and breaks both the openness⇒measurability step (`LinearMap.equivOfDetNeZero`) and the volume-scaling step. **Consequence:** the projective/DH machinery cannot express *any* phenomenon whose signature is a vanishing amplitude — interference nulls, dark ports, HOM. **What's needed:** a boundary extension, `fsMeasure_bornRegionN_of_born_zero : ‖⟨e_k,ψ⟩‖² = 0 ⟹ fubiniStudyMeasure p₀ (bornRegionN ψ hψ0 k) = 0`. Sketch: the region sits inside the preimage of `range (replaceMap b i)`, a *proper* subspace when the det vanishes, hence Lebesgue-null; push through `fs_volume_eq_dirichlet` by monotonicity of the (outer) measure, intersecting with `openSimplexFree` to satisfy that lemma's subset hypothesis. The care needed is that `replaceMap_image_subset` also assumes `b ∈ openSimplexFree`. **Workaround in place:** the **record layer** has no such restriction — `volume_cdfCell` carries no positivity hypothesis (a zero rate is simply a zero-width CDF cell), so `Empirical/CSD/HongOuMandelVolume.lean` states the degenerate case there instead. This is a concrete architectural argument for the record layer over the DH route, independent of the foundational one. | `Empirical/CSD/HongOuMandelVolume.lean` |
| ~~**Error discretization** (why four Paulis suffice)~~ | **DONE 2026-07-27** (`Empirical/QM/QEC/ErrorDiscretization.lean`). `pauli_decomposition` (every `2×2` complex matrix is `c₀I + c₁X + c₂Z + c₃XZ`, coefficients read off the entries) + `pauli_span_top` (`span ℂ {I,X,Z,XZ} = ⊤`), lifted to the three-qubit code by `kron3` linearity (`error_discretization_qubit₁/₂/₃`) and to states (`errored_codeword_eq`). Dimension-free; the conceptual content that makes `ThreeQubit` + `PhaseFlip` a general error-correction claim rather than two special cases. **Discretization half only** — not a claim that the 3-qubit code corrects arbitrary errors (its correctable set is `{I,X₁,X₂,X₃}`); the syndrome-collapse half and the concatenated Shor-9 claim are the row below. | `EMPIRICAL.md` |
| ~~**Syndrome collapse** (join discretization to the four point-checks)~~ | **DONE 2026-07-27** (`Empirical/QM/QEC/SyndromeCollapse.lean`). Before it, `ErrorDiscretization` ("an arbitrary error is a combination of four") and `ThreeQubit` ("each of the four is corrected") were **unconnected** — a superposition of error branches is not obviously reducible to one branch, so discretization was true but bore no load. `errored_pairwise_orthogonal`: the four errored codewords are mutually orthogonal, their supports being disjoint (`{000,111}`, `{100,011}`, `{010,101}`, `{001,110}`) — the concrete form of the distinct `(Z₁Z₂,Z₂Z₃)` syndrome pairs, available directly so no spectral theorem is needed. `branch_overlap_X1/X2/X3`: the overlap of the corrupted codeword with branch `k` is exactly `cₖ·⟪branchₖ,branchₖ⟫`, so the syndrome measurement reads off one coefficient and is blind to the rest. `three_qubit_corrects_span_error` bundles decomposition + orthogonality + extraction + recovery: **the code corrects an arbitrary error in `span ℂ {I,X₁,X₂,X₃}`** — a continuum, not four points. Still the bit-flip span (the code's actual correctable set); all four Paulis per qubit needs the row below. | `EMPIRICAL.md` |
| ~~**BB84 sequential twin + rotated context**~~ | **DONE 2026-08-02** (`Empirical/CSD/Crypto/BB84Sequential.lean` + `SigmaLayer/RotatedContext.lean` + `prep_outcome_pos` in `SequentialMeasurement.lean`; foundational-triple, 6 pins) — the crypto-twins gap from the empirical audit, closed at its most valuable point: the QM module's classical-marginal Eve becomes the calibrated-swap dynamics (`csd_sequential_born`), Bob reads the **rotated context** `basisContext b` (any orthonormal basis — the object the unitary-covariance extension quantifies over; the equivariance *law* is still the open item), and `hpos` is now a **theorem** at concrete preparations (`prep_outcome_pos`). Remaining from that audit note: ~~**B92/Wiesner sequential twins**~~ **DONE 2026-08-02** (`B92Sequential.lean` + `WiesnerSequential.lean`, 6 pins — unambiguity as a null basin + false clicks at `½`; the `¾` counterfeit value attained by measure-resend, optimality out of scope), and the fully dynamical **eraser process** (still needs the composite arena, not just the rotated context). | crypto twin 2026-08-02 |
| ~~**QuantumEraser ontic twin (record route)**~~ | **DONE 2026-08-02** (`Empirical/CSD/QuantumEraserVolume.lean`, foundational-triple, 5 pins) — routed through the **record layer** exactly as the gating note below prescribed: the conditioned fringe as a fibre-typicality volume at every phase (`eraser_fringe_typicality`), the dark fringe an exact ontic null four ways (cell / P5 record / measurement / **v1.0 global basin** — at `φ=π` the conditioned state is the vertex `[e₁]`), rates derived as joint-over-marginal from `Empirical/QM/QuantumEraser.lean`. The `hpos` boundary was routed around, not extended — that row stands. **Remaining (recorded, not in-flight):** a *fully dynamical* eraser — marker measurement as calibrated-swap dynamics on the two-qubit composite, screen read via a rotated context field — needs the unitary-covariance extension step. **Effort L**, folds into the extension-steps row. | eraser twin 2026-08-02 |
| ~~**KCBS ontic volume twin**~~ | **DONE 2026-08-02** (`Empirical/CSD/Contextuality/KCBSVolume.lean`, foundational-triple, 3 pins) — representative pentagon context completed by the cross product, orthonormality transported from the QM side, engine instantiated, apex weight `1/√5` realised (`kcbs_pentagon_weight`). *(Original queue entry:)* **QUEUED 2026-08-02 (empirical-coverage audit).** `Empirical/QM/KCBS.lean` exists with no CSD twin — the only flagship test missing one for no structural reason: pentagon measurements involve no vanishing amplitudes, so the `hpos` boundary does not bite. Build `Empirical/CSD/KCBSVolume.lean` on the volume-series pattern (nearest relatives: `ContextVolume`, `KS18Volume`). **Effort M.** Same audit also recorded: the **QuantumEraser** twin is *structurally gated* on the `hpos` boundary row (interference nulls = zero Born weights) — route it through the record layer as `HongOuMandelVolume` was, or wait for the boundary extension; and **BB84/B92/Wiesner** CSD twins remain planned-not-done (`specs/qm-empirical-tests.md`). | empirical audit 2026-08-02 |
| ~~**Shor 9-qubit code (concatenation)**~~ | **DONE 2026-08-13** (`QEC/ShorNine.lean`, Q5/E1) — the assessment's concatenation-combinator option delivered: the block-Kronecker action lemma reduces all 512-dim statements to the landed 8-dim block facts, and the full single-Pauli correction set (X/Z/XZ at all nine positions, Z degenerately) is proven with no entry-wise work above 8 dims. The 9-qubit syndrome-collapse half is NOT restated (recorded in `SyndromeCollapse.lean`'s scope note). *(Superseded assessment kept below.)* Formerly: **OPEN — assessed 2026-07-27. Not conceptually blocked; blocked on idiom.** The syndrome-collapse half is now **done at 3 qubits** (row above), so what remains is purely the 9-qubit lift. Both halves already exist: `QEC/ThreeQubit.lean` (bit-flip) and `QEC/PhaseFlip.lean` (phase-flip, whose own docstring calls itself "the phase-flip half of the 9-qubit code"), and the discretization content is now landed (row above). The obstacle is dimensional: the corpus does QEC **entry-wise** on `Fin 2 × Fin 2 × Fin 2` (8-dim, 64 entries, `fin_cases`-friendly, 309 lines); Shor-9 is 512-dim / 262 144 entries and entry-wise tactics do not survive it. **What's needed:** either a **stabiliser-formalism layer** (Pauli group, (anti)commutation, syndrome as eigenvalue pattern — `PhaseFlip.lean` is already half-structural, and this would also unlock Steane-7 and CSS codes generally) or a **code-concatenation combinator** (inner code ∘ outer code, with the correctable set of the composite). The collapse argument itself is settled in form (`SyndromeCollapse.lean` at 3 qubits); it must be re-established at 9. **Effort:** L. The last iconic empirical item. | `EMPIRICAL.md`, `project_empirical_tests` |

| ~~**Unconsumed declarations**~~ (found by `scripts/check-vacuity.sh`, 2026-07-28) | **RESOLVED 2026-07-28 for the three substantive cases; three harmless aliases remain.** The scan found six declarations nothing consumed. **The three `Prop` fields now have consumers:** `liftA_liftB_unitary_commute` (`LF3/Projectors/TensorModel.lean`) is consumed by the new `liftA_liftB_apply_comm` — local operations on independent wings commute pointwise, the kinematic core of no-signalling. ⚠️ *Correction: an earlier version of this row said that field's prose implied it was load-bearing. It did not — its own docstring already stated "`MeasurementUnitary.factorises` itself does not consume it". The corpus was honest; the row was unfair.* `A_right` / `B_right` (`LF3/SectorSeparation.lean`) are consumed by `pointerOverlapA_pos` / `_discriminates` (and the B-wing twins) and the capstone `pointerLeakage_nonvacuous`: **`A_wrong`/`B_wrong` alone are satisfied by a completely uninformative pointer** (set every overlap to 0), and it is `A_right`/`B_right` that force the pointer to register and to discriminate. So the two-sided reading is now supported by theorems rather than assumed. **Remaining (harmless, deliberately not deleted):** `bell_prep_compose`, `mzOutput`, `productMeasureBridge` — aliases and a bundled re-export, dead but not misleading. | `scripts/check-vacuity.sh` |

| **Adjudicate the contradiction-scan shortlist** (`scripts/check-contradictions.sh`, 2026-07-28) | **OPEN — 8 identifiers described as settled in one place and open in another.** ⚠️ **The scan has LOW PRECISION and does not catch its own motivating case** — see its header; both hits spot-checked were benign (`BornFromFlow`: proved *given* a Birkhoff hypothesis, open *because* Mathlib lacks it; `fs_born_volume_ratio_N`: proved, with a known open extension — the `hpos` boundary row above). Treat the list as reading material, not as defects. The eight: `IsKahlerSector`, `isFubiniStudyKahler`, `BornFromFlow`, `born_eq_P_st`, `fs_born_volume_ratio_N`, `productSector_hasFubiniStudyPushforward`, and two more the run prints. **What's needed:** read each pair and, where the two descriptions genuinely disagree, fix the wrong one rather than softening both until they agree. ✅ **One genuine contradiction adjudicated and corrected 2026-07-30** (found by external review, not by the scan — another data point on its recall): `MomentMapRace.lean` asserted "there is **no separate dynamical problem to solve**" while `DeIsolationFlow.lean:36-38` correctly states the open obligation to exhibit an `H_int(M)` generating the basins. `MomentMapRace` was wrong and its docstring is now corrected — what the moment map dissolves is the **statistical** residual (no stochastic postulate needed *given* the basins), never the **dynamical** one (`basin_rate` is a hypothesis field). | `scripts/check-contradictions.sh` |

| **★★ Is the fibred Σ a legitimate A1 ontic sector?** *(successor to the parked A7 row)* | **OPEN — now the load-bearing question, and nothing is pointed at it.** The A7 chain's substantive output is that **the fibre is where contextuality has to live at `N ≥ 3`** ([`sigma-fibre-contextuality.md`](sigma-fibre-contextuality.md)) — which makes the fibre's own legitimacy critical. ⚠️ **SHARPENED 2026-07-29 — the gap is not what this row first said.** It is *not* "`FibredSigma` uses `ℝ`, `KSigma` is compact, swap them". The corpus has **two** record-layer constructions and **compactness and fibre-activity sit in different ones**: `KSigmaRecord.lean` puts a P5 `RecordSemantics` on the **compact** `KSigma = ℂℙⁿ⁻¹ × T²`, but its events are `π⁻¹' bornRegion ψ i` — **pulled back from the base**, so the torus fibre is *inert* and the construction inherits the preparation-indexed base regions. `FibredSigma.lean` has an **active** fibre (it carries the CDF Born cells, `cdfCell`) but that fibre is `ℝ` with Lebesgue restricted to `[0,1)` — **non-compact**. **Neither module has both properties**, and since the A7 work concluded contextuality must live in the *fibre*, the one we need is precisely active-fibre-on-compact-Σ. Also: no Kähler structure on the fibre, and its measure not shown Liouville, while Paper C A1 wants a compact Kähler ontic surface. **FIBRE HALF LANDED 2026-07-29** (`SigmaLayer/CircleFibre.lean`): the Born partition now lives on a **compact** fibre. `CircleFibre = AddCircle 1` (the same factor `KTorus` is built from) is compact with Haar a genuine **probability** measure (`circleFibre_volume_univ`) — what restricted Lebesgue on `ℝ` only had by fiat. `circleCell` is a **preimage** under the canonical-representative map, not an image of `cdfCell`, so measurability is immediate; `volume_circleCell` gives **exactly** the Born weight `rᵢ` (via Mathlib's `AddCircle.measurePreserving_equivIoc`), `volume_circleBornCell` gives `‖ψ i‖²`, and `circleCell_pairwiseDisjoint` keeps outcomes exclusive. **So compactifying the fibre changes nothing about the outcome probabilities.** **Record-semantics counterpart LANDED 2026-07-30** (`SigmaLayer/CircleRecord.lean`): `circleRecordSemantics` (P5, measurable + exclusive), `compatibleSet_circle_single` (P6 isolation = conditioning), `circleOutcome_eq_record` (the ontic selection *is* the record), `circleBornMeasurement_prob` (`= ‖ψ i‖²`, the same weight `ℝ` gave), `circleBornMeasurement_ae_total`. ★ The `RecordSignature` is reused **verbatim** — it never mentioned the fibre — so only the *semantics* changed, which is why no Born weight moved. ★ And `ae_total` came out *stronger* — but ⚠️ **CORRECTED 2026-08-11**, this row previously gave the wrong reason ("on `ℝ` it had to be restricted to `[0,1)` by hand because Lebesgue measure on the line is infinite"). `fibreTypicality` is `volume.restrict (Ico 0 1)`, a **probability** measure, not Lebesgue on the line, and the restriction was never forced: `fibreTypicality_uncovered_univ` proves the `univ` form on `ℝ`. The real content is the one this row already states correctly two sentences earlier — on the circle the probability mass is Haar mass, *what restricted Lebesgue on `ℝ` only had by fiat* — and `fibreTypicality_Ici_one` makes the fiat explicit: the fibre's complement, of infinite Lebesgue measure, gets typicality zero, so an uncovered point there is excused by the measure rather than covered by a cell.

⚠️ **TWO CORRECTIONS, 2026-07-30 (external review, both verified against the files).**

**(1) The re-plumbing is NOT done — this row said "DONE" and the landing commit was headlined "the record layer now runs on the compact fibre". Both are retracted.** What exists is a **parallel compact counterpart** of the record semantics, proved in full. The corpus's actual capstones — `Measurement.lean`, `RecordLayerClosure.lean`, `FiniteQMClosure.lean`, `KSigmaRecord.lean` — are untouched since 2026-07-25 and still run on the `ℝ` fibre with `fibreTypicality`, and **nothing outside `AxiomAudit.lean` imports the circle modules**. Retiring the `ℝ` closure is real, open work.

**(2) A single circle CANNOT complete A1, and the reason is parity, not tooling.** This row (and `CircleFibre.lean`) classified the missing Kähler structure as *permanently scoped per §2a — blocked on Mathlib's absent manifold exterior calculus*. **That was a misclassification.** `ℂℙⁿ⁻¹` has real dimension `2n-2`, so `ℂℙⁿ⁻¹ × AddCircle 1` has real dimension `2n-1` — **odd** — and no odd-dimensional manifold admits a symplectic form (`ωᵏ` must be a volume form), hence none admits a Kähler structure. More Mathlib API would not change this. ⚠️ The same objection applies **retroactively to `FibredSigma`'s `ℂℙⁿ⁻¹ × ℝ`**, also `2n-1`: that gap was never a tooling gap either.

**The fix is already in the corpus and is cheap.** `LF4/KahlerInstance.lean` has `KTorus = AddCircle 1 × AddCircle 1` and `KSigma N = CPN N × KTorus` — real dimension `2n`, **even**, compact, a product of Kähler manifolds. Put `circleCell` on the **first** torus coordinate and keep the second as its symplectic partner; every `CircleFibre` theorem is stated about one `AddCircle 1` and transports, so what is missing is the product-measure step, not new fibre mathematics.

**So: the compactness objection is closed; the A1 objection is NOT scoped, it is open and tractable.** What is left of this row: (a) ~~move the partition onto `KSigma`'s `T²`~~ **DONE 2026-07-30** (`SigmaLayer/TorusFibre.lean`, foundational-triple, 6 pins verified in isolation) — `torusCell r i = circleCell r i ×ˢ univ` puts the Born cells on the **first** torus coordinate with the second free; `volume_torusCell` gives **exactly** `rᵢ` (the free coordinate contributes a factor of `1`, `T²`'s Haar being a probability measure), `volume_torusBornCell` gives `‖ψ i‖²`, plus exclusivity and a.e. totality. ★ `mem_torusCell_iff` states the second coordinate's freedom — "the symplectic partner carries no record content" — as a *theorem* rather than prose. ⚠️ **This REMOVES AN OBSTRUCTION to A1; it does not ESTABLISH A1.** `KSigma` is not proved Kähler here, and the partition is still kinematic and still preparation-indexed. Nothing outside `AxiomAudit.lean` consumes it yet; (b) exhibit the fibre measure as Liouville rather than merely Haar; (c) ~~retire the `ℝ` closure~~ **DONE 2026-07-31** (`SigmaLayer/GlobalRecordClosure.lean`, foundational-triple, 4 pins). `GlobalRecordClosure` certifies **the same five record-layer facts** as `RecordLayerClosure` — exclusivity, selection-is-record, isolation-is-conditioning, Born, a.e. totality — but on `KSigma = ℂℙⁿ⁻¹ × T²` for a `ContextField` rather than on `ℝ` for `bornContext ψ`. ★ **The record event is now a function of `(context, outcome, time)` and nothing else** — visible in the type, needing no theorem: the *same* set serves every preparation and only the epistemic measure moves, where `fibreRecordSemantics`'s event was `cdfCell (bornRate ψ)` and moved with `ψ`. ★ `globalOutcome` is literally `circleOutcome` read at the point's own base, so the ontic selection needed no new machinery. ★ `ae_total` strengthens from `Ico 0 1` to the whole space. **That the five fields are otherwise identical is the evidence that neither defect was ever load-bearing for the record layer's content.** ⚠️ `RecordLayerClosure` is **superseded, not deleted** (still true, still consumed), and **`FiniteQMClosure` still carries the older `vnPointerOutcome` readout** — swapping that is a separate migration on the `productDynamics` engine and is **not** done. Still kinematic. Related and also open: the fibre *mechanism* (softmax/Gumbel) is **posited, not derived** from de-isolation dynamics, and the Gumbel form is injected noise rather than CSD-native. **Effort: M–L** (revised down from L — the `T²` route needs no Mathlib API).

**★ SUCCESSOR TARGET — the global context-fixed basin on `T²`** — **LANDED 2026-07-30** (`SigmaLayer/GlobalBasin.lean`, foundational-triple, 6 pins; plus `LF4.continuous_momentMap` / `measurable_momentMap`). `ContextField` bundles a measurement context as a **measurable simplex-valued rate field on the base**; `globalBasin c i = {x  **PARITY GAP CLOSED 2026-08-09** (`SigmaLayer/TorusRecord.lean`): the compact fibre `S¹` is one-dimensional, so `ℂℙ^{N-1} × S¹` has ODD real dimension and cannot carry a symplectic or Kähler structure at all — compactness was fixed, parity was not. `TorusFibre` had already moved the Born cells onto `T²` (cells constrain `θ₁`, `θ₂` free); this ports the rest of the record layer there, so the **active fibre and the A1-admissible arena are finally the same object**: `torusRecordSemantics` (P5), `compatibleSet_torus_single` (P6), `torusOutcome_eq_record` (the ontic selection IS the record), Born weight `‖ψ i‖²` unchanged. ⚠️ **Not an A1 discharge**: no Kähler form is constructed on the arena and the fibre measure is not shown to be a Liouville volume for one (KG-1's Mathlib forms block). Parity was a *necessary* condition that was violated and now is not; necessity is not sufficiency, and the row stays open on the Kähler/Liouville half. | x.2.1 ∈ circleCell (c.rate x.1) i}` reads the rate **at the ontic point**, so **no `ψ` appears in the definition**. `measurableSet_globalBasin` (two inequalities between measurable real functions — the step the moment-map regularity unblocks), `globalBasin_pairwiseDisjoint`, `globalBasin_prob` (conditioning the epistemic state on `p` returns `rate p i`), `globalBasin_ae_total`, and the headline **`globalBasin_born`: at preparation `ψ` the basin's probability is exactly `‖⟨eᵢ,ψ⟩‖²`** — Born, from a partition that never mentions the preparation. ⚠️ **What this closes is the PREPARATION-INDEXING defect, not general-`N` A7 outright.** Whether Paper C intends the regions to be *base-only* — in which case the parked chain still governs — is a question about the axiom, not about the file. Still **kinematic** (no `H_int(M)`), `δ_p ⊗ Haar` is the **epistemic** measure taken as a definition rather than a disintegration, and `KSigma` is still not proved Kähler. ⚠️ **Estimate correction:** the `momentMap` measurability prerequisite was logged **M**; it was **S** — `Mathlib/LinearAlgebra/Projectivization/{Topology,MeasureSpace}.lean` already staged `continuous_iff_continuous_comp_mk'` and the Borel instance, so the descent was four lines. *(Original assessment retained:)* Define `Bᵢ(M) = {(p, θ₁, θ₂) : θ₁ ∈ cdfCell (m_M p) i}`, where `m_M` is the torus moment map in the basis `M` selects. The point: `m_M` is evaluated **at the ontic point `p`**, not at the preparation, so `Bᵢ(M)` depends on the apparatus context **alone** — which is what Paper C A7 literally asks for, and what `bornContext ψ` does not give. Born follows by conditioning on the preparation and integrating the fibre: the slice over `p` has Haar measure `(m_M p) i`. This does **not** collide with the parked general-`N` chain — that chain constrains **base-only** densities, and this is genuinely fibred, exactly as `sigma-fibre-contextuality.md` concluded. It is not circular either: `bornRate_eq_momentMap` already establishes the rates as *forced* by the Kähler structure and the `Tⁿ` action. ⚠️ **Design point to state, not gloss:** conditioning on the preparation means conditioning on `p = [ψ]`, a **null set** in `ℂℙⁿ⁻¹`. Take the isolation-conditioned epistemic measure to be `δ_[ψ] ⊗ Haar` outright (a legitimate product measure) rather than disintegrating — and be explicit that this is the *epistemic* measure, **not** the sector's Liouville measure. ⚠️ And it stays **kinematic**: it does not touch the Paper D `H_int` obligation. ⚠️ **KNOWN PREREQUISITE, found 2026-07-30 while landing the arena:** `Bᵢ(M)`'s measurability needs `momentMap` to be measurable (or continuous), and **the corpus has no such lemma** — `LF4/MomentMap.lean` proves `momentMap_nonneg`, `momentMap_le_one` and `momentMap_sum_eq_one` (which *are* exactly the rate hypotheses `volume_torusCell` wants) but says nothing about regularity. `momentMap` is a ratio of norms on a projective quotient, so this is a genuine small piece of work, not a `fun_prop` call. It is a detour, not a wall — but it is why this target is **M**, not **S**. | `sigma-fibre-contextuality.md`, `SigmaLayer/FibredSigma.lean` |

| **★ General no-signalling over a non-factorising Σ (C1 item 16)** | **OPEN — recorded 2026-08-10, deliberately not closed.** Determine sufficient **primitive** conditions on setting-dependent measure-preserving CSD dynamics over a non-factorising ontic Σ that **imply** operational remote marginal invariance, *without* assuming Bell-local pointwise response independence. ⚠️ **Why this row exists rather than a theorem:** the measure-level predicate `LF3.OperationalNoSignalling` is close to a restatement of the conclusion rather than a premise doing derivational work, so verifying it in the constructed sector (`localDeisolation_no_signalling_A/B`) is a **verification, not a derivation**. C1 §4.2 must say so in those words. ⚠️ **And the pointwise form is not merely too strong — it is FALSE in this sector:** imposing `F_A(a,b,x) = F_A(a,b',x)` on both wings over a deterministic shared state yields exactly the setting-local response pair `no_product_partition_realises_singlet` forbids. ⚠️ **Hidden premise now named:** `OperationalNoSignalling` fixes ONE `μ` across all four contexts, and that fixture **is measurement independence** — a genuine Bell premise that was previously carried silently by the shape of the definition. No axiom is added for this row. | `LF3/OperationalNoSignalling.lean`; `specs/c1-correction-plan.md` §1 |
| **★★ The dynamical measurement layer (Paper D `H_int`)** — *successor to the context-fixed basin row* | **OPEN — and the diagnosis that opened it is sharper than the "still kinematic" caveat `GlobalBasin` carried.** ⚠️ **`globalBasin_ae_total` shows the basins cover `Σ` up to a null set, so a.e. every point ALREADY carries a record and there is no apparatus-ready state of positive measure.** A flow cannot *create* a record in such a space — so "add dynamics to `GlobalRecordClosure`" is not merely incomplete, it is **structurally impossible**. The repair (external review 2026-08-01) splits the hidden **selector** from a pointer **register**: `Σ_meas = ℂℙⁿ⁻¹ × T²_λ × T²_R`, real dimension `2n+2` — even, compact, a Kähler product (parity checked). ★ **CONSISTENCY CHECK RUN FIRST, before building anything** (`SigmaLayer/MeasurementConstraints.lean`, foundational-triple, 3 pins) — the cheap-failure discipline the `ℂℙⁿ⁻¹ × S¹` parity argument should have had. **Result 1 — the measure check comes back NEGATIVE, i.e. it does *not* obstruct:** `pointer_region_measure_ge` gives `μ_sel(S)·μ_R(R₀) ≤ μ_R(B)`, which granting the (unformalised) `μ_sel(Sᵢ) = 1/n` reads `μ_R(Bᵢ) ≥ μ_R(R₀)/n` — satisfiable with room. That is a **green light to attempt the concrete `H_int`, not evidence a witness exists**; measure preservation is a weak invariant. `ready_region_measure_le` is the summed form and is recorded **as weak** rather than dressed up. **★ Result 2 — the constraint with teeth:** `no_everywhere_correlation` — a continuous `Φ` sends the preconnected `S ×ˢ R₀` to a preconnected image, which cannot meet two disjoint *open* pointer regions, so an **everywhere** correlation is impossible at `n ≥ 2`. **Therefore the `a.e.` in the correlation theorem is mathematically necessary, not a convenience:** the exceptional set must be **non-empty**, it contains the seams between selector sectors, and its image threads the gaps between pointer regions. Two consequences: (i) any candidate `H_int` advertised as giving an *exact* correlation is wrong on these grounds alone, without inspection; (ii) a witness that leaves the seam unspecified has not addressed the hardest part of its own statement. **REMAINING WORK, and the ordering matters:** the plan's items 1–2 and 4–8 (arena, two-time propagator interface, dynamic Born, persistence on a finite `[T_M, T_M+τ_R]`, Lüders update, generalisation, capstone) are tractable Lean engineering; **item 3 — an explicit `H_int(M)` or propagator proved to arise from one, with `Φ(Sᵢ × R₀) ⊆ Bᵢ` a.e. — is the research result and is unbounded.** ⚠️ **Build only the minimum interface needed to *state* item 3, then attack item 3.** Executing 1→9 in order risks a large scaffold with a hypothesis at its heart, which is the failure the plan itself warns against (`basin_rate` is already one). **ARC COMPLETE THROUGH RANK-ONE LÜDERS (2026-08-01/02, eleven commits `750327f..3f8b9a9`).** What landed, in dependency order: **(a)** the interface (`MeasurementProtocol`, two-time propagator, ready/pointer regions; `CorrelatesOn`/`PointerInvariantOn` kept as *hypotheses*, never fields); **(b)** persistence + post-measurement ensemble (`RecordPersistence` — selection vs disturbance finally expressible); **(c)** the **shear witness** discharging both hypotheses from an explicit propagator, with measure preservation and dynamical Born (`ShearWitness`, `DynamicBorn`); **(d)** ⚠️ `shear_base_marginal_unchanged` — a machine-checked **negative** result: the shear cannot collapse, structurally (no back-reaction ⟺ no Lüders); **(e)** the **collapse no-gos** (`no_exact_collapse` — pointwise collapse impossible for measure-preserving dynamics, so collapse must be *relocation of a null slice*; `collapse_accuracy_bound` — approximate collapse priced in ready-state improbability, Landauer as a measure inequality, *forcing* the Dirac-calibration convention); **(f)** the **calibrated-swap witness** (`SwapWitness`, design from a Fable review with one substantive correction found by verification — the forward-only crossing flag fails the two-time law on non-monotone triples; the involution `G` fired symmetrically repairs it, all 8 cases closing) — record-triggered bank swap, measure-preserving by the new partition lemma; **(g)** ★★ **the Lüders theorem** (`SwapLuders`): conditioned on outcome `i`, the post-measurement system marginal **equals** `epistemicMeasure [eᵢ]` as a pushforward, so follow-up statistics for any context are the collapsed state's Born weights (`swap_luders_born`). Items 7–8 landed alongside (`OutcomeField`/`blockField`/`outcomeBasin` — degenerate rate fields reach the basins; `DynamicMeasurementClosure` + `CsdFiniteQMClosure`, now carrying the `luders_followup` field). **The plan's boxed completion criterion is met line-by-line for nondegenerate computational-basis measurements.** **STILL OPEN on this row:** (i) **degenerate Lüders** — **PROBLEM FORMALISED + BOUNDARY PROVED 2026-08-02** (`SigmaLayer/DegenerateLuders.lean`, foundational-triple, 4 pins): `BlockLudersObligation` states the demand (§8.3 `_statement` pattern — post-marginal = `epistemicMeasure [Πᵢψ]`, **ψ-dependent**); ★★ `swap_not_blockLuders` proves the calibrated-swap witness fails it for **any** fixed calibration whenever a block has dim ≥ 2 (the swap's preparation-*independence*, `swap_luders_marginal`, turned against it at two in-block vertices); ★ `degenerate_selector_born` proves the positive half that survives — the block-selector's sectors carry exactly the coarse-grained Born weights, the dynamical form of `blockField`. **Statistics generalise; the update is what does not.** The **witness construction stays open**: it must relocate `[ψ] ↦ [Πᵢψ]` while preserving measure, so the lost base data must be *stored* (`no_exact_collapse` governs); route = the **projective-join decomposition** `ℂℙ^{N-1} ≈ join(ℂℙ^{dᵢ-1}, ℂℙ^{N-dᵢ-1})`, wall = the FS-measure decomposition under the join, unformalised geometry, effort **L**. **BRICK 1 LANDED 2026-08-02** (`SigmaLayer/BlockCollapse.lean`, foundational-triple, 4 pins): the measurable ray-level collapse `blockCollapse : [ψ] ↦ [Πᵢψ]` (quotient descent + `measurable_iff_measurable_comp_mk'`); ★ **collapse as relocation** (`luders_target_eq_relocation`, `blockLudersObligation_iff_relocation` — the §8.3 target *is* the pushforward of the preparation under a deterministic relocation, fibre untouched); and the **vector-level witness core** `componentSwap` on `ℂᴺ ⊕ ℂᴺ` (involutive, summed-norm-preserving; with a block-calibrated slot it collapses *with the residual stored*). **The wall, sharpened:** the ray-pair descent fails on the *relative scale* the product `ℙ×ℙ` quotient forgets — a surviving relative `U(1)` is what the join needs. Routes: (i) FS disintegration under join coordinates (original); **(ii) NEW — a phase-carrying slot** (sphere-level bank, quotient at readout), likely cheaper. ~~`swap_not_blockLuders` remains the boundary; no ray-level witness claimed. Remaining effort **L** (route ii: M–L);~~ **BRICK 2 LANDED 2026-08-02 — route (ii) WORKS** (`SigmaLayer/PhaseSlot.lean`, foundational-triple, 4 pins): ★★ `phase_slot_block_luders` — phase-orbit preparation (uniform over the ontic phase; `readout_phasePrep` shows the enrichment adds no epistemic content) + **fixed** block-calibrated slot + the **total, involutive, measurable** partial pair swap (`pairSwap`) ⇒ ray-level post-state **exactly** `δ_{[Πᵢψ]}`, preparation-dependent from a fixed calibration. The no-go is *evaded, not contradicted*: it killed full swaps (post = slot prior); the partial swap keeps the system's block part and stores the complement — and is only well-defined on the phase-enriched arena, as the sharpened wall predicted. ~~**Brick 3 (remaining, effort M):** register/sector protocol plumbing on the enriched arena (mirror `SwapWitness`) + Liouville preservation via a unitarily-invariant reference measure on the doubled space (Gaussian route; `componentSwap_norm_sum` is the invariance driver);~~ **BRICK 3's LIOUVILLE HALF LANDED 2026-08-02** (`SigmaLayer/JoinArena.lean`, foundational-triple, 4 pins) — and the Gaussian route was not needed: **the phase-enriched pair arena IS the projective join `ℙ(ℂ^{N+N})`** (global phase quotiented, relative phase survives in the point). There the component swap is a **permutation unitary**, so ★★ Liouville preservation = FS unitary invariance (`joinSwap_measurePreserving`, one line from `fubiniStudyMeasure_smul_invariant`), and ★★ the Lüders update is **pointwise deterministic** (`join_block_luders` — every join microstate reads out to exactly `[Πᵢψ]`; slot nonvanishing not even needed). The recorded join route's original wall (FS disintegration) was never crossed — it was **dissolved**: the join arena needs no disintegration because the dynamics is unitary on the whole space. ~~**Remaining, final piece (mechanical M, no new mathematics):** the register/sector `MeasurementProtocol` on `ℙ(ℂ^{N+N}) × T²_R` firing `joinSwap` (mirror `SwapWitness`) + the conditioned-marginal bookkeeping to a `BlockLudersObligation` instance;~~ **BRICK 4 (the protocol) LANDED 2026-08-02** (`SigmaLayer/JoinProtocol.lean`, foundational-triple, 4 pins): the degenerate measurement runs as a `MeasurementProtocol` — arena (join point, system fibre, **ancilla fibre**) × register, record-triggered `joinG` (join unitary + fibre exchange; the original fibre **stored**), two-time law (the eight-case crossing proof), `CorrelatesOn`/`PointerInvariantOn` discharged, ★ full-propagator **Liouville preservation** (`joinEvolve_measurePreserving`). Regions/readout/sectors inherited from `shearProtocol` by structure update. ~~**Brick 5 (the last):** the sector-conditioned post-marginal for the canonical phase-orbit preparation = `epistemicMeasure [Πᵢψ]` — the `BlockLudersObligation` instance, mirroring `SwapLuders`'s conditioning bookkeeping (pointwise input `join_block_luders` already proved); effort **M**;~~ **BRICK 5 LANDED 2026-08-02 — THE DEGENERATE-LÜDERS ARC IS CLOSED** (`SigmaLayer/JoinLuders.lean`, foundational-triple, 4 pins): ★★ `joinWitness_blockLuders` — **`BlockLudersObligation` is inhabited**. The demand `swap_not_blockLuders` proved impossible for every fixed ray-level calibration is delivered by the join witness: ψ-dependent post-states `epistemicMeasure [Πᵢψ]` from a fixed block-supported calibration family, through Liouville-preserving dynamics in the standard protocol architecture, with positivity derived from `Πᵢψ ≠ 0` alone (`goodTheta_vol_pos` — no measure hypothesis). The five bricks: relocation target → phase slot → join arena → protocol → obligation. `swap_not_blockLuders` stands as the boundary theorem for the ray-pair arena. **Row CLOSED**; (ii) **Hamiltonian generation — REOPENED 2026-08-02** (external review; the earlier same-day "permanently scoped (Mathlib gap)" classification was **misjustified** — the second `check-impossible-before-scoped` failure: we verified *parity*, not *continuity*). The review's claim is now **machine-checked**: ★ `shearEvolve_not_continuous` (`SigmaLayer/ShearDiscontinuity.lean`) — the witness propagator displaces the register by the *discrete* `shearAmt (basinIndex x)`, and a continuous propagator would give a clopen partition of the (proved-connected) `KSigma`; so the witness is a measurable, measure-preserving **piecewise** map and **cannot be the time slice of any continuous flow**. Writing down `H_int = g(t)·Â_sel ⊗ P̂_R` does not make the witness its flow: that generator's flow *is* continuous, so it is a **different map**. Kept honest in both directions: `no_everywhere_correlation` proves *some* seam set is forced for every exact-record dynamics — the witness's jumps sit exactly on the seams the no-go requires, so the discontinuity is structural, not a construction defect. **Two repair routes, decision pending (authorial):** (1) **smooth corridor regularisation** — mollify the selector across seam corridors; records/Born correct up to `ε`, priced by `collapse_accuracy_bound`; effort M–L, genuinely new mathematics; (2) **classify CSD measurement dynamics as piecewise Hamiltonian with null seam set** — standard in dynamics (billiards, impact systems), physically justified by the forced-seam no-go; effort S (docs + the classification statement). The narrower `X_H = ω⁻¹dH` *formalisation* gap (symplectic form + exterior derivative in Lean) remains a genuine Mathlib matter, but it is no longer the load-bearing obstacle; ~~**DECIDED + LANDED 2026-08-02 (route 2: piecewise Hamiltonian)**~~ **REOPENED AGAIN 2026-08-02 (second external review), on two corrected grounds:** **(flux)** the basin-cylinder pieces are rigid torus translations — symplectic and measure-preserving, but **not globally Hamiltonian**: `ι_Xω = a·dp` is closed-not-exact on `T²` (`∮dp ≠ 0`), so no global generating function exists and the `hᵢ = aᵢ·p_R` reading was wrong; the correct name for the landed classification is **piecewise rigid symplectic translation with null seam set** (`PiecewiseHamiltonian.lean`'s theorems all stand; its docstring carries the dated correction, and the theorem names are retained for pin stability); **(overread)** `no_everywhere_correlation` forces an *exceptional non-correlating set*, **not** discontinuity — a continuous, even smooth globally-Hamiltonian propagator that maps seams to transition states outside `⋃Bᵢ` remains **open**. **★ ROUTE CONFIRMED 2026-08-03 (author decision: retain the smooth-Hamiltonian architecture; execution plan `pointer-witness-plan.md`): the compact Kähler pointer `ℂℙ^K`** — replace the torus register with a projective pointer (one ready state, `K` record states); unitary rotations on `ℂℙ^K` **are** globally Hamiltonian (`H¹(ℂℙ^K) = 0`, moment maps exist), killing the flux obstruction at the root. Obligations for the new witness: continuity/smoothness, Hamiltonian generation (moment-map level), measure preservation (FS unitary invariance — free), a.e. record creation, Born sector weights, persistence, and the Lüders composition. **Effort L.** ~~The papers question (Paper C A2 / TN6 require smooth Hamiltonian flow — retain that architecture, per the review's strong recommendation, vs. amend the papers) is **authorial**;~~ **DECIDED 2026-08-03 (author): the smooth-Hamiltonian architecture is RETAINED** — Paper C A2 / TN6 stand as written, and the `ℂℙ^K` pointer witness is the obligation that must satisfy them; the landed piecewise witnesses stay as the exact-record horn of the `no_everywhere_correlation` trade-off, not as the architecture's final word; **★★ NEGATIVE RESULT 2026-08-10 on the LAST obligation in that list (`SigmaLayer/RelocationObstruction.lean`, foundational-triple, 4 pins; plus 4 staging pins).** `PointerGeneration.lean` discharged the *record-CREATING* obligation (`rampedU_schrodinger`: the stroke solves the Schrödinger ODE for an explicit Hermitian generator at every time and every weight vector). The **Lüders-composition** obligation **cannot be discharged as posed**, and the obstruction is topological, not flux. **Horn 1** (`pointerBankSwap_not_flow_time_one`): the relocation exchanges the system factor with bank slot `j`; embed a circle in the system's torus angle and the exchange **collapses that section** (afterwards the readout coordinate reports slot `j`, which the section held constant), and the circle is not contractible — so no jointly continuous family joins the identity to the relocation. ⚠️ **This is NOT the `PiecewiseHamiltonian` flux obstruction**: flux obstructs a symplectomorphism *within* the identity component; the exchange **never reaches that component**, and the `H¹(ℂℙ^K) = 0` escape that saved the record stroke does not apply, because the obstruction lives in the **bank's product structure**, not the pointer's topology. **Horn 2** (`pointerImprint_not_injective` / `_not_homeomorph`): the obvious non-permutation repair — write the system into a slot instead of exchanging with it — is **not injective**, hence not a homeomorphism, hence not a flow map either, for a completely independent reason. **Scope, stated in-module:** this constrains the **swap architecture**, not collapse-as-dynamics in general. What survives: a generated relocation must be a **bijection that is not a factor exchange**, which points at the join and phase-slot routes (`JoinLuders.lean`, `PhaseSlot.lean`), where the ψ-dependence is produced by the dynamics rather than by permuting coordinates. Read together with `swap_not_blockLuders` (same architecture, obstructed by calibration rather than topology), the swap route is a **witness that cannot be generated**. **Enabling infra — two genuine Mathlib gaps filled:** `Mathlib/Topology/Homotopy/CircleFundamentalGroup.lean` (π₁(S¹) as the deck group 2πℤ — Mathlib has the whole covering apparatus and `Circle.isAddQuotientCoveringMap_exp` but nowhere states the consequence, checked at the pin) and `Mathlib/Topology/Homotopy/FactorExchangeObstruction.lean` (the obstruction, stated **basepoint-free**: routing through `contractible_iff_id_nullhomotopic` avoids the π₁ basepoint-conjugation bookkeeping entirely, which was the predicted cost and did not materialise). Also constrains **record broadcasting**: carrier maps that relocate a record by permuting factors inherit horn 1, imprint-style copies inherit horn 2, so redundancy built either way is stuck with hand-built maps; **★★ RESOLVED THE SAME DAY — THE JOIN RELOCATION *IS* GENERATED** (`SigmaLayer/JoinGeneration.lean`, foundational-triple, 6 pins). The obstruction really was about the **swap architecture**, exactly as its scope note claimed. **Why the join escapes both horns, structurally:** the bank arena is a **product** `(Σ × pointer) × (Fin N → Σ)` and `pointerBankSwap` exchanges two of its **factors**; the join arena is `ℂℙ^{N+N-1}`, a **single** projective space, and `joinSwap b i p = joinU b i • p` is one unitary acting on one connected space — so there are no two arena factors to exchange (horn 1 has nothing to act on) and a projective unitary is bijective (horn 2 has nothing to act on). **And the escape is constructive, not merely permitted:** `joinMat` is a real permutation matrix of an **involutive** permutation (`joinPerm_involutive`), hence **Hermitian** (`joinMat_conjTranspose`) and an **involution** (`joinMat_mul_self`), hence has eigenvalues ±1; so `joinProj := ½(1 - P)` is a Hermitian **idempotent** (`joinProj_mul_self`); so `joinFlowMat t := (1 - Q) + e^{iπt}Q` is **unitary at every time** (`joinFlowMat_mem_unitaryGroup`) with `U 0 = 1` and `U 1 = joinMat`; and ★★ `joinFlowMat_hasDerivAt` proves it solves the **Schrödinger equation** `U'(t) = (i·H)·U(t)` for the **explicit Hermitian generator** `H = π·Q` (`joinGen_isHermitian`). Payoff ★★ `joinSwap_eq_flowTimeOne`: the join relocation is the **time-one map of a Hamiltonian flow**, the exact property `pointerBankSwap` provably lacks. *Technique note:* **no matrix exponential is needed** — on an idempotent the exponential series collapses to `1 + (e^z - 1)Q`, so writing that closed form down directly turns the whole construction into algebra (the `Matrix.Norms.L2Operator` scope is still needed for `HasDerivAt` on matrices). **So: collapse CAN be dynamics.** With `rampedU_schrodinger` (record-creating stroke) and this (relocation stroke), both strokes of a measurement now have explicit Hermitian generators — on **different** architectures, which is the honest statement. ⚠️ **Remaining, and it is the real frontier:** the two-stroke composite is still **triggered** by a readout, and the trigger is where `no_everywhere_correlation` bites. What is now settled is that the **relocation is not the obstacle**; the **trigger** is. Design rule extracted: a generated relocation must be a **bijection that is not a factor exchange** — satisfied by being a unitary on an irreducible arena rather than a permutation of coordinates on a reducible one; (iii) ~~unitary covariance~~ **DONE 2026-08-02** (`SigmaLayer/RotatedSwap.lean`: ★★ `measurement_covariance` — the six-fact closure for a first measurement in **every** orthonormal basis, rotated bank + rotated selector; context-generic accounting `sector_born_ctx`/`prep_outcome_pos_ctx`; the BB84 **primal round** lands as `bb84_primal_wrong_basis`, dual-round caveat retired); mixed preparations / POVMs remain (plan's extension steps 3, 4); (iv) the **engine migration** (operational and dynamical closures still a conjunction over different arenas). **Step 1 of 2 DONE 2026-08-02** (`SigmaLayer/SwapClosure.lean`, review step 2): all six dynamical facts now live on the **swap arena alone** (`SwapMeasurementClosure`/`swapMeasurementClosure`, foundational-triple) — with the Born field upgraded to the **dynamical** sector measure (`swap_sector_born`, consuming `measure_outcomeSector_eq_of_correlates`) and the Lüders field carrying **no** measure hypothesis (`prep_outcome_pos`, relocated to `SigmaLayer`). ~~Step 2 — migrating the **operational** closure onto the same arena — remains **OPEN, effort L**.~~ **Step 2 DONE 2026-08-02** (`SigmaLayer/UnifiedArena.lean`, foundational-triple, 4 pins): ★★ `unifiedArenaClosure` — **one ontic model carries the finite-QM reconstruction**. `UnifiedArena M` with `arenaLiouville = swapMeasure (kMuL p₀)`: the lifted `exp(-itH)` preserves it and projects to Schrödinger; `swapEvolve` preserves the *same* measure; FS bridge, i.i.d. Born frequencies and mixed-state Born weights transfer through the system-slot marginal; the six dynamical facts embed as a field. All eleven operational fields accounted for (migrated/upgraded/superseded — the mapping table is the module header). ~~**Recorded residues (S):** the mixed-state two-stage LLN on the arena (`mixtureMeasure` lift, mechanical); the composed **round-trip theorem** (isolate → measure → isolate with records intact) — the natural next statement on the now-shared arena.~~ **Both residues DISCHARGED 2026-08-02 (same day):** `arena_mixed_born_frequency` (two-stage LLN through the system-slot marginal) and ★ `arena_round_trip` (isolate → measure → isolate; the record survives because the pointer register is a conserved coordinate of the lifted flow, `readout_arenaIso` — definitional). The engine-migration row is **CLOSED**. `CsdFiniteQMClosure` stays as the historical two-arena capstone. *(Original row detail retained below.)* **First witness candidate: the von Neumann shear** `H_int(t) = g(t)·Â_sel ⊗ P̂_R` — textbook, Hamiltonian, fits the nondegenerate computational-basis case; the wrinkle is that on a *compact* `T²_R` the translation wraps, so the `Bᵢ` must be arcs with `T_M` and coupling chosen to keep images disjoint. ⚠️ **Note on the plan's item 9:** it proposes rewriting Paper C §5 on the grounds that base-only regions are "no longer supported". **That over-reads the Lean.** Our result shows a *fibred* construction **works**; the `ContextFixedA7` chain does **not** show base-only **fails** — it is parked, open in both directions. Changing the paper is an authorial call, but not on those grounds. **Effort: interface M; `H_int` unbounded.** | external review 2026-08-01; `SigmaLayer/MeasurementConstraints.lean` |

| ~~**★ A5 approximate `(ε,T)`-projectability**~~ | **DONE 2026-08-02** (`Mathlib/Analysis/Matrix/DuhamelBound.lean` + `SigmaLayer/ApproxProjectability.lean`, foundational-triple, 7 pins) — the row's stated shape delivered in full: **the predicate** (`EpsProjectable`, oscillation form — the derivative form `sup‖d(δH)|_V‖ ≤ ε` is the §2a-scoped manifold statement, substitution stated not silent); **the exact case as the `ε = 0` instance, as an iff** (`epsProjectable_zero_iff` — zero fibre-oscillation ⇔ factors through `π`, tying to `kSectorDataFlow_projectable`); non-vacuity (`diagOnticEnergy_epsProjectable` — the corpus's own moment-map energies are exactly projectable); and **the tracking theorem** (`quantum_effective_shadowing` + `_state`, via the integral-free **Duhamel bound** `‖e^{tC}−e^{tA}‖ ≤ |t|‖C−A‖` for skew-Hermitian generators, unitarity from `StoneC1` meeting the L2 norm's C*-structure): `‖H−H₀‖ ≤ ε` and `|t| ≤ T` give sector dynamics tracking true dynamics to within `ε·T` — what "selects the sector" means operationally. ⚠️ **Residue, stated:** the shadowing lives on the Hilbert side where the corpus's dynamics runs; an ontic Hamiltonian *generating* the flow is **A2's** row, not A5's. *(Original row:)* **OPEN — the highest-value remaining item after the fibre, and NOT blocked on Mathlib** (identified by the A1–A7 audit, 2026-07-29). Only the **exact** case `H = h∘π` is formalised (`kSectorDataFlow_projectable`, `SigmaLayer/DynamicsBridge.lean`); the corpus contains **no `ε`-projectability at all**. But A5's *physical* content is the approximate case — `sup‖d(δH)|_V‖ ≤ ε` over a window `T` — which is what makes a Hamiltonian **quantum-effective** rather than arbitrary, i.e. what **selects the sector**. So the axiom that does the selecting has its actual content unformalised. Ingredients are already here: operator norms, matrix-exp differentiability, `Mathlib/Analysis/Matrix/StoneC1.lean`. **What's needed:** state `(ε,T)`-projectability as a predicate on `(H, π, V)`, prove the exact case is the `ε = 0` instance, and show the projected flow tracks `e^{-itH}` to within a bound controlled by `ε` and `T`. **Effort: M–L.** | `reconstruction-status.md` §2a |

## L — weeks

| Item | Status / what's needed | Former source |
|---|---|---|
| **Operator convexity → unconditional SSA** | ★ **REFRAMED 2026-07-31 — this may not need to be *proved* here at all.** `[LeanQIT2026]` (`QuAIR/Lean-QIT`, Apache-2.0, commit `48d4515`) **already proves DPI and SSA**: `relativeEntropy_dataProcessing_channel_ge` and `condMutualInfo_nonneg`. The repo is 322 modules with **zero `axiom` and zero `sorry`**, and is fully module-system like ours. ⚠️ **SSA here REMAINS CONDITIONAL** — this is a citation, not an import, and nothing in the corpus changed. **Why not imported:** Lake resolves one version per dependency, and upstream targets Lean `v4.30.0` / Mathlib tag `v4.30.0` (2026-05-26) against our `v4.33.0-rc1` / master `c732b96d` (2026-07-20). **Why that is fine:** `hDPI` is stated entirely in *our* vocabulary, so discharging it means producing a term of that `Prop` — doable **outside this repo**. Plan: a separate **`csd-qit-bridge`** package depending on both **by tag**, holding the adapter and the unconditional SSA; neither project merges, and `CsdLean4` keeps its Mathlib-only footprint. ⚠️ **Vendoring was considered and rejected:** the transitive closure of their DPI module is **71 modules / ~60,800 lines**, against our whole corpus of 374 files / ~104,000 lines — ~59% of our mass in someone else's code, which would relocate the toolchain problem onto us permanently, require auditing 61k unfamiliar lines to our zero-axiom standard, and muddy provenance on a result that would then be partly theirs. **Highest-leverage next action: ask QuAIR about a toolchain bump — or whether DPI/Lieb concavity is headed for Mathlib**, which would dissolve the question entirely for both projects. **The ladder below stays the fallback**, not the plan of record. *(Original entry:)* **Deferred by user 2026-07-23** (leave as-is; SSA stays honestly conditional on `hDPI`). **Endpoint:** discharge `hDPI` (= data-processing inequality = joint convexity of relative entropy = Lieb concavity) in `strong_subadditivity_of_relEntropy_monotone` — SSA already follows from it genuinely; only `hDPI` itself is open (no axiom, no `sorry` — the wall is isolated as an explicit hypothesis, not papered). **Ladder status** (`operator-convexity-plan.md`, `Mathlib/Analysis/Matrix/OperatorConvex*.lean`): DONE — L.1 inverse operator-convexity (Schur), L.2-resolvent concavity, CStarMatrix↔Matrix bridge, the reframing lemma (operator concavity ↔ `ConcaveOn` on the Löwner order), cfc-integral commutation + order-topology instances, `x^p` endpoints `p∈{0,1}`. **LIVE WALLS:** (1) `x^p` interior `p∈(0,1)` — integral-rep assembly, *prerequisites already landed, closest to breaching*; (2) operator concavity of `log`; (3) L.4 operator-Jensen/Ando + L.5 Lieb concavity ⇒ `hDPI` (not started, the summit). Structural obstruction: `NonUnitalCStarAlgebra (Matrix n n ℂ)` fails, so `rpow` can't transport via CStarMatrix. **Effort:** ~4–7 working weeks (Mathlib-scale; Lieb concavity is not in Mathlib). Scoped probe available: the `x^p`-interior rung alone. (The prior "step 0 / PartialOrder ℂ cascade" note was stale — that order-instance layer has since landed.) | `operator-convexity-plan.md` |
| **GKSL / Lindblad open-systems tier** (LF6-9) | **Generator tier DONE 2026-07-20** (`LF6/LindbladGenerator.lean`): the general GKSL generator `ℒ(ρ)=−i[H,ρ]+Σₖ(LₖρLₖ†−½{Lₖ†Lₖ,ρ})` + trace-annihilation (`lindbladGenerator_trace`), Hermiticity-preservation, CP of the jump part (`lindblad_dissipation_posSemidef`, reusing the Choi/Kraus witness); dephasing shown to **be** a GKSL instance (`dephasingGenerator_eq_lindblad`) and to **solve** its master equation (`dephasingChannel_master_equation`). **Semigroup tier DONE 2026-08-13** (`LF6/LindbladSemigroup.lean`, Q5): `Φₜ = exp(tℒ)` for arbitrary GKSL generators — semigroup law, master equation, trace + Hermiticity preservation at the flow level. **Remaining (genuinely Mathlib-scale):** CP of the *exponentiated* `e^{tℒ}` for arbitrary generators (needs Lie–Trotter or resolvent positivity, neither in Mathlib). Unblocks LF6-2 full + Metrology A4. | `lf6-plan.md` |
| **§14 observable correspondence — general-N, all self-adjoint** | **DONE 2026-07-22** (`LF4/ObservableCorrespondenceN.lean`). `observable_correspondence_diagonal`: for any `N` and any real eigenvalue vector `lam`, `⟨ψ, diagonal(lam·) ψ⟩ = ∑ₖ lam k · vol(bornRegionN ψ k)` — the Hilbert expectation of a diagonal observable is the eigenvalue-weighted sum of its ontic Born-region Fubini–Study volumes (`diag_expectation` Hilbert side + `fsMeasure_bornRegionN` unifying `fs_born_volume_ratio_N`/`_apex` via `Fin.lastCases`). Foundational triple, AxiomAudit-pinned, carving-free, Gleason-free. Generalises the single-qubit `sg_observable_correspondence` to all N + all eigenvalues. Both the pointwise-volume form (`observable_correspondence_diagonal`) and the **canonical integral form** (`observable_correspondence_diagonal_integral`: `⟨ψ,Aψ⟩ = ∫ A_ontic dμ` with `A_ontic = ∑ₖ lam k·𝟙_{Rₖ}` an explicit measurable Σ-function, `bornRegionN_measurableSet`) are landed. **Now extended to ALL self-adjoint observables** (`hermitian_observable_correspondence` / `_integral`): via the spectral theorem `A = U·diag(λ)·Uᴴ`, `⟨ψ,Aψ⟩ = ∑ₖ λₖ·vol(bornRegionN φ k)` where `φ = Uᴴψ` is the state transported by the spectral unitary (`hermitian_expectation_transport` + isometry `transport_norm`) — no separate §13 Σ-flow machinery needed. §14 observable correspondence **fully discharged**. Foundational triple, AxiomAudit-pinned. | `LF4-todo §14` |
| **§14 *states* obligation** | **Pure-state / projector part DONE 2026-07-23** (`LF4/ObservableCorrespondenceN.lean`, `pure_state_born_prob_eq_volume`): `‖⟨Φ,ψ⟩‖² = vol(bornRegionN (Wᴴψ) 0)` — any pure-state / rank-one-projector Born probability (SuperdenseCoding's Bell projectors, Teleportation's input state) realised as an ontic Fubini–Study volume, via a unitary `W` with `W e₀ = Φ` (`exists_unitary_e_zero_eq`) + the unitary adjoint move (`inner_toEuclideanLin_adjoint`). Foundational triple, AxiomAudit-pinned. **Mixed-state / density-operator part also DONE 2026-07-23** (`Empirical/CSD/MixedStateBornVolume.lean`, `mixed_state_born_eq_ensemble_volume`): `ρ` realised as an ontic eigen-mixture — `Tr(ρ·|φ⟩⟨φ|) = ∑ᵢ (ρ.eigenvalues i)·vol(bornRegionN (Wᴴeᵢ) 0)`, composing `SigmaLayer.mixedEnsemble_capstone` + `LF2.born_quadratic` + `pure_state_born_prob_eq_volume`. §14 states obligation **fully discharged** (pure + mixed), foundational triple, AxiomAudit-pinned. | `BRIDGE-OBLIGATIONS.md` |
| ~~**Axiom pin: `rankOneDensity_unique_of_certainty`**~~ | **DONE 2026-08-09** (pinned in `Tests/AxiomAudit/Foundations.lean` beside the effect-Gleason pin; `lake build CsdLeanTests` green). *(Original row:)* **QUEUED 2026-08-09** (landing-surface rewrite, axiom reconciliation). The theorem is proved and its footprint is the foundational triple (verified directly), but it carries no `#print axioms` pin in `Tests/AxiomAudit/`, so CI would not catch future drift. One-line addition to `Tests/AxiomAudit/Foundations.lean`. Not made during the rewrite because that pass does not touch Lean source. Effort **S**. | `specs/papers-vs-repo.md`, `TODO-MANUAL.md` |
| ~~**★ Necessity audit of the constraint set**~~ | **DONE 2026-08-09 — `specs/necessity-audit.md`.** All thirty declared headlines (`CsdLean4/Headlines.lean`, CL-001…CL-030) classified against the five-class rubric, by the **Lean statement** rather than the docstring. Result: **1 necessity, 8 conditional necessity, 3 impossibility, 16 sufficiency, 2 instantiation.** The single unconditional necessity among the headlines is CL-024 `wigner_rigidity` (hypothesis: transition-probability preservation, nothing else). **The corpus's strongest-direction results are mostly NOT ledger rows** — `Matrix.StoneC1.stone_continuous`, `CV.no_exact_finite_ccr`, the three `SigmaLayer/MeasurementConstraints.lean` no-gos (pure topology / pure measure theory, zero CSD hypotheses), and `compositeAlgReconstruction` are all absent from the ledger. **Verdict:** the constraint set pins the substrate **up to a large family, not uniquely**, and the family is bounded more by the exclusions than by the positive results; what *is* pinned tightly is the layer one level above the substrate (symmetry, dynamics, probability, composition), all of which take the complex Hilbert space or matrix algebra as given. **Two systematic conditioners identified as the highest-value structural targets:** the symmetry group `U(N)`, taken as given in every measure-forcing result, and transition-probability preservation, taken as a hypothesis in every Wigner-selection result on a `KahlerOnticSetup` (and *not* derivable from `flow_preserves_volume` — measure ≠ metric). **Structural find:** `LF1.OnticSetup.hΦ_pres` (Liouville) is declared and **never consumed** — only `measurable_Φ` is extracted, disclosed in `LF1/Setup.lean`; symmetrically `KahlerOnticSetup.flow_preserves_volume` enters no Wigner or Schrödinger conclusion. **15 docstring-overstates-statement findings** recorded on the page, headed by the general-`N` Born headlines not flagging that `bornRegion ψ` is **preparation-indexed** (the `qubitBorn` contrast is context-fixed and says so). ⚠️ **This row's own premise was wrong and is corrected on the page:** `composite_is_tensor_product` is the **sufficiency** half (`SigmaLayer/TensorSolved.lean` says so in a section headed "SUFFICIENCY here, UNIQUENESS in `TensorReconstruction.lean`"); the necessity converse is `compositeAlgReconstruction` / `composite_dim_eq`. The Lean and its docstrings were right; this ledger prose was not. No Lean changed; docs-only, eight guards green. *(Original row, superseded:)* The corpus constrains the ontic substrate from above, which is the right method given that the substrate is only observed through what it produces. But the constraints are of mixed logical strength, and nothing records which is which. *Sufficiency* results say a posited structure yields observed behaviour (local generators ⟹ light cone, `norm_commutator_spatial_factorial_le`; posited SU(n) symmetry ⟹ `μ_FS` unique). *Necessity* results say observed behaviour forces the structure (local tomography + locality ⟹ `⊗` — named here as `composite_is_tensor_product`, **which the audit found to be the sufficiency half**; the necessity converse is `compositeAlgReconstruction`). **Task:** audit every constraint-bearing headline, classify it sufficiency vs necessity vs conditional-necessity (and on what posit), and state what the accumulated set actually pins down: uniquely, or up to a family. This is a question about the constraints, not about the substrate, so it is answerable. Value: it is the honest measure of how much the reconstruction has achieved, and it is currently unmeasured. Effort **M** (audit + a `specs/` page; no new mathematics unless a gap is found). | `specs/reconstruction-status.md`, `AXIOMS.md` |
| **★ Adopt the remaining upstream Lean checks** | **OPEN, scoped and measured 2026-08-11.** Survey of what other Lean repositories gate on, tested against this tree rather than estimated. **(1) `lake exe runLinter` — AVAILABLE NOW via the Mathlib dependency, and it works: `lake exe runLinter CsdLean4` reports **122 findings in 7620 declarations** across 4 of 14 linters — `unusedArguments` 53, `defsWithUnderscore` 34, `docBlame` 20, `simpNF` 15.** Triage before gating: many `unusedArguments` hits are instance binders (`[FiniteDimensional ℂ E]`, `[NeZero N]`) kept deliberately for API uniformity, and `defsWithUnderscore` is a Mathlib naming convention this repo may not want. ⚠️ But **`unusedArguments` is exactly the class the necessity audit found BY HAND** (`LF1.OnticSetup.hΦ_pres` declared and never consumed) — an unused *hypothesis* is epistemically significant here, since it means a stated assumption does no work. That subset is worth reading as findings, not noise. `simpNF` (15) is the cheapest real win: a simp lemma not in normal form silently never fires. **(2) `lean4checker` — NOT installed; the biggest genuine gap.** Independent kernel re-verification of the built `.olean`s: `#print axioms` reads the environment, `lean4checker` re-checks it from scratch, catching metaprogramming that adds declarations, `implemented_by`/`unsafe` escapes, and tampering that axiom inspection cannot see. Needs building against the exact toolchain and re-checks Mathlib too, so it belongs on a **scheduled** run, not per-push. **(3) `shake` (unused imports) — NOT available** as an exe from the dependency; would need vendoring. **(4) `assert_not_exists` — DONE**, as `scripts/check-import-negative.sh`. **(5) mutation testing of the guards — DONE**, as `scripts/check-guards.sh`. | `prose-audit.md`, `necessity-audit.md` |
| **★ Prose audit: reasons given for formal restrictions** | **IN PROGRESS 2026-08-11 — `specs/prose-audit.md`.** The C1 correction found two false claims that lived **only in prose**, and neither was reachable by any guard: both were prose giving a *reason* for a formal fact. `check-claim-provenance` targets claims *about objects*, so a wrong reason attached to a **true** statement trips nothing, and Lean cannot help because nothing in Lean states reasons. **Surface: 201 doc blocks** that both explain a restriction and mention a hypothesis, exclusion or degeneracy. **Pass 1** (quantitative prose): no defect; one candidate cleared (`Einselection.lean`'s `3/2` off-diagonal is correct — the witness is deliberately unnormalised, trace 5). **Pass 2** (SigmaLayer/LF4): ★ **one defect found and corrected.** Five modules and a BACKLOG row explained the `Ico 0 1` restriction in `fibreTypicality_uncovered` by "Lebesgue measure on the line is infinite" — wrong twice: `fibreTypicality` is `volume.restrict (Ico 0 1)`, a **probability** measure (`instIsProbabilityMeasure`, proved a dozen lines above), and the restriction was never forced (`fibreTypicality_uncovered_univ` proves the `univ` form on `ℝ`). The comparison is still real but different: it is about **where the mass one comes from** — on `ℝ` by fiat (`fibreTypicality_Ici_one`: the complement has infinite Lebesgue measure and zero typicality, so uncovered points are *excused by the measure*), on the compact fibre by Haar. ⚠️ **This BACKLOG row itself carried both readings at once**, the correct diagnosis three sentences before the wrong one — a defect being known somewhere does not stop it spreading. **Method that works, and the one to keep: when prose gives a reason for a restriction, try to prove the restriction unnecessary.** That converts an unfalsifiable sentence into a pinned Lean obligation. **~1/5 of the surface covered; no guard can close this class** — it needs a reader. | `prose-audit.md`, `c1-closure-report.md` |
| ~~**Mathlib upstream batch 1**~~ | **SUPERSEDED 2026-08-06 by the B6 retirement** (author decision, `CONVENTIONS.md` §7: Mathlib PRs do not advance CSD and are not a need of this repository). This row predates that decision and was left standing by oversight; struck 2026-08-09. The staging discipline is untouched, the readiness record in the struck B6 row stands, and the option stays open for a future decision. *(Original row:)* **QUEUED 2026-08-02** (`MATHLIB-GAPS.md` is the public-facing cut). Self-contained staged files with no pause dependencies: Projectivization `Topology` + `MeasureSpace`, `StoneC1`, `DuhamelBound`, `PiecewisePreserving`, `QuantumInfo/{PartialTrace, TraceDistance}`. Effort per PR **S–M**; elapsed time weeks (review cycles). Value: credibility + de-risking toolchain drift (the corpus tracks a release candidate). | `MATHLIB-GAPS.md` |
| **Lévy / spherical isoperimetry** (TH-1) | Canonical-typicality concentration (single-state typicality). Mathlib lacks spherical isoperimetry; the mean is proved. Optional strengthening. | `thermo-plan.md` |
| ~~**Continuity-only Stone**~~ | **DONE 2026-07-23** (`Mathlib/Analysis/Matrix/StoneC1.lean`, `stone_continuous`): a *strongly continuous* one-parameter unitary group is `t ↦ exp(t•A)` for skew-Hermitian `A` — differentiability **derived** (not assumed) via integral averaging (`∫₀ˢU` invertible near 0, by FTC + `s⁻¹•∫₀ˢU → 1`) + the change-of-variables `U t·∫₀^{s₀}U = ∫_t^{t+s₀}U`, then the C¹ core `eq_exp_of_hasDeriv`. Foundational triple, AxiomAudit-pinned. Closes W5-S2 fully. | `future-work.md` (W5-S2) |

## XL — Mathlib-scale (depth, not correctness)

| Item | Status | Former source |
|---|---|---|
| **Manifold exterior calculus** (Kähler `dω=0`, symplectic gradient — KG-1/2/3) | Genuine upstream gap; the volume is forced and the pointwise Kähler core is proved, so this is formalization-depth, not a correctness gap. | `future-work.md` (KG-1/2/3) |
| ~~Pointwise Birkhoff ergodic theorem~~ | **Do not pursue** — the single-flow route is a proved dead-end CSD does not take (NG1). | — |

## Research — physics-first, not formalization tasks

| Item | Note | Source |
|---|---|---|
| ~~**SO-1 — "derive Σ / the sector origin"**~~ | **RETIRED as "derive Σ" — a non-question (2026-07-24).** Σ is the floor and is everything; nothing beneath it. **But *constraining* Σ (≠ deriving it) is legitimate, ongoing work:** Σ is hidden, so pin its structure/symmetry/measure down as tightly as possible from above (e.g. μ_FS *forced* as the unique SU(n)-compatible measure — `LocalisedTypicality`, Paper B). The frontier for completing QM is the record layer below. Kept struck so it is not re-proposed as a "derive the sector" goal. | `CSD-CHARTER.md`, `reconstruction-status.md §7` |
| **★ The record layer (MD-1)** — measurement: context-fixed regions + the ontic record | **KINEMATIC INTERFACE BUILT 2026-07-25; MD-1 OPEN** (`record-layer-plan.md` §4). The record layer is formalized end-to-end, foundational-triple, no `sorry`, all axiom-pinned: fibre Born partition (`BornFibrePartition`), outcome distribution + flow⟹Born bridge (`DeIsolationFlow`), the P5 `RecordSemantics` (`FibreRecord`), the closure bundle (`RecordLayerClosure`), the **rates = the Kähler torus moment map** (`MomentMapRace`, `bornRate_eq_momentMap`), the measurement-as-`context+microstate→record` object (`Measurement`), **Born = the law of large numbers over the unknown microstate** (`Measurement.bornMeasurement_frequency` — no dynamical postulate; randomness = ignorance of the initial condition), the migration onto the corpus's real Σ (`ProjectiveRecord`, `projRecord_frequency`), the base×fibre ontic space `Σ = CPN × ℝ` with epistemic base / ontic fibre (`FibredSigma`), arbitrary observable (`BasisMeasurement`), and — the wire-in — `KSigmaRecord.born_frequency_region_eq_record`: the region `FiniteQMClosure.born_frequency` lands in **is definitionally the record-layer event** on the actual `Σ = KSigma`. **Only residue:** the pinned `unifiedFiniteQMClosure.records_time_physical` field still *names* the coarse `vnPointerOutcome` (a block-sum of these events) — rewriting it carries no new theorem and is deliberately not done. ⚠️ **SCOPE, corrected 2026-07-28 (external review).** What is built is the **kinematic** record interface: *given* a basin partition, an unknown microstate selects one definite record and repeated i.i.d. preparations obey the LLN. Three things are **not** established. (i) **General-`N` A7 is open** — the partition is built from the preparation (`bornContext ψ`, `cdfCell (bornRate ψ)`), so `Ωᵢ(M)` fixed by the apparatus alone is proved only at `N = 2`; see the re-opened row above. ⚠️ **UPDATED 2026-07-31 — this splits in two, and one half is closed.** `SigmaLayer/GlobalBasin.lean` + `GlobalRecordClosure.lean` give a **fibred** context-fixed partition at every `N` (`globalBasin` reads a `ContextField`'s rate *at the ontic point*; the record event is a function of `(context, outcome, time)` alone), so the **preparation-indexing** half is closed and the statement above now describes only the older `FibreRecord`/`FiniteQMClosure` path. The **base-only** half — whether `Ωᵢ(M)` can live on `ℂℙⁿ⁻¹` itself — is the ⏸ parked chain and stays open. Which of the two Paper C intends is a question about the axiom. (ii) **The dynamics are not derived** — `DeIsolationInteraction.basin_rate` is a *hypothesis field*, and no `H_int` generating those basins is constructed (`DeIsolationFlow.lean` says so; an earlier staging row calling this "DISSOLVED" was wrong and is retracted). (iii) The fibred `Σ = ℂℙⁿ⁻¹ × ℝ` is a **measurable record model**, not shown to be an **A1 ontic sector** — non-compact fibre, no Kähler structure, and its measure not shown to be a Liouville measure. ⚠️ *Sharpened 2026-07-30: "no Kähler structure" here is a **parity** fact, not a missing-tooling one — `ℂℙⁿ⁻¹ × ℝ` has odd real dimension `2n-1`. See the ★★ row.* So this row is "kinematic interface DONE", not "MD-1 discharged". | `record-layer-plan.md §4`, `CSD-CHARTER.md` |
| ~~**★ Qubit context-fixed Born rule (A7 faithfulness)**~~ | **DONE 2026-07-26** (`LF4/QubitBorn.lean` `qubitBorn`, foundational-triple, pinned, `check-claims.sh` PASS). Discharges the A7 residual flagged by the record layer: makes the epistemic partition genuinely **context-fixed** (a function of the measurement axis `n` alone, *not* the preparation — unlike the corpus's preparation-indexed `bornRegion ψ`) and *derives* the Born weight from the ontic Fubini–Study typicality volume: `∫ ½(1 + rsign(2·blochProj n − 1))·4(2·blochProj ψ − 1)₊ dμ_FS = |⟨n\|ψ⟩|²`. 7-module `CP¹` chain (all foundational-triple, all pinned): `QubitReflection` (reflection identity) → `BlochProjection` (general-axis Born weight + `U(2)`-equivariance) → `AxisBridge` (general-axis hat-box) → `QubitDipole` (`R_n=2\|n⟩⟨n\|−I` unitary; dipole `=(2c−1)/2`) → `QubitCrossTerm` (antipode via Haar right-invariance; cross-term `=0`) → `QubitBorn` (assembly), + `HatBox`. Next: general-`N` context-fixed partition (foundation is `N`-agnostic; `CP¹=S²` reflection geometry is the `N`-specific part). | `record-layer-plan.md §2/§4`, `reconstruction-status.md` A7 |
| **★ General-`N` BASE-ONLY A7** *(demoted from axiom-gating to special-case question, 2026-08-02)* | **DECISION 2026-08-02: the fibred reading of A7 is canonical (author call), so A7 itself is DISCHARGED at every `N` — kinematically and dynamically (v0.7.0). This chain now characterises whether a BASE-ONLY realisation also exists at `N ≥ 3` — the qubit-special-case question — and no longer gates the axiom. Content below unchanged.** **⏸ PARKED 2026-07-29 — a well-characterised open problem. DO NOT RESUME without a new idea.** *Parked, not refuted.* The decision-relevant output is in hand: a five-step **derived** constraint chain (`SigmaLayer/ContextFixedA7*.lean`) forcing any base-only density onto measure `≤ 1/n` **and** above `(n−1)/n`, with the `N = 2` / `N ≥ 3` threshold confirmed **three independent ways** and the bound sharp and attained at `N = 2`. Remaining path is blocked: `hdense` (M) → generic-`ψ` (L) → harmonic analysis on `ℂℙⁿ⁻¹` (**XL, Mathlib has no API**). One refutation route is closed too — the Fubini–Study triangle inequality is *exactly* tight (`arccos√σ + arccos√(1−σ) = π/2`), so it yields nothing; the system sits **on** the boundary, which is why neither direction falls out. **Rationale for parking: proving the no-go and assuming it lead to the same next action** — the fibre carries contextuality at `N ≥ 3` either way. Canonical record: [`sigma-fibre-contextuality.md`](sigma-fibre-contextuality.md). **Successor: the fibre/A1-sector row below.** *(Superseded detail retained:)* **OPEN — re-opened 2026-07-28 after an external review. This row previously read "NOT A TARGET — provably dead"; that was an overclaim and is retracted.** What actually exists against base-only regions at `N≥3` is (a) a **numerical** experiment (`scripts/experiments/`, FS-Voronoi density forced negative at `N=3`) and (b) an **informal** operator argument (the affine dipole reproduces Born but is signed; the nonneg-restoring monopole is ψ-independent per cell only via the qubit `CP¹=S²` antipode, which has no `N≥3` analogue). Numerics plus an unformalised argument is a **conjecture**, not a no-go — and "provably" had no business in a corpus whose claim to attention is that its results are machine-checked. **Two admissible routes, no-go first:** (1) **prove the no-go** as a Lean theorem under explicitly stated hypotheses (covariance, non-negativity, base-only dependence, the region family's regularity) — if it goes through it settles the architecture, and if it fails the failure names the hypothesis to drop; (2) otherwise **construct** `{Ωᵢ(M)}` depending on the measurement context alone, together with a preparation law `ρ_ψ^ep`, satisfying Paper C A7's `P(i∣M,ψ) = ∫_{Ωᵢ(M)} ρ_ψ^ep dμ_FS`. **Step one LANDED 2026-07-28** (`SigmaLayer/ContextFixedA7.lean`) — the **support reduction**. Evaluating A7 at the `n` basis-vector preparations `ψ = eⱼ` (Born weights `δᵢⱼ`) makes a **non-negative** integrand integrate to **zero** over `Ωᵢ` for `i ≠ j`, forcing it to vanish a.e. there (`ae_eq_zero_of_setIntegral_eq_zero` — non-negativity does all the work, exactly what a *signed* density escapes). Hence: each support lies in its own region (`overlapSupport_ae_subset`), the `n` supports are pairwise a.e. disjoint (`overlapSupports_ae_disjoint`), and their measures sum to `≤ 1` (`sum_measure_overlapSupport_le_one`) — so each is `≤ 1/n` by symmetry while still carrying total integral `1`. **A base-only density must spike**, which is what the `N = 2` solution `4(2s−1)₊`, supported exactly on `(½,1]`, does. Stated over an abstract probability space, so it survives a move to a fibred Σ. **Step two LANDED 2026-07-28 — the CAP.** If `S_g` contained a positive-measure set of overlap values **below `½`**, two coordinates could both land in it (they would sum to `< 1`, so nothing forbids it) and at `N ≥ 3` a third coordinate absorbs the remainder, so such states occur with **positive** measure — putting positive measure in `Aⱼ ∩ Aₖ` and contradicting step one's disjointness. Hence `cap_of_joint_nondegenerate`: **`g` vanishes a.e. below `½`.** Sharp and attained — the `N = 2` solution `4(2s−1)₊` is supported exactly on `(½,1]`. ★ **And it makes the qubit's escape precise:** `joint_degenerate_of_sum_eq_one` — at `N = 2` the two Born weights exhaust the state (`sⱼ + sₖ = 1`), so they can *never* both be below `½`; the set is literally **empty** and the abundance hypothesis fails identically. That degeneracy *is* the escape route, and it closes at `N ≥ 3` where the coordinates stop being functionally dependent. `base_only_density_confined` assembles both halves: the density lives on measure `≤ 1/n` **and** only above `½`, while still integrating to `1`. The abundance input (`hjoint`) is an explicit hypothesis rather than derived — deriving it is the Dirichlet pushforward of `μ_FS`, real work and orthogonal to the argument; stating it that way is what makes the `N = 2` contrast visible. **Step three LANDED 2026-07-28 — the cap is UNCONDITIONAL at `N ≥ 3`** (`SigmaLayer/ContextFixedA7FS.lean`). `fs_joint_abundance` discharges the abundance hypothesis for the actual Fubini–Study measure via the corpus's own pushforward `fs_volume_eq_dirichlet_inter` (`μ_FS` pushes to the uniform/Dirichlet measure on the open simplex); abundance then reduces to Lebesgue positivity on `Fin M → ℝ` with an explicit witness — the two chosen coordinates in `T`, every other in a small `(0,ε)`. The subtlety handled by `exists_trunc_of_volume_pos`: `T ⊆ (0,½)` bounds `tⱼ + tₖ < 1` *pointwise but not uniformly*, so one first passes to a positive-measure part of `T` bounded away from `½`. ★ `fs_cap_unconditional`: **a base-only, `U(N)`-covariant, non-negative preparation density reproducing Born on the FS sector vanishes a.e. on overlap values below `½`** — no hypothesis left over. **`N ≥ 3` is exactly the threshold**: `M = N−1` free simplex coordinates, and two *distinct* ones exist precisely when `M ≥ 2`; at `N = 2` the second Born weight is `1 − s₁`, functionally dependent. The dimension count that powers the result is the same one that exempts the qubit. This replaces the **numerical** evidence the retracted "provably dead" row rested on with a **derived** constraint. **Step four LANDED 2026-07-29 — the CONSTRUCTION ATTEMPT, and the first generic-`ψ` input.** Asked to *build* `{Ωᵢ(M)}` + `ρ_ψ` rather than refute it, the sharpest untried lever turned out to be **orthogonal preparations**: `ψ ⊥ eᵢ` gives Born weight `0`, so non-negativity forces `g ∘ sψ` to vanish a.e. on `Ωᵢ` — for a whole *family* of `ψ`, not one. `orthogonal_preparation_vanishes` + `vanishes_on_interval_of_dense` (`SigmaLayer/ContextFixedA7.lean`). The geometric input: for unit `ψ` in the sphere of `eᵢ^⊥`, `|⟨ψ|φ⟩|²` peaks at the normalised projection of `φ` into `eᵢ^⊥` with value `1 − sᵢ(φ)`, and tilting `ψ` inside `eᵢ^⊥` sweeps it continuously to `0` — **which needs `dim eᵢ^⊥ ≥ 2`, i.e. `N ≥ 3`.** At `N = 2` the orthocomplement is a line and only one value is realised. **Third independent appearance of the same threshold.** **Consequence (analysis done, covering step not yet formalized): `g` vanishes below `(n−1)/n`** — sharpening the cap from `½`, and at `n = 2` reading "below `½`", exactly where `4(2s−1)₊` lives. ⚠️ **THE CONSTRUCTION DID NOT SUCCEED, and one plausible refutation route is also closed.** The natural kill — a state `ψ` whose cap cannot reach a region `Ωᵢ` with small `sᵢ(ψ)` — fails: the Fubini–Study triangle inequality gives `arccos√σ + arccos√(1−σ) = π/2` **exactly**, so the bound it yields is vacuous. The constraint system is critical rather than contradictory, which is why neither a construction nor a refutation falls out. **Step five LANDED 2026-07-29 — `(n−1)/n` is now a Lean theorem** (`vanishes_below_of_balanced`): given step four's output and the balanced-state abundance, `g` vanishes on every overlap value below `(n−1)/n`. Short for a structural reason — step four's conclusion is *pointwise in `g`* (it dies on a whole interval, for a.e. `φ`), and `g` is a fixed function, so **one** suitable `φ` suffices; the helper `exists_mem_of_measure_pos_of_ae` is the whole bridge from measure to pointwise. ⚠️ **STILL CONDITIONAL for `μ_FS` on one input, NOT landed:** balanced-state abundance — that for every `c > 1/(M+1)` the states with *all* moment coordinates `≤ c` are non-null. True, and the proof shape is settled (`fs_volume_eq_dirichlet_inter` reduces it to Lebesgue positivity; a barycentre box of side `min(b, c−b)/(M+1)` with `b = 1/(M+1)` witnesses it, the two constraints being `M·δ < b` to stay inside `∑ t < 1` and `M·δ < c − b` to keep the dropped coordinate below `c`). **LANDED 2026-07-29 on the second attempt** (`fs_balanced_abundance`). The first attempt failed across four iterations because `b`, `δ` were introduced with `set`, making them opaque local definitions `linarith` cannot see through; the fix was to **split geometry from arithmetic** — `box_in_simplex` takes `b`, `d` as *abstract* reals constrained by linear relations plus the single identity `M·b = 1 − b`, with the concrete choice made once outside. ⚠️ **This does NOT make `(n−1)/n` unconditional.** Step four's *other* input — `hdense`, the tilt fact that unit `ψ` in the sphere of `eᵢ^⊥` sweep overlaps across `[0, 1 − sᵢ(φ)]` — remains a hypothesis and **has not been attempted**; it needs genuine projective/inner-product geometry, not arithmetic. **Effort: M.** ⚠️ **And A7 REMAINS UNPROVEN IN BOTH DIRECTIONS.** Everything in this chain is a *necessary condition* on `g`; none of it shows a base-only construction is impossible, nor exhibits one. After `hdense`: the residual generic-`ψ` gap (L) and the harmonic argument (XL, blocked on Mathlib). Foundational-triple, pinned. ⚠️ **This is NOT the no-go** — it constrains `g`, it does not refute it. **Two things remain:** (a) the **generic-`ψ`** requirement is entirely untouched (everything so far comes from the `n` basis-vector preps, and the suspected obstruction lives at generic `ψ` where the cap around `ψ` straddles several regions); (b) the **harmonic argument** — `g(|⟨ψ|φ⟩|²)` integrated over a fixed region has components of every degree `(k,k)` while the target `|⟨eᵢ|ψ⟩|²` is pure `(1,1)`, and killing the higher harmonics for all regions and contexts is what should fail at `N ≥ 3` — needs representation theory / harmonic analysis on `ℂℙⁿ⁻¹` that **Mathlib does not have**, so it is XL-to-infeasible as things stand. **Status of what is proved:** `N = 2` is genuinely done (`LF4/QubitBorn.lean` `qubitBorn`). `N ≥ 3` is unresolved in either direction. Note the record layer does **not** discharge this: its partition is built from the preparation (`bornContext ψ`). See also the `hpos` finding below — the DH region machinery is blind to vanishing amplitudes, a second structural limit at the same place. | external review 2026-07-28; `record-layer-plan.md` |
| **Track B — quantum relaxation** (Valentini H-theorem) | The only route to *new predictions* past the "empirically identical" ceiling. | `project` note |
| **CV chain** — continuous spectra / the EFT direction | Extend Born-as-volume past finite `ℂℙⁿ`. **Stage 0 DONE 2026-07-25** (`CsdLean4/CV/OscillatorBorn.lean`): the truncated single mode is now a genuine record-layer measurement — its number/energy measurement = the standard-basis `Measurement` (number basis = standard basis, `hamiltonian` diagonal), `numberMeasurement_prob` = `‖⟨n\|ψ⟩‖²`, `numberMeasurement_frequency` = Born as the LLN over the unknown microstate, and `numberBornProb_embed` = **cutoff-independence** of the Born content (the Born analogue of `oscEnergy_cutoff_independent`). **Stage 1 DONE 2026-07-25** (`CsdLean4/CV/FieldModes.lean`): a free scalar field at a cutoff = finitely many modes as the (truncated) tensor product `FieldSpace K N = EuclideanSpace ℂ (Fin K → Fin N)` (configs). `fieldHamiltonian_mulVec_single` (free field = sum of oscillators, diagonal, eigenvalue `∑ₖ oscEnergy (c k)`), `fieldEnergy_cutoff_independent`, `sum_fieldBornProb_unit` (config Born distribution), `norm_sq_tprodState` (product state `‖⊗ψₖ‖²=∏‖ψₖ‖²` — the composite/tensor structure), and **`modeMarginal_tprod_unit` = mode-wise Born** (the marginal of a product state = the single-mode Born weight `‖ψ_{k₀} n‖²`). **Stage 2a DONE 2026-07-27** (`CsdLean4/CV/Dispersion.lean`): relativistic dispersion `ω(m,p)=√(p²+m²)`. `omega_sq_sub_sq` = the **mass shell** `ω²−p²=m²` (the Lorentz-invariant content, and why `m` is the mass); `abs_le_omega` (`|p| ≤ ω` — excitations do not outrun the light cone); `abs_mass_le_omega` + `omega_zero` (the **mass gap** `|m| ≤ ω`, attained at rest); `omega_massless` (`ω=|p|`, the light cone exactly); `omega_le_newtonian` (`ω ≤ m + p²/2m` — the non-relativistic limit as a clean inequality, no asymptotics); `omega_mono`. On the field: `relFieldHamiltonian_mulVec_single` + `_isHermitian` (still **diagonal** in the configuration basis, so the `OscillatorBorn` record-layer account carries over verbatim — only the eigenvalues change), **`relFieldEnergy_quantum`** (the headline: one quantum in mode `k₀` costs exactly `ω(m, p k₀)`, so the excitations *are* relativistic particles of mass `m` — the dispersion is about the particle content, not a parameter choice), `relFieldEnergy_vacuum` (zero-point `½∑ω`), `relFieldEnergy_cutoff_independent`. **Stage 2b DONE 2026-07-27** (`CsdLean4/CV/ModeLocality.lean`): the **Haag–Kastler kinematic locality axiom** at the finite cutoff — `commute_of_disjointSupport`, operators supported on **disjoint mode sets commute**, so observables of disjoint regions are jointly measurable and the record layer can assign them outcomes simultaneously (proof = uniqueness of the intermediate configuration). Non-vacuous: `modeOp_supportedOn` (every single-mode matrix inhabits `SupportedOn {k₀}`) + `commute_modeOp` (the concrete instance at distinct modes). ⚠️ **HONEST SCOPE:** this is *subsystem* locality — spatial only under the position-space reading of the modes (`CV/Position.lean`); under the momentum reading the modes are delocalised and mode-disjointness is not spatial separation. **Continuum microcausality `[φ(x),φ(y)]=0` at spacelike separation is NOT proved and does NOT hold exactly at a finite cutoff** — it needs the continuum limit, deliberately deferred (`ApproxCCR.no_exact_finite_ccr`). **CV-5 DONE 2026-08-07** (`CsdLean4/CV/FreeFieldFloquet.lean` + `Empirical/CSD/QuantumChaos/FreeFieldClosure.lean`): the free field's **stroboscopic dynamics** — `freeFieldU K N τ` = the diagonal-phase unitary `e^{-iτE(c)}`, ★ `freeFieldU_eq_exp` proves it **IS** `exp (-(iτ)•H_field)` (generation by the field Hamiltonian is a theorem, not a gloss), `freeFieldFloquet` plugs it into the §H2 `FloquetEvolution` interface (information preservation, projective dynamics, Loschmidt diagnostics apply verbatim), and ★ `freeField_pilotClosure` lands the full §H3 pilot closure for every cutoff through the `freeFieldUFin` reindex (`card_fieldConfig = N^K`) — the CV vertical and the quantum-chaos vertical meet on the same closure instance. **CV-6 DONE 2026-08-07** (`CsdLean4/CV/DynamicalLocality.lean`): **dynamical mode locality** — Heisenberg conjugation `heisenberg U A = U†AU` by any mode-additive diagonal phase preserves `SupportedOn` (`heisenberg_phaseDiagU_supportedOn`: the conjugating phase difference collapses to the support modes), so every free-field period preserves support (`heisenberg_freeFieldU_pow_supportedOn`) and ★★ `commute_heisenberg_freeFieldU_pow`: **disjointly supported observables still commute after ANY number of periods** — the Stage-2b Haag–Kastler axiom is stable under the free dynamics. ⚠️ Honest scope: free (mode-diagonal) drive only; interacting drives can spread support — that spreading is the scrambling the §H diagnostics track. **Stage 3a+3b DONE 2026-08-07** (plan: [`cv-stage3-plan.md`](cv-stage3-plan.md); rows ~~CV-7~~/~~CV-8~~ in `future-work.md`): the **interacting drive** (`CV/Interaction.lean` — diagonal pair couplings, EXACT step, ★ `interactingU_eq_exp`, §H3 closure via `interacting_pilotClosure`) and the **local-algebra light cone** (`CV/LocalAlgebra.lean` + `CV/SupportSpreading.lean` — `SupportedOn` is a unital *-subalgebra, disjoint couplings act trivially, ★★ `heisenberg_graphInteractingU_pow_supportedOn`: `n` periods spread support at most onto the coupling graph's `n`-ball, one edge per period, with the `K=N=2` non-vacuity witness `spreadKick_not_supportedOn`). **Stage 3c DONE 2026-08-07** (~~CV-9~~, `CV/InteractionPrice.lean`): locality violation **priced linearly in the coupling** — ★★ `heisenberg_interactingU_near_supported` (within `2|τ||λ|C‖A‖` of an `S`-supported operator), with `freeField_perturbed_exp_dist_le` covering non-diagonal (hopping) perturbations too (Duhamel engine generalized to any finite index; diagonal L2-norm brick staged). **Stage 3d DONE 2026-08-07** (~~CV-10~~, `CV/PowerCounting.lean`): finite-cutoff **power counting** — ‖Q‖ ≤ √(2N) via the C*-identity (no spectral asymptotics), field embedding norm-nonincreasing, and the grade-m interaction Q_k^m priced at ≤ |τ||λ|√(2N)^m with ★★ cutoff-UNIFORM price under the renormalized coupling λ₀/√(2N)^m — relevant/irrelevant as a theorem about price-bound scaling (upper bounds only; no RG, no continuum). **Stage 3 (CV-7..CV-10) COMPLETE** — the EFT interaction stage is closed at the cutoff. Recorded residues (not queued): non-diagonal light cone (exp-closure route, plan §3b prose), price attainment (InteractionPrice ledger note). **Stage 4 COMPLETE 2026-08-09 (CV-11..CV-16)** ([`eft-stage4-plan.md`](eft-stage4-plan.md); rows CV-11..CV-16): non-diagonal light cone (exp-closure), matrix Lie–Trotter (constructible arbitrary-Hermitian drives), ★★ the finite free propagator (`freeTwoPoint` at the dispersion frequency) + priced corrections, boost covariance of the mass shell, the renormalization-trivial class, and RG matching as a gated decision point. Landed: the non-diagonal (kicked) light cone via exp-closure; matrix Lie–Trotter (staged, no Trotter in Mathlib at pin) making arbitrary-Hermitian drives constructible; ★★ the **finite free propagator** `⟨vac|Q_k(n)Q_l|vac⟩ = ½e^{-inτ}δ_{kl}` — the chain's first computed correlation function — with the interacting correction priced; boost covariance of the mass shell (the Lorentz content as a theorem); and the renormalization-trivial class (relevant/irrelevant now a theorem on both sides). CV-16's gate changed its own statement: exact unitary RG matching is IMPOSSIBLE for support-spreading drives (`exists_unitary_compress_not_unitary` — decimation loses norm), so the effective low-cutoff theory is necessarily open; exact matching holds for the occupation-defined class (`compressCfg_interactingU`), and the RG proper is re-scoped to a channel-level statement with an error budget (research, unqueued). **Stage 5 COMPLETE 2026-08-09 (CV-17, CV-18, CV-19)** (`CV/LiebRobinson.lean`: the Heisenberg flow's derivative, the interaction-picture split, and the **linear** bound `‖[A(t),B]‖ ≤ 4|t|‖T‖‖A‖‖B‖` — information cannot leave a region instantaneously, at a rate set by the cut coupling alone; the field instantiation takes `[A,B]=0` from CV-8). CV-19's gate was then run: the pass found a concrete route to the **Grönwall (exponential-in-time)** bound, which landed (`norm_commutator_gronwall_le`, prefactor `‖[T,B]‖`, so a coupling that does not reach the probe contributes nothing at any time — the seed of a light cone). A second pass landed the **combinatorial half of the spatial cone** (nested commutators stay in the `k`-ball and commute with a distant probe exactly below the graph distance), and a third landed the **analytic half** (the Taylor remainder of the adjoint series, bounded by the mean-value inequality). ★★★ `norm_commutator_spatial_le` assembles them: `‖[A(t),B]‖ ≤ 2‖A‖‖B‖(2‖S‖|t|)^d` for every `d` whose ball has not reached the probe, so for `2‖S‖|t| < 1` the commutator **decays geometrically in the graph distance** and the propagation speed is bounded by the coupling strength. A fourth pass took the recorded strengthening: `norm_flowRemainder_le_factorial` (integral estimate in place of mean-value) gives ★★★ `norm_commutator_spatial_factorial_le`, `‖[A(t),B]‖ ≤ 2‖A‖‖B‖(2‖S‖t)^d/d!`, **the textbook Lieb–Robinson shape**, decaying in the graph distance at every time. **Stage 5 is complete with nothing recorded against it.** No velocity constant is extracted or claimed optimal. **PLANNED 2026-08-09** ([`eft-stage5-plan.md`](eft-stage5-plan.md); rows CV-17..CV-19): the feasibility verdict is split and stated up front — CV-17 (commutator flow) and ★★ CV-18 (the **linear** bound: `‖[τ_t(A),B]‖ ≤ 4|t|‖H−H_X‖‖A‖‖B‖`, i.e. no instantaneous signalling quantitatively) are tractable now on the existing `DuhamelBound` pattern with CV-8 supplying `[A,B]=0`; CV-19 (the exponential/velocity form) is XL research with an **abort criterion agreed in advance** — stop at CV-18 if the iteration step has no concrete route after one focused pass. To our knowledge Lieb–Robinson is unformalized in any proof assistant. The continuum posture stays deliberately deferred (`ApproxCCR.no_exact_finite_ccr`). **Stage 6 OPENED 2026-08-13** ([`eft-stage6-plan.md`](eft-stage6-plan.md)): ~~CV-20~~ the **velocity** extracted same day (★★★ `norm_commutator_velocity_le` — the Stage-5 non-claim discharged, constant `2e²‖S‖` not claimed optimal); ~~CV-21~~ vacuum clustering DONE same day (`vacuum_clustering` — the statics companion); ~~CV-22~~ Wick four-point gate PASSED same day, landed pattern-resolved (`eqFourPoint` table). **Stage 6 COMPLETE 2026-08-13.** | `project` note |

## Hardening / conventions (from the 2026-07-20 Lean-QIT / Physlib comparison — `CONVENTIONS.md §8`)

| Item | What / why | Size |
|---|---|---|
| ~~**Zero-`axiom` CI gate**~~ | **DONE 2026-07-21**. `check-claims.sh` now sets `DECLARED_AXIOMS=""` and fails on ANY `^axiom ` under `CsdLean4/` (the whitelist is empty now that `busch_effect_gleason` is discharged). Gates the Physlib route. (`CONVENTIONS.md §8.1`) | **S** |
| ~~**Discharge `busch_effect_gleason`**~~ | **DONE 2026-07-21 — axiom deleted, corpus imports ZERO axioms.** Proved as `OperationalPackage.effect_gleason_representation` (`LF2/EffectGleason.lean`, foundational-triple, AxiomAudit-pinned); the `axiom` was removed from `BornWrapper.lean` and its consumers `born_form_of_package` / `pure_state_born_weights_of_certainty` relocated to `EffectGleason.lean` (signatures unchanged). Step 4 pieces: `qmatrix_posSemidef`, `qmatrix_trace_one`, `qdensity`, `qdensity_unique` (via the polarisation lemma `matrix_eq_zero_of_quadForm_zero`). Guards updated (`check-claims.sh` zero-axiom gate, `AxiomAudit` pins, `AXIOMS.md §2.2`). Full history below. — Finite-dim effect-Gleason → "three axioms, zero imported". **Steps 1–2 LANDED** (`LF2/EffectGleason.lean`, 2026-07-20): step 1 foundational layer (`Effect.smul`, `p_zero`, `p_mono`, `p_smul_add`, `p_smul_mono`) + **step 2 homogeneity** `p(t•E)=t·p E` on `[0,1]` (`p_smul_homog`, via `smulVal_natMul`→`smulVal_rat`→`smulVal_homog`, the monotone-Cauchy density squeeze) — all foundational-triple, no `sorry`. **Step 3 spectral reduction LANDED**: `p_finsetSum` (finite additivity), `Effect.eigenvalues_le_one`, `Effect.sum_eigenEffect_M`, `p_eq_eigen_sum` (`p(E)=∑ᵢλᵢ·p(\|eᵢ⟩⟨eᵢ\|)`) — reduces the problem to `p` on rank-one projectors, foundational-triple, no `sorry`. **Step 3b polarisation identities LANDED**: `outerProduct_parallelogram` (`|u+v⟩⟨u+v|+|u−v⟩⟨u−v|=2|u⟩⟨u|+2|v⟩⟨v|`, cross terms cancel) + `outerProduct_polarization_real` — the algebraic core letting `p` inherit the parallelogram law, foundational-triple, no `sorry`. **Step 3b groundwork LANDED**: `outerEffect` (sub-unit rank-one `\|v⟩⟨v\|` for any `‖v‖≤1`, via `1-\|v⟩⟨v\|=(1-\|v̂⟩⟨v̂\|)+(1-‖v‖²)\|v̂⟩⟨v̂\|`), `outerProduct_smul`, and `p_outerEffect_smul` (degree-2 homogeneity `p(\|c·v⟩⟨c·v\|)=c²·p(\|v⟩⟨v\|)`) — foundational-triple, no `sorry`. **Step 3b `p`-parallelogram LANDED**: `one_sub_two_outerProduct_posSemidef` (CS sum-of-projectors `≤I`, not the feared eigenvalue bound) + `p_outerEffect_sqrt2` (√2-doubling) + **`p_parallelogram`** (`p(|u+v⟩⟨u+v|)+p(|u−v⟩⟨u−v|)=2p(|u⟩⟨u|)+2p(|v⟩⟨v|)` for `‖u‖²+‖v‖²≤½`) — foundational-triple, no `sorry`. **Step 3b-final (ρ-build) LANDED 2026-07-21**: `qform` (`q v=‖v‖²·p(\|v̂⟩⟨v̂\|)`, the degree-2 homogeneous extension off the unit ball) with `qform_eq_p` / `qform_smul` / **`qform_parallelogram`** (unrestricted parallelogram law); the Jordan–von Neumann core `qpolar_add_half`→**`qpolar_add_left`** (bi-additivity) and **`qpolar_smul_real`** (`ℝ`-homogeneity) via the new general lemma **`additive_bounded_linear`** (bounded additive `ℝ→ℝ` is linear — replaces the classical *continuity* step, which is unavailable since `p` is an arbitrary assignment, by the bound `0≤q≤‖·‖²`); the complex polarisation **`qsesq`** `S(u,v)=¼f(u,v)−(i/4)f(u,i·v)` shown sesquilinear (`qsesq_add_right`, `qsesq_smul_right`, `qsesq_conj_symm`) with `qsesq_self : S(v,v)=q v`; and the matrix **`qmatrix`** `R j k=S(eⱼ,eₖ)`, Hermitian (`qmatrix_isHermitian`), with **`p_outerEffect_eq_trace`** (`p(\|v⟩⟨v\|)=Tr(R·\|v⟩⟨v\|)`) and — through the spectral reduction — **`p_eq_trace` : `p E = Tr(R·E)` for EVERY effect**. All foundational-triple, no `sorry`. **Remaining (step 4 only):** `R.PosSemidef` (from `OP.nonneg`), `R.trace=1` (from `OP.total_one`), uniqueness (non-degenerate trace pairing) → package `R` as a `DensityOperator` and rewrite the axiom as a theorem in `BornWrapper.lean`; then update `AXIOMS.md §2.2` + add the zero-`axiom` CI rule. Cosmetic for CSD (NG2) but clears the last `axiom` + gates Physlib. | **S–M** |
| **`REFERENCES.json` + line-precise citations** | Machine-readable provenance; docstrings cite `[Key, file:Lstart-Lend]` incl. exact CSD-preprint lines. Biggest auditability win. (`§8.2`) | **M** |
| **`_statement`/`_of_`/final-theorem pattern** | Turn `BRIDGE-OBLIGATIONS.md` prose into explicit `_of_` hypotheses; discharging an obligation = removing a hypothesis. (`§8.3`) | **M** (incremental) |
| ~~**`autoImplicit=false`**~~ + module-system migration + tagged Mathlib pin | **`autoImplicit=false` DONE 2026-07-31** — package-level `leanOptions = { autoImplicit = false, relaxedAutoImplicit = false }` in `lakefile.toml`, applying to **both** targets; both green. **Cost: 5 edits in 3 files** (`PartialTrace` `S`; `ModMul` `n` ×2; a missing `variable {n : ℕ}` in `CircleFibre`/`CircleRecord`/`TorusFibre`) — all explicit binders, no proof changed. Promoted ahead of schedule because it bit for real: `CPN N` (needing `LF4.CPN N`) was **silently auto-bound** instead of erroring, surfacing as a cascade of unrelated failures that never named the mistake — the exact hazard that turns a typo into a silent generalisation once anything external builds against the corpus. **Module-system migration** also DONE (2026-07-22, `§8.4`). **Still open: the tagged Mathlib pin** — the repo intentionally tracks master (`c732b96d`) for newest APIs; switch to a tag when a tagged release catches the toolchain. | **S**/**L** |

---

## ecdsa.fail — EXTRACTED to a separate repository (2026-07-20)

**DONE.** The ecdsa.fail / ECDLP quantum-cryptanalysis track has been extracted to its own
repository and removed from this one. What was removed: the code (`CsdLean4/Ecdsafail/`, 23
files), its docs (`specs/ecdsa/`), the `Ecdsafail` `lean_lib` target (`lakefile.toml`), and
the `check-claims` exclude path. The new repo carries a **copy** of the shared
reversible-arithmetic DSL (`Mathlib/QuantumInfo/Reversible/`, 20 of 26 modules in Ecdsafail's
transitive closure) pinned to the same Mathlib commit.

**Stays here:** the `Reversible/` DSL — it is shared with core-QM (Shor +
`MeasurementGidneyAdder`/`Uncompute*`) and is not ecdsa-specific.

---

## Done this session (2026-07-19)

Honesty guard (`check-claims.sh`) · Track A#1 Schrödinger derivation · Track A#2 Kähler de-vacuum · A5/L7 ergodic bracket · §13.2 all 9 gates · §14 measurement connections (SG/Uncertainty/Hardy) · Mach–Zehnder · Double-slit + complementarity · **composite mixed-Born on `DensityOperatorIx`** (SL-T3 T9) · **Choi converse — Choi's theorem CP⟺PSD** (`LF2/ChoiConverse.lean`) · **Gisin's theorem — every entangled pure two-qubit state violates CHSH** (`LF6/GisinTheorem.lean`, closes LF6-6) · **Lindblad/GKSL generator tier** (`LF6/LindbladGenerator.lean`, LF6-9 generator level).

## Settled non-goals — do not re-litigate (see `reconstruction-status.md §7a`)

- **NG1** — single-flow / Birkhoff / ergodic route to Born: proved dead-end; CSD forces typicality by the LLN.
- **NG2** — Busch–Gleason "needed for CSD": no; the ontic Born rule is Gleason-free.
