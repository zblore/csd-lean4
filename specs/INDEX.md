# specs/ index — orientation map for plans, todos, and references

**Start here.** This indexes every planning / todo / reference doc in the corpus, with
one-line status. Updated 2026-06-11. When starting a session, read this first, then the
LIVE doc for the tranche you are on.

## Live (actionable) — pick up work here

| Doc | What it is | Status |
|---|---|---|
| [`active-todo.md`](active-todo.md) | **THE persistent session work queue** — the full task list (Category / Complexity / Status / Blocked-by) that survives session loss; re-seed the in-memory task tracker from it. Includes the ECDSA.fail score ledger, the #16 D1c plan, and the #15 open-system-empirical next-tranche scoping. | **LIVE, updated 2026-06-29.** Read first to see what is open and what is next. |
| [`ecdsafail-correlation.md`](ecdsafail-correlation.md) | **ECDSA.fail benchmark alignment layer.** Normalises the comparison: ECDSA.fail scores ONE reversible point addition (quantum target + **classical** offset) by `avg-Toffoli × peak-qubits`; the repo is a scalar-mult / generic-q×q scaffold. Correlation table + the sequenced bridge: (1) correlate [this file], (2) reproduce their Rust harness [BLOCKED: their source not in-tree, user `! cargo run`], (3) build `ECDLP/PointAddBenchmark.lean` (`onePointAddCost ≤ B`, classical-offset specialised, peak-qubit + Toffoli score), (4) only then optimise. | **Step 1 DONE 2026-06-27.** Guards against citing scalar-mult numbers as an ECDSA.fail score. Step 3 (the point-add benchmark theorem) is the next Lean bridge. |
| [`metrology-plan.md`](metrology-plan.md) | **Quantum sensing / metrology validation program** (parameter estimation, not static states): A1 Ramsey interferometry (volume-preserving phase flow → `cos²(φ/2)` fringe, reuses `MalusVolume`), A2 QFI = Fubini-Study metric, A3 Heisenberg `1/N` via dilated manifolds, A4 decoherence as open symplectic drift. | **A1 (Ramsey) / A2 (QFI = FS-metric pullback) / A3 (Heisenberg `F_Q^GHZ = N²` via the GHZ probe) DONE 2026-06-28; A4 (decoherence = D1) open.** A3: `Heisenberg.lean`, `ghz_qfi = N²` vs `sqlQFI = N`, `heisenberg_advantage` (N-fold), genuine `N`-qubit `Fin (2^N)` GHZ family + genuine derivative, foundational triple only. A4 tied to D1. |
| [`tn4-measurement-architecture.md`](tn4-measurement-architecture.md) | **Orientation map for the measurement stack** (TN4): the single architectural view — pure → density → channels → {projective, POVM} → Naimark → Born → CSD volume → LF5 dynamics — annotated with the real theorem names, the two readings (QM-validity vs CSD-ontic; operational vs ontic stratum), where Born comes from (the DH cluster, derived not postulated), and the honest D1/A5/`Φ=id` residue. Descriptive, not a plan. | **Reference doc** (written 2026-06-27). Read first when picking up measurement-theory work. |
| [`carve-out-plan.md`](carve-out-plan.md) → **D1** | **The open frontier.** Exercise real measurement *dynamics* on `Σ`; the deepest structural debt, beneath A5. | **OPEN at the deeper strata.** `Φ≠id` (non-collapse) done 2026-06-05 (`LF4/ObservableFlow.lean`); collapse semantics DECIDED 2026-06-09 (§6: the de-isolation reading); **the measurement-event half is now exercised at the single-system projective tier — LF5 COMPLETE 2026-06-11** (`measurement_flow_born_frequency`); **the per-microstate outcome map is DONE 2026-06-14** (LF5-F: `bornRegion_pairwiseDisjoint` → `vnPointerOutcome` → `measurement_flow_outcome_frequency`, a single union event per pointer). Remaining: entangled/non-local de-isolation, A5 sector origin, `SectorData` instances still `Φ = id`. |
| [`lf5-plan.md`](lf5-plan.md) | Staged plan for **LF5 (measurement dynamics, the D1 frontier)**: realise a projective measurement as a measure-preserving von Neumann de-isolation flow `Φ_vN ≠ id` on the joint `ℂℙ^{N·N−1}`, pointer regions = `blockProj` (apparatus basis, context-fixed), committed volume = Born, chained to frequencies. **Heavily reuses the POVM Naimark tranche** (block-Born = FS-volume already proved); the genuine new content is realising the static dilation isometry as a dynamical flow. | **COMPLETE 2026-06-11.** LF5-A (vN unitary, 06-09); LF5-B (measurement flow `Φ_vN ≠ id`, FS-preserving, 06-10); LF5-C (de-isolation realises the dilation: `vnNaimark`, flow carries `[ψ⊗a₀] ↦ [Vψ]`, 06-11); LF5-D (the **unconditional engine** `LF4/BornRegionUncond.lean` — retires the `hpos` genericity of the general-`N` + POVM engines, additively — + `vnDilation_pointer_volume` / `vnDilation_pointer_frequency`, 06-11); LF5-E (capstone, 06-11): **layer headline `measurement_flow_born_frequency`** (`LF5/Capstone.lean`) — `Φ_vN ≠ id` + FS-preserving + dilation realised ∀ preparations + pointer-block volume = Born + a.s. frequencies → Born, every unit `ψ`. All foundational-triple-only, AxiomAudit-pinned, Tier-A audited SOUND. **LF5-F (post-plan, 06-14): the per-microstate outcome map** — `bornRegion_pairwiseDisjoint` (`LF4/BornRegionDisjoint.lean`) → `vnPointerOutcome` → `measurement_flow_outcome_frequency` (`LF5/PointerOutcome.lean`, single union event per pointer) + the `aeece86`-owed `block_born_frequency_volume_event`; closes the outcome-function caveat. **LF5-G (post-plan, 06-15): QEC syndrome readout as a coarse-grained de-isolation flow** (`LF5/SyndromeFlow.lean`) — stabilisers computational-basis-diagonal ⟹ syndrome = fixed `synClass : Fin 8 → Fin 4`, `Φ_syn = measurementFlow 8` coarse-grained; `syndromeRegion_fs_volume` (block FS volume = syndrome Born weight = block sum of FS volumes), deterministic codeword syndrome + recovery; headline `syndrome_flow_born_volume`, projective/coherent-error tier (decoherence/partial-trace origin gated on the entangled tier). **LF5-G.2 (06-16, `LF5/SyndromeOutcome.lean`): syndrome frequency + per-microstate outcome map** — `syndrome_flow_born_frequency` (a.s. syndrome-class freq → `syndromeWeight`), `synOutcome` (= `vnPointerOutcome.map synClass`), `syndrome_flow_outcome_frequency` (single-event), completing the QEC readout to match LF5-D/F; + the machine-checked anchor `errorSyndrome_synClass3`. Remaining D1 strata: entangled/non-local tier, decoherence error model, A5. |
| [`povm-plan.md`](povm-plan.md) | **POVM tranche** (DONE 2026-06-03): the ontic Born derivation extended to general (non-projective) POVMs via Naimark dilation. | **CLOSED** — P.1–P.5 done (type, dilation + Born transfer, volume reading, frequency capstone, canonical-dilation existence); all foundational-triple-only, Gleason-free. P.6 docs done. |
| [`LF4-todo.md`](LF4-todo.md) | Ledger of items deferred from LF2/LF3 to LF4, numbered §1–§14, with bridge-discipline rules. | LIVE ledger. §14.2 done; general-N DH + LF3 re-route done; §2 done for pure states. ⏸ **§13 Wigner-rigidity programme PAUSED 2026-06-09 at the (2c-iii) phase-cocycle crux** — `transProb` API + `TransProbPreserving` + injectivity + frame reduction (`reducedMap` fixes basis rays) all built/audited SOUND in `Mathlib/.../Projectivization/{TransitionProbability,WignerRigidity}.lean`; residual = one Wigner normal-form lemma; decision deferred (full proof vs `wigner_fs_rigidity` axiom). See §13 banner. Other items still open. |
| [`qm-empirical-tests.md`](qm-empirical-tests.md) | QM-validity regression-suite roadmap (Bell, no-cloning, teleportation, GHZ, Hardy, gates, crypto…). | LIVE. Many items done; E7/E91/BB84 + full no-broadcasting deferred (need fidelity/CPTP/LF5). |
| [`qi-qec-roadmap.md`](qi-qec-roadmap.md) | Forward roadmap for the QI / QEC / algorithm branches: the four keystone gaps (entropy, channels, fidelity, LF5), the QEC tranche, algorithms + stabilisation as operational faces of D1, and the project-structure decision (topics as subfolders under QM+CSD; infra → Mathlib staging; LF5 as a new layer). | LIVE plan. **K2 channels DONE** (C1–C4); **K3 trace-distance metric COMPLETE** (`TraceDistance.lean`; defs + nonneg + distinguishability + symmetry + **triangle inequality** done 2026-06-08; only the data-processing inequality deferred); QEC 3-qubit codes + bit-flip channel DONE. **K3 COMPLETE 2026-06-09** — metric (triangle) + **data-processing inequality** (`channel_traceDist_le`, via the channel adjoint `Φ.adjoint` + variational characterisation) + closers (`traceDist_le_one` bounded `[0,1]`; `traceDist_conj_unitary` unitary invariance); all in `DataProcessing.lean`, Tier-A audited SOUND. Remaining keystones: K1 entropy (see [`k1-plan.md`](k1-plan.md)), K4/LF5. |
| [`k1-plan.md`](k1-plan.md) | Staged plan for **K1 — von Neumann entropy `S(ρ) = −Tr ρ log ρ`** and the entropy/information stratum (subadditivity, SSA, data-processing, downstream Holevo/Schumacher/thermo). Mostly Cat-1 `QuantumInfo` infrastructure *beside* the CSD layers; the only thesis-relevant slice is the Landauer / ontic-entropy touchpoint (`S = log de-isolated volume`, `↔ μ_L`). K1-A reuses `TraceDistance.lean`'s `IsHermitian.cfc` + `re_trace_cfc` + eigenvalues. | **K1-A DONE 2026-06-16** (`Mathlib/QuantumInfo/Entropy.lean`): `vonNeumannEntropy` + operator-form `−Tr(ρ log ρ)` + `S ≥ 0` + pure-state `S = 0` + unitary invariance, foundational-triple-only, AxiomAudit-pinned. **Tensor additivity UNCONDITIONAL** (`vonNeumannEntropy_kronecker`, K1-A.2 DONE 2026-06-16): the Kronecker spectrum `spectral_sum_kronecker` (eigenvalues of `ρ⊗σ` = products `λρ·λσ`, in permutation-invariant spectral-sum form via charpoly + `diagonal_kronecker_diagonal`, no Mathlib Kronecker spectral theorem needed) discharges the former eigenvalue-product hypothesis; foundational-triple-only, AxiomAudit-pinned. The conditional `vonNeumannEntropy_kronecker_of_eigenvalues` is retained. `S ≤ log d` deferred (Jensen). **K1-B.1 partial trace DONE 2026-06-16** (`Mathlib/QuantumInfo/PartialTrace.lean`): `partialTrace{Right,Left}` (Mathlib has no matrix partial trace) + linearity (bundled LinearMaps) + trace preservation + tensor reduction `Tr_B (ρ⊗σ) = (Tr σ)•ρ` (trace of the traced-out factor; 2×2⊗2×2 sanity-probed) + IsHermitian/PSD preservation (via `v⊗eₖ` witnesses) + density ↦ density; foundational-triple-only, AxiomAudit-pinned, Mathlib-upstreamable. The shared prerequisite for K1-B.2 subadditivity + the gated decoherence / entangled D1 tier + the Landauer touchpoint. **K1-B.2 (Klein → subadditivity / Araki–Lieb) PARTIAL 2026-06-16** (`Mathlib/QuantumInfo/Subadditivity.lean`): **Klein's inequality LANDED** (`relEntropy_nonneg` / `klein_inequality`, `D(ρ‖σ) ≥ 0` for **σ positive-definite**) + relative entropy + the doubly-stochastic overlap matrix (`overlapV_row_sum` / `_col_sum`) + the **cross-term spectral expansion** `trace_mul_cfc_eq` (`Tr(ρ·cfc g σ) = ∑ᵢⱼ pᵢ g(qⱼ)‖Vᵢⱼ‖²`, trace of a product in DIFFERENT eigenbases) + reduced-trace identities `trace_mul_kronecker_one_right`/`_left`; all sorry-free, foundational-triple-only, AxiomAudit-pinned. Klein routes through the **scalar** `log_le_sub_one` + double stochasticity (NOT concave-Jensen, which is false at junk `log 0`). **The wall is now CLOSED — K1-B.2 subadditivity DONE 2026-06-16:** the **Kronecker-log operator split** `cfc_log_kronecker` (`log(ρ_A⊗ρ_B) = log ρ_A⊗I + I⊗log ρ_B`, PD factors) landed via the **decomposition-independent** `cfc_eq_conj_diagonal` (cfc of `U diag(d) Uᴴ` = `U diag(f∘d) Uᴴ` for ANY diagonalizing `U`, proved by **Lagrange interpolation** + charpoly-root eigenvalues — the cleaner route than the feared `StarAlgHomClass.map_cfc` continuity yak-shave); hence the headline **`vonNeumannEntropy_subadditive`** `S(ρ_AB) ≤ S(ρ_A)+S(ρ_B)` under `ρ_AB` PSD + **marginals PosDef** (NOT `ρ_AB` PD — pure entangled states covered), audited SOUND + non-vacuous at a correlated non-product state, foundational-triple-only, AxiomAudit-pinned. **`S ≤ log d` (`vonNeumannEntropy_le_log_card`, concave Jensen) + Araki–Lieb `|S_A−S_B| ≤ S_AB` (`vonNeumannEntropy_araki_lieb`, for ρ_AB PosDef) DONE 2026-06-17 — so K1-A and K1-B are COMPLETE.** Araki–Lieb built via the genuine purification route (`exists_purification` + Schmidt symmetry `pure_marginal_entropy_eq` (MMᴴ/MᴴM cospectrum) + `reshapeABR` + subadditivity + a swap-reindex for the other side); audited SOUND (Bell + asymmetric 2×3 + correlated full-rank ρ_AB probes; PD-scope honestly excludes the pure-entangled saturating case). Only the singular-marginal Araki–Lieb generalisation deferred. (Provenance: both the subadditivity wall-closure and this K1-A/B remainder were written in remote-control sessions and left uncommitted — the first with a build-breaking typo, the second build-clean; both recovered, audited SOUND, pinned, committed.) **K1-C strong subadditivity SCAFFOLD + CONDITIONAL REDUCTION LANDED 2026-06-17** (`Mathlib/QuantumInfo/StrongSubadditivity.lean`): tripartite marginals (`rhoAB`/`rhoBC`/`rhoB`/`rhoA` via reassoc + bipartite partial traces, reindexing identities proved) + the **mutual-information identity** `relEntropy_kronecker_eq_entropy_sub` (`D(ρ‖ρ_X⊗ρ_Y) = S_X+S_Y−S`, unconditional, from `cfc_log_kronecker` + reduced traces) + the **conditional reduction** `strong_subadditivity_of_relEntropy_monotone` (SSA from the data-processing inequality carried as explicit hypothesis `hDPI`; genuine reduction, correct direction `I(A:BC)≥I(A:B)`, non-vacuous on correlated ρ_ABC). **No axiom, no sorry, foundational-triple-only, AxiomAudit-pinned.** Scout CONFIRMED the deep input absent from Mathlib HEAD: operator MONOTONICITY present (`CFC.log_monotoneOn`, `monotone_rpow`) but operator CONVEXITY / Lieb / joint convexity / DPI ABSENT (explicit TODOs in `ExpLog/Order.lean`). `hDPI` (DPI = joint convexity = Lieb concavity) is the isolated wall; **FORK DECIDED 2026-06-17: route (a), BUILD the operator-convexity stratum** (genuine new math, not an axiom) — see [`operator-convexity-plan.md`](operator-convexity-plan.md). K1-D planned. |
| [`operator-convexity-plan.md`](operator-convexity-plan.md) | The new-math ladder to discharge K1-C's `hDPI` (data-processing / joint convexity of relative entropy) WITHOUT an axiom: L.1 operator convexity of `x⁻¹` → L.2 operator concavity of `log` → L.3 operator convexity of `x log x` → L.4 Effros perspective / joint convexity of `D` → L.5 DPI → unconditional SSA. Cat-1 (`Matrix` namespace, `Mathlib/Analysis/Matrix/OperatorConvex.lean`); multi-week. | **L.0 predicate + L.1 DONE 2026-06-17**: `OperatorConvexOn`/`OperatorConcaveOn` (genuine all-dimensions Löwner notion) + `operatorConvexOn_inv` (`x⁻¹` operator convex on `(0,∞)`, via the Schur-complement witness `fromBlocks_inv_posSemidef` + PSD-cone convexity; reuses Mathlib's `Matrix.Order` Löwner order + `PosDef.fromBlocks₁₁`). Audited SOUND (predicate all-dim/Löwner verified; L.1 holds for non-commuting PD pairs, correct direction — numeric forward-holds/reverse-fails probe), foundational-triple-only, AxiomAudit-pinned. **L.2-resolvent rungs DONE 2026-06-17**: shifted CFC↔inverse bridge `cfc_add_inv_posDef`, resolvent operator convexity `inv_shift_loewner_convex`, the **per-shift concave rung** `operatorConcaveOn_neg_add_inv` (`x ↦ −(x+s)⁻¹` operator concave on `(0,∞)`, the negation of the shifted resolvent, correct Löwner direction verified on the all-`n` predicate form), and the affine-output algebra `cfc_affine_output` + `OperatorConcaveOn.affine_output` (Step C: `c·f+d` preserves concavity for `c≥0`); all foundational-triple-only, AxiomAudit-pinned. **L.2 (log operator concavity) still WALLED** at a sharper boundary: `Matrix n n ℂ` is NOT a `CStarAlgebra` at the default instance (`infer_instance` fails), so Mathlib's C⋆-generic `CFC.log` / `tendsto_cfc_rpow_sub_one_log` / `monotone_rpow` / rpow integral-rep do not fire on the predicate's carrier — both routes shared a prerequisite `CStarMatrix ↔ Matrix` CFC/order bridge. **Bridge DONE 2026-06-18** (`Mathlib/Analysis/Matrix/OperatorConvexBridge.lean`): B.1 `cstar_cfc` (cfc naturality across the synonym `StarAlgEquiv`, the two distinct cfc instances agree via `StarAlgHomClass.map_cfc`) + B.2 `cstar_le_iff` (Löwner ↔ spectral order via `StarRingEquivClass.instOrderIsoClass.map_le_map_iff`) + `cstar_isStrictlyPositive` + **B.3 `matrix_log_le_log`** (operator MONOTONICITY of `log` on PD matrices in the Löwner order, transported from `CFC.log_le_log`); foundational-triple-only, AxiomAudit-pinned, non-vacuity witnessed. **rpow transport WALLED**: `NonnegSpectrumClass ℝ (CStarMatrix n n ℂ)` + the `Pow A ℝ` notation instance underlying `CFC.rpow_le_rpow` do not RESOLVE on `CStarMatrix` (provable as terms, not found by synthesis — the repeated-index discrimination key blocks `Fintype`/`DecidableEq` side-conditions); `log` is unaffected (needs only `[CStarAlgebra A]`). **REFRAMING LEMMA DONE 2026-06-18** (`OperatorConvex.lean`, bridge-INDEPENDENT on the `Matrix` carrier): `operatorConcaveOn_iff_concaveOn` (+ the two one-directional `operatorConcaveOn_of_concaveOn` / `concaveOn_of_operatorConcaveOn`) — `OperatorConcaveOn s f` ↔ ordinary `ConcaveOn ℝ (spectralSet s n) (fun A => cfc f A)` in the Löwner-ordered matrix codomain, for every dimension `n`; `convex_spectralSet_Ioi` (PD-cone convexity) supplies the `(0,∞)` domain-convexity side condition. Faithful (auditor-checked: genuine Löwner order, all-`n`, not a trace surrogate), foundational-triple-only, AxiomAudit-pinned. This makes Mathlib's whole `ConcaveOn` API (`ConcaveOn.add`/`.smul`/`.add_const`/Jensen) apply to operator concavity. **L.3a (`x^p` concave, `p∈[0,1]`) PARTIAL**: endpoints `operatorConcaveOn_rpow_{zero,one}` (`x^0=1`, `x^1=id`, on `Real.rpow`) LANDED via the reframing; interior `p∈(0,1)` WALLED — the matrix-carrier "cfc commutes with the integral" identity `cfc(·^p) A = ∫ cfc(integrand t) A ∂μ` needs `cfcₙ_setIntegral`, which requires `[NonUnitalCStarAlgebra (Matrix n n ℂ)]` (FAILS at default instances), so the `CStarMatrix` rpow instance-resolution wall OR a from-scratch matrix Bochner-cfc build gates it; the Schur/geometric-mean `p=1/2` route also walls (no operator geometric mean / sqrt concavity in Mathlib). No `x^p`/`log` interior concavity claimed. **A1 cfc-integral commutation + Löwner-order topology DONE 2026-06-18** (`OperatorConvex.lean` `Integral` section): `cfc_integral_commute` (`∫ cfc (g s) A ∂μ = cfc (∫ g s · ∂μ) A` on the bare `Matrix` carrier, via the spectral/entrywise route — both sides `U diag(·) Uᴴ` with `U` constant, reducing to finite scalar `integral_ofReal` identities; the from-scratch matrix Bochner-cfc build that sidesteps the failed `NonUnitalCStarAlgebra (Matrix n n ℂ)`), plus the order-topology instances `integral_concaveOn_of_integrand_ae` consumes (`isClosed_posSemidef`, `ClosedIciTopology`, `OrderClosedTopology`, `PosSMulMono`, `SMulPosMono`, `IsOrderedModule`, all scoped to the Frobenius norm). This breaches both halves of the L.3a-interior route-(ii) wall; the remaining step is the final `x^p` interior assembly. L.2/L.4/L.5 not started. |
| [`csd-departures-eft.md`](csd-departures-eft.md) | The "beyond reproducing QM" thread: candidate CSD-vs-QM departures (G3b → third-order/Sorkin interference, V≈1−I → complementarity, determinism → finite-sample statistics, finite-N), their physical-test handles, the CSD-specific negative tests, and the finite-EFT correction tower. | LIVE map. Theory-gated (corrections not pinned); leading order = QM is done, subleading owed by the paper sequence. |
| [`nqubit-register-plan.md`](nqubit-register-plan.md) | Plan for the **n-qubit register** + quantum-algorithm branch (Deutsch–Jozsa → QFT → Grover → Shor). `QReg n = EuclideanSpace ℂ (Fin n → Fin 2)`; Cat-1 in `Mathlib/QuantumInfo/Register.lean`, algorithms in `Empirical/QM/Algorithms/`. | **COMPLETE 2026-06-08.** R1–R5 + Grover done 2026-06-06; full Shor done 2026-06-07/08 → see [`shor-plan.md`](shor-plan.md). All AxiomAudit-pinned, foundational-triple-only. Coverage breadth, not the thesis. |
| [`shor-plan.md`](shor-plan.md) | Plan for **Shor's algorithm**, staged S1–S7 + assembly. In scope (finite QM + finite number theory, never field theory). | **COMPLETE 2026-06-08 — entire chain machine-checked end to end.** Quantum core (M1/M1.5/S4, `ShorCore.lean`), recovery (S5, `ShorRecovery.lean`), factoring + bridge (S6, `ShorRecovery.lean`), random-`a` success ≥ 1/2 for arbitrary odd composite `N` (S7 + S7-gen, `ShorRandomA.lean`), factoring capstone (`ShorCapstone.lean`). All foundational-triple-only, AxiomAudit-pinned, Tier-A-audited SOUND. Honest-scope deferrals (not load-bearing): constructive CF computation of `r`; two-register `r∤T` joint marginal. |
| [`ecdlp-resource-plan.md`](ecdlp-resource-plan.md) | **ECDLP / reversible-arithmetic resource-accounting** programme: a *derived* gate-list cost model (cost = function of the gate list, not annotations) toward verified Shor/ECDLP resource bounds on secp256k1. NOT re-proving Shor — building the missing reversible-arithmetic + EC + cost-accounting bridge. Cat-1, CSD-free. Two passes: abstract cost bounds, then exact Toffoli/qubit/depth counts. | **Tranche 1 DONE 2026-06-19** (`Mathlib/QuantumInfo/Reversible/{Circuit,Cost}.lean`, GREEN, 0 sorry, wired into `CsdLean4.lean` + AxiomAudit-pinned). **1a Circuit**: reversible-gate DSL (`X`/`CX`/`CCX`/`swap`, each a degenerate-form involution), `denote` (gate-list fold), `denote_append`/`reversible_comp_correct`, `inverse = List.reverse` with `reversible_inverse_correct` (both sides) + `denote_bijective` (4 proof bugs in the unverified draft fixed on first build). **1b Cost**: `structure Cost` (qubits/ancilla/toffoli/toffoliDepth/cnot/tCount/meas) + `gateCost` (X→0, CX→1 cnot, CCX→1 toffoli depth 1, swap→3 cnot) + `circuitCost` (derived field-wise gate-list sum, NOT annotated) + composition lemmas (`cost_comp_toffoli_count`/`cnot_count` exact `+`; `cost_comp_toffoli_depth_le` `≤`-subadditive; `cost_comp_qubits_le` `≤ max`) + `cost_inverse_*`. **Tranche 2 Pass 1 DONE 2026-06-19** (`Reversible/ModAdd.lean`, GREEN, auditor-SOUND): `regVal` little-endian register readout + `regVal_lt_two_pow` + `regVal_update_eq`; verified `fullAdder` (4-gate gadget, full all-inputs `decide` correctness — sum = `a⊕b⊕cin`, carry = `majority`, inputs preserved) + derived `fullAdder_cost` (2 toffoli, 2 cnot); `rippleAdder` with linear cost `2*slices.length` (composed through Tranche-1 `cost_comp_*`, not annotated). **Pass 2 Stage A DONE 2026-06-19** (localisation/frame lemma `denote_apply_of_forall_not_mem` + `fullAdder_correct_general`, the gadget correctness over arbitrary distinct `Fin n` wires). **Pass 2 Stage B DONE 2026-06-20** (`rippleCirc_correct`: the ripple adder computes `regValRange_B(out) = (regValRange_A + regValRange_B) mod 2^n` over a disjoint-wire `RippleLayout`, by the 4-clause `rippleCirc_invariant` carry induction lifting the gadget via the frame lemma; non-vacuity witnessed by `rippleLayout2`, auditor ran end-to-end probes incl. carry-out truncation). **Tranche 2 COMPLETE** — cost + full modular-addition correctness. **Tranche 3 Stage A DONE 2026-06-20** (`ModMul.lean`): the `ZMod N` semantic target `mulConst` (Shor `mulOracle` action) + `mulConst_bijective` (unit ⇒ reversible permutation) + the shift-and-add `multiplier` with DERIVED cost (`multiplier_toffoli`/`_cnot` = list-sum of block counts; `rippleCirc_toffoli` = `2n`; `multiplier_ripple_toffoli` = `2·n·m'`). Honest scope: cost only, no `Acc = a·Y` claimed. **Tranche 3 Stage B.1 DONE 2026-06-20** (`ModMul.lean`): the per-step multiplication-accumulation correctness — `accStep` (one full-remaining-width shifted ripple add does `accVal += 2^i·Y`, carry-propagating, low bits preserved, no-overflow drops the mod) + `regValRange_split` (readout split, no division) + `rippleCirc_preserves_external` (circuit frame lemma). Auditor cross-checked `accStep` vs kernel circuit eval (both = 5), `hno` load-bearing. **Tranche 3 Stage B.2 DONE 2026-06-21** — the fold to `Acc = a·Y`: `mulCircuit_correct` (the shift-and-add multiplier leaves the accumulator holding `Acc + (∑ 2^sh)·Y`, via the `accStep` fold over shifts threading Y-preservation + carry-freshness) + `MulLayout` (bounded-field wire geometry, the unbounded version was uninhabitable/vacuous — fixed) + `stepLayout`/`mulCircuit` + `mulLayout1` non-vacuity witness (auditor independently ran multiply-by-3 → 3). **Tranche 3 Stage B.3 DONE 2026-06-21** — the modular `ZMod N` capstone `mulCircuit_correct_zmod`: the multiplier's output register, read in `ZMod N`, IS `mulConst N a Y` (Shor's `y ↦ a·y mod N`); the `ZMod N` cast does the reduction (N free, NO `N=2^W` assumption; auditor verified at N=3, N=0, and `↑3=1` in ZMod 2). **TRANCHE 3 COMPLETE** — modular multiplication: cost + correctness + oracle-action connection. **Tranche 4 DONE 2026-06-21** (`ModInv.lean`, REUSE layer): `modInv N a = a⁻¹` oracle + algebra (`mul_modInv_of_unit`, `modInv_modInv` involution, `modInv_isUnit_iff_coprime` coprime bridge, all wrapping Mathlib `ZMod.inv`) + the reversibility link `mulConst_modInv_{left,right}Inverse` / `mulConst_modInv_bijective` (the Tranche-3 multiplication oracle `mulConst a` is undone by `mulConst a⁻¹`). NO inversion circuit (extended-Euclid huge; ECDLP avoids per-op inversion via projective coords) — honest residue named; auditor confirmed scope accurate, IsUnit load-bearing. **Tranche 5 DONE 2026-06-21** (`ECDLP/EllipticCurve.lean`, WRAP Mathlib `WeierstrassCurve.Affine.Point` — group law NOT reproved): `scalarMul P k = k•P` (the `[k]P` discrete-log map) + structural algebra (`scalarMul_add` homomorphism `[j+k]P=[j]P+[k]P`, `scalarMul_addOrderOf` period `[ord P]P=0`) + the ECDLP statement `IsDLog`/`Solvable` + `isDLog_add_addOrderOf` (solution shifts by the order — the period Shor finds). Auditor SOUND: genuine Mathlib EC point group (`scalarMul = k•P` by rfl), faithful ECDLP, non-vacuous, no overclaim. **Tranche 6 DONE 2026-06-21** (`ECDLP/ScalarMul.lean`): the binary `doubleAndAdd` algorithm for `[k]P` + correctness `doubleAndAdd_eq_nsmul`/`_eq_scalarMul` (closes the loop to Tranche-5 `scalarMul`; auditor verified it COMPUTES `k•P` at concrete values, `rfl` fails ⇒ non-circular) + DERIVED logarithmic cost `doubleAndAddCost_le ≤ 2·Nat.size k` (exact `= size + popcount`; genuinely O(log k), `doubleAndAddCost 1000 = 16`). The first ECDLP resource factor. **Tranche 7 CAPSTONE DONE 2026-06-21 — PROGRAMME COMPLETE (7/7)** (`ECDLP/Secp256k1.lean` + `ECDLP/ResourceBounds.lean`): the secp256k1 curve `y²=x³+7` over `𝔽_p` (`p=2^256−2^32−977`) + the end-to-end estimate `secp256k1_scalarMul_toffoli_bound` (`[k]P ≤ 2·256·(M·(2·256·256)) = O(256³)·M` Toffolis), composing T6's O(log k) op count and T3's `2n²` multiplier. Auditor SOUND + HONEST (overclaim check passed all points; bound non-vacuous, numeric `≤ 2^26` at M=1; secp256k1 params correct; no primality faked). **Honest claim: a sorry-free semantic + resource scaffold for ECDLP over secp256k1 (O(n³) structure), NOT a verified attack** — the concrete reversible EC-addition circuit (fixing `M`), `p`-primality, and a concrete on-curve point are named residue. **All stages auditor-reviewed SOUND.** **Phase 2 (hardening) IN PROGRESS:** **S1 framework DONE 2026-06-22, auditor-SOUND** (`Depth.lean`): the layered-circuit depth model — `denoteLayered_eq_denote_flatten` (correctness bridge) + `layeredToffoli_eq` (gate-count bridge) + `LayerWF` (wire-disjoint = parallel) + ripple `O(n)` sequential depth + `parallelXLayer` (depth ≪ count captured) + `reduceTree4` (a balanced XOR reduction tree, depth `2 = log₂ 4`, LayerWF proven, parity-correct over all 2^4 states — the framework captures genuine `O(log n)` COMPUTATION, the carry-lookahead building block). **S1 CLOSED 2026-06-22, auditor-SOUND** (`ResourceBounds.lean`): the secp256k1 `(Toffoli, depth, qubits)` triple, two tiers each — verified `(6.0×10⁸, ≈gate-count depth, O(n²)=65536 qubits)`, documented-optimised `(1.1×10⁸, ~5.9×10⁵ CLA-depth, ~2330 qubits)`; verified-vs-documented status explicit per metric (the depth-baseline "verified link" overstatement caught by the auditor and demoted to "documented estimate applying the verified `sequential_depth` principle"). **S4 DONE 2026-06-22 — mod-`N` reduction completeness** (`Reversible/ModReduce.lean`): the prior figures omitted the modular reduction; S4 closes the gap — modular reduction = compare-and-conditional-subtract, and the comparison flag IS the ripple adder's carry-out. VERIFIED: `rippleCirc_carryout` (`C n = decide (2ⁿ ≤ A+B)`, the overflow/compare flag, no new circuit) + `rippleCirc_modReduce_ge` (for `N≤x<2N`, preset `A=2ⁿ−N`, the adder leaves `B = x mod N` — verified single-step reduction, x≥N branch; auditor refuted vacuity with witnesses). DOCUMENTED residue: the flag-controlled subtract (cost `modReduceToffoli=4n`, `modMultToffoli=6n²` ≈3×, `secp256k1ToffoliWithReduction=1.81×10⁹`). Auditor SOUND. **S2 keystone DONE 2026-06-22 — the controlled (quantum×quantum) adder** (`Reversible/CtrlAdd.lean`): S2 verified-squaring was BLOCKED by a missing primitive (squaring is `Y²` quantum×quantum; the Tranche-3 multiplier is quantum×classical `a·Y`), needing a CONTROLLED adder, which needs 3-control gates (DSL tops at `CCX`). Built via clean-ancilla `CCCX=CCX;CCX;CCX` (shared ancilla, restored per slice): `cfullAdder` (8 CCX, decide-correct both branches + ancilla restore, general-Fin-n, frame, cost 8 Toffoli/0 CNOT) → `CRippleLayout` + 7-clause `cRippleCirc_invariant` → **`cRippleCirc_correct`** (`B ← (A+B) mod 2ⁿ` if ctrl else B unchanged) + `cRippleCirc_toffoli=8n`. Auditor SOUND (both branches witnessed `1+2→3`/unchanged; clean-ancilla hyp PROVED load-bearing). 4 pins. **S2.3 DONE 2026-06-23 — the quantum×quantum multiplier** (`Reversible/CtrlMul.lean`): folds the controlled ripple adder over the bits of the first factor register `X` (per-partial-product control bound to `X_i`, shared ancilla re-cleaned between steps) — `cAccStep` (controlled accumulation) + `CMulLayout`/`cStepLayout`/`cMulCircuit` + `cMulCircuit_correct` (`Acc ← Acc + (∑[X_sh]·2^sh)·Y`) + `cMulCircuit_eq_mul` (`Acc ← Acc + X·Y`, both factors quantum; `X:=Y` ⇒ squaring). Auditor SOUND, kernel-checked the control gates (`1·1=1` vs `0·1=0`); closes the S2 honesty point (real EC field mults multiply two quantum coordinates). 8 pins. **S6.1 DONE 2026-06-23 — derived field-mult count for `a=0` doubling** (`ECDLP/PointDouble.lean`): a derived-cost straight-line field-arithmetic program model (`Instr`/`Program`/`evalProgram`/`mulCount`, T1-style — counts only register×register `mul`/`sq`) + the secp256k1 (`a=0`) Jacobian **doubling** program; `doublingProgram_correct` (= Mathlib `dblX/dblY/dblZ`, a `ring` identity — formula↔group-law correctness REUSED from Mathlib, not reproved) + **`M_dbl = 8` DERIVED** (honest: one above EFD `2M+5S=7`, gap = un-shared squarings) + bound-level resource bridge (`doublingToffoli w = 64w²`). Assembled circuit = S6.3 residue. Auditor SOUND, 4 pins. **S6.2 DONE 2026-06-24 — mixed-addition `M_add`** (`ECDLP/PointAdd.lean`): reuses the S6.1 program model; `additionProgram_correct` (+ `_vector`) shows the schedule outputs = `(−(Pz·Qz)) • addXYZ P Q`, a unit multiple of Mathlib's group-law representative. Addition subtlety (not in doubling): raw `addX` keeps a `2·a₆·Pz⁴Qz⁴` term (`a₆=7≠0`), so the `Equation P/Q` hypotheses are LOAD-BEARING for X/Y (eliminate it via `addX_eq'`), `out_Z` hyp-free — auditor verified by probe (out_X fails without `hP`; off-curve disagreement). **`M_add = 17` DERIVED** (13 mul + 4 sq; honest vs EFD `add-2007-bl` 16, one above, gap = un-shared products); bridge `additionToffoli w = 136w²`. Auditor SOUND, 5 pins. **S6.3a DONE 2026-06-24 — complete single-step modular reduction** (`Reversible/ModReduceCtrl.lean`): closes S4's documented residue (the flag-controlled wrapper + `x<N` branch). `modReduce = rippleCirc(+2ⁿ−N) ++ X(flag) ++ cRippleCirc(+N)`; `modReduce_correct` verifies BOTH branches (`regValRange B = x mod N` for `x<2N`), `modReduce_in_range` (output `<N`), derived cost `modReduceCtrl_toffoli = 10n` (vs S4's documented `4n`; controlled add-back is `8n`). Composes only verified primitives. This is the depth-faithful **Option 1** route for S6.3 (discharges the `⟦c⟧=op` denotation obligation the cost-only reading skips). Residue: flag/step-1 carries left set; full product reduction iterates this. Auditor SOUND, 3 pins. **S6.3b DONE 2026-06-24 — verified modular ADDER** (`Reversible/ModularAdd.lean`): `modAdd = rippleCirc(addStep) ++ modReduce(reduceStep)` chains the verified register add (`B = a+b`, no wrap since `a+b < 2N ≤ 2ⁿ`) with S6.3a's `modReduce`; `modAdd_correct` (`regValRange B = (a+b) mod N`), `modAdd_preserves_operand`, `modAdd_in_range`, cost `modularAdd_toffoli = 12n`. Witness `ModAddLayout 25 3` (`n=3` a genuine boundary — add wraps at `n=2`, auditor-verified). Value-correct modular-addition atom in the fresh-ancilla model (carries/flag dirty; uncontrolled). Auditor SOUND, 4 pins. **S6.3c DONE 2026-06-24 — controlled modular adder** (`Reversible/ModularAddCtrl.lean`): `cModAdd = cRippleCirc(cAddStep) ++ modReduce(reduceStep)`; `cModAdd_correct`: `regValRange B = if ctrl then (a+b) mod N else b` (the unconditional reduce is the identity on the ¬ctrl branch since `b<N` is already a residue). + `cModAdd_preserves_operand`/`_ctrl` (control bit read-only, the multiply loop needs it), `cModAdd_in_range`, cost `18n` (`8n`+`10n`). Witness `CModAddLayout 27 3`, both branches. The inner-loop primitive of the modular multiply. Auditor SOUND, 5 pins. **Eval harness DONE 2026-06-24** (`Reversible/Eval.lean`): strict `Array Bool` evaluator `runArr` + proven bridge `runArr_apply`/`regValRangeArr_eq` to `denote`/`regValRange`, so circuit outputs `#eval` instantly (the `Fin n→Bool`/`Function.update` `denote` was exponential — a CCX reads 3 bits, lazy reads recurse 3^depth); foundational-triple, auditor-SOUND, closes the witness cross-check gap. **S6.3d-1 DONE 2026-06-24 — modular doubling** (`Reversible/ModularDouble.lean`): `modDouble = copyReg ++ modAdd` (copy `a` into the operand by `n` CNOTs, reuse S6.3b `modAdd` with both inputs `=a` ⟹ `2a mod N`); `modDouble_correct` (`regValRange B = (2*a) mod N`), `copyReg_correct_operand`/`_B` (B preserved via the copy invariant since `B k` is a control wire), cost `12n`. Harness `#eval`-cross-checked across 5 `a` incl. both wrap branches. Auditor SOUND, 6 pins. **S6.3d-2a DONE 2026-06-24 — Horner step + proven n=2 modular multiply** (`Reversible/ModularMul.lean`): `hornerStep = modDouble ++ cModAdd`; `hornerStep_correct` = `regValRange B = (2*c + if ctrl then Yval else 0) mod N` (the loop body `acc ← (2·acc + [X_i]·Y) mod N`) + `hornerStep_preserves_Y` + cost `30n`; **`mulStep2_correct`** proves a 2-bit `acc = ((2X₁+X₀)·Y) mod N` (concrete `Fin 92` instance harness-cross-checked X=3,2,1→0,1,2). New `*_preserves_external` frame lemmas carry the general loop. Auditor SOUND (both ctrl branches + wrap regimes cross-checked, hp_* satisfiable), 8 pins. **S6.3d-2b DONE 2026-06-24 — VERIFIED modular field-multiply `X·Y mod N`** (`Reversible/ModularMulLoop.lean`, the capstone of the modular-arithmetic chain): `mulLoop = fold of hornerStep over all n bits`; **`mulLoop_correct`**: `regValRange B (denote (mulLoop L) s) n = (regValRange X s n * Yval) mod N` (full n-bit X times Y, mod N), by induction (`mulLoop_invariant`) with the MSB-first bridge `regValRange_eq_hornerVal_bits` + residue-fold `horner_mod_step` + per-bank frame via a non-vacuous `Touches`/`Clean` schema (no unbounded `ℕ→Fin m`). Cost `mulLoop_toffoli = 30n²`. Witness `MulLoopLayout 135 3`, harness `#eval`-checked on 11 cases (incl. the X=1/2/4 bit-order discriminator) + a complete joint-satisfiability `example`. Auditor SOUND, 6 pins. Honest: modular MULTIPLY over registers (NOT the EC point op = S6.3e+), Θ(n²)-qubit fresh-ancilla (carry-clean Θ(n) adder is orthogonal residue). **S6.3e-1 DONE 2026-06-24 — modular SUBTRACTION `a−b mod N`** (`Reversible/ModularSub.lean`): the missing field primitive; `decide`-verified `fullSub` cell (`[X]++fullAdder++[X]`) rippled into a borrow chain (`rippleSub_borrowout = decide(a<b)`) + S6.3a `cRippleCirc` add-`N`-back gated directly on the borrow; `modSub_correct` = `regValRange B = (a+N−b) % N`, + `modSub_preserves_subtrahend`, cost `10n`. Both branches harness-cross-checked incl. the `a<b` wrap. Auditor SOUND, 7 pins. (`modNeg`/`nsmul` compose from `{modSub,modAdd,modDouble}`.) **S6.3e-2a DONE 2026-06-26 — the last two field-op gadgets** (`Reversible/ModularConst.lean`): completes the gadget set `{modAdd, modSub, modDouble, mulLoop}`. `modConstMul` = repeated `modAdd` (`c·a mod N` for CLASSICAL constant `c`, `O(c)` schedule; `modConstMul_correct`/`_preserves_operand`/`_in_range`, cost `c·12n`) + `modNeg` = `modSub` with zero minuend (`(N−b) mod N`; `modNeg_correct`/`_in_range`, cost `10n`). Worked instances `cMulLayout0/1/2/8` harness-`#eval`-cross-checked; double-and-add `O(log c)` speedup deferred (EC `nsmul` coeffs ≤ 8). Auditor SOUND, 7 pins. **S6.3e-2b STEP 1 DONE 2026-06-26 — SLP→circuit router infrastructure** (`Reversible/ProgramRouter.lean`): `RegBlockLayout` scalable register-block schema (`contigBlock_injOn` injective for every `numRegs` with `numRegs·n ≤ m`) + the six-opcode `ℕ-%p ↔ ZMod p .val` bridge (`add/mul/nsmul/neg/sub_bridge`; genuinely-new = `sub_bridge`'s wraparound-safe form, rest thin Mathlib restatements) + the abstract SSA fold `compile_correct` (final block = `(evalProgram inputs prog out).val`, a real induction not a tautology — auditor's highest-risk item cleared) + a `modNeg` worked instance (`neg(neg 3)=3`, harness-`#eval`-cross-checked). 11 pins. Auditor SOUND. **S6.3e-2b STEP 2 DONE 2026-06-26 — dispatcher + `M_dbl=8` exhibited** (`Reversible/ProgramRouterDoubling.lean`, auditor-SOUND, carve line HONEST): `compileInstr` (six `rfl` dispatch equations) + the frame lemmas `mulLoop_preserves_external`/`modConstMul_preserves_external` + copyReg-wrapped SSA adders `addWrap_correct`/`subWrap_correct` + `doubling_mulLoop_emissions_eq_8`. 8 pins. **HONEST WALL:** `M_dbl=8` is an OPCODE-level count of emitted `mulLoop` gadgets (= `mulCount doublingProgram`), NOT yet a count over an assembled circuit proven to compute the doubling over `ZMod p` — the full assembly through `compile_correct` is blocked at symbolic/secp256k1 width by nonlinear-stride `omega` (general-`n` `mulLoop` layout) + `2^256` preset decidability; the expert stopped rather than weaken. **S6.3e-2b STEP 3 PARTIAL DONE 2026-06-26 — single-opcode folds PROVEN field-correct** (`Reversible/DoublingAssembly.lean`, c786538, auditor-SOUND, honest partial no overclaim): `mul`/`sq`-gadget/`nsmul` one-step programs routed through the REAL `compile_correct` fold at full `(evalProgram … out).val` strength (`mul_step_assembly_correct` = `(X·Y)%p`, `nsmul_step_assembly_correct` = `(c·a)%p`, `#eval`-cross-checked 2·2 mod 3=1, 3·4 mod 5=2) + the load-bearing new multiplier-register preservation lemma `mulLoop_preserves_X` + `doubling_field_mul_count_eq_8_verified` (count ∧ dispatch). 8 pins. **The full `doublingProgram` was NOT assembled** (expert staged the PROGRAM not the claim): newly-surfaced walls = the `sq` copy-wrapper gap (`mulLoop` needs disjoint X/Y wires, `sq i` reads block i twice) + ~1200-wire / thousands-of-line layout boilerplate for the 17-step `hstep` + the unchanged `2^256` preset wall. **S6.3e-2b STEP 4 DONE 2026-06-26 — per-opcode fold CLOSURE** (`Reversible/DoublingAssemblyOps.lean`, auditor-SOUND): `sq`/`add`/`sub` one-step programs proven field-correct through the REAL `compile_correct` fold (`sq` via a faithful `copyReg ++ mulLoop` copy-wrapper placing X/Y disjoint; `add`=`(a+b)%p`; `sub`=`(a−b)` exercising the `a<b` wraparound) + `modAdd/modSub_preserves_block` + `all_six_opcodes_through_fold` (genuine 6-conjunct, name=type). `#eval`-checked. 9 pins. **This CLOSES the router's per-opcode field-correctness infrastructure: every `Instr` kind is a verified bit-circuit computing its `ZMod p` op through the fold.** S6.3 stopping point reached (user-chosen): `M_dbl=8` exhibited + every field op verified. Residue (declared low-payoff mechanical plumbing): the full multi-step `doublingProgram` layout assembly (~1200 wires), secp256k1 n=256 width (digit-extraction helper), carry-clean adder (orthogonal). **H1 figure reconciliation DONE 2026-06-26** (`ResourceBounds.lean`, auditor-SOUND): closes the gap where the headline `secp256k1ToffoliRefined = 6.0×10⁸` rested on the Tranche-3 `2n²` schoolbook multiplier (q×classical, reduction-free) not the verified S6.3 gadgets. New `secp256k1ToffoliVerifiedArith = 12 582 912 000 ≈ 1.26×10¹⁰` composes the DERIVED `M_dbl=8`/`M_add=17` with the verified per-multiply `verifiedModMulToffoli = 30n²` (tied to the value-correct `mulLoop` by the theorem `verifiedModMulToffoli_eq_mulLoop`, reduction-included, q×q) + bound `secp256k1_scalarMul_toffoli_verified_arith` via the real `doubleAndAddWeightedCost_le`. ~21× the schoolbook figure = ~15× per-multiply (30n² vs 2n², cause = absent carry-clean adder) × ~1.4× M-count (25 vs 18). Honest: value-correctness witness is n=3, the 256-bit figure is the proven cost-formula extrapolation; neither figure is "the cost to break secp256k1". 8 pins. README + this INDEX refreshed in the same pass. Remaining Phase 2 (deferred): **S6.3e-3** (addition + point-op triple); **carry-clean adder STAGE 1 DONE 2026-06-26** (`Reversible/CuccaroAdd.lean`, auditor-SOUND, general `n`): the in-place ancilla-restoring Cuccaro adder the corpus lacked — `cuccaroAdd_correct` (`A←(A+B) mod 2ⁿ` in place), `cuccaroAdd_ancilla_clean` (carry wire restored to `false`, the reuse-enabling property), `cuccaroAdd_toffoli = 2n`, one ancilla, `#eval`-cross-checked, 4 pins. Stage 1 of collapsing the verified-arith figure. **carry-clean adder STAGE 2 DONE 2026-06-26** (`Reversible/CuccaroModAdd.lean`, auditor-SOUND, general `n`): the carry-clean MODULAR adder `(a+b) mod N` (Beauregard flag-uncompute + masked-constant trick avoiding a C³X); `cuccaroModAdd_clean` restores ALL scratch wires (flag/carry-out/Mask/Z) — the Θ(n)-qubit-reuse enabler; `cuccaroModAdd_toffoli = 12n+10`. **Honest payoff correction:** the clean modular add is still `12n` Toffoli (modular reduction is irreducibly ~5 passes in a measurement-free DSL); the carry-clean prize is the **Θ(n) reusable qubit count** (vs Θ(n²)), NOT a big Toffoli drop — per-multiply collapses only `30n²→~12n²` (~2.5×), figure lands ~5×10⁹ not ~10⁸ (the ~10⁸ gap needs Gidney measurement adders, inexpressible here). **carry-clean adder STAGE 2b DONE 2026-06-27** (`Reversible/CuccaroModMul.lean`, auditor-SOUND, general `n`): the carry-clean modular MULTIPLY `(X·Y) mod N` via Horner, ONE reused scratch bank (Θ(n) qubits, NOT n banks); `cuccaroModDouble` (`2a mod N`, in-place `rotChain` shift + parity flag-uncompute requiring `N` odd) + `cuccaroCModAdd` (bit-gated, masked-operand) + `cuccaroModMul_correct`/`_clean` (shared bank restored ∀ step) `_toffoli = 20n²+14n`. 10 pins. Honest cost ~1.5× better than dirty `30n²`; prize = Θ(n) qubits. **carry-clean adder STAGE 3 DONE 2026-06-27** (`ECDLP/ResourceBounds.lean`, auditor-SOUND): re-cost + qubit prize. `secp256k1ToffoliCleanArith = 8 411 545 600 ≈ 8.4×10⁹` (clean `20n²+14n` tied to `cuccaroModMul_toffoli` by `cleanModMulToffoli_eq_cuccaro`; ~1.5× below the dirty `1.26×10¹⁰`) + `cleanModMulQubits = 6n+6 = 1542` with **`cleanModMulQubits_inhabited`** (a genuinely-inhabited Θ(n) layout ∀ n≥1, auditor-proved TIGHT via `IsEmpty (CuccaroMulLayout 6 0)`) — the verified Θ(n²)→Θ(n) qubit collapse, the real prize. 6 pins. **THE 3-STAGE CARRY-CLEAN PLAN IS COMPLETE.** Honest: ~1.5× Toffoli (~10⁸ literature out of DSL reach, needs measurement adders); the win is qubits. **Fermat modular INVERSION cost DONE 2026-06-27** (`ECDLP/Inversion.lean` + `ResourceBounds.lean`, auditor-SOUND): closes the inversion-omission gap (prior figures undercounted by omitting inversion, sitting below Roetteler's baseline). `fermatInv p a = a^(p-2)` with **`fermatInv_eq_inv [Fact p.Prime] (ha : a≠0) : = a⁻¹`** (real Fermat via `ZMod.pow_card_sub_one_eq_one`, the ⟦c⟧=op link; primality = named secp256k1 residue carried by `[Fact p.Prime]`) + the derived square-and-multiply op count `modExpFieldMults_le ≤ 2·Nat.size e`; `fermatInvToffoli n = 2n·cleanModMulToffoli n` (conservative `O(n³)`, one inversion = `672 923 648 ≈ 6.7×10⁸` at 256, tied to the verified multiply) → **`secp256k1ToffoliCleanArithWithInversion = 9 084 469 248 ≈ 9.1×10⁹`** (clean figure + one final coordinate-recovery inversion). Honest: cost is a derived op-count MODEL (no separate verified exponentiation circuit, same status as `doubleAndAddCost`) while the inversion VALUE-correctness is proven; Fermat is conservative (optimal Euclid/Kaliski is `O(n²)`, named not built); the 2nd scalar-mult + QFT wrapper remain omitted. 6 pins. `mod 2ⁿ` for Stage 1; S3 windowing, S5 measurement-aware DSL + Gidney adder, full `O(log n)` CLA adder. (Earlier blocked by a Smart App Control enforce policy — resolved by turning SAC off.) |
| [`lf6-plan.md`](lf6-plan.md) | **LF6 — the entangled de-isolation tier (D1 frontier).** Extends LF5 measurement-dynamics to the entangled / non-local stratum: a deterministic de-isolation account reproducing the LF3 singlet kernel `P_st`, non-locality located honestly (in the Σ-volume engine, derived; contextuality Bell-forced), no-signalling proven. | **A.1 DONE 2026-06-28** (`LF6/ForcedContextuality.lean`, auditor-SOUND, foundational-triple): the forced-contextuality crux — `no_product_partition_realises_singlet` (no setting-local ±1 product partition of Σ reproduces the singlet, via `lhvCHSH_abs_le_two` + the singlet `2√2`; both hyps proved load-bearing), `productPartition_nonvacuous`, `engine_joint_nonfactorises` (`P_st ≠ 1/4` at aligned axes), `engine_marginal_factorises` (no-signalling, LF3 reuse). Frame: factorisation = measurement; nudge ≠ carve; non-locality and Born are two outputs of one Σ-volume engine; residue = A5. **A.2 NEXT:** the full de-isolation flow on `ℂℙ¹⁵` (pointer-block volumes = `P_st`, a.s. frequencies), with the open design question (local flow `Φ_A⊗Φ_B` + contextual carve vs non-factorising flow) to settle first. |
| [`ecdsafail-correlation.md`](ecdsafail-correlation.md) + [`ecdsafail-optimization-plan.md`](ecdsafail-optimization-plan.md) | The **ECDSA.fail benchmark** alignment: correlate the repo's resource figures to the benchmark object (one affine point addition, classical offset, scored qubit×Toffoli), then optimise toward the best VERIFIED result. | **Step 1 (correlation) + Step 3 (comparable artefact) DONE 2026-06-28** (`ECDLP/PointAddBenchmark.lean`): `onePointAddToffoli = 676,880,936`, `onePointAddPeakQubits = 2,822`, `onePointAddScore = 1,910,158,001,392`, auditor-SOUND. Placement vs the late-June-2026 leaderboard best (~1,152 qubits × ~1.36M Toffoli ≈ 1.57×10⁹): **~1,220× worse, dead last** — the entire gap is the Fermat inversion (99.4% of Toffolis). **Optimization plan (step 4) WRITTEN 2026-06-28** (`ecdsafail-optimization-plan.md`): levers tiered V (verified-now: Karatsuba L1, windowed-Fermat L2, squaring L3 ⇒ ~10–25× in the CCX DSL) / X (needs DSL extension: Gidney measurement adders L5, safegcd inversion L6 ⇒ leaderboard parity) / E (out of scope: windowed multi-add L7). Step 2 (run their Rust harness) is the user's blocking machine action; Tier-X is a user-gated architecture fork. |
| [`trace-distance-triangle-plan.md`](trace-distance-triangle-plan.md) | Plan for the K3 trace-distance **triangle inequality** (= trace-norm subadditivity on Hermitian matrices), via the positive/negative-part decomposition + the TR-PSD linchpin (`0 ≤ Re Tr(S·P)` for PSD `S,P`), working with the `PosSemidef` predicate (Mathlib has no Loewner `≤`) and `IsHermitian.cfc` (defined for discontinuous `f`, dodging the `sgn` continuity gap). | **DONE 2026-06-08.** `traceNorm_add_le` + `traceDist_triangle` landed (metric complete), foundational-triple-only, AxiomAudit-pinned, Tier-A-audited SOUND. Jordan primitives `posPart`/`negPart`/`posProj` exposed for reuse. Remaining K3: the *data-processing* inequality (separate, larger tranche; its load-bearing half is already built here). |
| [`channels-plan.md`](channels-plan.md) | The K2 CPTP-channel infrastructure plan (Kraus core → Stinespring dilation → canonical channels → no-comm CPTP / QEC error channel). Built on the existing `canonicalNaimark` + `PartialTrace` primitives; the on-ramp to `Φ≠id`. | **C1–C4 DONE 2026-06-05** (`QuantumInfo.Channel`; TP/PSD/Hermitian core; Kraus↔Stinespring bridge; `unitaryChannel`/`traceOutChannel`/`mixedUnitaryChannel`; `tensorRight` + `channel_no_communication` **retiring the E3 CPTP gap**; `bitFlipChannel` error channel; all AxiomAudit-pinned). The **channel adjoint** `Φ.adjoint` (unital, positive, duality `Tr(P·Φρ)=Tr(Φ†P·ρ)`) landed 2026-06-09 with the K3 data-processing inequality; full CP / Choi-positivity (C5) remains, off critical path. |
| [`carve-out-plan.md`](carve-out-plan.md) | Carve-out diagnosis (Born = volume?) + the moment-map programme + the ontic-primitive (A5) / D1 frontier. | LIVE for the frontier (D1). Qubit Born-from-volume CLOSED; general-N superseded it (see in-file update). |
| [`audit-sweep-plan.md`](audit-sweep-plan.md) | Extend the adversarial audit **below the Tier-A headlines**: per-module csd-lean-auditor gap sweeps over the Tier-B-and-below strata (supporting lemmas, Cat-1 staging tree, bridge bundles), with a running per-sweep ledger. | **COMPLETE 2026-06-15 — SOUND.** All 5 sweeps done (12 sub-sweeps, 167 files): zero BLOCKER/zero MAJOR; findings all MINOR/NIT docstring-honesty (fixed) + upstream-prep hygiene (logged). Carving-honesty line verified correct throughout. See the ledger + findings in the plan file. |

## Done / historical (context, mostly closed)

| Doc | What it is | Status |
|---|---|---|
| [`general-n-dh-plan.md`](general-n-dh-plan.md) | General-N Duistermaat–Heckman / Born-from-Kähler-volume plan (Slices A–E). | **CLOSED 2026-06-02.** Headline + Born lift + apex + empirical capstone + N=2 reduction all done. |
| [`plan-b-detail.md`](plan-b-detail.md) | Plan B: discharge the qubit DH axiom `fs_moment_pushforward_uniform` (Gaussian + FS-uniqueness route). | CLOSED 2026-05-31 (axiom is now a theorem). |
| [`partial-trace-plan.md`](partial-trace-plan.md) | Partial-trace Cat-1 tranche (`PartialTrace.lean`, reduced density). | DONE. Tooling now feeds the POVM tranche. |
| [`pre-LF4-plan.md`](pre-LF4-plan.md) | Pre-LF4 plan: option-(B) chain design, the PureSingletPreparation bundle. | DONE (note: the chain was re-routed off Busch 2026-06-02; this doc describes the older Busch-mediated form). |
| [`LF1-plan.md`](LF1-plan.md), [`LF2-plan.md`](LF2-plan.md), [`LF3-plan.md`](LF3-plan.md) | Per-layer construction plans. | DONE (LF1/LF2/LF3 all merged + verified). |
| [`projectivization-plan.md`](projectivization-plan.md) | Mathlib-staging plan for the `Projectivization` topology/measure API. | DONE (the `CsdLean4/Mathlib/` staging tree). |
| [`empirical-csd-bridge-plan.md`](empirical-csd-bridge-plan.md) | Discipline for `Empirical/CSD/` bridge files (load-bearing-hypothesis rules). | LIVE discipline doc; see `../BRIDGE-OBLIGATIONS.md` for the ledger. |

## Reference (not todos)

| Doc | What it is |
|---|---|
| [`expert-system-prompt.md`](expert-system-prompt.md) | The project's expert-posture / system-prompt reference. |
| [`spec-to-lean.md`](spec-to-lean.md) | Spec-to-Lean mapping notes. |
| [`../README.md`](../README.md) | Authoritative status overview (layers, theorem tables, axiom posture). |
| [`../EMPIRICAL.md`](../EMPIRICAL.md) | Per-test index of the empirical suite (both branches: file, headline theorem, claim, source). |
| [`../CLAUDE.md`](../CLAUDE.md) | Working-with-the-code guide (architecture, conventions, module chains). |
| [`../AXIOMS.md`](../AXIOMS.md) | Axiom posture: the one standing axiom (`busch_effect_gleason`, §2.2; `invariant_measure_uniqueness` removed 2026-06-04, §2.1), discharged ones (§2.3–§2.4), structural assumptions (§3, incl. A5/D1/V≈1−I), per-theorem audit (§5). |
| [`../CONVENTIONS.md`](../CONVENTIONS.md) | Three-category (Cat-1/2/3) discipline, naming, namespace rules. |
| [`../BRIDGE-OBLIGATIONS.md`](../BRIDGE-OBLIGATIONS.md) | Ledger of load-bearing Empirical/CSD bridge fields with LF4-todo cross-refs. |
| [`../PLACEHOLDERS.md`](../PLACEHOLDERS.md) | Placeholder / stub tracking. |

## Current state of the programme (one paragraph)

LF1–LF5 + Empirical merged and machine-verified. Born is **derived as the Fubini–Study
typicality volume** on the ontic `Σ = ℂℙ^{N-1}` for general `N`, Gleason-free, and the
LF3 empirical chain runs on that derivation. The **POVM tranche is closed** (2026-06-03):
the ontic Born derivation now covers general (non-projective) measurements via the
canonical Naimark dilation — `povm_born_frequency_volume` lands the POVM Born weight as a
sum of FS volumes on a dilated `Σ'`, unconditionally and Gleason-free. The corpus has
exactly **one** standing axiom (`busch_effect_gleason`); `invariant_measure_uniqueness` was
**removed 2026-06-04** (the abstract bridge it served was unused; the concrete `ℂℙ^{N-1}`
uniqueness is a proved theorem). `busch_effect_gleason` is confined to the **operational
stratum** — it is no longer in the ontic Born path for *either* projective or POVM
measurements. The **quantum-algorithm branch** (Deutsch–Jozsa, QFT, Grover, full Shor) is
**complete 2026-06-08** — coverage breadth, foundational-triple-only, Tier-A-audited SOUND.
The **LF5 measurement-dynamics layer is complete** (2026-06-11, single-system projective
tier): the layer headline `measurement_flow_born_frequency` chains the de-isolation flow
`Φ_vN ≠ id` (FS measure-preserving) through the dynamically-realised Naimark dilation to
pointer-block volumes = Born and a.s. frequencies → Born, for every unit preparation (the
engine's `hpos` genericity retired by `LF4/BornRegionUncond.lean`; the corpus-wide
call-site migration onto the `_uncond` forms executed 2026-06-11 — the Empirical/CSD
volume capstones are now genericity-free, boundary preparations included). **LF5-F (2026-06-14)
adds the per-microstate outcome map** — `bornRegion_pairwiseDisjoint` (the moment-subdivision is a
genuine partition) → `vnPointerOutcome` (deterministic, total off an FS-null set, measurable
fibres) → `measurement_flow_outcome_frequency` (the conjunct-(5) upgrade to a single union event
per pointer); the outcome-function caveat owed since `aeece86` is discharged. The **canonical i.i.d.
trial witness** (`LF4/TrialWitness.lean`, 2026-06-11) inhabits the abstract trial bundle
`(Ω, Pr, X, hX, hlaw, hindep)` at the in-tree FS coordinate process; as of **2026-06-15** every
volume-frequency headline (the LF4 POVM engine plus all fifteen Empirical/CSD headlines, centralised
in `Empirical/CSD/VolumeCanonical.lean`) has a `_canonical` corollary discharging that bundle,
conclusions verbatim — so none is left merely classically-satisfiable. The **open frontier is
D1's deeper strata** (entangled/non-local de-isolation; the A5 sector origin; the concrete
`SectorData` instances still carry `Φ = id` — see `carve-out-plan.md` §6). Axiom posture is
regression-protected by
`CsdLean4/Tests/AxiomAudit.lean` (`#guard_msgs` against `#print axioms`); build with
`lake build` + `lake build CsdLeanTests`.
