# specs/ index — orientation map for plans, todos, and references

> **⚠️ [`BACKLOG.md`](BACKLOG.md) is the single canonical open-items list.** Everything
> below it is either the historical execution record (the per-phase `*-plan` files) or the
> status/claims layer. For "what is next", read `BACKLOG.md` — nothing else.

**Start here.** This indexes every planning / todo / reference doc in the corpus, with
one-line status. Updated 2026-06-30. When starting a session, read this first, then the
LIVE doc for the tranche you are on.

## Live (actionable) — pick up work here

| Doc | What it is | Status |
|---|---|---|
| [`connectivity-manifest.md`](connectivity-manifest.md) | **THE single source of truth for end-to-end connectivity claims.** Per-link status (Kähler geometry → Σ → flow → projected flow → Born / Schrödinger), each row CONNECTED (with a named backing theorem) or BROKEN/CONDITIONAL/TRIVIAL-WITNESS-ONLY (with the exact gap), plus the sequenced fix course C1–C6. No doc may claim a stronger connection than a CONNECTED row here. | **CREATED 2026-07-07 after a provenance audit found the "one posited object ⇒ both pillars" claim was NOT realized in code.** Enforced by `scripts/check-connectivity.sh`. Current state: the only fully-connected path is the trivial `Φ=id ⇒ H=0` witness; L1/L4/L5/L6/L7 BROKEN. Read before making any pillar/connectivity claim. |
| [`active-todo.md`](active-todo.md) | **THE persistent session work queue** — the full task list (Category / Complexity / Status / Blocked-by) that survives session loss; re-seed the in-memory task tracker from it. Includes the #16 D1c plan, and the #15 open-system-empirical next-tranche scoping. | **LIVE, updated 2026-06-29.** Read first to see what is open and what is next. |
| [`metrology-plan.md`](metrology-plan.md) | **Quantum sensing / metrology validation program** (parameter estimation, not static states): A1 Ramsey interferometry (volume-preserving phase flow → `cos²(φ/2)` fringe, reuses `MalusVolume`), A2 QFI = Fubini-Study metric, A3 Heisenberg `1/N` via dilated manifolds, A4 decoherence as open symplectic drift. | **A1 (Ramsey) / A2 (QFI = FS-metric pullback) / A3 (Heisenberg `F_Q^GHZ = N²` via the GHZ probe) DONE 2026-06-28; A4 (decoherence = D1) open.** A3: `Heisenberg.lean`, `ghz_qfi = N²` vs `sqlQFI = N`, `heisenberg_advantage` (N-fold), genuine `N`-qubit `Fin (2^N)` GHZ family + genuine derivative, foundational triple only. A4 tied to D1. |
| [`tn4-measurement-architecture.md`](tn4-measurement-architecture.md) | **Orientation map for the measurement stack** (TN4): the single architectural view — pure → density → channels → {projective, POVM} → Naimark → Born → CSD volume → LF5 dynamics — annotated with the real theorem names, the two readings (QM-validity vs CSD-ontic; operational vs ontic stratum), where Born comes from (the DH cluster, derived not postulated), and the honest D1/SO-1/`Φ=id` residue. Descriptive, not a plan. | **Reference doc** (written 2026-06-27). Read first when picking up measurement-theory work. |
| [`carve-out-plan.md`](carve-out-plan.md) → **D1** | **The open frontier.** Exercise real measurement *dynamics* on `Σ`; the deepest structural debt, beneath SO-1. | **OPEN at the deeper strata.** `Φ≠id` (non-collapse) done 2026-06-05 (`LF4/ObservableFlow.lean`); collapse semantics DECIDED 2026-06-09 (§6: the de-isolation reading); **the measurement-event half is now exercised at the single-system projective tier — LF5 COMPLETE 2026-06-11** (`measurement_flow_born_frequency`); **the per-microstate outcome map is DONE 2026-06-14** (LF5-F: `bornRegion_pairwiseDisjoint` → `vnPointerOutcome` → `measurement_flow_outcome_frequency`, a single union event per pointer). Remaining: entangled/non-local de-isolation, SO-1 (sector origin), MD-1 (A7 partition mismatch), `SectorData` instances still `Φ = id`. |
| [`lf5-plan.md`](lf5-plan.md) | Staged plan for **LF5 (measurement dynamics, the D1 frontier)**: realise a projective measurement as a measure-preserving von Neumann de-isolation flow `Φ_vN ≠ id` on the joint `ℂℙ^{N·N−1}`, pointer regions = `blockProj` (apparatus basis, context-fixed), committed volume = Born, chained to frequencies. **Heavily reuses the POVM Naimark tranche** (block-Born = FS-volume already proved); the genuine new content is realising the static dilation isometry as a dynamical flow. | **COMPLETE 2026-06-11.** LF5-A (vN unitary, 06-09); LF5-B (measurement flow `Φ_vN ≠ id`, FS-preserving, 06-10); LF5-C (de-isolation realises the dilation: `vnNaimark`, flow carries `[ψ⊗a₀] ↦ [Vψ]`, 06-11); LF5-D (the **unconditional engine** `LF4/BornRegionUncond.lean` — retires the `hpos` genericity of the general-`N` + POVM engines, additively — + `vnDilation_pointer_volume` / `vnDilation_pointer_frequency`, 06-11); LF5-E (capstone, 06-11): **layer headline `measurement_flow_born_frequency`** (`LF5/Capstone.lean`) — `Φ_vN ≠ id` + FS-preserving + dilation realised ∀ preparations + pointer-block volume = Born + a.s. frequencies → Born, every unit `ψ`. All foundational-triple-only, AxiomAudit-pinned, Tier-A audited SOUND. **LF5-F (post-plan, 06-14): the per-microstate outcome map** — `bornRegion_pairwiseDisjoint` (`LF4/BornRegionDisjoint.lean`) → `vnPointerOutcome` → `measurement_flow_outcome_frequency` (`LF5/PointerOutcome.lean`, single union event per pointer) + the `aeece86`-owed `block_born_frequency_volume_event`; closes the outcome-function caveat. **LF5-G (post-plan, 06-15): QEC syndrome readout as a coarse-grained de-isolation flow** (`LF5/SyndromeFlow.lean`) — stabilisers computational-basis-diagonal ⟹ syndrome = fixed `synClass : Fin 8 → Fin 4`, `Φ_syn = measurementFlow 8` coarse-grained; `syndromeRegion_fs_volume` (block FS volume = syndrome Born weight = block sum of FS volumes), deterministic codeword syndrome + recovery; headline `syndrome_flow_born_volume`, projective/coherent-error tier (decoherence/partial-trace origin gated on the entangled tier). **LF5-G.2 (06-16, `LF5/SyndromeOutcome.lean`): syndrome frequency + per-microstate outcome map** — `syndrome_flow_born_frequency` (a.s. syndrome-class freq → `syndromeWeight`), `synOutcome` (= `vnPointerOutcome.map synClass`), `syndrome_flow_outcome_frequency` (single-event), completing the QEC readout to match LF5-D/F; + the machine-checked anchor `errorSyndrome_synClass3`. Remaining D1 strata: entangled/non-local tier, decoherence error model, SO-1 (sector origin), MD-1 (A7 partition). |
| [`povm-plan.md`](povm-plan.md) | **POVM tranche** (DONE 2026-06-03): the ontic Born derivation extended to general (non-projective) POVMs via Naimark dilation. | **CLOSED** — P.1–P.5 done (type, dilation + Born transfer, volume reading, frequency capstone, canonical-dilation existence); all foundational-triple-only, Gleason-free. P.6 docs done. |
| [`LF4-todo.md`](LF4-todo.md) | Ledger of items deferred from LF2/LF3 to LF4, numbered §1–§14, with bridge-discipline rules. | LIVE ledger. §14.2 done; general-N DH + LF3 re-route done; §2 done for pure states. ⏸ **§13 Wigner-rigidity programme PAUSED 2026-06-09 at the (2c-iii) phase-cocycle crux** — `transProb` API + `TransProbPreserving` + injectivity + frame reduction (`reducedMap` fixes basis rays) all built/audited SOUND in `Mathlib/.../Projectivization/{TransitionProbability,WignerRigidity}.lean`; residual = one Wigner normal-form lemma; decision deferred (full proof vs `wigner_fs_rigidity` axiom). See §13 banner. Other items still open. |
| [`qm-empirical-tests.md`](qm-empirical-tests.md) | QM-validity regression-suite roadmap (Bell, no-cloning, teleportation, GHZ, Hardy, gates, crypto…). | LIVE. Many items done; E7/E91/BB84 + full no-broadcasting deferred (need fidelity/CPTP/LF5). |
| [`qi-qec-roadmap.md`](qi-qec-roadmap.md) | Forward roadmap for the QI / QEC / algorithm branches: the four keystone gaps (entropy, channels, fidelity, LF5), the QEC tranche, algorithms + stabilisation as operational faces of D1, and the project-structure decision (topics as subfolders under QM+CSD; infra → Mathlib staging; LF5 as a new layer). | LIVE plan. **K2 channels DONE** (C1–C4); **K3 trace-distance metric COMPLETE** (`TraceDistance.lean`; defs + nonneg + distinguishability + symmetry + **triangle inequality** done 2026-06-08; only the data-processing inequality deferred); QEC 3-qubit codes + bit-flip channel DONE. **K3 COMPLETE 2026-06-09** — metric (triangle) + **data-processing inequality** (`channel_traceDist_le`, via the channel adjoint `Φ.adjoint` + variational characterisation) + closers (`traceDist_le_one` bounded `[0,1]`; `traceDist_conj_unitary` unitary invariance); all in `DataProcessing.lean`, Tier-A audited SOUND. Remaining keystones: K1 entropy (see [`k1-plan.md`](k1-plan.md)), K4/LF5. |
| [`k1-plan.md`](k1-plan.md) | Staged plan for **K1 — von Neumann entropy `S(ρ) = −Tr ρ log ρ`** and the entropy/information stratum (subadditivity, SSA, data-processing, downstream Holevo/Schumacher/thermo). Mostly Cat-1 `QuantumInfo` infrastructure *beside* the CSD layers; the only thesis-relevant slice is the Landauer / ontic-entropy touchpoint (`S = log de-isolated volume`, `↔ μ_L`). K1-A reuses `TraceDistance.lean`'s `IsHermitian.cfc` + `re_trace_cfc` + eigenvalues. | **K1-A DONE 2026-06-16** (`Mathlib/QuantumInfo/Entropy.lean`): `vonNeumannEntropy` + operator-form `−Tr(ρ log ρ)` + `S ≥ 0` + pure-state `S = 0` + unitary invariance, foundational-triple-only, AxiomAudit-pinned. **Tensor additivity UNCONDITIONAL** (`vonNeumannEntropy_kronecker`, K1-A.2 DONE 2026-06-16): the Kronecker spectrum `spectral_sum_kronecker` (eigenvalues of `ρ⊗σ` = products `λρ·λσ`, in permutation-invariant spectral-sum form via charpoly + `diagonal_kronecker_diagonal`, no Mathlib Kronecker spectral theorem needed) discharges the former eigenvalue-product hypothesis; foundational-triple-only, AxiomAudit-pinned. The conditional `vonNeumannEntropy_kronecker_of_eigenvalues` is retained. `S ≤ log d` deferred (Jensen). **K1-B.1 partial trace DONE 2026-06-16** (`Mathlib/QuantumInfo/PartialTrace.lean`): `partialTrace{Right,Left}` (Mathlib has no matrix partial trace) + linearity (bundled LinearMaps) + trace preservation + tensor reduction `Tr_B (ρ⊗σ) = (Tr σ)•ρ` (trace of the traced-out factor; 2×2⊗2×2 sanity-probed) + IsHermitian/PSD preservation (via `v⊗eₖ` witnesses) + density ↦ density; foundational-triple-only, AxiomAudit-pinned, Mathlib-upstreamable. The shared prerequisite for K1-B.2 subadditivity + the gated decoherence / entangled D1 tier + the Landauer touchpoint. **K1-B.2 (Klein → subadditivity / Araki–Lieb) PARTIAL 2026-06-16** (`Mathlib/QuantumInfo/Subadditivity.lean`): **Klein's inequality LANDED** (`relEntropy_nonneg` / `klein_inequality`, `D(ρ‖σ) ≥ 0` for **σ positive-definite**) + relative entropy + the doubly-stochastic overlap matrix (`overlapV_row_sum` / `_col_sum`) + the **cross-term spectral expansion** `trace_mul_cfc_eq` (`Tr(ρ·cfc g σ) = ∑ᵢⱼ pᵢ g(qⱼ)‖Vᵢⱼ‖²`, trace of a product in DIFFERENT eigenbases) + reduced-trace identities `trace_mul_kronecker_one_right`/`_left`; all sorry-free, foundational-triple-only, AxiomAudit-pinned. Klein routes through the **scalar** `log_le_sub_one` + double stochasticity (NOT concave-Jensen, which is false at junk `log 0`). **The wall is now CLOSED — K1-B.2 subadditivity DONE 2026-06-16:** the **Kronecker-log operator split** `cfc_log_kronecker` (`log(ρ_A⊗ρ_B) = log ρ_A⊗I + I⊗log ρ_B`, PD factors) landed via the **decomposition-independent** `cfc_eq_conj_diagonal` (cfc of `U diag(d) Uᴴ` = `U diag(f∘d) Uᴴ` for ANY diagonalizing `U`, proved by **Lagrange interpolation** + charpoly-root eigenvalues — the cleaner route than the feared `StarAlgHomClass.map_cfc` continuity yak-shave); hence the headline **`vonNeumannEntropy_subadditive`** `S(ρ_AB) ≤ S(ρ_A)+S(ρ_B)` under `ρ_AB` PSD + **marginals PosDef** (NOT `ρ_AB` PD — pure entangled states covered), audited SOUND + non-vacuous at a correlated non-product state, foundational-triple-only, AxiomAudit-pinned. **`S ≤ log d` (`vonNeumannEntropy_le_log_card`, concave Jensen) + Araki–Lieb `|S_A−S_B| ≤ S_AB` (`vonNeumannEntropy_araki_lieb`, for ρ_AB PosDef) DONE 2026-06-17 — so K1-A and K1-B are COMPLETE.** Araki–Lieb built via the genuine purification route (`exists_purification` + Schmidt symmetry `pure_marginal_entropy_eq` (MMᴴ/MᴴM cospectrum) + `reshapeABR` + subadditivity + a swap-reindex for the other side); audited SOUND (Bell + asymmetric 2×3 + correlated full-rank ρ_AB probes; PD-scope honestly excludes the pure-entangled saturating case). Only the singular-marginal Araki–Lieb generalisation deferred. (Provenance: both the subadditivity wall-closure and this K1-A/B remainder were written in remote-control sessions and left uncommitted — the first with a build-breaking typo, the second build-clean; both recovered, audited SOUND, pinned, committed.) **K1-C strong subadditivity SCAFFOLD + CONDITIONAL REDUCTION LANDED 2026-06-17** (`Mathlib/QuantumInfo/StrongSubadditivity.lean`): tripartite marginals (`rhoAB`/`rhoBC`/`rhoB`/`rhoA` via reassoc + bipartite partial traces, reindexing identities proved) + the **mutual-information identity** `relEntropy_kronecker_eq_entropy_sub` (`D(ρ‖ρ_X⊗ρ_Y) = S_X+S_Y−S`, unconditional, from `cfc_log_kronecker` + reduced traces) + the **conditional reduction** `strong_subadditivity_of_relEntropy_monotone` (SSA from the data-processing inequality carried as explicit hypothesis `hDPI`; genuine reduction, correct direction `I(A:BC)≥I(A:B)`, non-vacuous on correlated ρ_ABC). **No axiom, no sorry, foundational-triple-only, AxiomAudit-pinned.** Scout CONFIRMED the deep input absent from Mathlib HEAD: operator MONOTONICITY present (`CFC.log_monotoneOn`, `monotone_rpow`) but operator CONVEXITY / Lieb / joint convexity / DPI ABSENT (explicit TODOs in `ExpLog/Order.lean`). `hDPI` (DPI = joint convexity = Lieb concavity) is the isolated wall; **FORK DECIDED 2026-06-17: route (a), BUILD the operator-convexity stratum** (genuine new math, not an axiom) — see [`operator-convexity-plan.md`](operator-convexity-plan.md). K1-D planned. |
| [`operator-convexity-plan.md`](operator-convexity-plan.md) | The new-math ladder to discharge K1-C's `hDPI` (data-processing / joint convexity of relative entropy) WITHOUT an axiom: L.1 operator convexity of `x⁻¹` → L.2 operator concavity of `log` → L.3 operator convexity of `x log x` → L.4 Effros perspective / joint convexity of `D` → L.5 DPI → unconditional SSA. Cat-1 (`Matrix` namespace, `Mathlib/Analysis/Matrix/OperatorConvex.lean`); multi-week. | **L.0 predicate + L.1 DONE 2026-06-17**: `OperatorConvexOn`/`OperatorConcaveOn` (genuine all-dimensions Löwner notion) + `operatorConvexOn_inv` (`x⁻¹` operator convex on `(0,∞)`, via the Schur-complement witness `fromBlocks_inv_posSemidef` + PSD-cone convexity; reuses Mathlib's `Matrix.Order` Löwner order + `PosDef.fromBlocks₁₁`). Audited SOUND (predicate all-dim/Löwner verified; L.1 holds for non-commuting PD pairs, correct direction — numeric forward-holds/reverse-fails probe), foundational-triple-only, AxiomAudit-pinned. **L.2-resolvent rungs DONE 2026-06-17**: shifted CFC↔inverse bridge `cfc_add_inv_posDef`, resolvent operator convexity `inv_shift_loewner_convex`, the **per-shift concave rung** `operatorConcaveOn_neg_add_inv` (`x ↦ −(x+s)⁻¹` operator concave on `(0,∞)`, the negation of the shifted resolvent, correct Löwner direction verified on the all-`n` predicate form), and the affine-output algebra `cfc_affine_output` + `OperatorConcaveOn.affine_output` (Step C: `c·f+d` preserves concavity for `c≥0`); all foundational-triple-only, AxiomAudit-pinned. **L.2 (log operator concavity) still WALLED** at a sharper boundary: `Matrix n n ℂ` is NOT a `CStarAlgebra` at the default instance (`infer_instance` fails), so Mathlib's C⋆-generic `CFC.log` / `tendsto_cfc_rpow_sub_one_log` / `monotone_rpow` / rpow integral-rep do not fire on the predicate's carrier — both routes shared a prerequisite `CStarMatrix ↔ Matrix` CFC/order bridge. **Bridge DONE 2026-06-18** (`Mathlib/Analysis/Matrix/OperatorConvexBridge.lean`): B.1 `cstar_cfc` (cfc naturality across the synonym `StarAlgEquiv`, the two distinct cfc instances agree via `StarAlgHomClass.map_cfc`) + B.2 `cstar_le_iff` (Löwner ↔ spectral order via `StarRingEquivClass.instOrderIsoClass.map_le_map_iff`) + `cstar_isStrictlyPositive` + **B.3 `matrix_log_le_log`** (operator MONOTONICITY of `log` on PD matrices in the Löwner order, transported from `CFC.log_le_log`); foundational-triple-only, AxiomAudit-pinned, non-vacuity witnessed. **rpow transport WALLED**: `NonnegSpectrumClass ℝ (CStarMatrix n n ℂ)` + the `Pow A ℝ` notation instance underlying `CFC.rpow_le_rpow` do not RESOLVE on `CStarMatrix` (provable as terms, not found by synthesis — the repeated-index discrimination key blocks `Fintype`/`DecidableEq` side-conditions); `log` is unaffected (needs only `[CStarAlgebra A]`). **REFRAMING LEMMA DONE 2026-06-18** (`OperatorConvex.lean`, bridge-INDEPENDENT on the `Matrix` carrier): `operatorConcaveOn_iff_concaveOn` (+ the two one-directional `operatorConcaveOn_of_concaveOn` / `concaveOn_of_operatorConcaveOn`) — `OperatorConcaveOn s f` ↔ ordinary `ConcaveOn ℝ (spectralSet s n) (fun A => cfc f A)` in the Löwner-ordered matrix codomain, for every dimension `n`; `convex_spectralSet_Ioi` (PD-cone convexity) supplies the `(0,∞)` domain-convexity side condition. Faithful (auditor-checked: genuine Löwner order, all-`n`, not a trace surrogate), foundational-triple-only, AxiomAudit-pinned. This makes Mathlib's whole `ConcaveOn` API (`ConcaveOn.add`/`.smul`/`.add_const`/Jensen) apply to operator concavity. **L.3a (`x^p` concave, `p∈[0,1]`) PARTIAL**: endpoints `operatorConcaveOn_rpow_{zero,one}` (`x^0=1`, `x^1=id`, on `Real.rpow`) LANDED via the reframing; interior `p∈(0,1)` WALLED — the matrix-carrier "cfc commutes with the integral" identity `cfc(·^p) A = ∫ cfc(integrand t) A ∂μ` needs `cfcₙ_setIntegral`, which requires `[NonUnitalCStarAlgebra (Matrix n n ℂ)]` (FAILS at default instances), so the `CStarMatrix` rpow instance-resolution wall OR a from-scratch matrix Bochner-cfc build gates it; the Schur/geometric-mean `p=1/2` route also walls (no operator geometric mean / sqrt concavity in Mathlib). No `x^p`/`log` interior concavity claimed. **A1 cfc-integral commutation + Löwner-order topology DONE 2026-06-18** (`OperatorConvex.lean` `Integral` section): `cfc_integral_commute` (`∫ cfc (g s) A ∂μ = cfc (∫ g s · ∂μ) A` on the bare `Matrix` carrier, via the spectral/entrywise route — both sides `U diag(·) Uᴴ` with `U` constant, reducing to finite scalar `integral_ofReal` identities; the from-scratch matrix Bochner-cfc build that sidesteps the failed `NonUnitalCStarAlgebra (Matrix n n ℂ)`), plus the order-topology instances `integral_concaveOn_of_integrand_ae` consumes (`isClosed_posSemidef`, `ClosedIciTopology`, `OrderClosedTopology`, `PosSMulMono`, `SMulPosMono`, `IsOrderedModule`, all scoped to the Frobenius norm). This breaches both halves of the L.3a-interior route-(ii) wall; the remaining step is the final `x^p` interior assembly. L.2/L.4/L.5 not started. |
| [`csd-departures-eft.md`](csd-departures-eft.md) | The "beyond reproducing QM" thread: candidate CSD-vs-QM departures (G3b → third-order/Sorkin interference, V≈1−I → complementarity, determinism → finite-sample statistics, finite-N), their physical-test handles, the CSD-specific negative tests, and the finite-EFT correction tower. | LIVE map. Theory-gated (corrections not pinned); leading order = QM is done, subleading owed by the paper sequence. |
| [`nqubit-register-plan.md`](nqubit-register-plan.md) | Plan for the **n-qubit register** + quantum-algorithm branch (Deutsch–Jozsa → QFT → Grover → Shor). `QReg n = EuclideanSpace ℂ (Fin n → Fin 2)`; Cat-1 in `Mathlib/QuantumInfo/Register.lean`, algorithms in `Empirical/QM/Algorithms/`. | **COMPLETE 2026-06-08.** R1–R5 + Grover done 2026-06-06; full Shor done 2026-06-07/08 → see [`shor-plan.md`](shor-plan.md). All AxiomAudit-pinned, foundational-triple-only. Coverage breadth, not the thesis. |
| [`shor-plan.md`](shor-plan.md) | Plan for **Shor's algorithm**, staged S1–S7 + assembly. In scope (finite QM + finite number theory, never field theory). | **COMPLETE 2026-06-08 — entire chain machine-checked end to end.** Quantum core (M1/M1.5/S4, `ShorCore.lean`), recovery (S5, `ShorRecovery.lean`), factoring + bridge (S6, `ShorRecovery.lean`), random-`a` success ≥ 1/2 for arbitrary odd composite `N` (S7 + S7-gen, `ShorRandomA.lean`), factoring capstone (`ShorCapstone.lean`). All foundational-triple-only, AxiomAudit-pinned, Tier-A-audited SOUND. Honest-scope deferrals (not load-bearing): constructive CF computation of `r`; two-register `r∤T` joint marginal. |
| [`lf6-plan.md`](lf6-plan.md) | **LF6 — the entangled de-isolation tier (D1 frontier).** Extends LF5 measurement-dynamics to the entangled / non-local stratum: a deterministic de-isolation account reproducing the LF3 singlet kernel `P_st`, non-locality located honestly (in the Σ-volume engine, derived; contextuality Bell-forced), no-signalling proven. | **A.1 DONE 2026-06-28** (`LF6/ForcedContextuality.lean`, auditor-SOUND, foundational-triple): the forced-contextuality crux — `no_product_partition_realises_singlet` (no setting-local ±1 product partition of Σ reproduces the singlet, via `lhvCHSH_abs_le_two` + the singlet `2√2`; both hyps proved load-bearing), `productPartition_nonvacuous`, `engine_joint_nonfactorises` (`P_st ≠ 1/4` at aligned axes), `engine_marginal_factorises` (no-signalling, LF3 reuse). Frame: factorisation = measurement; nudge ≠ carve; non-locality and Born are two outputs of one Σ-volume engine; residue = SO-1. **A.1–A.3 + B.1/B.2 DONE 2026-06-28** (`LF6/{ForcedContextuality,SingletDeisolationFlow,LocalDeisolationFlow,Decoherence}.lean`): A.2/A.3 = the `ℂℙ¹⁵` de-isolation flow `Φ ≠ id` realising the singlet dilation (`singletDeisolationFlow`, flow-realises-dilation), B.1/B.2/B.3 = the decoherence / partial-trace tier: purity-drop witness + the von Neumann entropy-increase witness (B.3, `decohere_vonNeumann_entropy_pos_of_superposition`, the pure→mixed entropy jump 0→S>0, DONE 2026-07-01). **C.1 + C.2 (general-N tier opened) DONE 2026-06-30** (`LF6/{GHZContextuality,GHZDeisolationFlow}.lean`): C.1 `no_product_partition_realises_ghz` — the GHZ forced-contextuality crux, a DETERMINISTIC all-or-nothing forcing (Mermin paradox, routes through the existing `no_lhv_assignment_for_ghz`) vs A.1's statistical CHSH bound (±1 via `pm_ae_eq` + locality both load-bearing). C.2 `ghzDeisolationFlow` — the GHZ de-isolation FLOW `Φ≠id` @ N=8, pointer-block FS volume = GHZ Born weight (composed), a.s. frequency; MINIMAL computational-basis carve (diagonal weights, NOT contextual). C.3 `GHZMerminCarve.lean` — the GENUINE dynamical contextual carve: new GHZ Pauli-context joint eigenstructure, `ghzDeisolation_blockVolume_correlation` (the sign-weighted block-volume sum = the Mermin expectation `re⟨ghz|σσσ|ghz⟩`, all four contexts) + `ghzDeisolation_carve_not_product` (four-context tie to C.1 CLOSED — the carve's OWN correlations, not a re-export; the A.2 analogue). The first DYNAMICAL realisation of GHZ non-locality. C.4 `GHZLocalFlow.lean` — the 3-party manifestly-LOCAL product flow `V₀⊗V₁⊗V₂` (three N=2 wings) realising the GHZ measurement (`ghzLocal_pullback` reuses A.3's 2-wing pullback for the inner wings), so the de-isolation needs no non-local interaction; non-locality is in the carve + entangled prep. **LF6-D (CGLMP statistical axis) CLOSED at FULL generality 2026-07-04** (`LF6/CGLMPQudit.lean`): the maximally-entangled qudit `Ψ_d`'s CGLMP violation `cglmp_maxEntangled_qudit_gt_two (hd:2≤d): cglmp d pQM > 2` for EVERY `d ≥ 2`, from `Ψ_d`'s genuine Born table (`pQM_closed`, via the `d`-th-roots-of-unity Dirichlet kernel) + a real analytic bound (`k=0` term `≥ 32/π²−8/9 > 2` uniformly in `d`, Jordan + `sin x ≤ x`); Bell force `no_lhv_realises_maxEntangled_cglmp_d`. Extends the `d=3` qutrit (LF6-D step 2, `CGLMPQutrit.lean`) to all `d`; foundational-triple, AxiomAudit-pinned. Together with the GHZ_n deterministic axis (`GHZnDeisolationFlow.lean`, LF6-E) forced non-locality is now reproduced in BOTH forms at general dimension AND general party-number. **NEXT: the wider general-`N` tier (bipartite d×d, GHZ_n); Lindblad; D1c-entangled.** |
| [`thermo-plan.md`](thermo-plan.md) | **Thermodynamics track** (CSD statistical mechanics): Born weights are equilibrium volume fractions, typicality is the ensemble, the FS-measure-preserving de-isolation flow is the reversible microdynamics, entropy increase is coarse-graining. TH1 canonical typicality → TH2 second law (coarse-grained entropy monotone) → TH3 temperature/free energy → TH4 Landauer. | **TH1 DONE 2026-07-05** (`Thermo/CanonicalTypicality.lean`, auditor-SOUND, foundational-triple): `fs_first_moment` (`∫|ψ⟩⟨ψ|dμ_FS = I/N`, a genuine twirl/Schur integral -- offdiag=0 via a signFlip change-of-variables, diag=1/N via permutation unitaries + `momentMap_sum_eq_one`) + `canonical_typicality_expectation` (`E_{μFS}[Tr_E|ψ⟩⟨ψ|] = I_S/d_S`, thermal equilibrium from FS volume). HONEST SCOPE: EXPECTATION (average) only; the typical-state concentration (Levy / spherical isoperimetry, not in Mathlib) is the named residual; NOT dynamical thermalisation. **TH2 DONE 2026-07-07** (`Thermo/SecondLaw.lean`, foundational-triple): the second law as the H-theorem `vonNeumannEntropy_le_pinching` (`S(ρ) ≤ S(pinch ρ)` -- pinching/dephasing to the pointer-basis diagonal never decreases vN entropy, via Klein's inequality + the diagonal cross-term identity) + `entropy_reversible_then_coarsegrain` (fine-grained unitary conserves entropy, coarse-graining produces it) + `entropy_production_nonneg` (pure-state instance = LF6-B.3). Honest scope: the Klein support hypothesis; a SPECIFIC coarse-graining, not a universal second law. **TH3 DONE 2026-07-07** (`Thermo/FreeEnergy.lean`, foundational-triple): temperature / free energy / the Gibbs variational principle -- `gibbs_free_energy_min` (the Gibbs state `ρ_β = exp(-βH)/Z` minimises `F(ρ) = Re Tr(ρH) − T·S(ρ)`, `F(ρ_β) ≤ F(ρ)`, via Klein `β(F(ρ)−F(ρ_β)) = D(ρ‖ρ_β) ≥ 0`) + `gibbs_free_energy_eq` (`F(ρ_β) = −T log Z`) + the crux `cfc_log_gibbsState` (`log ρ_β = −βH − (log Z)·1` via `cfc_eq_conj_diagonal` on H's eigenbasis) + `gibbsState_posDef`/`_trace` (a genuine density). Honest scope: the inequality (not equality-iff-Gibbs); `[Nonempty n]`. **TH4 DONE 2026-07-07** (`Thermo/Landauer.lean`, foundational-triple): Landauer's principle in the Reeb–Wolf form -- `landauer_bound` (a system coupled by a global unitary to a bath in the Gibbs state obeys `S(ρ_S) − S(ρ_S') ≤ β·ΔQ`, entropy removed ≤ β × heat dumped) via entropy conservation (`conj_unitary`+`kronecker`) + `subadditive` + `bath_clausius` (`D(ρ_B'‖τ_B) ≥ 0` through the TH3 Gibbs log identity); `landauer_one_bit` (erasing a maximally-mixed bit costs `ΔQ ≥ T log 2 = kT ln 2`). Honest scope: full-rank marginals + initial state; the standard finite-dim setting. **THERMO TRACK TH1–TH4 COMPLETE.** TH5 (ETH / fluctuation theorem) the optional stretch. Corpus support: `vonNeumannEntropy` (K1), `vonNeumannEntropy_subadditive`, the LF6-B entropy witness, `klein_inequality`/`relEntropy_nonneg`/`cfc_eq_conj_diagonal`, K2/K3, `μ_FS`+LLN typicality. Honest posture: QM stat-mech with a CSD reading; the deterministic-microdynamics claim rests on the SO-1/D1 residue. |
| [`future-work.md`](future-work.md) | **Open-work priority table** (reference-numbered): every pending item with a stable id (SL-/LF6-/TH-/CV-/W-/EC-/HY-), its dependencies, and a complexity grade (S/M/L/XL), plus a ★ flag for the thesis-critical (SO-1 / D1-reducing) rows and a priority read. Cite items by id ("do EC-1"). | **LIVING DOC, created 2026-07-04.** Tracks the deep frontier (SO-1 sector-origin, SL-2 D1c-entangled), LF6 remaining, thermodynamics (TH), CV, the W-series dynamics spine (W-2/W-3/W-5 DONE 2026-07-05, the Σ-flow→Schrödinger chain; S2 finite-dim Stone DONE 2026-07-05, S1 phase lift DONE 2026-07-07 with the W5 capstone `projectedFlow_schrodinger_form` = projected flow is `exp(-itH)`-conjugation on rays; W-3 clopen datum CLOSED 2026-07-07 via the Bargmann discriminator — ALL W-RESIDUES CLOSED), hygiene/audits (HY), and the pillar-completeness deferrals (P3 tensor derivation, IP-1 identical particles). **Priority order (user-set 2026-07-07): TH track (TH-2→TH-4 + TH-1 concentration residual) → CV track (CV-1 onwards) → EC (EC-6 et al. deprioritised).** |
| [`trace-distance-triangle-plan.md`](trace-distance-triangle-plan.md) | Plan for the K3 trace-distance **triangle inequality** (= trace-norm subadditivity on Hermitian matrices), via the positive/negative-part decomposition + the TR-PSD linchpin (`0 ≤ Re Tr(S·P)` for PSD `S,P`), working with the `PosSemidef` predicate (Mathlib has no Loewner `≤`) and `IsHermitian.cfc` (defined for discontinuous `f`, dodging the `sgn` continuity gap). | **DONE 2026-06-08.** `traceNorm_add_le` + `traceDist_triangle` landed (metric complete), foundational-triple-only, AxiomAudit-pinned, Tier-A-audited SOUND. Jordan primitives `posPart`/`negPart`/`posProj` exposed for reuse. Remaining K3: the *data-processing* inequality (separate, larger tranche; its load-bearing half is already built here). |
| [`channels-plan.md`](channels-plan.md) | The K2 CPTP-channel infrastructure plan (Kraus core → Stinespring dilation → canonical channels → no-comm CPTP / QEC error channel). Built on the existing `canonicalNaimark` + `PartialTrace` primitives; the on-ramp to `Φ≠id`. | **C1–C4 DONE 2026-06-05** (`QuantumInfo.Channel`; TP/PSD/Hermitian core; Kraus↔Stinespring bridge; `unitaryChannel`/`traceOutChannel`/`mixedUnitaryChannel`; `tensorRight` + `channel_no_communication` **retiring the E3 CPTP gap**; `bitFlipChannel` error channel; all AxiomAudit-pinned). The **channel adjoint** `Φ.adjoint` (unital, positive, duality `Tr(P·Φρ)=Tr(Φ†P·ρ)`) landed 2026-06-09 with the K3 data-processing inequality; full CP / Choi-positivity (C5) remains, off critical path. |
| [`carve-out-plan.md`](carve-out-plan.md) | Carve-out diagnosis (Born = volume?) + the moment-map programme + the ontic-primitive (SO-1) / D1 frontier. | LIVE for the frontier (D1). Qubit Born-from-volume CLOSED; general-N superseded it (see in-file update). |
| [`audit-sweep-plan.md`](audit-sweep-plan.md) | Extend the adversarial audit **below the Tier-A headlines**: per-module csd-lean-auditor gap sweeps over the Tier-B-and-below strata (supporting lemmas, Cat-1 staging tree, bridge bundles), with a running per-sweep ledger. | **COMPLETE 2026-06-15 — SOUND.** All 5 sweeps done (12 sub-sweeps, 167 files): zero BLOCKER/zero MAJOR; findings all MINOR/NIT docstring-honesty (fixed) + upstream-prep hygiene (logged). Carving-honesty line verified correct throughout. See the ledger + findings in the plan file. |

## Done / historical (context, mostly closed)

| Doc | What it is | Status |
|---|---|---|
| [`general-n-dh-plan.md`](general-n-dh-plan.md) | General-N Duistermaat–Heckman / Born-from-Kähler-volume plan (Slices A–E). | **CLOSED 2026-06-02.** Headline + Born lift + apex + empirical capstone + N=2 reduction all done. |
| [`plan-b-detail.md`](plan-b-detail.md) | Plan B: discharge the qubit DH axiom `fs_moment_pushforward_uniform` (Gaussian + FS-uniqueness route). | CLOSED 2026-05-31 (axiom is now a theorem). |
| [`partial-trace-plan.md`](partial-trace-plan.md) | Partial-trace Cat-1 tranche (`PartialTrace.lean`, reduced density). | DONE. Tooling now feeds the POVM tranche. |
| [`pre-LF4-plan.md`](pre-LF4-plan.md) | Pre-LF4 plan: option-(B) chain design, the PureSingletPreparation bundle. | DONE (note: the chain was re-routed off Busch 2026-06-02; this doc describes the older Busch-mediated form). |
| [`LF1-plan.md`](LF1-plan.md), [`LF2-plan.md`](LF2-plan.md), [`LF3-plan.md`](LF3-plan.md) | Per-layer construction plans. | DONE (LF1/LF2/LF3 all merged + verified). |
| [`projectivization-plan.md`](projectivization-plan.md) | Mathlib-staging plan for the `Projectivization` topology/measure API. | DONE (the `CsdLean4/Mathlib/` staging tree). |
| [`empirical-csd-bridge-plan.md`](empirical-csd-bridge-plan.md) | Discipline for `Empirical/CSD/` bridge files (load-bearing-hypothesis rules). | LIVE discipline doc; see `../BRIDGE-OBLIGATIONS.md` for the ledger. |

## Reference (not todos)

| Doc | What it is |
|---|---|
| [`expert-system-prompt.md`](expert-system-prompt.md) | The project's expert-posture / system-prompt reference. |
| [`spec-to-lean.md`](spec-to-lean.md) | Spec-to-Lean mapping notes. |
| [`../README.md`](../README.md) | Authoritative status overview (layers, theorem tables, axiom posture). |
| [`../EMPIRICAL.md`](../EMPIRICAL.md) | Per-test index of the empirical suite (both branches: file, headline theorem, claim, source). |
| [`../CLAUDE.md`](../CLAUDE.md) | Working-with-the-code guide (architecture, conventions, module chains). |
| [`../AXIOMS.md`](../AXIOMS.md) | Axiom posture: the one standing axiom (`busch_effect_gleason`, §2.2; `invariant_measure_uniqueness` removed 2026-06-04, §2.1), discharged ones (§2.3–§2.4), structural assumptions (§3, incl. SO-1/D1/V≈1−I), per-theorem audit (§5). |
| [`../CONVENTIONS.md`](../CONVENTIONS.md) | Three-category (Cat-1/2/3) discipline, naming, namespace rules. |
| [`../BRIDGE-OBLIGATIONS.md`](../BRIDGE-OBLIGATIONS.md) | Ledger of load-bearing Empirical/CSD bridge fields with LF4-todo cross-refs. |
| [`../PLACEHOLDERS.md`](../PLACEHOLDERS.md) | Placeholder / stub tracking. |

## Current state of the programme (one paragraph)

LF1–LF5 + Empirical merged and machine-verified. Born is **derived as the Fubini–Study
typicality volume** on the ontic `Σ = ℂℙ^{N-1}` for general `N`, Gleason-free, and the
LF3 empirical chain runs on that derivation. The **POVM tranche is closed** (2026-06-03):
the ontic Born derivation now covers general (non-projective) measurements via the
canonical Naimark dilation — `povm_born_frequency_volume` lands the POVM Born weight as a
sum of FS volumes on a dilated `Σ'`, unconditionally and Gleason-free. The corpus has
exactly **one** standing axiom (`busch_effect_gleason`); `invariant_measure_uniqueness` was
**removed 2026-06-04** (the abstract bridge it served was unused; the concrete `ℂℙ^{N-1}`
uniqueness is a proved theorem). `busch_effect_gleason` is confined to the **operational
stratum** — it is no longer in the ontic Born path for *either* projective or POVM
measurements. The **quantum-algorithm branch** (Deutsch–Jozsa, QFT, Grover, full Shor) is
**complete 2026-06-08** — coverage breadth, foundational-triple-only, Tier-A-audited SOUND.
The **LF5 measurement-dynamics layer is complete** (2026-06-11, single-system projective
tier): the layer headline `measurement_flow_born_frequency` chains the de-isolation flow
`Φ_vN ≠ id` (FS measure-preserving) through the dynamically-realised Naimark dilation to
pointer-block volumes = Born and a.s. frequencies → Born, for every unit preparation (the
engine's `hpos` genericity retired by `LF4/BornRegionUncond.lean`; the corpus-wide
call-site migration onto the `_uncond` forms executed 2026-06-11 — the Empirical/CSD
volume capstones are now genericity-free, boundary preparations included). **LF5-F (2026-06-14)
adds the per-microstate outcome map** — `bornRegion_pairwiseDisjoint` (the moment-subdivision is a
genuine partition) → `vnPointerOutcome` (deterministic, total off an FS-null set, measurable
fibres) → `measurement_flow_outcome_frequency` (the conjunct-(5) upgrade to a single union event
per pointer); the outcome-function caveat owed since `aeece86` is discharged. The **canonical i.i.d.
trial witness** (`LF4/TrialWitness.lean`, 2026-06-11) inhabits the abstract trial bundle
`(Ω, Pr, X, hX, hlaw, hindep)` at the in-tree FS coordinate process; as of **2026-06-15** every
volume-frequency headline (the LF4 POVM engine plus all fifteen Empirical/CSD headlines, centralised
in `Empirical/CSD/VolumeCanonical.lean`) has a `_canonical` corollary discharging that bundle,
conclusions verbatim — so none is left merely classically-satisfiable. The **open frontier is
D1's deeper strata** (entangled/non-local de-isolation; SO-1, the sector origin; MD-1, the A7
partition mismatch; the concrete
`SectorData` instances still carry `Φ = id` — see `carve-out-plan.md` §6). Axiom posture is
regression-protected by
`CsdLean4/Tests/AxiomAudit.lean` (`#guard_msgs` against `#print axioms`); build with
`lake build` + `lake build CsdLeanTests`.
