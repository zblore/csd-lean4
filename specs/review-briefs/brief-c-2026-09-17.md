# Brief C — independent review of the Category-1 closure after the Brief-B fixes (2026-09-17)

Paste everything below the line into Codex. It is self-contained.

---

You are reviewing Lean 4 / Mathlib library code as a Mathlib reviewer would. Be adversarial and
concrete. Read the files; do not take the docstrings' word for anything. Work read-only: do not edit
files. Build only if a claim needs it (`lake build --wfail CsdLean4` from the repository root; the
tree is already built, so incremental checks are cheap).

CONTEXT. Repository `csd-lean4`, this checkout, HEAD `2a71b34` (branch tip of `main` on
2026-09-17). Everything under `CsdLean4/Mathlib/` is "Category 1": Mathlib-only imports, written to
be readable, reviewed and possibly upstreamed on its own. `CsdLean4/Interop/Physlib/MANIFEST.md`
lists the 46-module, ~13,700-line closure this repository offers as nine concept slices
(`scripts/physlib-slices.txt` gives the slice of every module). Whether this code goes to Physlib at
all is an open question; review it as library code that must stand on its own either way.

Two external reviews already happened. Brief A (2026-09-16, on the Fisher–Rao bridge) and Brief B
(2026-09-16, on the closure) produced the rows B1–B13 in `specs/BACKLOG.md` #33 residue (e); all of
them were acted on in the seven commits `29099fd..2a71b34` of 2026-09-17, on top of the Brief-B
fixes of 2026-09-16 (`c504d9d`, `54276d1`). You are the third eye: check that the fixes are right and
complete, and find what the first two reviews missed. The commits, so you can diff them
(`git show <hash> --stat`, `git diff c20c719..2a71b34 -- CsdLean4/Mathlib`):

* `c20c719` B12′ — `stdBasis_addHaar`, `fsVolume_eq_topFormMeasure_addHaar`.
* `29099fd` B2 — `modelWithCornersSelf 𝕜 X` spelled `𝓘(𝕜, X)` throughout (648 sites); prose and
  code rewrapped so the closure has no line over 100 columns.
* `e9b19c3` B13 — new `MeasureTheory/Measure/Haar/PiComplex.lean` and
  `Analysis/SpecialFunctions/JapaneseBracketEuclidean.lean`.
* `ca87467` B5 (`IsKahler` over the whole atlas), B6 (Picard–Lindelöf pieces checked against
  Mathlib), B9 (Riemannian volume of Mathlib's `Bundle.RiemannianMetric`).
* `0895e77` B11′ — one order-generic smoothness proof for the Hamiltonian vector field.
* `2a71b34` B8 (naming) — `fubiniStudyMeasure*` → `fsMeasure*` repo-wide.

Earlier, on 2026-09-16 (`c504d9d`, `54276d1`): our `Projectivization.instMulAction` (which clashed by
name with Mathlib's) replaced by Mathlib's instance; namespace `RiemannianMetric` → `MetricFamily`;
three conjunction capstones removed from `KahlerForm.lean`; `metric_eq_real_inner`; three duplicates
of Mathlib deleted (`instMeasurableSingletonClass`, `compContinuousAlternatingMapL`, the hand-built
`unitaryHaarProb`, now `haarMeasure ⊤`); `mextDerivFamily` (family) vs `DifferentialForm.mextDeriv`
(bundled); `_def` equations for the twenty most-unfolded definitions; `topFormMeasure_addHaar_basis`.

QUESTIONS — answer each with file:line references.

Q1 Re-audit of the fixes. For each item below say whether the fix is correct, complete, and
idiomatic, and name anything it broke or left half-done.
  (a) `LinearAlgebra/Projectivization/Topology.lean`: the action of `V ≃ₗ[K] V` on `ℙ K V` is now
      Mathlib's `Projectivization.instMulAction` with `mapEquiv_smul_eq := rfl`. Is anything
      downstream (`Unitary.lean`, `UnitaryTransitive.lean`, `TransitionProbability.lean`) still
      relying on the old definitional unfolding?
  (b) `Geometry/Manifold/HamiltonianVectorField.lean` `IsKahler`: the defining field is now
      `J_localTriv_symmL` over every `e : atlas E M` through `(tangentBundleCore I M).localTriv e`,
      and `IsKahler.J_symmL` (the former field, over `chartAt E x₀`) is derived. Is the atlas form
      the textbook condition? Is the derivation faithful (it uses that `trivializationAt` IS
      `localTriv (achart E x₀)` definitionally)? On ℂℙⁿ, `fsForm_isKahler` proves the atlas field
      through `fsJ_localTriv_symmL` / `fderiv_chartAtIdx_transition_smul_I`
      (`Instances/ProjectiveSpaceFubiniStudySymplectic.lean`) — is every atlas member really some
      `chartAtIdx i` (`instChartedSpace` in `Instances/ProjectiveSpace.lean`)?
  (c) B11′: `ExteriorDerivative.lean` `contDiffAt_localRep` is stated at every order `n` with
      `[IsManifold 𝓘(ℝ, E) n M] [ContMDiffVectorBundle n E (TangentSpace 𝓘(ℝ, E)) 𝓘(ℝ, E)]`;
      `HamiltonianVectorField.lean` `contDiffAt_localHamiltonianVector_of_contMDiff` and
      `contMDiff_hamiltonianVectorField_of_contMDiff` take `hn : n + 1 ≤ n` (the infinite orders)
      and the same two instances, with the `∞` statements as corollaries under the old names and the
      `ω` ones as one-liners. Is `n + 1 ≤ n` the right way to say "`∞` or `ω`" in Mathlib's
      `WithTop ℕ∞`? Is the extra `ContMDiffVectorBundle` binder avoidable (Mathlib has the tangent
      bundle's instance only at `∞`, `ω`, `1` — is that so at pin `db584cd6d`)? Did any consumer
      lose generality or gain a hypothesis?
  (d) B12/B12′: `TopFormMeasure.lean` `topFormMeasure_addHaar_basis` (the glued measure against
      `e.addHaar` does not depend on `e`) and `Instances/ProjectiveSpaceFubiniStudyVolume.lean`
      `stdBasis_addHaar`, `fsVolume_eq_topFormMeasure_addHaar`. Is the "canonical measure of a top
      form" now stated in the form a user wants, or should there be a definition that takes no basis
      at all (e.g. through `Module.finBasis`)?
  (e) B13: `Haar/PiComplex.lean` (`parallelepiped_pi_basis`, `Complex.addHaar_pi_basisOneI_reindex`,
      `Complex.euclideanOrthonormalBasis`, `Complex.measurePreserving_ofLp`) and
      `JapaneseBracketEuclidean.lean` (`lintegral_pow_inv_one_add_norm_sq` for `finrank ℝ F = 2 * n`).
      Is the hypothesis shape right (`finrank = 2 * n` vs `Even (finrank ℝ F)` with the exponent
      written from the dimension)? Is anything here already in Mathlib at the pin (in particular a
      pi-basis parallelepiped lemma, a volume-preserving `ofLp` for complex `PiLp`, or a
      Haar-measure statement for `Pi.basis`)?
  (f) B9: `RiemannianVolume.lean` section "Mathlib's Riemannian metrics" —
      `Bundle.RiemannianMetric.toMetricFamily`, `.riemannianVolume`, `.riemannianVolume_congr_cover`,
      `.riemannianVolume_eq_smul_topFormMeasure`, and the `ContMDiffRiemannianMetric` versions. Are
      the names and the argument order what Mathlib would want, and is `MetricFamily` itself still
      needed once these exist?
  (g) B6: `IntegralCurve/GlobalFlow.lean`'s scope note now claims Mathlib's
      `IsPicardLindelof.of_contDiffAt_one` and `exists_forall_mem_closedBall_*` lack the confinement
      `∀ t, α t ∈ s` that `ContDiffAt.isPicardLindelof_subset` and
      `ContDiffAt.exists_forall_mem_closedBall_exists_eq_forall_mem_Ioo_hasDerivAt_mem` add. Verify that against `Mathlib/Analysis/ODE/`
      at the pin; if a Mathlib lemma does give confinement, say which and how to use it.
  (h) B8: the `fsMeasure*` rename and, earlier, `mextDerivFamily`, the `_def` equations, the
      `_of_contMDiff` / `_of_le` suffixes. Anything un-Mathlib about these names?

Q2 Statements vs docstrings, for every declaration added or changed in the seven commits (★ marks
in the module headers are the claims): list every case where the Lean statement is weaker, stronger
or differently hypothesised than its docstring says.

Q3 New duplication or drift. Anything the seven commits added that Mathlib at `db584cd6d` already
has (lemmas, instances, notation), and anything that will break on Mathlib master (the repository
keeps a forward-compat shim in `MeasureTheory/MapProbability.lean`; do not flag that one).

Q4 Soundness red flags across the closure, fresh: definitions that make a ★ theorem easier than it
looks (chartwise definitions through `idx x`, `|·|` conventions that hide orientation, junk values
of `mfderiv`/`fderiv` outside differentiability, `Classical.choose` in definitions whose theorems
then only hold for the chosen witness), hypotheses that are vacuous or unused, and any theorem whose
name promises more than its statement.

Q5 The one row still open, B8′: slice 1 (`LinearAlgebra/Projectivization/*`,
`LinearAlgebra/Matrix/Unitary*`) is stated over `EuclideanSpace ℂ (Fin N)` and
`Matrix.unitaryGroup (Fin N) ℂ`. The repository priced generalising to `[Fintype ι]` at M–L
because 233 consumer sites outside `CsdLean4/Mathlib/` pass `N` explicitly. Is that the right call,
and is there a cheaper shape (e.g. generic statements with `Fin N` corollaries under the old names)?

Q6 If this closure stayed in this repository and never went to Physlib: what would you change
first, and what would you delete?

OUTPUT. A numbered list of findings, most severe first, each with: severity (BLOCKER / SHOULD-FIX /
NIT), file:line, the defect in one sentence, and the concrete fix. Then, per question Q1(a)–(h), a
one-line verdict (correct / incomplete / wrong, with the finding number). Then a one-paragraph
overall verdict: is the closure in reviewable shape as library code, and what is the single most
valuable next change. Every concrete claim you make (a Mathlib lemma exists, a hypothesis is unused,
a site count) will be re-verified against the tree, so give the evidence (the lemma's file, the
grep) rather than the impression.
