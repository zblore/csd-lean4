# Future work — priorities, dependencies, complexity

> **⚠️ HISTORICAL LEDGER — not the open-items list.** Open work now lives in ONE place:
> [`BACKLOG.md`](BACKLOG.md). This file is the completed-work record (mostly DONE rows);
> do not add new open items here. Add / close work in `BACKLOG.md`.

**For a current review of what is machine-verified, see [`reconstruction-status.md`](reconstruction-status.md).**

**Living planning doc (created 2026-07-04).** Every open work item with a stable reference number,
dependencies, and complexity, so priorities can be evaluated and items cited by id ("do TH-2").

**Legend.** Complexity: **S** = bounded (hours), **M** = one tranche, **L** = multi-tranche / genuinely
hard, **XL** = research-grade / multi-session. **★** = actually reduces the deep A5/D1 residue
(thesis-critical); everything else is breadth, consolidation, or tooling. Status: OPEN unless noted.

## Foundations / the deep frontier

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| **SL-1** ★ | A5 → D1: derive the sector `(π,G)` + FS typicality from the deterministic dynamics | none (hard) | **XL** | The deepest residue under all of LF6/Wigner/thermo. The only work that closes the thesis gap. No clean on-ramp yet (onramps: `TypicalityForcing`, D1c). **Boundary now PROVED (`SL-A5N`, `SigmaLayer/SectorPostulateNoGo.lean`):** a single projective flow does NOT uniquely fix an invariant measure — a flow with two distinct fixed rays admits an invariant probability measure `≠ μ_FS` (`flow_admits_invariant_ne_fubiniStudy`), exhibited on the nontrivial `diag(1,-1)` on `ℂℙ¹` (`phaseFlip_admits_invariant_ne_fubiniStudy`). So "A5 is posited" is a theorem about the single-flow limit, not a formalisation debt: closing SL-1 requires MORE than the bare flow. **Partial (localized): `SL-A5L`** (`SigmaLayer/LocalisedTypicality.lean`) shows what suffices — A5 is discharged AT sectors carrying the full `U(N)` symmetry (the typicality measure and Born weights are symmetry-forced there); the residual is deriving that symmetry from the bare flow (the flow is a single one-parameter subgroup, not all of `U(N)`). No-go + localized-partial bracket the frontier from both sides. |
| ~~SL-2~~ ★ | D1c-entangled: thread a genuine `Φ≠id` through a concrete *entangled* SectorData | LF6 tier | **M** | **DONE 2026-07-09** (`LF4/SingletKahlerFlow.lean`): the singlet preparation rebuilt over `kSectorDataFlow p₀ sh` (`Φ = kFlow ≠ id`) — `ofKählerPreparationFlow`, `ofKählerPreparationFlow_phi_ne_id`. Since LF1's `preEvent = Φ⁻¹'Ω`, the capstone `ofKählerPreparationFlow_flow_frequency_convergence` scores the flow-EVOLVED trials `(kFlow∘X)⁻¹'kRegion` → `P_st`, with `kFlow`'s `μψ`-preservation (`kFlow_measurePreserving_muPsi`) load-bearing. AxiomAudit-pinned. Mirrors D1c-1 on the entangled sector. **Does NOT reduce A5** (the entangled sector still posited; `kFlow` dynamically trivial) — the deep residue stays **SL-1**. |
| ~~SL-3~~ ★ | §13.2 ontic lift: thread `Φ` + prove `TransProbPreserving f_Φ` on `kSectorData` | Wigner (done) | **M** | **DONE 2026-07-10** (`LF4/KahlerWignerLift.lean`): the `cpSectorActionBundle` analogue on the NON-TRIVIAL-FIBRE instance `kSectorData` (`Σ = ℂℙ^{N-1}×T²`, `π = pr₁` many-to-one). **Part 1 (thread Φ):** the sector flow `Φ = kFlow sh` descends along the many-to-one `π` to `f_Φ` on rays (`kSectorDataFlow_projectable`), which is `TransProbPreserving` (`kProjectedFlow_transProbPreserving`) and, fed through Wigner, realises the unitary branch (`kProjectedFlow_unitary_or_antiunitary`, with `U=1`). Honest but **degenerate**: `kFlow` is fibre-trivial on rays so `f_Φ = id` — the moving dynamics live in the `T²` fibre. **Part 2 (genuine, caveat C-1):** the sector `U(N)`-action carries the FS-isometry — `kSectorActionBundle`'s `U_isometry` is a Wigner OUTPUT (`kSectorActionBundle_U_isometry`), not a posit, built via `ofTransProbPreserving` + `transProbPreserving_unitary` + `smul_action_not_antiunitary`. AxiomAudit-pinned. **Does NOT** derive TPP from measure-preservation (the §13.2 trap / open D1 gap) — **A5 untouched** (that stays **SL-1**); this makes the forward A5→Wigner→U_isometry chain explicit on the many-to-one instance. |
| **C7** (Paper-C A3) — **DONE 2026-07-08; general-`N` capstone 2026-07-10** | **Genuine many-to-one `π` both-pillars object.** ✅ `LF4/ManyToOnePillars.lean`: `manyToOneSetup U p₀` is a `KahlerOnticSetup` on `Σ = ℂℙ^{N-1} × T²`, `π = Prod.fst` (many-to-one, fibres `= T²`, `manyToOneSetup_pi_not_injective`), flow rotates the base ray by `U t`; `manyToOneRotationSetup_both_pillars` fires BOTH pillars on the `N=2` witness (Schrödinger from `rotationSetup_schrodinger_form`, Born via `manyToOneSetup_born_frequency` on the fibred region `π⁻¹'(bornRegion)`). **General-`N` unified capstone (2026-07-10):** `manyToOneSchrodingerSetup H hH p₀` drives `manyToOneSetup` with `U t = exp(-itH) = schrodingerUnitary hH t` for an ARBITRARY Hermitian `H`, and `manyToOneSchrodingerSetup_both_pillars` delivers BOTH pillars at general `N` from the one Kähler space `Σ → π → ℂℙ^{N-1}`: (A) Schrödinger `π(Φ_t x) = exp(-itH)•π x` by construction (`manyToOneSchrodingerSetup_schrodinger_form`, `rfl` — no `N=2`, no Wigner selection, the flow is unitary by `expNegITH_unitary_group`), (B) Born frequencies → `‖⟨eᵢ,ψ⟩‖²` (general-`N` `manyToOneSetup_born_frequency`). **Schrödinger derivation EXERCISED (DONE 2026-07-19):** `LF4/ManyToOneSchrodingerDerived.lean` — the (A)-by-`rfl` form is now backed by an actual C¹-Stone derivation on the REAL nonzero generator at general `N`: `schrodingerUnitary_hasDerivAt` DISCHARGES the smoothness datum `U' t = U t·(-iH)` (via `hasDerivAt_exp_smul_const` under the `C^*` L2-operator norm, dodging the `CompleteSpace (Matrix)` diamond), and `manyToOneSchrodingerSetup_schrodinger_derived` exhibits the skew generator `A = -iH`, that discharged datum, the Stone conclusion `U t = exp(t•A)` (`CSD.StoneC1.eq_exp_of_hasDeriv`), and the pillar — no longer only the `A = 0` witness. AxiomAudit-pinned (foundational triple). AxiomAudit-pinned, connectivity link **L8**. | C1–C5, `KSigma` | **M** | Removed the `π = id` degeneracy and matched Paper C's A3 fibred-projection shape; the general-`N` capstone is the full FORWARD delivery of QM's two pillars from the Kähler sector at arbitrary `N` and arbitrary unitary dynamics. FORWARD only (CONSUMES the posited sector `(π,G,μ_FS)`); did NOT touch the deep A5 origin / weights-from-flow (that stays **SL-1** — Born trials sample `kMuL`, not evolved by the flow). |

## Kähler / symplectic differential geometry (blocked on Mathlib API)

These formalize Paper C's geometric substrate — currently carried as honest
interpretive prose (connectivity-manifest link L1) because Mathlib has no
Kähler / symplectic-form API on projective space. Each is unblocked only when
that API lands; the objects we use are already the correct ones (μ_FS is *the*
unique invariant measure, `fubiniStudyMeasure_unique`), so this is
formalization-DEPTH, not a correctness gap.

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| **KG-1** — **volume half UPGRADED 2026-07-10** (2-form still blocked) | Construct the Fubini–Study Kähler 2-form `ω` on `ℂℙ^{N-1}`, prove it closed and compatible with the complex structure `J`; identify `μ_FS = ω^{∧(N-1)}/(N-1)!`. Discharges the `IsKahlerSector` / full `IsLiouvilleKahlerVolume` posits (L1). | **Mathlib Kähler-form API (does not exist)** | **XL / blocked** | Flagged 2026-07-08. The interpretive core of "Σ is a Kähler manifold with Kähler volume μ_FS." **`IsLiouvilleKahlerVolume` content moved up (`LF4/KahlerVolumeForced.lean`)**: the volume is no longer just posited-normalized (C5) — `IsForcedKahlerVolume` characterizes it intrinsically as **the unique `U(N)`-invariant probability measure** (prob + invariant + UNIQUE, via `fubiniStudyMeasure_unique`), the measure-theoretic content of "`μ = ω^∧n/n!`". Proved: `fubiniStudyMeasure_isForcedKahlerVolume`; the sector volumes ARE it — `unitaryFlowSetup_liouville_isForcedKahlerVolume`, `manyToOneSetup_baseVolume_isForcedKahlerVolume` (`π_*kMuL` forced), `manyToOneSetup_liouville_eq_product` (`kMuL = forced-FS ⊗ Haar`). So the Kähler VOLUME is now an outcome of `Σ`+symmetry, not a free posit. **Pointwise form half BUILT (`Mathlib/Analysis/InnerProductSpace/KahlerForm.lean`, 2026-07-10):** the linear-algebra core of `ω` — the form-level analogue of `fubiniStudyMeasure`. On the tangent model `ψ^⊥` the Kähler triple `g = re⟪·,·⟫`, `ω = im⟪·,·⟫`, `J = i•·` is proved to satisfy the defining almost-Kähler relations pointwise & axiom-free: `J²=-1`, `ω` alternating `ℝ`-bilinear, J-compatibility `ω u v = g(Ju) v` + dual `g u v = ω u (Jv)`, `ω` J-invariant (a `(1,1)`-form), positivity `ω u (Ju)=‖u‖²` (`fubiniStudy_pointwise_kahler_compatibility`). **Still blocked** (needs manifold exterior calculus, absent from Mathlib): **closedness `dω=0`** (the defining *Kähler* condition) and the **global** `ω^∧(N-1)/(N-1)! = μ_FS` — the differential-geometric `IsKahlerSector` residue. So of the Kähler posit: the volume is *forced*, the form's *pointwise J-compatibility+positivity* is *proved*, and only the manifold-level closedness/2-form packaging stays blocked. **`IsKahlerSector` DE-VACUUMED (2026-07-19):** every `ℂℙ`-based `KahlerOnticSetup` instance now supplies `IsKahlerSector := IsFubiniStudyKahler N` (the pointwise compatibility above, proved by `isFubiniStudyKahler`) in place of `True`, and the trivial witness's `IsLiouvilleKahlerVolume` is now `IsProbabilityMeasure` — so **no `:= True` placeholder field remains in the corpus** (`scripts/check-claims.sh` enforces it). The sector's Kähler field is genuine formalizable content, not a label; only the manifold residual remains. AxiomAudit-pinned. FORWARD (takes `G=U(N)` as given; does not derive it — **SL-1**). |
| **KG-2** — **invariance half DONE 2026-07-11** (generation half blocked) | Derive the Σ-flow from an explicit Hamiltonian vector field `X_H` (symplectic gradient of `H` w.r.t. `ω`), matching Paper C's A2, rather than positing a unitary/rotation flow. | KG-1, symplectic-gradient API | **L / blocked** | **Dynamics-preserves-the-form half BUILT** (`LF4/SchrodingerKahlerInvariance.lean`): any `ℂ`-linear isometry preserves `g = re⟪·,·⟫` and `ω = im⟪·,·⟫` (`Kahler.kahler_structure_isometry_invariant`), so `exp(-itH)` (`schrodingerVectorFlow`) preserves BOTH the FS metric and symplectic form — `schrodinger_flow_kahler_symplectomorphism`: **Schrödinger evolution is a symplectomorphism of the `ℂℙ^{N-1}` Kähler geometry** (Kibble/Ashtekar–Schilling picture, pointwise/linear level). Ties the Kähler form to the Schrödinger pillar. **Still blocked (generation half):** the converse `X_H = ω⁻¹dH` (flow generated BY the form via the symplectic gradient) needs the manifold-differential API Mathlib lacks. AxiomAudit-pinned. |
| **KG-3** | Ashtekar–Schilling route to Schrödinger: projected quantum-effective Hamiltonian ⇒ holomorphic vector field on `ℂℙ^{N-1}` ⇒ `iψ̇ = Ĥψ`, matching Paper C §3.4 (we currently reach the same endpoint via Wigner-rigidity + phase-lift + Stone). | KG-1 | **L / blocked** | Alternative/complementary derivation; not required (endpoint already proved). |

## LF6 entangled tier (remaining)

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| ~~LF6-1~~ | Flow-capstone d-intrinsic reroute: route `maxEntangledDeisolation_flow_capstone` conjunct-7 through the general-d CGLMP force (not the Φ⁺ sector) | CGLMP general-d (done) | **S** | **DONE 2026-07-09** (`LF6/MaxEntangledCGLMPCapstone.lean`): `maxEntangledDeisolation_flow_capstone_cglmp` inherits conjuncts 1–6 and reroutes conjunct 7 to `no_lhv_realises_maxEntangled_cglmp_d` (no LHV table reproduces `pQM d`, since `cglmp d pQM > 2` in dimension `d`) — no 2×2 `Φ⁺`/CHSH reduction. New downstream file (CGLMPQudit imports the capstone, so an in-place edit would cycle). AxiomAudit-pinned. |
| **LF6-2** — **PARTIAL 2026-07-14** (T2 dephasing + T1 amplitude-damping semigroups done; general Lindblad residual) | Lindblad / continuous-time open-system de-isolation (T1/T2 semigroup) | LF6-B | **L** | Subsumes Metrology A4; the dynamics half of decoherence. **T2 dephasing quantum dynamical semigroup BUILT** (`LF6/DephasingSemigroup.lean`): the qubit dephasing channel `Φ_t(ρ)` (coherences damped by `e^{-γt}`, populations untouched); axiom-free `dephasingChannel_zero/_semigroup/_trace/_populations/_coherence/_coherence_tendsto_zero` (continuous-time einselection to the pointer basis). **T1 amplitude-damping semigroup BUILT (2026-07-14)** (`LF6/AmplitudeDamping.lean`): the population-transferring companion `Φ_t(ρ) = [[ρ₀₀+(1−e)ρ₁₁, √e·ρ₀₁],[√e·ρ₁₀, e·ρ₁₁]]` (`e = e^{-γt}`, `L = |0⟩⟨1|`); axiom-free `dampingChannel_zero/_semigroup` (`Φ_s∘Φ_t=Φ_{s+t}`)/`_trace` (channel)/`_excited_population` (`e^{-γt}ρ₁₁`)/**`_ground_population`** (the T1 signature: weight `(1−e^{-γt})ρ₁₁` flows `1→0`, absent from dephasing)/`_coherence` (`e^{-γt/2}ρ₀₁`, the `T2≤2T1` half-rate)/`_excited_tendsto_zero` + `_coherence_tendsto_zero` (relaxation to the ground state as `t→∞`, `γ>0`). Both canonical qubit dissipators now landed. AxiomAudit-pinned. **Residual (the general tier = LF6-9):** the general Lindblad generator (`dρ/dt = -i[H,ρ] + Σ L_k ρ L_k† − ½{L_k†L_k,ρ}`), complete positivity, and the generator/ODE derivation `Φ_t = e^{tℒ}`. |
| **LF6-3** | Marginal volume-drift geometry (symplectic drift of the reduced state) | LF6-B, LF5 | **M** | |
| **LF6-4** | Metrology A4: decoherence as open symplectic drift | LF6-2 (Lindblad) | **M** | D1-gated. |
| ~~LF6-5~~ — **DONE** (bound earlier; tightness 2026-07-11) | General-`d` CGLMP LHV bound `I_d ≤ 2` for all `d` (the counting argument) -- makes LF6-D's non-factorisation d-INTRINSIC (not routed through the 2×2 `Φ⁺` CHSH sector) | CGLMP infra (done) | **M/L** | **DONE** (`Mathlib/Probability/CGLMP.lean`). The general-`d` bound is the **sawtooth counting argument**, NOT `decide`: `scaledDetZ_le_general` (`scaledDetZ ≤ 2(d-1)` via `scaledDetZ = −2−2·d·t`, `t ≥ −1` from the cyclic constraint) + `cglmp_lhv_bound` (`I_d ≤ 2`, all `d`); the `decide` proofs survive only as `d=2,3,4` concrete anchors. **d-intrinsic non-factorisation** is delivered: `no_lhv_realises_maxEntangled_cglmp_d` consumes the general bound + `cglmp_maxEntangled_qudit_gt_two` (`I_d > 2` for the max-entangled qudit, all `d`), and `maxEntangledDeisolation_flow_capstone_cglmp` (LF6-1) reroutes conjunct 7 off the 2×2 Φ⁺ CHSH sector. **Tightness added 2026-07-11** (`scaledDetZ_tight_general`, `cglmp_detTable_tight_general`): the all-zero local strategy attains `I_d = 2` exactly for every `d`, so `2` is the EXACT LHV optimum in all dimensions (generalising the `decide` anchors `scaledDetZ_three_tight`/`_four_tight`). All AxiomAudit-pinned. |
| **LF6-6** — **DONE 2026-07-19** (correlation + non-factorisation witness) | Arbitrary (partial-Schmidt) entangled states general-`d` -- LF6-D covers only the maximally-entangled family | LF6-D | **M/L** | **Correlation extended beyond maximal entanglement** (`LF6/PartialSchmidtCorrelation.lean`): the general real-Schmidt two-qubit state `Ψ(c,s) = c|00⟩+s|11⟩` (`c²+s²=1`) has Hilbert-space Pauli correlation `⟨Ψ(c,s)|σ·a⊗σ·b|Ψ(c,s)⟩ = a_z b_z + 2cs·(a_x b_x − a_y b_y)` (`psQubit_pauli_correlation`, computed from the Hilbert space via `psExpectation_formula`), where `2cs` is the **concurrence** — `1` at maximal entanglement (`c=s=1/√2`, collapsing to `Φ⁺` by `psQubit_pauli_correlation_maximal`), `0` at a product state. **Non-factorisation witness DONE** via Gisin's theorem (`LF6/GisinTheorem.lean`, = LF6-8): every entangled `Ψ(c,s)` (`0<c,0<s`) violates CHSH, `gisin_chsh_violation` giving physical-expectation CHSH `2√(1+(2cs)²) > 2`. AxiomAudit-pinned. |
| ~~LF6-7~~ — **DONE 2026-07-12** | Symmetric-sector `Φ⁺↔ψ⁻` transport recompute (not yet done in LF6-D) | LF6-D | **S/M** | **DONE** (`LF6/MaxEntangledDeisolationFlow.lean`): the `xz`-reflection transport (`reflectXZ`), previously only at the correlation-function level, lifted to the HILBERT-SPACE expectation level. `phiPlus_pauli_correlation_reflectXZ` recomputes the singlet's `−a·b` directly from `Φ⁺`'s OWN derived expectation `⟨Φ⁺|σ·a⊗σ·(reflectXZ b)|Φ⁺⟩`; `phiPlus_transport_eq_singlet_expectation` proves this equals LF3's independently-derived `⟨ψ⁻|σ·a⊗σ·b|ψ⁻⟩` — so the two independent Bell derivations (Φ⁺ in LF6-D, ψ⁻ in LF3) are ONE under `reflectXZ`. Consolidates the antisymmetric ψ⁻ sector as the reflectXZ-image of the symmetric Φ⁺ sector. AxiomAudit-pinned. |

| **LF6-8** — **DONE 2026-07-19** | **Gisin's theorem**: every entangled pure two-qubit state violates CHSH (for suitable settings). The witness finishing LF6-6 — a non-factorisation for unequal Schmidt coefficients `c≠s`, where the equal-coefficient `reflectXZ→singlet` and CGLMP-Dirichlet routes both die. | LF6-6 correlation (done) | **M/L** | **Done** (`LF6/GisinTheorem.lean`): `gisin_chsh_value` computes the closed-form CHSH `2√(1+(2cs)²)` (= `2√(1+sin²2θ)` with `c=cosθ,s=sinθ`) on explicit trig-free `c,s`-dependent settings (`gisinA/gisinA'/gisinB/gisinB'`, the Horodecki optimum for `T=diag(2cs,−2cs,1)`); `gisin_chsh_violation` states the physical `⟨Ψ|σ·a⊗σ·b|Ψ⟩`-expectation form `> 2`. The feared "general Schmidt decomposition" was unnecessary — the real-Schmidt state and its correlation were already in `PartialSchmidtCorrelation.lean`. |
| **LF6-9** — **GENERATOR TIER DONE 2026-07-20** (exponential-CP residual) | **General Lindblad open-system tier**: the Lindblad generator `ℒ(ρ) = -i[H,ρ] + Σ L_k ρ L_k† − ½{L_k†L_k,ρ}`, its complete positivity (Choi/GKSL), the semigroup `Φ_t = e^{tℒ}`. Generalises the two proved bounded dissipators (T2 dephasing + T1 amplitude damping, LF6-2). | LF6-2 cores (done) | **L/XL** | **Generator BUILT** (`LF6/LindbladGenerator.lean`): the general GKSL generator `lindbladGenerator` (previously undefined) + `lindbladGenerator_trace` (trace annihilation `tr ℒ=0` ⟹ trace-preserving), `lindbladGenerator_isHermitian` (Hermiticity preservation), `lindblad_dissipation_posSemidef` (the jump part `ΣₖLₖρLₖ†` preserves PSD — the Choi/Kraus CP witness, reused from `QuantumChannel`/`ChoiConverse`). Dephasing tied to it: `dephasingGenerator_eq_lindblad` (it **is** GKSL with `H=0`, `L=√(γ/2)σ_z`) + `dephasingChannel_master_equation` (the exhibited T2 channel **solves** `d/dt Φ = ℒ_deph(Φ)` — the concrete `Φ_t=e^{tℒ}`). AxiomAudit-pinned, foundational triple. **Residual (genuinely Mathlib-scale):** complete positivity of the *exponentiated* `e^{tℒ}` for an arbitrary generator (matrix-exponential positivity, L2-operator norm scope). Unblocks LF6-2 full + LF6-4 (Metrology A4). |

**LF6 general-N entangled tier status:** the CORE is DONE (LF6-D: general `d×d` maximally-entangled
de-isolation + Born-from-volume + forced non-factorisation; CGLMP general-`d` violation; GHZ_n general-`n`
Mermin). LF6-5 DONE (general-`d` CGLMP bound + tightness, d-intrinsic non-factorisation); LF6-7 DONE (Φ⁺↔ψ⁻ transport recompute); LF6-6 DONE (partial-Schmidt correlation + non-factorisation-for-unequal witness = **LF6-8**, Gisin, `LF6/GisinTheorem.lean`); LF6-2 PARTIAL (T2 + T1 semigroups done; general Lindblad tier = **LF6-9**). All LF6 residuals now have their bounded cores landed; LF6-8 (Gisin) is done and LF6-9's **generator tier** is done (`LF6/LindbladGenerator.lean`), leaving only the exponential-CP (`e^{tℒ}` for arbitrary generators) piece of LF6-9 open.

## Thermodynamics track (`thermo-plan.md`)

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| ~~TH-1~~ | Canonical typicality: `E_{μFS}[ρ_S] = I/d_S` (avg) + Levy stretch | μ_FS + partial trace | **M** | **DONE (expectation core) 2026-07-07** (`Thermo/CanonicalTypicality.lean`): `E_{μFS}[ρ_S] = I/d_S`, thermal equilibrium from FS volume. Optional residue: the **Levy-concentration** stretch (needs spherical isoperimetry, not in Mathlib). |
| ~~TH-2~~ | Second law: coarse-grained vN entropy monotone under the de-isolation flow | TH-1 / LF6-B.3 | **M** | **DONE 2026-07-07** (`Thermo/SecondLaw.lean`): the H-theorem `vonNeumannEntropy_le_pinching` (`S(ρ) ≤ S(pinch ρ)`) + `entropy_reversible_then_coarsegrain` + `entropy_production_nonneg`, via Klein. |
| ~~TH-3~~ | Temperature / free energy: Gibbs max-entropy state, `T=∂S/∂E`, `F=E−TS`, variational principle | vN entropy (K1) | **M** | **DONE 2026-07-07** (`Thermo/FreeEnergy.lean`): `gibbs_free_energy_min` (`F(ρ_β) ≤ F(ρ)`) + `gibbs_free_energy_eq` (`F(ρ_β) = −T log Z`) + the Gibbs state (`gibbsState`, `cfc_log_gibbsState`), via Klein. |
| ~~TH-4~~ | Landauer erasure bound `≥ kT ln2` | TH-3, QEC tier | **M** | **DONE 2026-07-07** (`Thermo/Landauer.lean`): the Reeb–Wolf bound `landauer_bound` (`S(ρ_S)−S(ρ_S') ≤ β·ΔQ`) via entropy conservation + subadditivity + `bath_clausius`; one-bit corollary `landauer_one_bit` (`ΔQ ≥ T log 2`). |
| **TH-5** | ETH / fluctuation theorem (Jarzynski/Crooks) | TH-1..3 | **L** | Stretch. |

## CV / continuous-variable track

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| ~~W4~~ | ApproxCCR finite-dim CCR obstruction | — | — | DONE (committed). |
| ~~CV-1~~ | Finite position observable on a lattice | W4 | **S/M** | **DONE 2026-07-09** (`CV/Position.lean`): `positionOp = diag(x_j)` on the symmetric lattice — Hermitian, eigenvalues = lattice points (`positionOp_mulVec_single`), distinct (`latticePoint_injective`), bounded, centered. AxiomAudit-pinned. |
| ~~CV-2~~ / ~~CV-3~~ | Finite conjugate pair + approximate CCR on a low-energy sector | CV-1 / W4 | **M/L** | **DONE 2026-07-09** (`CV/Oscillator.lean`): took the truncated-oscillator route (cleaner + provable, and the sharp complement to W4) rather than lattice+DFT. `a`, `a†`, number op `a†a = diag(n)`; truncated CCR `[a,a†] = 1 − N·|N−1⟩⟨N−1|`; `Q,P` Hermitian; `[Q,P] = i·[a,a†]`; **`ccr_exact_on_bulk`**: `[Q,P]·eₙ = i·eₙ` exactly for every `n ≠ N−1` — the exact CCR on the low-energy sector, W4-forced defect confined to the top level. AxiomAudit-pinned. NB: the "‖·‖ ≤ ε" is realised as *exact on the bulk* (defect rank-one at the ceiling), stronger than a norm bound; the lattice+DFT semiclassical form is left as a harder alternative. |
| ~~CV-4~~ | Oscillator truncation recovers finite-energy predictions | CV-1..3 | **M** | **DONE 2026-07-09** (`CV/OscillatorSpectrum.lean`): `H = a†a + ½ = diag(n+½)`, Hermitian, with the Fock states as energy eigenstates (`hamiltonian_mulVec_single`: `H·eₙ = (n+½)·eₙ`). The energy `Eₙ = n+½` (`oscEnergy`) is manifestly **cutoff-independent** — no `N` — so the zero-point `½` (`hamiltonian_groundEnergy`), the uniform gap `1` (`oscEnergy_gap`), and every level below the ceiling are recovered exactly. AxiomAudit-pinned. **CV track (CV-1..CV-4) complete.** |

## W-series (CSD dynamics spine — the prize: finite-dim Schrödinger dynamics as projected CSD flow)

**Spec received 2026-07-04. SPINE COMPLETE 2026-07-05 (W-2/W-3/W-5 all DONE).** The chain built:
`Σ-flow → projected ℂℙ^{N-1} flow → FS-isometry / transition-probability-preserving flow → unitary
dynamics`. W1 (Wigner) + W4 (CCR obstruction) done; W-2/W-3/W-5 assemble the bridge, all auditor-SOUND,
foundational-triple, no global axioms. **W-5-S2 finite-dim Stone: DONE** (C^1 form,
`Mathlib/Analysis/Matrix/StoneC1.lean`, commit 23b2a36 -- differentiable one-parameter unitary group =
`exp(t.A)`, generator recovered; the CompleteSpace instance-diamond blocker resolved via the C^*-algebra
norm; full-continuity Stone is the named sub-residual). **W-5-S1 phase lift: DONE 2026-07-07**
(`LF4/PhaseLift.lean` + `Mathlib/LinearAlgebra/Projectivization/PhaseRigidity.lean`): phase rigidity
(ker `U(N)→PU(N)` = the circle, via `LinearMap.exists_eq_smul_id_of_forall_notLinearIndependent`)
extracts the `U(1)` cocycle of the projected-flow family (`projectedFlow_phase_cocycle`, the named
obstruction), the 2-cocycle law is proved (`phase_cocycle_identity`), and the coboundary datum `b`
(the honest cohomological input -- `H²(ℝ,U(1)) ≠ 0` algebraically, so some input is genuinely required)
yields the GENUINE vector-level one-parameter unitary group realising the same flow
(`projectedFlow_phase_lift`). Wired to S2 this closes the **W5 capstone**
`projectedFlow_schrodinger_form`: projected CSD flow = `exp(-itH)`-conjugation on rays, `H` Hermitian
recovered. Non-vacuity end-to-end on the trivial witness. Named follow-ons: Bargmann (continuity ⇒
coboundary datum, kills S1's input for continuous flows) and full-continuity Stone (S2's input).
**W-3 clopen datum: CLOSED 2026-07-07** (`LF4/BargmannSelection.lean` +
`Mathlib/LinearAlgebra/Projectivization/Bargmann.lean`): the Bargmann invariant (normalised triple
product on `ℙ³`, preserved by unitaries, CONJUGATED by `conjProj`) separates the Wigner branches at
the distinct values `Δ` vs `conj Δ` on a probe triple with `Im Δ = 1/4 ≠ 0` (exists ∀ `N ≥ 2`) --
(ii) (PU(N) disconnected in the FS-isometry group) is thereby PROVED (incl. exclusivity of the
Wigner disjunction, `not_projUnitary_and_projAntiunitary`), and (i) is REDUCED to the scalar datum
"the Bargmann observable `t ↦ Δ(Φ_t p,Φ_t q,Φ_t r)` is continuous", from which the clopen set is
DERIVED (`projUnitary_isClopen_of_bargmann_continuous`) and the selection fires
(`projectedFlow_unitary_of_bargmann_continuous`); `N ≤ 1` needs no datum. Named follow-ons:
continuity of the Bargmann function on `ℙ³` (local sections of `mk`; would derive the scalar datum
from raw flow continuity) and inhabiting the continuity datum on a non-trivial (`Φ≠id`)
`KahlerOnticSetup`. **ALL THREE W-RESIDUES ARE NOW CLOSED** (S1, S2, W-3 clopen).
**Substrate-linkage fix (2026-07-07):** a provenance audit found the W-series theorems consumed only
`d.projectedFlow` — the `KahlerOnticSetup` substrate fields (`flow`, `pi`, the descent equation
`projectable`) were inert, so the "Schrödinger from the ontic sector" reading was carried-but-unused
scaffolding. Fixed by `sigmaFlow_schrodinger_form` (`LF4/PhaseLift.lean`), which consumes
`d.projectable`/`d.flow`/`d.pi` to land `d.pi (d.flow t x) = exp(-itH) • d.pi x` — the deterministic
Σ-flow, projected, IS Schrödinger evolution. Enforced by `scripts/check-sector-linkage.sh`. (The Born
pillar has the analogous gap — its general-`N` frequency capstones run on abstract `CPN` + `μ_FS`, not
an `OnticSetup` with a deterministic Σ-flow; wiring that through is the D1/SL-1 frontier, HY-5 below.)
Leave the P3 tensor derivation alone until there is a paper proof.

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| **W-2** | `LF4/KahlerOnticSetup.lean`: the `structure KahlerOnticSetup N` interface (8 genuine fields + 2 honest Kähler-geometry placeholders, NO global axioms; `trivialKahlerOnticSetup` witness; projective target = Wigner's). | none (interface) | **M** | **DONE 2026-07-05** (`53ad012`). |
| **W-3** | `LF4/UnitarySelection.lean`: transProb-preservation (a HYPOTHESIS, not derived from Liouville) ⇒ unitary ∨ antiunitary via `wigner_rigidity_unitaryGroup`; continuous-from-id ⇒ unitary branch, STAGED on the clopen datum (connectedness on `PreconnectedSpace ℝ` proved). | W-2, Wigner | **M/L** | **DONE 2026-07-05** (`c119ffc`). Residue (clopen datum) CLOSED 2026-07-07 via the Bargmann discriminator (`LF4/BargmannSelection.lean`). |
| **W-5** | `LF4/ProjectedDynamics.lean`: `projectedFlow_eq_unitary_family` (projected flow = projective action of a one-parameter unitary family) + ray-level group law under explicit hypotheses + `expNegITH_unitary_group` (converse: `exp(-itH)` a genuine unitary group from Hermitian H). `U_t=exp(-itH)`-for-the-flow STAGED. | W-2, W-3 | **L** | **DONE 2026-07-05** (`ff97830`). Residues S2 (finite-dim Stone) DONE 2026-07-05 + S1 (phase lift) DONE 2026-07-07 (`LF4/PhaseLift.lean`, capstone `projectedFlow_schrodinger_form`). |

**Follow-on (auditor-recommended, S):** inhabit the W-3 continuity datum (now the Bargmann-observable
continuity, after the 2026-07-07 closure) on a non-trivial (`Φ≠id`) `KahlerOnticSetup`, not only the
identity witness, so the unitary-branch selection is non-vacuous beyond the base case. **A genuine
`Φ≠id` inhabitant now EXISTS** (`rotationSetup`, `LF4/NonTrivialSetup.lean`, connectivity fix C1
2026-07-07); firing the W3/W5/S1/S2 chain on it is fix **C2** (the next connectivity step). NB: `kFlow`
is NOT usable here — it acts trivially on rays (`projectedFlow=id`); the C1 witness uses a projective
unitary flow. See [`specs/connectivity-manifest.md`](connectivity-manifest.md).

**Honest posture of the W-series (load-bearing):** this is the FORWARD direction -- GIVEN the Kähler sector
(as hypotheses/fields), it derives the unitary Schrödinger dynamics (via Wigner). It CONSUMES the sector
(A5-level structure); it does NOT derive the sector from the dynamics (that reverse is D1/SL-1, untouched).
So the W-series completes "QM dynamics from the posited sector", not the deep residue. Not ★.

## ecdsa.fail / ECDLP — EXTRACTED (2026-07-20)

The ecdsa.fail / ECDLP quantum-cryptanalysis track (EC-1..EC-9: safegcd inversion, half-GCD,
Karatsuba/Toom multiply, secp256k1 point-add/double, the three-track cost ledger) was extracted
to its own repository and removed from this repo. The generic reversible-arithmetic DSL it used
(`Mathlib/QuantumInfo/Reversible/`) and the measurement-adder work (`Empirical/QM/Measurement*`,
the Gidney / AND / constprop cost results, formerly EC-3/EC-6/EC-9) STAY here — they are shared
with the Shor algorithm tier and are recorded in code + `Tests/AxiomAudit.lean`.

## Hygiene / audits

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| **HY-1** | kSectorData inhabitation + bridge audit (first non-trivial-fibre instance) | — | **S/M** | Older-code auditor's recommended next. |
| **HY-2** | Vacuity re-audit of the earliest Empirical files | — | **M** | Completes the older-code health pass. |
| **HY-3** | Doc-currency sweep: CLAUDE.md `SectorData` field drift (MulAction migration) + stale plan rows | — | **S** | Audit-flagged. |
| **HY-4** | Deprecation sweep (`EuclideanSpace.single_apply → PiLp.single_apply` etc.) | — | **S** | Keeps the build warning-clean. |
| ~~HY-5~~ ★ | Born-pillar Σ-linkage: route the general-`N` Born-frequency capstones through a Σ with a DETERMINISTIC flow, not the abstract i.i.d. SLLN engine on bare `CPN`+`μ_FS`. The provenance-audit analogue of the W-series `sigmaFlow` fix, on the Born side. | `unitaryFlowSetup` flow | **L** | **DONE 2026-07-09** (`LF4/BornFlowLinkage.lean`): `unitaryFlowSetup_born_frequency_evolved` + `povm_born_frequency_volume_evolved` — the general-`N` Born / POVM capstones now score trials EVOLVED by the sector's own flow `Φ_t = (unitaryFlowSetup …).flow t`, with `flow_preserves_volume` (= `U(N)`-invariance of `μ_FS`) load-bearing (pins the evolved law). The substrate `flow`/`flow_preserves_volume` fields are now consumed on the Born side. AxiomAudit-pinned. Does NOT derive weights-from-flow (that stays **SL-1**); trials are still an i.i.d. posit before evolution. |

## Pillar completeness (named deferrals)

The pillar map of ordinary QM, for manuscript honesty (see the README pillar ledger). Two textbook
pillars are deliberately NOT in the work programme; they are recorded here so their absence is a
decision, not an oversight.

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| ~~**P3**~~ (solved via local tomography) | Tensor-product / composite-system derivation (why `⊗`) | local tomography axiom | **XL→done** | **SOLVED 2026-07-15** (`SigmaLayer/TensorSolved.lean` `composite_is_tensor_product`): the composite observable algebra IS the tensor product of the local ones — `compositeTensorEquiv` (= Mathlib `kroneckerLinearEquiv`) is a BIJECTIVE linear iso `M_{NA} ⊗ M_{NB} ≃ M_{NA·NB}` sending `U ⊗ₜ Q ↦ aliceOp U · bobOp Q`. So **locality** (`aliceOp_bobOp_commute`) + **local tomography** (`joint_mem_span_local`, PROVED for the quantum case) FORCE composition to be `⊗` with dim `NA·NB` — the operational GPT-reconstruction answer to "why `⊗`" (Hardy; CDP). P3 is reduced to the single clean axiom of local tomography (the axiom that singles out quantum `⊗` among GPTs) and the implication proved as a theorem. Residual: local tomography itself is not derived from nothing (nor can it be — non-tomographic GPTs exist), but the standing-instruction "posit `⊗` per instance" is discharged. |
| **IP-1** | Identical particles / spin-statistics (symmetrisation postulate, boson/fermion sectors) | P3 | **XL** | NOT in the corpus or plans (recorded 2026-07-07 for pillar-map completeness). Out of current scope; any "CSD covers ordinary QM" claim should name this deferral. |

## Σ-layer (projective-sector, Paper C) (Paper C) (`CsdLean4/SigmaLayer/`, namespace `CSD.SigmaLayer`)

A dedicated ontological layer built to strict anti-circularity discipline (no Born weight, frequency
claim, FS equality, projected unitary dynamics or Schrödinger equation is ever a structure FIELD; every
quantum claim is a theorem target or a NAMED bridge assumption). The canonical core is
`ConstraintDynamics` (finite Liouville measure + deterministic measure-preserving one-parameter-group
flow) plus `ProjectiveSector` (the measurable projection to `ℂℙ^{N-1}`); the four pre-existing ontic-setup
abstractions (`LF1.OnticSetup`, `LF2.SectorData`, `LF4.KahlerOnticSetup`, `SigmaLayer.ConstraintDynamics`) are
reconciled by one-directional adapters (the Rosetta in `SigmaLayer/Adapters.lean`), not a big-bang refactor, so
the multiple-setup DRIFT is contained without disturbing the existing corpus.

**PRIMARY STRUCTURAL BLOCKER — RESOLVED 2026-07-15 (`SL-T5`, `SigmaLayer/UnifiedMeasurement.lean`):**
`unified_projectiveSector_capstone` now puts the isolated Hamiltonian flow (`productDynamics`, `exp(-itH)•`) AND
the de-isolation measurement (`measurementFlow` on the base fibre) on ONE `(Σ = ℂℙ^{M}×T², μL=μFS⊗vol,
π=Prod.fst)` — measure-preserving isolated flow + Schrödinger projectability + FS pushforward +
measure-preserving interaction + a.e. readout + record establishment, all on the same model. The
forward-vs-measurement split (forward on `ℂℙ^{N-1}×T²` + Hamiltonian flow, measurement on dilated `ℂℙ^{M}`
+ `trivialDynamics`) is removed. Secondary review points (still valid, follow-on residue): the
concrete record model ignores the recorded time in the outcome event and has no persistent-memory /
nonzero-post-outcome-measure / conditional-to-Lüders link; the Bell/contextuality/no-signalling/
interference adapters demonstrate ledger-compatibility, not derivation-from-`ProjectiveSector`; docs/CI drift
(README, connectivity manifest, `AXIOMS.md` missing the P1-P9/B1-B7 ledger, `CONVENTIONS.md` lacks a
`7-SigmaLayer` category, CI does not run the three static guards). (Review predates T7/T8/T9 — those rows in it
are now closed.)

| Ref | Item | Depends on | Cx | Notes |
|---|---|---|---|---|
| ~~SL-T5~~ ★(structural) | **Unified many-to-one measurement capstone** — the primary closure blocker | SL-T2b, C7 | **L** | **DONE 2026-07-15** (`SigmaLayer/UnifiedMeasurement.lean`): dynamics AND measurement on ONE ontic model. `unifiedDeisolationModel` is a `DeisolationModel` over the NONTRIVIAL isolated dynamics `productDynamics H hH p₀` (the `exp(-itH)` Hamiltonian flow on `Σ = ℂℙ^{M}×T²`), NOT `trivialDynamics`; its interaction is the LF5 measurement flow lifted to the fibre, `Φ_meas(p,θ) = (measurementFlow p, θ)`, readout `vnPointerOutcome ∘ π`. **`unified_projectiveSector_capstone`** delivers on the SAME `(Σ, μL=μFS⊗vol, π=Prod.fst)`: (1) isolated flow measure-preserving; (2) projectable through `π` to `exp(-itH)•` (Schrödinger, `productDynamicsBridge`); (3) `π_*μL = μFS` (B1); (4) the de-isolation interaction measure-preserving (via `MeasurePreserving.prod`); (5) a.e. defined readout (T6, `unifiedDeisolationModel_ae_total`, lifting the base statement through `Prod.fst` since `π_*kMuL = μFS`); (6) record establishment (B5). Removes the forward-vs-measurement model split. Born FREQUENCIES are the base-space `vnDeisolationModel_born_frequency` (readout factors through `π`). AxiomAudit-pinned. **Follow-on residue (now essentially closed):** ~~physical record persistence / recorded-time in the event~~ **DONE 2026-07-15** (`SigmaLayer/TimeIndexedRecord.lean`): `flowedSemantics` makes the record event `Φ_t⁻¹'(region c i)` genuinely use the recorded time; `flowedSemantics_event_measure` proves the record probability is conserved under isolated evolution (`μL(event ⟨c,i,t⟩) = μL(region c i)`); `flowedSemantics_event_flow` proves record covariance `event ⟨c,i,t+s⟩ = Φ_s⁻¹'(event ⟨c,i,t⟩)`. (Latching apparatus MEMORY remains a separate record-stability model, not required under the time-indexed-evidence reading.) ~~nonzero post-outcome preparation~~ **DONE 2026-07-15** (`SigmaLayer/PostMeasurement.lean`): `HistoryPreparation.appendFact` constructs the post-measurement preparation on the extended history `history ++ [r]`, with PROVEN nonzero compatible measure (`compatibleSet ∩ event r`) when the outcome is possible; `appendFactOfPos` builds it from positive conditional probability `conditionalMeasure(event r) ≠ 0`; `appendFact_conditionalMeasure_apply` gives the post-measurement Bayesian update `μL(A ∩ (compatible ∩ event))/μL(compatible ∩ event)`. Closes the measure/record loop. ~~conditional→Lüders link~~ **DONE 2026-07-15** (`SigmaLayer/ConditioningLink.lean`): `bayesianConditional w = w(fine)/w(coarse)`; BOTH the projective Lüders update (`ludersUpdate_isBayesianConditional`, over the Born weight) and the ontic record-history conditioning (`historyConditioning_isBayesianConditional`, over the Liouville measure) are instances; `luders_record_conditioning_correspondence` bundles both — one conditioning rule, two weights that agree on the sector via Born-from-volume (`π_*μL = μFS`, B1). AxiomAudit-pinned. Under projective sector this, not SL-1, was the closure item. |
| ~~SL-T7~~ ★(structural) | **Tiered finite-dimensional QM closure** — assemble the scattered proved facts into ONE honest record | SL-T5, #2, #5, ConditioningLuders | **M** | **DONE 2026-07-17** (`SigmaLayer/FiniteQMClosure.lean`): closes L9. **`FiniteQMClosure` / `unifiedFiniteQMClosure`** collects the nine facts GENUINELY PROVED on the single model `productDynamics H hH p₀` as record fields, each discharged by its source lemma: (Tier 1) `isolated_flow_measure_preserving`, `schrodinger_projection`, `fubini_study_bridge` (B1), `measurement_preserving`, `readout_ae_total` (T6), `records_established` (B5) — the six `unified_projectiveSector_capstone` properties — plus `records_time_physical` (#5, `unified_records_persistence`), `born_frequency` (#2, `unified_born_frequency`), `conditioning_is_luders` (#3/#4, `conditioning_luders_effect_equivalence`). The other tiers are stated HONESTLY in the module header, NOT encoded as fields (no `sorry`): **Tier 2 assumed under projective sector** — the interpretive posit that the Fubini-Study/Liouville measure IS the ontic law (a stance, not a Lean Prop), plus B6 composite tensor structure (dischargeable via `CompositeSector.ofReconstruction`, `composite_dim_eq`); **Tier 3 QM adapters** — `SigmaLayer/Adapters.lean`, `SigmaLayer/CompositeAdapters.lean`; **Tier 4 open** — full mixed-state/ensemble representation (#8) and the independent ECDLP circuit capstone. AxiomAudit-pinned `[propext, Classical.choice, Quot.sound]`. FORWARD only — consumes the posited sector (A5/SL-1 remains the one deep gap, outside Tier 1). |
| ~~QM-CHAN~~ | **Quantum channels (CPTP maps)** — the general open-system dynamics pillar (Kraus / Stinespring / Choi) | LF2 (`DensityOperator`), `PartialTrace` | **L** | **DONE 2026-07-18** (`LF2/QuantumChannel.lean`): channels in **Kraus form** (`QuantumChannel ι N M`, `∑ₖ Kₖ† Kₖ = 1`). **T1 CPTP-forward:** `channelApply` sends `DensityOperator N → DensityOperator M` (`apply_posSemidef` via `PosSemidef.mul_mul_conjTranspose_same` + `posSemidef_sum`; `apply_trace` via trace cyclicity + the constraint), `unitaryChannel` (closed dynamics as the single-Kraus case), `comp` (channels compose → a composition monoid). **T2 Stinespring:** `dilation` stacks the Kraus family into an isometry `V : Fin N → Fin M × ι`, `dilation_isometry` (`V†V = 1`), and **`stinespring`** `Φ(ρ) = Tr_E(V ρ V†)` (`partialTraceRight`) — open-system dynamics is closed dynamics on a larger space. **T3 Choi:** `choiMatrix = ∑ₖ vec(Kₖ) vec(Kₖ)†` and **`choiMatrix_posSemidef`** (the Choi–Jamiołkowski complete-positivity witness — the easy direction of Choi's theorem). AxiomAudit-pinned. QM-adapter level (Stinespring connects to the reconstruction's `DeisolationModel`/Naimark infra). **Extension:** the converse direction of Choi's theorem (Choi PSD ⇒ Kraus exists), and Born-on-`DensityOperatorIx`. |
| ~~SL-T8~~ | **Identical-particle statistics / symmetrization postulate (n=2)** — the exchange-statistics pillar | none | **M** | **DONE 2026-07-18** (`SigmaLayer/Symmetrization.lean`): the two-particle symmetrization postulate on `H⊗H = EuclideanSpace ℂ (Fin N × Fin N)`. Particle exchange `swap` (from `Equiv.prodComm` via `piLpCongrLeft`) is a **self-adjoint involution** (`swap_involutive`, `swap_isSymmetric` via `inner_map_map` — the `±1` exchange observable). The symmetric/antisymmetric projectors `symProj`/`antisymProj = ½(1 ± swap)` are **complementary orthogonal projections** (`symProj_idem`, `antisymProj_idem`, `symProj_antisymProj = 0`, `symProj_add_antisymProj = id`, each `IsSymmetric`). **Exchange dichotomy** (`swap_eq_self_iff`, `swap_eq_neg_iff`): `Sym` = the `+1` eigenspace (bosons), `Anti` = the `−1` eigenspace (fermions), meeting only in `0` (`eq_zero_of_swap_self_and_neg`) — so `H⊗H = Sym ⊕ Anti`. **Pauli exclusion** `antisymProj_tprod_self`: `antisymProj (v ⊗ v) = 0` (no two fermions in the same single-particle state). AxiomAudit-pinned. Closes the one clear textbook postulate the reconstruction lacked. **Extension:** general-`n` (Sₙ action / `ExteriorPower` fermionic Fock structure). |
| ~~SL-T1~~ | Tranche 1: the ontology skeleton | none | **M** | **DONE 2026-07-13.** `ConstraintSurface`, `ConstraintDynamics` (core + `flow_bijective`/`flow_neg_*`), `RecordedFact`/`RecordSemantics`/`compatibleSet`, `IsolationPreparation` (LF1 adapter, isolation = conditioning: `conditionalMeasure_apply` = `μL(A∩Ω)/μL(Ω)`), `ProjectiveSector` (+ Kähler adapter), `TheoremTargets` (uninhabited Prop predicates B/T), `Adapters` (postulate ledger P1–P9/B1–B7/T1–T15 + faithfulness lemmas). AxiomAudit-pinned. |
| ~~SL-T2~~ | Tranche 2: the concrete product model + forward capstone | SL-T1, C7 | **M** | **DONE 2026-07-13.** `MeasureBridge` (`productDynamics H hH p₀` via the many-to-one Schrödinger sector; **bridge B1 PROVED**, `productSector_hasFubiniStudyPushforward`: `π_*(μFS⊗vol) = μFS`), `DynamicsBridge` (projectability + **target T5 inhabited**, `exp(-itH)` Hamiltonian realisation), `MeasurementRecord` (the `DeisolationModel` B4/B5 interface + `compatibleSet_appendEstablishedFact` conditioning update), `ForwardCapstone` (`product_projectiveSector_forward_capstone`: measure preservation + projectability + T5 + B1, no open hypotheses). Explicitly does NOT claim the contextual readout / a.e. unique outcome — that is SL-T2b. AxiomAudit-pinned. |
| ~~SL-T2b~~ | Tranche 2b: the concrete de-isolation MEASUREMENT model (with Born statistics) | SL-T2, LF5 pointer machinery | **M** | **DONE 2026-07-13** (`SigmaLayer/LiftedMeasurement.lean`): `vnDeisolationModel p₀ e ψ' hψ'0` is a fully theorem-backed `DeisolationModel` on `CP^{M}` (`M+1 = N·N`) — every field discharged by an LF5 lemma. Interaction = `measurementFlow` (a genuine measure-preserving unitary), readout = `vnPointerOutcome` (the per-microstate pointer outcome), outcome regions = the pointer fibres. **Proved:** `vnDeisolationModel_records` (the readout records the established outcome, bridge B5); `vnDeisolationModel_ae_total` (**target T6**: the outcome is established for a.e. initial ontic state, by transferring `bornOutcome_ae_isSome` through the measure-preserving interaction); **`vnDeisolationModel_born_frequency`** (the measurement content: for the dilated system state `ψ'` and i.i.d. FS-typical trials, the frequency of pointer-`i` readouts converges a.s. to the Born weight `‖⟨eᵢ,ψ⟩‖²` — the LF5 outcome-frequency capstone `measurement_flow_outcome_frequency` transferred through the measure-preserving interaction via the composed trial process `measurementFlow ∘ fsTrial`, so it is a statistic of the model's OWN outcome, not the raw microstate); `lifted_projectiveSector_measurement_capstone` (measure-preservation + pairwise-disjoint fibres/uniqueness + B5 + T6, no open hypotheses) and **`lifted_projectiveSector_measurement_born_capstone`** (the full measurement: the four + Born frequencies). This delivers the measurement content the product capstone did not claim — a defined, unique outcome a.e. AND Born statistics — from a genuine de-isolation interaction rather than an assumed instance. Isolated dynamics is trivial here (`trivialDynamics`); the physical content is the de-isolation. AxiomAudit-pinned. **FORWARD only** — CONSUMES the posited sector; does not touch A5 (stays **SL-1**). |
| ~~SL-T3~~ | Tranche 3: composite / mixed-state / POVM / contextuality / Bell as bridge interfaces | SL-T2b, LF6 no-signalling/CHSH/CGLMP | **L** | **DONE 2026-07-13** (`SigmaLayer/CompositeInterface.lean` + `SigmaLayer/CompositeAdapters.lean`): the composition/measurement targets (ledger T9-T15) as bridge interfaces + uninhabited generic predicates, each inhabited by an adapter wiring an existing LF6/Empirical capstone. **B6** `CompositeSector` (joint sector + tensor-dimension as a NAMED parked-P3 field). **T15** `HasNoSignalling` inhabited by `singlet_hasNoSignalling` (from `Bell.no_signalling_alice/bob`). **T14** `NoLocalHiddenVariableTable` inhabited by `maxEntangled_noLocalHiddenVariable` (the `d`-intrinsic CGLMP `no_lhv_realises_maxEntangled_cglmp_d`), and `HasTsirelsonSeparation` inhabited by `singlet_hasTsirelsonSeparation` (`chsh_classical_bound_violated` `2 < 2√2` + `chsh_singlet_tsirelson_bound` saturation). **T13** `NoNonContextualValuation` inhabited three ways: `cabello18_noNonContextualValuation` (Kochen-Specker `ks_no_value_assignment_cabello18`), `merminPeres_noNonContextualValuation`, `ghz_noNonContextualValuation`. **T10** `POVMWeightsProbability` inhabited by `povm_weightsProbability` (`POVM.weights_sum_eq_one`); the full T10 frequency content IS `LF4.povm_born_frequency_volume_canonical` (not restated). **T9** `DensityOperatorIx.IsPure` purity predicate defined; the mixed-Born/ensemble content is now **DONE 2026-07-19** (`LF2/MixedEnsembleIx.lean`: `traceForm`, affine `traceForm_ensemble`, spectral `eq_eigen_ensemble`/`mixedEnsemble_capstone` on the composite indexed type) — the reported density-matrix gap on `DensityOperatorIx` is closed. **T11/T12** (composite joint probabilities / entangled predictions) not given separate predicates: T11 = the existing joint Born-frequency capstones, T12 = T14; both await the parked **P3** tensor derivation for a sector-intrinsic form. AxiomAudit-pinned. **FORWARD only** — CONSUMES the posited sectors; the entanglement/composite structure is posited per instance (P3 parked), so this does NOT touch A5 (stays **SL-1**). |
| ~~SL-T9~~ | Mixed-state representation: close the density-matrix gap (statistical side) | SL-T3 | **M** | **DONE 2026-07-14** (`SigmaLayer/MixedState.lean`): the statistical-mixture layer Mathlib lacks, built on `LF2.DensityOperator`/`Effect`/`traceForm`. `mix p ρ₁ ρ₂` -- the convex combination `p ρ₁ + (1-p) ρ₂` is a genuine density operator (Hermitian/PSD/trace-one; PSD via `PosSemidef.smul`+`.add` under `open scoped ComplexOrder`). `traceForm_mix`/`mixedState_capstone`: the Born rule is **affine in the state**, `Tr((p ρ₁ + (1-p) ρ₂) E) = p Tr(ρ₁ E) + (1-p) Tr(ρ₂ E)` -- the defining property of a mixture and the reason density operators (not vectors) are the state space. `IsPure` (`ρ²=ρ`) + `rankOneDensity_isPure` (pure = rank-one projector) + `IsPure.trace_sq_one`; `maximallyMixed` (`I/N`) + `maximallyMixed_not_isPure` (`N ≥ 2`) witness genuine mixedness (non-vacuity). AxiomAudit-pinned. **Purity converse now proved (2026-07-14):** `isPure_of_trace_sq_one` / `isPure_iff_trace_sq_one` (`IsPure ρ ↔ Tr(ρ²)=1`) via Mathlib's Hermitian spectral theorem (`spectral_theorem`, `trace_eq_sum_eigenvalues`, `eigenvalues_nonneg`): `∑λᵢ = ∑λᵢ² = 1` with `λᵢ ≥ 0` forces `λᵢ ∈ {0,1}`, so `ρ² = ρ` by diagonalisation. **Convex-ensemble-of-many components + spectral decomposition now DONE 2026-07-17** (`SigmaLayer/MixedEnsemble.lean`, #8 A+B): **A** `ensemble w ρ = ∑ᵢ wᵢ ρᵢ` over a finite index type is a density operator (PSD via `Matrix.posSemidef_sum`), with the affine Born rule `traceForm_ensemble : Tr((∑ᵢ wᵢ ρᵢ) E) = ∑ᵢ wᵢ Tr(ρᵢ E)` (the many-component `traceForm_mix`). **B** `density_eq_eigen_ensemble : ρ = ∑ᵢ λᵢ |eᵢ⟩⟨eᵢ|` from the Hermitian spectral theorem (`Matrix.IsHermitian.spectral_theorem`, `eigenvectorBasis`, `eigenvectorUnitary_apply`), `eigenvalues_isProbability` (λ a probability distribution), `density_isPureEnsemble` (**every density operator IS a convex ensemble of pure states**), and `traceForm_eq_pureEnsemble` / `mixedEnsemble_capstone` (**the Born rule of a mixed state is the eigenvalue-weighted average of the pure Born rules**). AxiomAudit-pinned. **Ontic-side mixed representation now DONE (weight level) 2026-07-17** (`SigmaLayer/MixedOntic.lean`, #8 C): `mixed_ontic_born_weight` — for any density operator `ρ` and pointer outcome `i`, the classical mixture over `ρ`'s spectral ensemble `(λⱼ, eⱼ)` of ontic Born-region measures `∑ⱼ λⱼ · μL(π⁻¹ bornRegion(eⱼ) i)` equals `Tr(ρ Eᵢ) = traceForm ρ (rankOneEffect eᵢ)` — composing `onticRegion_measure_eq_born` (the ontic weight agreement) with `traceForm_eq_pureEnsemble` (#8 B) + `born_quadratic`. So `productDynamics H hH p₀` represents MIXED states, not only pure; wired as the new **`FiniteQMClosure.mixed_born`** field (closure now pure AND mixed). AxiomAudit-pinned. **Mixed-state FREQUENCY LLN now DONE 2026-07-19** (`SigmaLayer/MixedFrequency.lean`, #8 C a.s. limit, roadmap item A1): `unified_mixed_born_frequency` — for i.i.d. **two-stage** trials `Y` (draw a spectral component of `ρ ~ λ` via `eigenvalueMeasure`, then an ontic microstate `~ μL`), whose law is `mixtureMeasure = eigenvalueMeasure ⊗ μL`, the frequency in `mixtureRegion i` (component `j`'s eigenvector lands in the `i`-th ontic Born region) converges a.s. to `Tr(ρ Eᵢ)`. Key lemma `mixtureMeasure_region_toReal`: the two-stage region measure equals `Tr(ρ Eᵢ)` (via `Measure.prod_prod` + `eigenvalueMeasure_singleton` + `mixed_ontic_born_weight`), then `born_frequency_convergence_partition`. Wired as the **eleventh** `FiniteQMClosure` field `mixed_born_frequency` — closing the closure's last open QM item (mixed-state Born now holds as both weights and certified frequencies). AxiomAudit-pinned. **Born-on-`DensityOperatorIx` NOW DONE 2026-07-19** (`LF2/MixedEnsembleIx.lean`): the affine Born (`DensityOperatorIx.traceForm_ensemble`) and spectral ensemble (`eq_eigen_ensemble`, `eigenvalues_isProbability`, `mixedEnsemble_capstone` — mixed Born = eigenvalue-weighted avg of pure Born) ported from `DensityOperator (Fin N)` to the composite indexed type `DensityOperatorIx ι` (arbitrary Fintype index — the type the bipartite interface uses via `reduced`/`reducedLeft`). Closes the density-matrix gap on the composite type. AxiomAudit-pinned. |
| ~~SL-T7~~ | General (non-projective) conditional state update | SL-T8 | **M** | **DONE 2026-07-14** (`SigmaLayer/ConditionalUpdate.lean`): the general Kraus/effect update `stateUpdate M x = (‖M x‖)⁻¹ • M x` for a measurement operator `M` (effect `E = M† M`), of which Lüders (T8) is the sharp special case (`stateUpdate_eq_ludersUpdate`, `rfl`). `conditionalUpdate_capstone`: **normalisation** (`stateUpdate_norm`), **Born consistency** (`updateWeight_eq_re_inner`: weight `‖M x‖² = Re⟨x, M† M x⟩`, the effect/POVM form), and the **sequential (Wigner) rule** (`stateUpdate_sequential`: `updateWeight N (stateUpdate M x) = updateWeight N (M x) / updateWeight M x`, general conditionalisation of the Born weights for a second measurement `N`). Unlike T8 it needs neither self-adjointness nor idempotence of `M`; covers all POVM measurements (`M = √E`). AxiomAudit-pinned. |
| ~~SL-T8~~ | Lüders (projective) update: close the T8 gap | SL-T2b | **M** | **DONE 2026-07-14** (`SigmaLayer/Luders.lean`): the projective post-measurement update `ludersUpdate p x = (‖p x‖)⁻¹ • p x` for a projection `p` (self-adjoint idempotent, `IsProjection`), with Born weight `projWeight p x = ‖p x‖² = Re⟨x, p x⟩` (`projWeight_eq_re_inner`, the effect/POVM form). `luders_capstone` proves the three defining properties: **normalisation** (`ludersUpdate_norm`), **repeatability** (`ludersUpdate_repeatable`: `p` fixes the updated state, so re-measurement is certain), and **Lüders = conditional probability** (`ludersUpdate_conditional`: for a finer projection `q ∘ p = q`, the updated weight of `q` is `projWeight q x / projWeight p x` — Bayesian conditionalisation of the Born weights). This is the state-space counterpart of the ontic conditioning `compatibleSet_appendEstablishedFact`, closing the gap `MeasurementRecord.lean` flagged (conditioning not called Lüders until the equality is proved). `isProjection_toEuclideanLin` connects matrix projectors. General finite-dim complex inner product space; AxiomAudit-pinned. |
| ~~SL-P3r~~ | P3 partial resolution: the tensor product reduced into the local algebras | SL-T3 | **M** | **DONE 2026-07-15** (`SigmaLayer/TensorGeneration.lean`): `single_prod` proves the joint basis matrix `single (i,k) (j,l) 1 = aliceOp(single i j 1) · bobOp(single k l 1)`, and `joint_mem_span_local` proves the two commuting local subalgebras (`M_{NA}⊗I`, `I⊗M_{NB}`) GENERATE the entire joint algebra `M_{NA·NB}` — every joint observable is a linear combination of local Alice×Bob products. So the `⊗` carries NO observables beyond local ones and their products: B6 reduces from "posit `⊗`" to "posit two full local matrix algebras that act and commute" (locality + completeness). Does NOT derive WHY composition is `⊗` from first principles (the residual P3 / SL-1 direction) — it shows `⊗` is not free structure OVER the local algebras. AxiomAudit-pinned. |
| ~~SL-A5L~~ | A5 localized: typicality forced in the appropriate places | SL-1 | **M** | **DONE 2026-07-15** (`SigmaLayer/LocalisedTypicality.lean`): A5 does not need to hold universally, only where the dynamical symmetry acts. `forcedVolume_unique` / `region_measure_symmetry_forced` — any two `U(N)`-invariant probability measures give the SAME region weights, so the Born weights are SYMMETRY-forced, not measure-chosen. `localised_sectorPostulate_capstone` — for the concrete unitary-flow sector: (i) its ray-space typicality measure is the unique `U(N)`-invariant probability measure (forced, not posited, via `IsForcedKahlerVolume`); (ii) the deterministic projected flow `U t •` preserves it (a one-parameter subgroup of the symmetry); (iii) every symmetric measure gives the same weights. So A5 is discharged AT the sectors carrying full `U(N)` symmetry — "the right appropriate places". **Does NOT close the universal A5** (SL-1): the bare flow is a single one-parameter subgroup that does not itself generate `U(N)`; the sector still carries the symmetry as construction data. AxiomAudit-pinned. |
| ~~SL-BG~~ | Bell/contextuality generality: universal bounds behind the per-instance witnesses | SL-T3 | **M** | **DONE 2026-07-14** (`SigmaLayer/BellGenerality.lean`): lifts the per-instance T13/T14 adapters to the UNIVERSAL bounds the corpus already proves. `lhv_chsh_le_two` (every local-hidden-variable model: `|S| ≤ 2`, general Bell-CHSH), `qm_chsh_le_tsirelson` (every unit state: `|S| ≤ 2√2`, general Tsirelson), `cglmp_lhv_le_two` (every LHV table, every `d ≥ 2`: `cglmp ≤ 2`), `bell_general_separation` (the universal gap: quantum `≤ 2√2`, classical `= 2 < 2√2`, attained by the singlet), `general_ks_noNonContextualValuation` (any parity-`(18,9)` config, not just Cabello-18). AxiomAudit-pinned. Universal ∀-statements over all LHV/states/configs, vs the single violation witnesses of Tranche 3. |
| ~~SL-T4~~ | Interference target (T16) + tensor weave | SL-T3 | **S** | **DONE 2026-07-14** (`SigmaLayer/Interference.lean` + `SigmaLayer/TensorSector.lean`). **Interference (T16):** `HasBornInterference` (the two-path Born probability `(1 + Re⟨a,b⟩)/2` carries the phase cross term) inhabited by `hadamardTest_hasBornInterference` (from `hadamard_test_prob`). Interference is a CONSEQUENCE of P7 (complex sector) + T1/T2 (Born), not a postulate; the swap test `(1 + ‖⟨ψ,φ⟩‖²)/2` and the Ramsey fringe `cos²(φ/2)` are the same phenomenon. **Tensor weave:** the finite `ℂ^{NA} ⊗ ℂ^{NB} = ℂ^{NA·NB}` is DERIVED, not posited — `tensorIndexEquiv` (`Fin NA × Fin NB ≃ Fin (NA·NB)`, `finProdFinEquiv`), the local operator algebra `aliceOp_bobOp_commute` (Alice/Bob commute, the no-signalling root), and operator no-signalling `tensorSector_no_signalling` (from `no_communication`); `CompositeSector.tensorIndex` exhibits the B6 dimension posit AS the tensor-product index. The ONLY tensor posit is B6 (composite-is-tensor); the "why ⊗" derivation (P3) stays parked (**SL-1** frontier). AxiomAudit-pinned. |

## Priority read (user-set sequencing, 2026-07-07; supersedes the 2026-07-04 recommendation)

- **THERMO TRACK COMPLETE (TH-1..TH-4, all DONE 2026-07-07):** canonical typicality (expectation),
  the second law / H-theorem, the Gibbs free-energy variational principle, and Landauer's `kT ln 2`
  bound. Remaining thermo residues are optional: the **TH-1 Levy-concentration** upgrade (needs
  spherical isoperimetry, not in Mathlib) and the **TH-5 stretch** (ETH / Jarzynski–Crooks
  fluctuation theorem). **NEXT PER SEQUENCING: the CV track (CV-1 onwards)** — the continuous-variables
  pillar.
- **THEN — the CV track, CV-1 onwards:** finite position observable → finite momentum (finite
  Fourier) → approximate CCR `‖[Q_N,P_N]−iℏ·1‖ ≤ ε` → oscillator truncation. The
  continuous-variables pillar.
- **DEPRIORITISED — EC-6 / the ecdsa.fail Tier-X fork:** still opted in, but sequenced BEHIND the TH
  and CV tracks (previously "after the W-residues"; the W-residues are now all closed, 2026-07-07).
  Same for the other EC rows.
- **Thesis goal (unchanged):** only the ★ rows move it. **SL-2** (D1c-entangled, M) is the most
  tractable genuine A5 attack; **SL-1** (A5→D1, XL) is the prize but has no clean on-ramp.
- **Cheap wins, clear anytime:** **LF6-1** (S), **HY-3** + **HY-4** (S).
