# Mathlib gaps this project has hit — and what closing them would unlock

*(Created 2026-08-02. Two lists: genuine absences in Mathlib that gate corpus work, and the
material this repo has already staged for upstream. Each gap names the corpus item it blocks,
so contributors can see the physics payoff of a library PR. The single open-work list remains
[`specs/BACKLOG.md`](specs/BACKLOG.md); this page is the Mathlib-facing cut of it.)*

**Triage 2026-08-22** ([`specs/mathlib-gaps-plan.md`](specs/mathlib-gaps-plan.md)): every row
below was re-probed at the pin. Birkhoff and Lévy verified standing; the `CStarAlgebra`
row is a SOFT wall (the instances exist at the pin — the failure is discrimination-tree
resolution, probe-first as MG-3); the FS-metric and polynomial-zero-sets rows are **attackable
in-corpus** (MG-1, MG-2 — the latter converts "almost every composite state is entangled" from
research-gated prose to a bounded three-brick chain).

## Genuine absences (no Mathlib API today)

| Gap | What it is | What it blocks here | Current workaround |
|---|---|---|---|
| ~~**Kähler / symplectic manifold API**~~ **CLOSED IN-CORPUS 2026-09-07 → 2026-09-11** | Differential forms *on manifolds* with `dω = 0`, compatible complex structure, Liouville volume `ωⁿ/n!`, Hamiltonian vector fields `X_H = ω⁻¹dH`. ⚠️ *Narrowed 2026-08-06:* upstream now has the exterior derivative **on normed spaces** (`Mathlib/Analysis/Calculus/DifferentialForm/`, flat space, with `d² = 0`; manifold forms are that file's own TODO), so the FLAT half of the gap is gone | The *manifold* spelling only. The `X_H = ω⁻¹dH` sentence is no longer pure prose: the linear-level identification is proved (`Mathlib/Analysis/InnerProductSpace/HamiltonianVectorField.lean` — `ι_Xω = dH` with the Schrödinger field `-(i•Ax)` exhibited, instantiated on the pointer coupling in `RecordLayer/PointerHamiltonianField.lean`); the chart-level Poisson fragment is A3 (`ChartBracket.lean`). What stays blocked: the quotient/manifold statement on `ℂℙ^{N-1}` and the arena (`reconstruction-status.md` §2a) | Measures (`fsMeasure`, Haar products) carry all consumed content; the geometric reading above the linear/chart fragments is documented prose (`AXIOMS.md` §3.1). Flat `dω = 0` for the constant fundamental form is **PROVED** (2026-08-06, `Mathlib/Analysis/InnerProductSpace/KahlerClosed.lean`: `extDeriv_const` + `Kahler.extDeriv_fundamentalFormAlt`). ⚠️ *Narrowed again 2026-08-22 (MG-4, `KahlerPotential.lean`):* the **non-constant** case is now proved too — a form presented as `dd^c` of a potential is closed by `d² = 0` (`extDeriv_ddcForm`), and the genuine Fubini–Study chart form (potential `log(1+‖z‖²)`, `contDiff_fsPotential`) is therefore closed (★★ `extDeriv_fsChartForm`). ⚠️ *Narrowed again 2026-09-01:* residue (ii) — identifying the potential-defined chart form with `Kahler.fundamentalForm` — is **DONE** (`fsChartForm_apply` computes the components from the second derivative of `log(1+‖z‖²)`; `fsChartForm_zero` gives `fsChartForm 0 = (-4:ℝ) • fundamentalFormAlt`), so `extDeriv_fsChartForm` is now closedness of an *identified* object. **What is left in this gap:** (i) the quotient/manifold spelling on `ℂℙ^{N-1}`, and (iii) the top-power volume identity `ω^{N-1}/(N-1)! = μ_FS`, which cannot even be STATED upstream — there is no continuous wedge product at the pin. MATHLIB-ABSENT(file:Mathlib/Geometry/Manifold/DifferentialForm, ContinuousAlternatingMap.domCoprod) ⚠️ **Staged plan recorded 2026-09-06** in `specs/BACKLOG.md` (XL section, "Manifold exterior calculus"): five steps, probed at the pin — `ℂℙ^{N−1}` as a manifold (**M**), the continuous wedge (**S**), forms on manifolds (**XL**, upstream's own TODO), top-forms→measures (**L**), the symplectic/Kähler/moment-map layer (**L–XL**). Steps 0–1 alone convert the top-power identity from *unstatable* to *stated and open*. ⚠️ **STEP (1) LANDED 2026-09-07** — `Mathlib/Analysis/Normed/Module/Alternating/Wedge.lean`: `ContinuousAlternatingMap.wedge`, the exterior product of two CONTINUOUS alternating maps. ⚠️ It could not be built in Mathlib's tensor-valued shape — `F ⊗[𝕜] G` carries no topology at the pin, so `E [⋀^ιa ⊕ ιb]→L[𝕜] (F ⊗[𝕜] G)` is not a type — so the codomains are paired through a continuous bilinear `B`, which is what every use needs anyway and which recovers the tensor form the moment a topology on `⊗` exists. `ContinuousAlternatingMap.domDomCongr` had to be built too (Mathlib has it for `AlternatingMap` and for `ContinuousMultilinearMap`, not for this one) — without it a product indexed by `ιa ⊕ ιb` cannot re-enter the `Fin n`-indexed differential-form API. Witness: `Mathlib/Analysis/InnerProductSpace/KahlerWedge.lean`, ★ `Kahler.extDeriv_fundamentalFormSq` — `d(ω ∧ ω) = 0` on the flat model, the first term of the shape the top power needs. ⚠️ **STEP (0) LANDED 2026-09-07 too** — `Mathlib/Geometry/Manifold/Instances/ProjectiveSpace.lean`: `Projectivization.instChartedSpace` and `Projectivization.instIsManifold` on the standard affine atlas, and the manifold is **analytic (`ω`)**, not merely `C^∞` — the transition maps are ratios of coordinates of `Fin.insertNth i 1 w`, so `ContDiffOn.div` gives `ω` from the same argument. ⚠️ The continuity of the chart is the one non-mechanical step: the chart is defined through `Projectivization.rep`, which is a **choice function with no continuity**, so it comes from `mk'` restricted to the open chart domain still being a quotient map (`IsQuotientMap.restrictPreimage_isOpen`) — the same manoeuvre `Projectivization.continuous_bargmann` needed (CR-10 successor, 2026-09-05). Model space is `Fin n → ℂ` rather than `EuclideanSpace ℂ (Fin n)` (`WithLp` is a structure at the pin, so every chart formula would otherwise carry a `toLp`); norm-equivalent, same analytic structure. Corpus-side transport: `CSD.LF4.cpn_isManifold`. **Still open in this row:** steps (2)–(4), and the identity itself — steps (0) and (1) made it *sayable*, not true. ⚠️ **Step (2a) was attempted 2026-09-07 and stopped at a named upstream gap.** Building differential forms as *smooth* sections needs a `ContMDiffVectorBundle` instance for the alternating-map bundle (the analogue of `Geometry/Manifold/VectorBundle/Hom.lean`; `Geometry/Manifold/` contains no `AlternatingMap` declarations at all), and the smoothness of that bundle's coordinate change needs a **`ContDiff`/analyticity lemma for the alternating pullback `compContinuousLinearMapCLM` which upstream does not have**: it has the first derivative (`Analysis/Calculus/FDeriv/ContinuousAlternatingMap.lean`) and the *multilinear* analyticity (`Analysis/Analytic/CPolynomial.lean`), but the existing alternating continuity proof reflects along an inducing embedding, which does not carry to `ContDiff`. MATHLIB-ABSENT(ContinuousAlternatingMap.contDiff_compContinuousLinearMapCLM) ⚠️⚠️ **THAT LEMMA IS NOW PROVED HERE (2026-09-07, same day):** `Mathlib/Analysis/Normed/Module/Alternating/Pullback.lean` — ★★ `analyticAt_uncurry_compContinuousLinearMap` and its `ContDiff` corollaries. **What unlocked it was a retraction**: alternatization is a continuous linear map (built here; upstream has only the `AddMonoidHom`) which multiplies an already-alternating map by `(card ι)!`, so in characteristic zero it inverts the inclusion into the multilinear maps and analyticity reflects along it from upstream's multilinear result. ⚠️⚠️ **STEP (2a) IS COMPLETE (2026-09-07)** — `Mathlib/Geometry/Manifold/DifferentialForm.lean`: `DifferentialForm` (a `C^n` section of the alternating bundle on the tangent bundle) and ★ `projectiveDifferentialForm_nonempty` — **`ℂℙⁿ` carries ANALYTIC differential forms of every degree**, which is steps (0), (1) and (2a) in one statement. ⚠️ **The witness is the ZERO form**: the type exists; the Fubini–Study form as a section does NOT (chart-overlap agreement, a real computation, not attempted). ⚠️ **Still no `d`** — that is (2b), upstream's own TODO — so `dω = 0` and the top-power identity are now *sayable* and no more provable than before. Layer detail: layers 3 and 4 landed the same day (`Mathlib/Geometry/Manifold/VectorBundle/AlternatingMap.lean` — ★ the `C^n` coordinate change and ★★ `ContMDiffVectorBundle.continuousAlternatingMap`, MATHLIB-ABSENT(ContMDiffVectorBundle.continuousAlternatingMap)). What is left is the **last mile**: forms as smooth sections is one definition away but its total-space topology does not synthesise for the (tangent, trivial) pair. Superseded description follows — the `ContMDiffOn` coordinate change, the `ContMDiffVectorBundle` instance (the `Hom.lean` analogue), and forms as smooth sections. ⚠️ Note the split the attempt exposed: a form as a *family* over the tangent spaces needs no instance and is definable today; the instance is what buys the word **smooth**. Re-rated **M–L → L**. The row also now covers **`momentMap`**, which is the same wall (`specs/TERMS.md`). ⚠️⚠️ **THE FS FORM AS A SECTION LANDED 2026-09-07** (`Mathlib/Geometry/Manifold/Instances/ProjectiveSpaceFubiniStudy.lean` + `ProjectiveSpaceFubiniStudyForm.lean`, 24 pins): ★★ `Projectivization.fsForm`, a `C^∞` global 2-form on `ℂℙⁿ` — a term of `DifferentialForm 𝓘(ℝ, Fin n → ℂ) (ℙ ℂ (Ambient n)) ∞ (Fin 2) ℝ` — with ★★ `fsForm_ne_zero` (`n ≥ 1`), so step (2a)'s witness is no longer the zero form. The chart-overlap agreement is ★★ `fsChartForm_transE`: under an affine transition the potential changes by the pluriharmonic `-2 log ‖zⱼ‖`, and `dd^c` naturality (`KahlerPluriharmonic.lean`: Cauchy–Riemann in `d^c` form, a local holomorphic logarithm, `ddcForm_comp`) kills the correction. The bundle-level assembly is ★ `localRep_fsSection` (the tangent coordinate change is the derivative of the transition, `VectorBundleCore.trivializationAt_symmL`). ⚠️ `C^∞`, not `ω` — `fsPotential` is only known `C^∞`. ⚠️ Still no `d`, so `d fsForm = 0` is unstated. **What is left in this gap:** (2b) `d` on manifolds (hence closedness on `ℂℙⁿ`), non-degeneracy, and (iii) the top power. ⚠️⚠️ **STEP (2b) LANDED THE SAME EVENING (2026-09-07)** — `Mathlib/Geometry/Manifold/ExteriorDerivative.lean` (15 pins): `mextDeriv`, the exterior derivative of a smooth section of the alternating bundle, defined as the flat `extDeriv` of the local representative in the chart at the point and glued by ★★ `DifferentialForm.localRep_mextDeriv` (the local representative of `d ω` in EVERY chart is the flat `d` of the local representative of `ω`: `extDeriv_pullback` on the chart transition plus the tangent-bundle cocycle); ★★ `contMDiff_mextDeriv` (so `d` iterates: `DifferentialForm.mextDeriv`) and ★★ `mextDeriv_mextDeriv` (`d ∘ d = 0`). Scope: real boundaryless model `𝓘(ℝ, E)`, smoothness `∞`, degrees `Fin k` — which is every finite-dimensional real manifold the corpus uses. **And the payoff: ★★★ `Projectivization.fsForm_mextDeriv` — `d ω_FS = 0` on `ℂℙⁿ`**, the first manifold-level Kähler statement in the corpus (the local representative of `fsForm` is the flat chart form, whose `d` is `extDeriv_fsChartForm`). The scoping note's XL was the plumbing of §3.1, and that plumbing had just been written for `fsSection`. MATHLIB-ABSENT(mextDeriv) **What is left in this gap now:** (iii) the top-power identity `ωⁿ/n! = μ_FS` (top forms → measures, step (3)), non-degeneracy of `fsForm`, the Palais formula / naturality / Leibniz rule for `mextDeriv`, and the symplectic/moment-map layer (step (4)). ⚠️⚠️ **NON-DEGENERACY AND `IsSymplectic` LANDED, LATER THE SAME EVENING (2026-09-07)** — `Mathlib/Geometry/Manifold/SymplecticForm.lean` (`DifferentialForm.IsSymplectic`: closed + non-degenerate at every point; a predicate, nothing derived) and `Instances/ProjectiveSpaceFubiniStudySymplectic.lean` (★ taming at every chart point, `fsChartForm x (v, Jv) = -4(1+‖x‖²)⁻²((1+‖x‖²)‖v‖² − |⟪x,v⟫|²) < 0` by Cauchy–Schwarz; ★★ `fsForm_nondegenerate`; ★★★ **`fsForm_isSymplectic` — `ℂℙⁿ` with the Fubini–Study form is a symplectic manifold**), 7 pins. **What is left in this gap now:** (iii) the top-power identity `ωⁿ/n! = μ_FS` (step (3), top forms → measures); a manifold-level Kähler predicate packaging metric and complex structure; and the generator / moment-map layer (step (4)) — which is `R-016`'s wall and `momentMap`'s. ⚠️⚠️ **STEP (3) BEGUN 2026-09-08 — the measure of a top form on a manifold is BUILT** (`Mathlib/Geometry/Manifold/TopFormMeasure.lean`, plan `specs/top-power-scoping.md` M1+M3, 16 pins): chart densities `|coefficient against a basis|`, chart measures pushed to `M`, ★★ `chartMeasure_congr` (change of variables along the chart transition — upstream's `lintegral_image_eq_lintegral_abs_det_fderiv_mul` — with the Jacobian rule ★ `compContinuousLinearMap_apply_basis` of `Alternating/TopForm.lean`), glued along the measurable partition of a finite chart cover given as data (`ChartCover`; `ℂℙⁿ`'s affine atlas is one, `affineChartCover`) into ★★ `topFormMeasure`, independent of the cover. MATHLIB-ABSENT(DifferentialForm.topFormMeasure) Upstream has no measure or integration on manifolds at the pin. **Left for the identity:** the wedge of sections (M2), the smooth `U(N)` action with pullback and invariance (M4), naturality of the measure (M5), finiteness / non-vanishing / uniqueness (M6), the constant (M7). ⚠️ **M2 LANDED 2026-09-08** (`Alternating/WedgeCLM.lean`, `Geometry/Manifold/WedgeForm.lean`, 29 pins): the wedge is bounded bilinear in the pair of forms, pullback commutes with it, and the wedge / reindexing / constant `0`-form of smooth sections are smooth sections — so `fsTopForm n`, the `2n`-th power of the Fubini–Study form, exists as a `DifferentialForm`. **Left now:** M4, M5, M6, M7. ⚠️⚠️ **M4, M5, M6(a),(c) LANDED 2026-09-08** (29 pins): `U(n+1)`-invariance of the chart form in every chart pair (`fsModelForm_uTrans`), a form-preserving homeomorphism preserves the measure of a top form (`topFormMeasure_map_eq`), the measure of a smooth top form on a compact manifold is finite (`isFiniteMeasure_topFormMeasure`), and ★★★ `fsVolumeNormalized_eq_fsMeasure`: **the normalised volume of the top power of the Fubini–Study form is `fsMeasure p₀`, under the premise `fsVolume n ≠ 0`**. MATHLIB-ABSENT(DifferentialForm.topFormMeasure_map_eq) **What is left in this gap now:** (iii)'s last piece, M6(b): the flat non-vanishing of the `n`-th power of the standard symplectic form on the standard basis (a shuffle count), which discharges the premise; M7 the constant; a manifold-level Kähler predicate; the generator / moment-map layer (step (4)). ⚠️⚠️ **M6(b) LANDED 2026-09-08 (later the same day; 39 pins):** `Alternating/WedgeShuffle.lean` — the shuffle sum of `α ∧ β` (`β` a 2-form) on a **pair family** collapses to `k + 1` classes, each with a two-disjoint-transposition representative of sign `+1` (★★ `wedge_mul_apply_pairs`), so the count `wedgePow_stdForm_pairFamily` (`= k!`) is an induction with no sign bookkeeping; `TopFormMeasure.lean` — ★ `topFormMeasure_ne_zero_of_localRep_ne_zero`, one nonzero chart coefficient makes the measure of a top form nonzero; hence ★★ `fsVolume_ne_zero` and **★★★ `fsVolumeNormalized_eq_fsMeasure` — the normalised volume of the top power of the Fubini–Study form IS `fsMeasure p₀` — now holds with NO premise.** MATHLIB-ABSENT(ContinuousAlternatingMap.wedge_mul_apply_pairs) **What is left in this gap now:** M7 the constant (the total mass of `fsVolume n`, which would make the literal `ωⁿ/n!` a theorem); a manifold-level Kähler predicate; the generator / moment-map layer (step (4)). ⚠️⚠️ **M7 LANDED 2026-09-08 (30 pins) — step (3) is COMPLETE.** `Analysis/SpecialFunctions/JapaneseBracketIntegral.lean`: ★★ `lintegral_pi_pow_inv_one_add_sum_norm_sq`, `∫_{ℂⁿ} (1 + ‖w‖²)^{-(n+1)} = πⁿ/n!` (upstream's `JapaneseBracket.lean` has integrability only, never a value) — polar coordinates on one factor and a Fubini induction over `measurePreserving_piFinSuccAbove`. MATHLIB-ABSENT(MeasureTheory.lintegral_pi_pow_inv_one_add_sum_norm_sq) `Instances/ProjectiveSpaceFubiniStudyMass.lean`: the density of the top power everywhere (`(-4)ⁿ n! (1 + ‖w‖²)^{-(n+1)}`, by a unitary rotation to the first axis where the chart form is a diagonal pullback of the form at the origin; the real determinant of a complex matrix is `|det|²` via `LinearMap.det_restrictScalars`), the null hyperplane `z₀ = 0`, ★★ `fsVolume_univ = (4π)ⁿ`, and **★★★ `fsVolume_eq_smul_fsMeasure` — `ω_FS^{∧n} = (4π)ⁿ · μ_FS` on `ℂℙⁿ`, with every factor visible.** (iii) is closed. **What is left in this gap now:** a manifold-level Kähler predicate packaging metric and complex structure; the generator / moment-map layer (step (4)). ⚠️ **Step (4) scoped 2026-09-08** (`specs/generator-layer-scoping.md`, sub-bricks G1–G7): the shelf now has integral curves on manifolds with existence and uniqueness (`Geometry/Manifold/IntegralCurve/`), vector fields with `mlieBracket`, and the interior product `ContinuousAlternatingMap.curryLeft`; it has **no** Hamiltonian vector field, moment map, Poisson bracket, global flow, Lie derivative or Liouville theorem, and **no manifold instance on `AddCircle`**. Recommended G1 → G6 (the manifold-level moment-map equation for the torus action on ℂℙⁿ, **L**); Liouville at manifold level (G5) is **XL** and not scheduled. ⚠️⚠️ **G1 and G6 LANDED 2026-09-08 (evening):** `Geometry/Manifold/HamiltonianVectorField.lean` (`IsHamiltonianVectorField`, `IsLocallyHamiltonian`, `dH (X) = 0`, uniqueness for a symplectic form; 13 pins) and `Instances/ProjectiveSpaceMomentMap.lean` (★★★ `torusField_isHamiltonianVectorField`: **the torus action on ℂℙⁿ is Hamiltonian for the Fubini–Study form, with `2 ∑ θₖ momentMap` as its Hamiltonian** — the cell law's moment-map equation on the symplectic manifold, the field proved to be the velocity of the action; 15 pins). MATHLIB-ABSENT(DifferentialForm.IsHamiltonianVectorField) **What is left in this gap now:** existence and smoothness of `X_H` (G2–G4), a manifold-level Kähler predicate (G7), Liouville at manifold level (G5, blocked on flows and Cartan calculus upstream), and the arena statement `R-016′`. ✅ **CLOSED 2026-09-11.** Every item of this row is now a theorem in the corpus's staged manifold layer (`CsdLean4/Mathlib/Geometry/Manifold/`, the G series of `specs/generator-layer-scoping.md`): differential forms on manifolds (`DifferentialForm`, `mextDeriv` with `d² = 0`), `dω = 0` for the Fubini–Study form on `ℂℙⁿ` (`fsForm_mextDeriv`), the symplectic predicate and instance (`fsForm_isSymplectic`), the compatible complex structure in both senses (`fsForm_isKahler`, `nijenhuis_fsJ_eq_zero`), the Liouville volume `ωⁿ/n!` as a measure with its constant (`fsVolume_eq_smul_fsMeasure`, `riemannianVolume_fsMetric`), and Hamiltonian vector fields `X_H = ω⁻¹dH` with existence, uniqueness, smoothness, analyticity and integral curves (`HamiltonianVectorField.lean`; on `ℂℙⁿ` `schrodingerField_isHamiltonianVectorField`, `torusField_isHamiltonianVectorField`). The arena statement `R-016′` and Liouville for a *general* Hamiltonian flow (Q29) are the residue, priced in `BACKLOG.md` ▶ OUTSTANDING. Upstream still has none of this; every module below is a candidate. |
| **Pointwise Birkhoff ergodic theorem** | A.e. time-average = space-average for measure-preserving flows | `BornFromFlow` (manifest L7 / T3): frequencies along a *single flow trajectory* rather than i.i.d. sampling | The strong-law route (i.i.d. trials of `μL`); the no-gos (`flow_admits_invariant_ne_fubiniStudy`) show a single flow cannot pin `μ_FS` anyway. MATHLIB-ABSENT(Ergodic.ae_tendsto_birkhoffAverage) |
| **Operator convexity — the SUMMIT only (Effros/Lieb)** ⚠️ *narrowed 2026-08-30:* `log` operator-concavity and `x^p` (`p∈[0,1]`) concavity are **no longer gaps — upstream proved them** (`CFC.concaveOn_log`, `CFC.concaveOn_nnrpow`), and `CStarAlgebra (Matrix n n ℂ)` **exists** as a scoped instance (`Matrix.Norms.L2Operator`), so the "not a C⋆-algebra" wall was a scope question. What remains: **Effros perspective / Lieb concavity → joint convexity of relative entropy → DPI** | Unconditional **strong subadditivity** (`StrongSubadditivity.lean` carries the reduction with `hDPI` as an explicit hypothesis); the quantum data-processing inequality | The ladder is part-built (`OperatorConvex.lean`: predicate, `x⁻¹` rung, resolvent rungs, reframing lemma, cfc-integral commutation); the wall is instance resolution, next row. MATHLIB-ABSENT(CFC.convexOn_mul_log, liebConcavity) |
| ~~**`CStarAlgebra`-instances for `Matrix n n ℂ`**~~ **RESOLVED IN-CORPUS** (re-probed 2026-09-01: `CStarMatrix.instCStarAlgebra` exists at the pin, and the two registered shims — `instCStarMatrixRealCFC`, `instCStarMatrixNonnegSpectrumClass` — make all four previously-failing synthesis goals fire. Not an absence row: it was always instance *resolution*) | The default instances on `Matrix` don't provide `CStarAlgebra`/`NonUnitalCStarAlgebra`; on the `CStarMatrix` synonym, **exactly two** generic instances fail to fire through the discrimination tree (probe 2026-08-22, `specs/mathlib-gaps-plan.md` MG-3): the ℝ-CFC over `IsSelfAdjoint` and `NonnegSpectrumClass ℝ` — each provable by a one-line application of its own generic provider | Everything C⋆-generic on matrices. **Narrowed 2026-08-22:** with the two shims registered, the ENTIRE upstream monotonicity tier fires on `CStarMatrix` (`CFC.monotone_rpow`/`monotone_nnrpow` — which now exist upstream — `monotone_sqrt`, `rpow`, `log_le_log`); the remaining C⋆-side absence for the DPI ladder is operator CONVEXITY (Lieb), upstream's own TODO | The bridge file (B.1–B.3, shim 1) + a from-scratch matrix Bochner-cfc (`cfc_integral_commute`); B.4 LANDED 2026-08-22 (shim 2 + `matrix_nnrpow_le_nnrpow` + `matrix_sqrt_le_sqrt` — the rpow-order transport, in the bridge); the two-instance witness is a ready-made upstream instance-hygiene report |
| **Lévy concentration / spherical isoperimetry** | Measure concentration on high-dimensional spheres | Canonical-typicality **exponential** concentration (TH1 carries expectation + the Q24 Chebyshev tier) | Named residual, recorded; expectation form proved (`fs_first_moment`); polynomial-rate concentration proved (Q24, 2026-08-21: `fs_chebyshev_concentration`, twirl-algebra second moments — no isoperimetry); only the `exp(−c·d_E·ε²)` rate still waits on this gap. MATHLIB-ABSENT(levyConcentration, file:Mathlib/Analysis/Convex/Isoperimetric) |
| ~~**Fubini–Study metric on `ℙ`**~~ **CLOSED IN-CORPUS 2026-08-22 (MG-1)** | ~~A `MetricSpace` instance on `Projectivization` (Mathlib has topology only, staged here; no `dist` anywhere)~~ **Staged**: `Projectivization/Metric.lean` — the rank-one projection embedding `p ↦ P_p` is a closed embedding from the compact `ℙ` into the operator space, and `IsEmbedding.comapMetricSpace` pulls the metric back with the metric topology DEFINITIONALLY the staged quotient topology; `dist p q = ‖P_p − P_q‖` | ~~The quantified ε-ball forms of the C2 support arc~~ Now statable: `CPN N` inherits the instance automatically. The C2 ε-ball corollaries themselves are the recorded follow-up (Q28 rider) | Joins the upstream first batch — the first metric on `Projectivization` anywhere |
| ~~**Polynomial zero sets are null**~~ **DISSOLVED 2026-08-22 (MG-2)** | ~~Zero set of a nonzero multivariate polynomial has measure zero, transportable through the Haar orbit map~~ **The consumer never needed the general lemma**: the Segre cone lies in the zero set of ONE coordinate quadratic (the `2×2` minor at a fixed corner, read through the index bijection), null by two-step Fubini slicing. The general `MvPolynomial` version is recorded as a pure-optionality upstream candidate — nothing in flight needs it | ~~`segre_range_null` — prose-strength only~~ **PROVED**: `compositeFubiniStudy_range_segre_null` + `ae_not_mem_range_segre` — *almost every composite state is entangled* (Q28 item 5, de-research-gated) | Staged `Projectivization/FubiniStudyLebesgue.lean` (FS as a Lebesgue-a.c. pushforward + the null-transport principle) is the general-purpose tool this produced |
| ~~**Hilbert tensor factorisation of registers**~~ **CLOSED IN-CORPUS 2026-08-22 (MG-5)** | ~~`EuclideanSpace`/`PiLp` has no tensor-split API~~ **Staged**: `QuantumInfo/RegisterTensor.lean` — `regTensorEquiv : QReg (a+b) ≃ₗᵢ[ℂ] QReg a ⊗[ℂ] QReg b`, built from the wire-index currying plus `OrthonormalBasis.tensorProduct` (both sides carry orthonormal bases indexed by the product, so the isometry is the change of basis); Mathlib's inner-product tensor product supplied the target | ~~The measurement-gadget hybrid~~ The *API* half is delivered, including `tensorFirst` (an operator on a wire block, identity elsewhere) and its basis-state computation rule. ⚠️ The gadget hybrid itself is **not** thereby proved: the n-fold amplitude argument is open work, and `MeasurementAdder.lean`'s wall note is updated to say so | Joins the upstream batch (a `Mathlib.QuantumInfo` would want it beside `Register.lean`) |
| **Stone's theorem (general, infinite-dimensional)** | Strongly continuous one-parameter unitary groups ↔ self-adjoint generators. **XL and not close**: the pin has `LinearPMap` self-adjointness (definitional layer) and a finite-dimensional/compact spectral theorem, but **no spectral measures / PVMs, no Borel functional calculus, no C₀-semigroup theory, no Hille–Yosida** (all re-probed 2026-08-30) | Nothing in flight — the corpus dynamics spine is `Matrix (Fin N) (Fin N) ℂ` throughout; the CV track would eventually want it | ⚠️ *Workaround column corrected 2026-08-30:* the **finite-dimensional continuity-only** theorem is **proved**, not merely C¹ — `Matrix.StoneC1.stone_continuous` (2026-07-23) derives smoothness by integral averaging. The live residue is **plumbing, not a Mathlib gap**: nothing consumes it, and `LF4/PhaseLift.lean`'s Schrödinger capstones still take the C¹ derivative datum. MATHLIB-ABSENT(spectralMeasure, ProjectionValuedMeasure) |
| ~~**Bargmann's theorem**~~ **NOT NEEDED — the consumer was closed in-corpus 2026-09-01** | ~~Continuity ⇒ vanishing of the projective-representation cocycle~~ The general theorem is still absent from Mathlib and stays out of scope. MATHLIB-ABSENT(Bargmann) | ~~Would discharge the coboundary datum in the W-series phase lift~~ **Discharged**, without the general theorem: the corpus only needed the **one-parameter** case, where the obstruction is not hard but *absent* (`Λ²(ℝ) = 0`). `Matrix/ProjectiveLift.lean` — ★★ `exists_continuous_phase_trivialisation`: determinants reduce `N` phases to one on the circle, the circle covering lifts it (`ℝ` simply connected), and the residual `N`-th root of unity is constant by connectedness | Composes with `stone_continuous` (it returns a **continuous** `b`, exactly that theorem's missing hypothesis), so `projectedFlow_schrodinger_form_of_continuous_flow` now derives the Schrödinger form from continuity alone — W5's S1 **and** S2 both discharged |
| ~~**Toral automorphisms**~~ ⚠️ **ROW WAS FACTUALLY WRONG — corrected 2026-09-01** | ~~Hyperbolic automorphisms of `Tⁿ` (cat map) and their Haar invariance~~ **Mathlib provides the Haar invariance**: `AddMonoidHom.measurePreserving` (`MeasureTheory/Measure/Haar/Unique.lean`, the `to_additive` twin of `MonoidHom.measurePreserving`) takes continuity + surjectivity + equal total mass, which is exactly the cat map's situation. Verified by compiling `measurePreserving_cat` on the corpus's own `KTorus` — 28 lines, zero errors. What Mathlib genuinely lacks is torus ergodicity above dimension 1 (`AddCircle.ergodic_zsmul` is 1-D) and **any** definition of mixing | ~~The **invertible** mixing fibre witness~~ **BUILT 2026-09-01** (`Mathlib/Dynamics/CatMapWitness.lean`: ★★ `cat_hasCorrelationDecay` + `bijective_cat`), discharging caveat (2) of `LF4/KahlerFibreMixing.lean`'s honest scope. Note `Q12-d`'s mixing formulation was **retired as mis-specified 2026-08-24**, so this unblocks no live frontier item — it settles that the obstruction was the *choice of map*, not the ontic space | Endomorphism still used. ⚠️ *The row's "the character argument is the same shape" was also wrong*: the landed witness (`CorrelationDecayWitness.lean`) uses **no character argument** — it is a translation/sign-flip symmetry that relies on the doubling map *killing* a torsion point, which a bijection cannot do. A cat-map witness needs a genuinely new (but standard, and de-risked by compiling probes) character-orthogonality argument. Total ≈ 250–350 lines, **M**; the Haar half alone is **S** |
| ~~**Wigner normal form**~~ **NOT A GAP — the chain closed 2026-07-02** | ~~The last normal-form lemma behind full Wigner rigidity~~ The lemma the row named ("a `TransProbPreserving` map fixing every basis ray is the identity on rays") is **false as stated** — the diagonal-phase freedom and the antiunitary branch (`conjProj`) both obstruct it, which is why it never closed in that form | Nothing. `Projectivization.wigner_rigidity` and `wigner_rigidity_unitaryGroup` are **proved and pinned** (foundational triple, `Tests/AxiomAudit/MathlibStaging.lean`); the landed proof quotients the phase freedom (`diagUnitary`) and resolves the branch by a **dichotomy** (`diagReducedMap_dichotomy`) rather than an identity | Row corrected 2026-08-30 (it described the 2026-06-09 tree; the 2026-08-22 triage re-affirmed it without re-probing). Genuine residues are elsewhere and are not normal-form lemmas: **G11** (author hand-pass over five tactic blocks, gates promoting CL-024) and **§13's D1 half** (deriving `TransProbPreserving` from a general `μL`-flow — XL, and measure-preservation does not imply metric preservation) |
| ~~**Kronecker spectral theorem**~~ **DISSOLVED — the consumer never needed it** | ~~Eigenvalues of `A ⊗ B` = products, as a spectral statement~~ Still absent upstream. MATHLIB-ABSENT(spectrum_kronecker) | ~~Was needed for entropy additivity~~ Nothing: `vonNeumannEntropy_kronecker` is unconditional via `spectral_sum_kronecker` (charpoly + `diagonal_kronecker_diagonal`, permutation-invariant spectral-sum form), which is the shape the consumer actually wanted | Row struck 2026-09-01 during a list re-read: it was sitting in the live table describing a workaround that is not a workaround but the intended proof |
| ~~**Unitarily invariant Gaussian on `ℂⁿ`**~~ **DISSOLVED** (see workaround column — the projective join made FS invariance sufficient) | Multivariate complex Gaussian + invariance under `U(n)` | Was the recorded route to Liouville preservation for the phase-slot dynamics | Dissolved — the projective join made the dynamics a unitary on `ℙ(ℂ^{2N})`, so FS invariance sufficed |

## Staged in this repo, ready to edge upstream (`CsdLean4/Mathlib/`)

All CSD-free (Category 1), Mathlib naming/docstring discipline, foundational-triple only.

| Staged file(s) | Contents | Upstream target |
|---|---|---|
| `LinearAlgebra/Projectivization/Topology.lean` | Quotient topology instance, `continuous_mk'`, open-map/quotient-map, T2, compactness, `Projectivization.map` continuity, `mapEquiv`, **connectedness** (`connectedSpace_of_isConnected_nonzero`) | `Mathlib.LinearAlgebra.Projectivization.Topology` |
| `…/Projectivization/MeasureSpace.lean` | Borel instance, `measurable_mk'`, Borel = coinduced coincidence, `measurable_iff_measurable_comp_mk'`, lift measurability | ditto (measure file) |
| `…/Projectivization/FubiniStudy.lean` (+`Unique`) | `fsMeasure` as Haar pushforward, unitary invariance, uniqueness | new file |
| `Geometry/Manifold/Instances/ProjectiveSpace.lean` | **`ℂℙⁿ` as an analytic manifold** (2026-09-07, step (0) of the manifold plan): the standard affine atlas `chartAtIdx`, `instChartedSpace` and `instIsManifold` at `ω`. `Projectivization` had a charted-space instance NOWHERE upstream. ⚠️ Stated over `ℂ` on the Euclidean ambient space; the same charts work over any nontrivially normed field and that generalisation is not made | `Mathlib.Geometry.Manifold.Instances` (beside `Sphere.lean`) |
| `Analysis/Normed/Module/Alternating/Wedge.lean` | **The exterior product of continuous alternating maps** (2026-09-07, step (1) of the manifold plan): `ContinuousAlternatingMap.wedge` with the codomains paired through a continuous bilinear map, `wedge_toAlternatingMap` (it IS `AlternatingMap.domCoprod`, so the algebra transfers) and `wedge_apply`; plus `ContinuousAlternatingMap.domDomCongr`, the missing reindexing. ⚠️ Binary product only — no associativity, no graded commutativity, no iterated power, no norm bound | `Mathlib.Analysis.Normed.Module.Alternating` (beside `Basic.lean`); `domDomCongr` belongs in `Mathlib.Topology.Algebra.Module.Alternating.Basic` |
| `Analysis/Normed/Module/Alternating/{WedgeCLM, WedgeShuffle, TopForm, Pullback}.lean` | The wedge as a continuous bilinear map with `‖α ∧ β‖ ≤ ‖α‖‖β‖`; the shuffle sum of a wedge with a 2-form on a pair family (sign-free); the Jacobian rule for the coefficient of a top form (`compContinuousLinearMap_apply_basis`); pullback along a linear map (2026-09-08) | `Mathlib.Analysis.Normed.Module.Alternating` |
| `Geometry/Manifold/VectorBundle/AlternatingMap.lean` | **The alternating-map bundle is a `ContMDiffVectorBundle`** (step (2a), 2026-09-07): the instance that makes `C^n` differential forms on a manifold a type | `Mathlib.Geometry.Manifold.VectorBundle` |
| `Geometry/Manifold/{DifferentialForm, ExteriorDerivative, WedgeForm}.lean` | **Differential forms on manifolds as `C^n` sections; the exterior derivative `mextDeriv` with chart-independence, `C^∞`, and `d² = 0`; the wedge of forms and `wedgePow`** (steps (2a)/(2b), 2026-09-07/08). Mathlib's `DifferentialForm/Basic.lean` is flat only and lists manifold forms as its TODO | `Mathlib.Geometry.Manifold.DifferentialForm` |
| `Geometry/Manifold/{SymplecticForm, HamiltonianVectorField}.lean` | **`IsSymplectic`** (closed + non-degenerate); **`IsHamiltonianVectorField`** (`ι_X ω = dH`), existence and uniqueness via the musical isomorphism, `C^∞` and `C^ω` sections, local existence / global uniqueness of integral curves, energy conservation; **`IsAlmostKahler`**, **`IsKahler`** (atlas sense) with the Nijenhuis tensor via `VectorField.mlieBracket` proved zero; `J` as a smooth section of `Hom(TM,TM)` (G1–G4, G7, G11, G14–G19, 2026-09-08 → 2026-09-11) | `Mathlib.Geometry.Manifold.Symplectic` (new) |
| `Geometry/Manifold/{TopFormMeasure, RiemannianVolume}.lean` | **The measure of a top form on a manifold** (chart densities glued along a `ChartCover`, chart- and cover-independent by change of variables, preserved by form-preserving homeomorphisms, finite on compact `M`); **the Riemannian volume of a metric family** by Gram densities, chart- and cover-independent for bilinear families (steps (3), G17, Q30, 2026-09-08 / 2026-09-11). Mathlib has no measure theory on manifolds at the pin | `Mathlib.Geometry.Manifold.MeasureTheory` (new) |
| `Geometry/Manifold/Instances/ProjectiveSpace{ChartCover, FubiniStudy, FubiniStudyForm, FubiniStudySymplectic, FubiniStudyVolume, FubiniStudyMass, FubiniStudyRiemannian, UnitaryAction, MomentMap, SchrodingerFlow}.lean` | **`ℂℙⁿ` with the Fubini–Study form**: the form from the potential `log(1+‖z‖²)`, chart-invariant, `C^∞`/`C^ω`, closed, non-degenerate — **a symplectic and Kähler manifold**; `U(n+1)`-invariance via a holomorphic pluriharmonic-log lemma; the top power's measure `= (4π)ⁿ μ_FS` (`∫_{ℂⁿ}(1+‖w‖²)^{-(n+1)} = πⁿ/n!`); the Riemannian volume `= ω^{∧n}/n!`; the torus action Hamiltonian with `2∑θₖμₖ` (moment map unique up to a constant, image the simplex, orbits the integral curves); the Schrödinger flow Hamiltonian with `-2⟨H⟩` and its own integral curve (2026-09-07 → 2026-09-11) | `Mathlib.Geometry.Manifold.Instances` (beside `Sphere.lean`) |
| `Analysis/SpecialFunctions/JapaneseBracketIntegral.lean` | `∫_{ℂⁿ} (1 + ‖w‖²)^{-(n+1)} = πⁿ/n!` by polar coordinates on one factor and a Fubini induction (M7, 2026-09-08) | `Mathlib.Analysis.SpecialFunctions` |
| `…/Projectivization/Metric.lean` | **The first metric on `Projectivization`** (MG-1, 2026-08-22): the projection-embedding `MetricSpace`, topology-compatible by construction; `dist_eq` | new file |
| `…/Projectivization/FubiniStudyLebesgue.lean` | **FS as a Lebesgue-a.c. pushforward** (MG-2, 2026-08-22): normalized punctured-ball measure → `mk` → `fsMeasure` by uniqueness; the null-transport principle `fsMeasure_null_of_cone`; Fubini-slicing null lemmas for coordinate hyperplanes and the coordinate quadratic | new file |
| `…/Projectivization/{TransitionProbability, WignerRigidity, Bargmann, PhaseRigidity, Unitary…}.lean` | Transition probabilities, Wigner rigidity chain, Bargmann invariant, `U(N)→PU(N)` kernel | staged pending the §13 pause decision |
| `Analysis/Matrix/StoneC1.lean` | Finite-dimensional C¹ Stone theorem | `Mathlib.Analysis.Matrix` |
| `Analysis/Matrix/DuhamelBound.lean` | `‖e^{tC}−e^{tA}‖ ≤ \|t\|‖C−A‖` for skew-Hermitian generators (integral-free) | ditto |
| `Analysis/Matrix/L2OpNormEntry.lean` | `‖M a b‖ ≤ ‖M‖` (entry ≤ L2 operator norm) + `EuclideanSpace.norm_coord_le_norm` — the bridge from operator-norm estimates to entrywise (Pi-topology) continuity | `Mathlib.Analysis.CStarAlgebra.Matrix` (beside `entry_norm_bound_of_unitary`) |
| `LinearAlgebra/Matrix/PathSum.lean`, `Analysis/Matrix/SumOverPaths.lean` | **The sum over paths** (2026-09-19): the entries of `M ^ (n + 1)` as sums over index paths of the product of the entries traversed (`pow_succ_apply_eq_sum_pathWeight`; Mathlib has only the adjacency-matrix walk count), and the Lie–Trotter formula read entry by entry through it — the matrix element of `exp (A + B)`, and of the propagator of a split Hamiltonian, as the limit of sums over discrete paths of products of one-step amplitudes (`exp_add_apply_tendsto_sum_pathWeight`, `exp_neg_I_mul_smul_add_apply_tendsto_sum_pathWeight`) | `Mathlib.LinearAlgebra.Matrix`, `Mathlib.Analysis.Matrix` |
| `Analysis/Matrix/DysonSeries.lean` | **The Dyson series** (2026-09-19): for skew-Hermitian generators the propagator `exp (t (A + B))` is the convergent sum of the interaction-picture terms `Dₙ₊₁(t) = exp (tA) ∫₀ᵗ exp (−sA) B Dₙ(s) ds` (`hasSum_dysonTerm`), with the Duhamel identity as a Bochner integral of matrices under the operator norm (`exp_add_sub_exp_eq`) and the truncation error `(‖B‖ t)ⁿ/n!` (`norm_dysonRemainder_le`) | `Mathlib.Analysis.Matrix` |
| `Analysis/Matrix/DysonVertex.lean` | **The vertex bookkeeping of the Dyson series** (2026-09-20): the interaction picture and the time-ordered recursion (`dysonTermI_succ`), the sum over vertex labellings for an interaction that is a sum of vertex types (`dysonTerm_sum_eq_sum_dysonTermLabelled`), entries of matrix-valued interval integrals (`intervalIntegral_apply`), and in the eigenbasis of a diagonal free generator the old-fashioned perturbation theory recursion with its first-order transition amplitude and two-vertex amplitude (`dysonTermI_succ_apply_diagonal`, `dysonTerm_one_apply_diagonal_of_ne`, `dysonTermI_two_apply_diagonal`) | `Mathlib.Analysis.Matrix` |
| `Analysis/Matrix/{OperatorConvex, OperatorConvexBridge}.lean` | Löwner operator-convexity predicate + rungs; the `CStarMatrix ↔ Matrix` CFC/order bridge | `Mathlib.Analysis.Matrix` (and an instance-hygiene issue upstream) |
| `MeasureTheory/PiecewisePreserving.lean` | Measurability/measure-preservation by partition; `swapSlot` on `Measure.pi` | `Mathlib.MeasureTheory` |
| `QuantumInfo/RegisterTensor.lean` | **The register tensor factorisation** (MG-5, 2026-08-22): `QReg (a+b) ≃ₗᵢ[ℂ] QReg a ⊗[ℂ] QReg b` via wire-index currying + `OrthonormalBasis.tensorProduct`; `prodTensorEquiv` (the product-index model of a Hilbert tensor product, reusable beyond qubits); `tensorFirst` + basis-state rules | ditto (beside `Register.lean`) |
| `QuantumInfo/Fidelity.lean` | Uhlmann fidelity: the spectral definition, symmetry (via the corpus's rectangular-spectrum lemma), and **`F ≤ 1`** for positive-definite states. Carries two pieces Mathlib lacks and that are useful on their own: **Hilbert–Schmidt Cauchy–Schwarz** on matrices (`norm_trace_conjTranspose_mul_le` — upstream gives `Matrix` a Frobenius *norm* but no inner product) and the **polar decomposition of an invertible matrix** (`exists_unitary_conjTranspose_mul_eq_sqrt`; upstream has singular *values* only) | a future `Mathlib.QuantumInfo`; the HS and polar lemmas belong beside `Mathlib.Analysis.Matrix` |
| `QuantumInfo/{Entropy, PartialTrace, Subadditivity, StrongSubadditivity, Channel, TraceDistance, Register, Helstrom}.lean` | von Neumann entropy (+ subadditivity, Araki–Lieb, conditional SSA), matrix partial trace, CPTP/Kraus/Stinespring, trace distance with DPI, n-qubit registers, Helstrom bound | a future `Mathlib.QuantumInfo` (none exists today) |
| `MeasureTheory/MomentDeterminacy.lean` | **Hausdorff moment determinacy** (2026-08-23/24): `ext_of_forall_integral_pow_eq` (two finite Borel measures on a compact interval with equal moments are equal) + the `ℝ`-supported transfer, `eq_of_forall_integral_mul_pow_eq` (continuous functions with equal moments against all powers), and `intervalMeasure` — **Mathlib has no `MeasureSpace` instance on `Set.Icc a b`**, so the carrier had to be built. Mathlib provisions Weierstrass and `ext_of_forall_integral_eq_of_IsFiniteMeasure` but never states the conclusion | `Mathlib.MeasureTheory.Measure` |
| `Probability/{CompetingExponentials, IidClockRace}.lean` | **The competing-risks characterisation** (2026-08-24): first-to-fire is proportional to the rate **iff** the waiting-time law is exponential (`hasRaceProperty_iff_exists_expMeasure`), with the kernel identity, the `k`-clock moment family, and the probability integral transform falling out with **no regularity hypothesis**. Mathlib has the exponential distribution and `HittingTime` but neither direction of this | `Mathlib.Probability.Distributions.Exponential` |
| `Dynamics/Kac.lean` | **Kac's formula** (2026-08-24): mean return time `= 1/μ A` for an ergodic measure-preserving map. **Mathlib has Poincaré recurrence and ergodicity but no return times at all.** Proved by telescoping on *preimages*, avoiding the Kakutani tower | `Mathlib.Dynamics.Ergodic` |
| `Dynamics/{CorrelationDecay, CorrelationDecayWitness, CompactGroupNoMixing}.lean` | Quantitative correlation decay + the circle-doubling witness + **no flow generated by a compact group has decaying correlations** (`not_hasCorrelationDecay_of_compactGroup`, with a `to_additive` twin so one proof serves a group action and an additive shift) | `Mathlib.Dynamics.Ergodic` |
| `Topology/Algebra/CompactRecurrence.lean` | Powers recur in a compact group (`exists_le_pow_mem_of_compactSpace`, `@[to_additive]`) | `Mathlib.Topology.Algebra.Group` |

**Suggested first upstream batch** (self-contained, no pauses touching them):
Projectivization `Topology` + `MeasureSpace` + `Metric`, `StoneC1`, `DuhamelBound`, `PiecewisePreserving`,
`PartialTrace`, `TraceDistance`. See `specs/BACKLOG.md` for the tracked item.
