# Mathlib gaps this project has hit — and what closing them would unlock

*(Created 2026-08-02. Two lists: genuine absences in Mathlib that gate corpus work, and the
material this repo has already staged for upstream. Each gap names the corpus item it blocks,
so contributors can see the physics payoff of a library PR. The single open-work list remains
[`specs/BACKLOG.md`](specs/BACKLOG.md); this page is the Mathlib-facing cut of it.)*

**Triage 2026-08-22** ([`specs/mathlib-gaps-plan.md`](specs/mathlib-gaps-plan.md)): every row
below was re-probed at the pin. Birkhoff and Lévy verified standing; the `CStarAlgebra`
row is a SOFT wall (the instances exist at the pin — the failure is discrimination-tree
resolution, probe-first as MG-3); the FS-metric and polynomial-zero-sets rows are **attackable
in-corpus** (MG-1, MG-2 — the latter converts "almost every composite state is entangled" from
research-gated prose to a bounded three-brick chain).

## Genuine absences (no Mathlib API today)

| Gap | What it is | What it blocks here | Current workaround |
|---|---|---|---|
| **Kähler / symplectic manifold API** | Differential forms *on manifolds* with `dω = 0`, compatible complex structure, Liouville volume `ωⁿ/n!`, Hamiltonian vector fields `X_H = ω⁻¹dH`. ⚠️ *Narrowed 2026-08-06:* upstream now has the exterior derivative **on normed spaces** (`Mathlib/Analysis/Calculus/DifferentialForm/`, flat space, with `d² = 0`; manifold forms are that file's own TODO), so the FLAT half of the gap is gone | The *manifold* spelling only. The `X_H = ω⁻¹dH` sentence is no longer pure prose: the linear-level identification is proved (`Mathlib/Analysis/InnerProductSpace/HamiltonianVectorField.lean` — `ι_Xω = dH` with the Schrödinger field `-(i•Ax)` exhibited, instantiated on the pointer coupling in `SigmaLayer/PointerHamiltonianField.lean`); the chart-level Poisson fragment is A3 (`ChartBracket.lean`). What stays blocked: the quotient/manifold statement on `ℂℙ^{N-1}` and the arena (`reconstruction-status.md` §2a) | Measures (`fubiniStudyMeasure`, Haar products) carry all consumed content; the geometric reading above the linear/chart fragments is documented prose (`AXIOMS.md` §3.1). Flat `dω = 0` for the constant fundamental form is **PROVED** (2026-08-06, `Mathlib/Analysis/InnerProductSpace/KahlerClosed.lean`: `extDeriv_const` + `Kahler.extDeriv_fundamentalFormAlt`). ⚠️ *Narrowed again 2026-08-22 (MG-4, `KahlerPotential.lean`):* the **non-constant** case is now proved too — a form presented as `dd^c` of a potential is closed by `d² = 0` (`extDeriv_ddcForm`), and the genuine Fubini–Study chart form (potential `log(1+‖z‖²)`, `contDiff_fsPotential`) is therefore closed (★★ `extDeriv_fsChartForm`). ⚠️ *Narrowed again 2026-09-01:* residue (ii) — identifying the potential-defined chart form with `Kahler.fundamentalForm` — is **DONE** (`fsChartForm_apply` computes the components from the second derivative of `log(1+‖z‖²)`; `fsChartForm_zero` gives `fsChartForm 0 = (-4:ℝ) • fundamentalFormAlt`), so `extDeriv_fsChartForm` is now closedness of an *identified* object. **What is left in this gap:** (i) the quotient/manifold spelling on `ℂℙ^{N-1}`, and (iii) the top-power volume identity `ω^{N-1}/(N-1)! = μ_FS`, which cannot even be STATED upstream — there is no continuous wedge product at the pin. MATHLIB-ABSENT(file:Mathlib/Geometry/Manifold/DifferentialForm, ContinuousAlternatingMap.domCoprod) |
| **Pointwise Birkhoff ergodic theorem** | A.e. time-average = space-average for measure-preserving flows | `BornFromFlow` (manifest L7 / T3): frequencies along a *single flow trajectory* rather than i.i.d. sampling | The strong-law route (i.i.d. trials of `μL`); the no-gos (`flow_admits_invariant_ne_fubiniStudy`) show a single flow cannot pin `μ_FS` anyway. MATHLIB-ABSENT(Ergodic.ae_tendsto_birkhoffAverage) |
| **Operator convexity — the SUMMIT only (Effros/Lieb)** ⚠️ *narrowed 2026-08-30:* `log` operator-concavity and `x^p` (`p∈[0,1]`) concavity are **no longer gaps — upstream proved them** (`CFC.concaveOn_log`, `CFC.concaveOn_nnrpow`), and `CStarAlgebra (Matrix n n ℂ)` **exists** as a scoped instance (`Matrix.Norms.L2Operator`), so the "not a C⋆-algebra" wall was a scope question. What remains: **Effros perspective / Lieb concavity → joint convexity of relative entropy → DPI** | Unconditional **strong subadditivity** (`StrongSubadditivity.lean` carries the reduction with `hDPI` as an explicit hypothesis); the quantum data-processing inequality | The ladder is part-built (`OperatorConvex.lean`: predicate, `x⁻¹` rung, resolvent rungs, reframing lemma, cfc-integral commutation); the wall is instance resolution, next row. MATHLIB-ABSENT(CFC.convexOn_mul_log, liebConcavity) |
| ~~**`CStarAlgebra`-instances for `Matrix n n ℂ`**~~ **RESOLVED IN-CORPUS** (re-probed 2026-09-01: `CStarMatrix.instCStarAlgebra` exists at the pin, and the two registered shims — `instCStarMatrixRealCFC`, `instCStarMatrixNonnegSpectrumClass` — make all four previously-failing synthesis goals fire. Not an absence row: it was always instance *resolution*) | The default instances on `Matrix` don't provide `CStarAlgebra`/`NonUnitalCStarAlgebra`; on the `CStarMatrix` synonym, **exactly two** generic instances fail to fire through the discrimination tree (probe 2026-08-22, `specs/mathlib-gaps-plan.md` MG-3): the ℝ-CFC over `IsSelfAdjoint` and `NonnegSpectrumClass ℝ` — each provable by a one-line application of its own generic provider | Everything C⋆-generic on matrices. **Narrowed 2026-08-22:** with the two shims registered, the ENTIRE upstream monotonicity tier fires on `CStarMatrix` (`CFC.monotone_rpow`/`monotone_nnrpow` — which now exist upstream — `monotone_sqrt`, `rpow`, `log_le_log`); the remaining C⋆-side absence for the DPI ladder is operator CONVEXITY (Lieb), upstream's own TODO | The bridge file (B.1–B.3, shim 1) + a from-scratch matrix Bochner-cfc (`cfc_integral_commute`); B.4 LANDED 2026-08-22 (shim 2 + `matrix_nnrpow_le_nnrpow` + `matrix_sqrt_le_sqrt` — the rpow-order transport, in the bridge); the two-instance witness is a ready-made upstream instance-hygiene report |
| **Lévy concentration / spherical isoperimetry** | Measure concentration on high-dimensional spheres | Canonical-typicality **exponential** concentration (TH1 carries expectation + the Q24 Chebyshev tier) | Named residual, recorded; expectation form proved (`fs_first_moment`); polynomial-rate concentration proved (Q24, 2026-08-21: `fs_chebyshev_concentration`, twirl-algebra second moments — no isoperimetry); only the `exp(−c·d_E·ε²)` rate still waits on this gap. MATHLIB-ABSENT(levyConcentration, file:Mathlib/Analysis/Convex/Isoperimetric) |
| ~~**Fubini–Study metric on `ℙ`**~~ **CLOSED IN-CORPUS 2026-08-22 (MG-1)** | ~~A `MetricSpace` instance on `Projectivization` (Mathlib has topology only, staged here; no `dist` anywhere)~~ **Staged**: `Projectivization/Metric.lean` — the rank-one projection embedding `p ↦ P_p` is a closed embedding from the compact `ℙ` into the operator space, and `IsEmbedding.comapMetricSpace` pulls the metric back with the metric topology DEFINITIONALLY the staged quotient topology; `dist p q = ‖P_p − P_q‖` | ~~The quantified ε-ball forms of the C2 support arc~~ Now statable: `CPN N` inherits the instance automatically. The C2 ε-ball corollaries themselves are the recorded follow-up (Q28 rider) | Joins the upstream first batch — the first metric on `Projectivization` anywhere |
| ~~**Polynomial zero sets are null**~~ **DISSOLVED 2026-08-22 (MG-2)** | ~~Zero set of a nonzero multivariate polynomial has measure zero, transportable through the Haar orbit map~~ **The consumer never needed the general lemma**: the Segre cone lies in the zero set of ONE coordinate quadratic (the `2×2` minor at a fixed corner, read through the index bijection), null by two-step Fubini slicing. The general `MvPolynomial` version is recorded as a pure-optionality upstream candidate — nothing in flight needs it | ~~`segre_range_null` — prose-strength only~~ **PROVED**: `compositeFubiniStudy_range_segre_null` + `ae_not_mem_range_segre` — *almost every composite state is entangled* (Q28 item 5, de-research-gated) | Staged `Projectivization/FubiniStudyLebesgue.lean` (FS as a Lebesgue-a.c. pushforward + the null-transport principle) is the general-purpose tool this produced |
| ~~**Hilbert tensor factorisation of registers**~~ **CLOSED IN-CORPUS 2026-08-22 (MG-5)** | ~~`EuclideanSpace`/`PiLp` has no tensor-split API~~ **Staged**: `QuantumInfo/RegisterTensor.lean` — `regTensorEquiv : QReg (a+b) ≃ₗᵢ[ℂ] QReg a ⊗[ℂ] QReg b`, built from the wire-index currying plus `OrthonormalBasis.tensorProduct` (both sides carry orthonormal bases indexed by the product, so the isometry is the change of basis); Mathlib's inner-product tensor product supplied the target | ~~The measurement-gadget hybrid~~ The *API* half is delivered, including `tensorFirst` (an operator on a wire block, identity elsewhere) and its basis-state computation rule. ⚠️ The gadget hybrid itself is **not** thereby proved: the n-fold amplitude argument is open work, and `MeasurementAdder.lean`'s wall note is updated to say so | Joins the upstream batch (a `Mathlib.QuantumInfo` would want it beside `Register.lean`) |
| **Stone's theorem (general, infinite-dimensional)** | Strongly continuous one-parameter unitary groups ↔ self-adjoint generators. **XL and not close**: the pin has `LinearPMap` self-adjointness (definitional layer) and a finite-dimensional/compact spectral theorem, but **no spectral measures / PVMs, no Borel functional calculus, no C₀-semigroup theory, no Hille–Yosida** (all re-probed 2026-08-30) | Nothing in flight — the corpus dynamics spine is `Matrix (Fin N) (Fin N) ℂ` throughout; the CV track would eventually want it | ⚠️ *Workaround column corrected 2026-08-30:* the **finite-dimensional continuity-only** theorem is **proved**, not merely C¹ — `Matrix.StoneC1.stone_continuous` (2026-07-23) derives smoothness by integral averaging. The live residue is **plumbing, not a Mathlib gap**: nothing consumes it, and `LF4/PhaseLift.lean`'s Schrödinger capstones still take the C¹ derivative datum. MATHLIB-ABSENT(spectralMeasure, ProjectionValuedMeasure) |
| ~~**Bargmann's theorem**~~ **NOT NEEDED — the consumer was closed in-corpus 2026-09-01** | ~~Continuity ⇒ vanishing of the projective-representation cocycle~~ The general theorem is still absent from Mathlib and stays out of scope. MATHLIB-ABSENT(Bargmann) | ~~Would discharge the coboundary datum in the W-series phase lift~~ **Discharged**, without the general theorem: the corpus only needed the **one-parameter** case, where the obstruction is not hard but *absent* (`Λ²(ℝ) = 0`). `Matrix/ProjectiveLift.lean` — ★★ `exists_continuous_phase_trivialisation`: determinants reduce `N` phases to one on the circle, the circle covering lifts it (`ℝ` simply connected), and the residual `N`-th root of unity is constant by connectedness | Composes with `stone_continuous` (it returns a **continuous** `b`, exactly that theorem's missing hypothesis), so `projectedFlow_schrodinger_form_of_continuous_flow` now derives the Schrödinger form from continuity alone — W5's S1 **and** S2 both discharged |
| ~~**Toral automorphisms**~~ ⚠️ **ROW WAS FACTUALLY WRONG — corrected 2026-09-01** | ~~Hyperbolic automorphisms of `Tⁿ` (cat map) and their Haar invariance~~ **Mathlib provides the Haar invariance**: `AddMonoidHom.measurePreserving` (`MeasureTheory/Measure/Haar/Unique.lean`, the `to_additive` twin of `MonoidHom.measurePreserving`) takes continuity + surjectivity + equal total mass, which is exactly the cat map's situation. Verified by compiling `measurePreserving_cat` on the corpus's own `KTorus` — 28 lines, zero errors. What Mathlib genuinely lacks is torus ergodicity above dimension 1 (`AddCircle.ergodic_zsmul` is 1-D) and **any** definition of mixing | ~~The **invertible** mixing fibre witness~~ **BUILT 2026-09-01** (`Mathlib/Dynamics/CatMapWitness.lean`: ★★ `cat_hasCorrelationDecay` + `bijective_cat`), discharging caveat (2) of `LF4/KahlerFibreMixing.lean`'s honest scope. Note `Q12-d`'s mixing formulation was **retired as mis-specified 2026-08-24**, so this unblocks no live frontier item — it settles that the obstruction was the *choice of map*, not the ontic space | Endomorphism still used. ⚠️ *The row's "the character argument is the same shape" was also wrong*: the landed witness (`CorrelationDecayWitness.lean`) uses **no character argument** — it is a translation/sign-flip symmetry that relies on the doubling map *killing* a torsion point, which a bijection cannot do. A cat-map witness needs a genuinely new (but standard, and de-risked by compiling probes) character-orthogonality argument. Total ≈ 250–350 lines, **M**; the Haar half alone is **S** |
| ~~**Wigner normal form**~~ **NOT A GAP — the chain closed 2026-07-02** | ~~The last normal-form lemma behind full Wigner rigidity~~ The lemma the row named ("a `TransProbPreserving` map fixing every basis ray is the identity on rays") is **false as stated** — the diagonal-phase freedom and the antiunitary branch (`conjProj`) both obstruct it, which is why it never closed in that form | Nothing. `Projectivization.wigner_rigidity` and `wigner_rigidity_unitaryGroup` are **proved and pinned** (foundational triple, `Tests/AxiomAudit/MathlibStaging.lean`); the landed proof quotients the phase freedom (`diagUnitary`) and resolves the branch by a **dichotomy** (`diagReducedMap_dichotomy`) rather than an identity | Row corrected 2026-08-30 (it described the 2026-06-09 tree; the 2026-08-22 triage re-affirmed it without re-probing). Genuine residues are elsewhere and are not normal-form lemmas: **G11** (author hand-pass over five tactic blocks, gates promoting CL-024) and **§13's D1 half** (deriving `TransProbPreserving` from a general `μL`-flow — XL, and measure-preservation does not imply metric preservation) |
| ~~**Kronecker spectral theorem**~~ **DISSOLVED — the consumer never needed it** | ~~Eigenvalues of `A ⊗ B` = products, as a spectral statement~~ Still absent upstream. MATHLIB-ABSENT(spectrum_kronecker) | ~~Was needed for entropy additivity~~ Nothing: `vonNeumannEntropy_kronecker` is unconditional via `spectral_sum_kronecker` (charpoly + `diagonal_kronecker_diagonal`, permutation-invariant spectral-sum form), which is the shape the consumer actually wanted | Row struck 2026-09-01 during a list re-read: it was sitting in the live table describing a workaround that is not a workaround but the intended proof |
| ~~**Unitarily invariant Gaussian on `ℂⁿ`**~~ **DISSOLVED** (see workaround column — the projective join made FS invariance sufficient) | Multivariate complex Gaussian + invariance under `U(n)` | Was the recorded route to Liouville preservation for the phase-slot dynamics | Dissolved — the projective join made the dynamics a unitary on `ℙ(ℂ^{2N})`, so FS invariance sufficed |

## Staged in this repo, ready to edge upstream (`CsdLean4/Mathlib/`)

All CSD-free (Category 1), Mathlib naming/docstring discipline, foundational-triple only.

| Staged file(s) | Contents | Upstream target |
|---|---|---|
| `LinearAlgebra/Projectivization/Topology.lean` | Quotient topology instance, `continuous_mk'`, open-map/quotient-map, T2, compactness, `Projectivization.map` continuity, `mapEquiv`, **connectedness** (`connectedSpace_of_isConnected_nonzero`) | `Mathlib.LinearAlgebra.Projectivization.Topology` |
| `…/Projectivization/MeasureSpace.lean` | Borel instance, `measurable_mk'`, Borel = coinduced coincidence, `measurable_iff_measurable_comp_mk'`, lift measurability | ditto (measure file) |
| `…/Projectivization/FubiniStudy.lean` (+`Unique`) | `fubiniStudyMeasure` as Haar pushforward, unitary invariance, uniqueness | new file |
| `…/Projectivization/Metric.lean` | **The first metric on `Projectivization`** (MG-1, 2026-08-22): the projection-embedding `MetricSpace`, topology-compatible by construction; `dist_eq` | new file |
| `…/Projectivization/FubiniStudyLebesgue.lean` | **FS as a Lebesgue-a.c. pushforward** (MG-2, 2026-08-22): normalized punctured-ball measure → `mk` → `fubiniStudyMeasure` by uniqueness; the null-transport principle `fubiniStudyMeasure_null_of_cone`; Fubini-slicing null lemmas for coordinate hyperplanes and the coordinate quadratic | new file |
| `…/Projectivization/{TransitionProbability, WignerRigidity, Bargmann, PhaseRigidity, Unitary…}.lean` | Transition probabilities, Wigner rigidity chain, Bargmann invariant, `U(N)→PU(N)` kernel | staged pending the §13 pause decision |
| `Analysis/Matrix/StoneC1.lean` | Finite-dimensional C¹ Stone theorem | `Mathlib.Analysis.Matrix` |
| `Analysis/Matrix/DuhamelBound.lean` | `‖e^{tC}−e^{tA}‖ ≤ \|t\|‖C−A‖` for skew-Hermitian generators (integral-free) | ditto |
| `Analysis/Matrix/L2OpNormEntry.lean` | `‖M a b‖ ≤ ‖M‖` (entry ≤ L2 operator norm) + `EuclideanSpace.norm_coord_le_norm` — the bridge from operator-norm estimates to entrywise (Pi-topology) continuity | `Mathlib.Analysis.CStarAlgebra.Matrix` (beside `entry_norm_bound_of_unitary`) |
| `Analysis/Matrix/{OperatorConvex, OperatorConvexBridge}.lean` | Löwner operator-convexity predicate + rungs; the `CStarMatrix ↔ Matrix` CFC/order bridge | `Mathlib.Analysis.Matrix` (and an instance-hygiene issue upstream) |
| `MeasureTheory/PiecewisePreserving.lean` | Measurability/measure-preservation by partition; `swapSlot` on `Measure.pi` | `Mathlib.MeasureTheory` |
| `QuantumInfo/RegisterTensor.lean` | **The register tensor factorisation** (MG-5, 2026-08-22): `QReg (a+b) ≃ₗᵢ[ℂ] QReg a ⊗[ℂ] QReg b` via wire-index currying + `OrthonormalBasis.tensorProduct`; `prodTensorEquiv` (the product-index model of a Hilbert tensor product, reusable beyond qubits); `tensorFirst` + basis-state rules | ditto (beside `Register.lean`) |
| `QuantumInfo/{Entropy, PartialTrace, Subadditivity, StrongSubadditivity, Channel, TraceDistance, Register, Helstrom}.lean` | von Neumann entropy (+ subadditivity, Araki–Lieb, conditional SSA), matrix partial trace, CPTP/Kraus/Stinespring, trace distance with DPI, n-qubit registers, Helstrom bound | a future `Mathlib.QuantumInfo` (none exists today) |
| `MeasureTheory/MomentDeterminacy.lean` | **Hausdorff moment determinacy** (2026-08-23/24): `ext_of_forall_integral_pow_eq` (two finite Borel measures on a compact interval with equal moments are equal) + the `ℝ`-supported transfer, `eq_of_forall_integral_mul_pow_eq` (continuous functions with equal moments against all powers), and `intervalMeasure` — **Mathlib has no `MeasureSpace` instance on `Set.Icc a b`**, so the carrier had to be built. Mathlib provisions Weierstrass and `ext_of_forall_integral_eq_of_IsFiniteMeasure` but never states the conclusion | `Mathlib.MeasureTheory.Measure` |
| `Probability/{CompetingExponentials, IidClockRace}.lean` | **The competing-risks characterisation** (2026-08-24): first-to-fire is proportional to the rate **iff** the waiting-time law is exponential (`hasRaceProperty_iff_exists_expMeasure`), with the kernel identity, the `k`-clock moment family, and the probability integral transform falling out with **no regularity hypothesis**. Mathlib has the exponential distribution and `HittingTime` but neither direction of this | `Mathlib.Probability.Distributions.Exponential` |
| `Dynamics/Kac.lean` | **Kac's formula** (2026-08-24): mean return time `= 1/μ A` for an ergodic measure-preserving map. **Mathlib has Poincaré recurrence and ergodicity but no return times at all.** Proved by telescoping on *preimages*, avoiding the Kakutani tower | `Mathlib.Dynamics.Ergodic` |
| `Dynamics/{CorrelationDecay, CorrelationDecayWitness, CompactGroupNoMixing}.lean` | Quantitative correlation decay + the circle-doubling witness + **no flow generated by a compact group has decaying correlations** (`not_hasCorrelationDecay_of_compactGroup`, with a `to_additive` twin so one proof serves a group action and an additive shift) | `Mathlib.Dynamics.Ergodic` |
| `Topology/Algebra/CompactRecurrence.lean` | Powers recur in a compact group (`exists_le_pow_mem_of_compactSpace`, `@[to_additive]`) | `Mathlib.Topology.Algebra.Group` |

**Suggested first upstream batch** (self-contained, no pauses touching them):
Projectivization `Topology` + `MeasureSpace` + `Metric`, `StoneC1`, `DuhamelBound`, `PiecewisePreserving`,
`PartialTrace`, `TraceDistance`. See `specs/BACKLOG.md` for the tracked item.
